---
phase: 11-close-v2-0-audit-debt-f-1-agents-md-rewrite-doc-hygiene-batc
plan: 03
type: execute
wave: 3
depends_on: ["11-02"]
files_modified:
  - scripts/i18n-keycheck.mjs
  - geohist/index.html
  - js/i18n/es.json
  - .planning/phases/11-close-v2-0-audit-debt-f-1-agents-md-rewrite-doc-hygiene-batc/red-gate-proof.md
autonomous: true
requirements: [P-10-3]
user_setup: []

estimate:
  tokens: 26000
  raw_tokens: 26000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "A Tier-1 row with a missing star SVG fails CI; a dictionary value containing a text star fails CI; a duplicated star SVG fails CI — malformed Tier-1 rows can never ship unflagged at owner flip (D-10)"
    - "The invariant is enforced fail-closed: missing OR duplicated = red, proven by observed FAIL output + exit 1 in both directions, never by reasoning alone"
    - "The gate rides the existing validate chain — no new workflow, no new dependencies (D-10)"
    - "The owner's Tier-1 flip (remove hidden, edit the score span) cannot red the gate — flip-compat documented in the gate header"
  artifacts:
    - "scripts/i18n-keycheck.mjs — header rule 3, constants TIER1_NS and STAR (\\u2605 escape), all-values star sweep in the value loop, markup check (exactly 1 proof-row-star, 0 star literals)"
    - ".planning/phases/11-.../red-gate-proof.md — 3 recorded cycles (mutation / command / observed output / exit code) + final green battery, per the Phase 6 template"
  key_links:
    - "scripts/i18n-keycheck.mjs ↔ npm run validate chain (validate:i18n) — the new assertion runs in CI with zero workflow edits"
    - "Markup check needle ↔ geohist/index.html:86-95 — the single proof-row-star SVG is the invariant's anchor"
    - "Gate invariant ↔ 10-RUNBOOK.md flip ritual — the gate protects the flip; the flip never trips the gate"
    - "red-gate-proof.md ↔ Phase 6 template (06-changelog-page/red-gate-proof.md) — the repo's evidence standard for gate changes"
---

<objective>
P-10-3 (per D-10, folded in): extend scripts/i18n-keycheck.mjs with a fail-closed star-uniqueness assertion — the Tier-1 row's star is exactly ONE inline SVG; the star character (U+2605) never appears as text in any dictionary value or in the markup; missing or duplicated = red — and prove it with a red-gate proof in both directions before landing it as the phase's 4th atomic commit (research OQ5, pinned).

Purpose: closes the last audit nit permanently. Today the invariant is green on the live tree (0 star characters in all 19 dictionaries, exactly 1 proof-row-star SVG, 0 star literals in markup — session-verified) but nothing enforces it: the star is NOT in the CJK_PUNCT regex, so a malformed Tier-1 row could ship unflagged at owner flip.
Output: extended keycheck gate; red-gate-proof.md; D-06-shape commit 4 (the P-10-3 commit per research OQ5) landed locally.

Scope decision pinned (research OQ2): Option B — the star sweep covers ALL dictionary values (not only geohist.tier1.* keys) plus the markup checks. Stronger than D-10's minimum wording, matches the invariant 10-VERIFICATION actually verified (zero star chars in any dictionary value), and keeps ADR-550 D4 fail-closed spirit: a future surface wanting a text star must route through a visible gate decision, not slip through. TIER1_NS is still declared (documents the invariant's origin) and used in the FAIL message context for tier1-scoped violations.
</objective>

<execution_context>
@C:/Users/Familia/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/Familia/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-close-v2-0-audit-debt-f-1-agents-md-rewrite-doc-hygiene-batc/11-RESEARCH.md
@.planning/phases/11-close-v2-0-audit-debt-f-1-agents-md-rewrite-doc-hygiene-batc/11-CONTEXT.md
@scripts/i18n-keycheck.mjs
@.planning/phases/06-changelog-page/red-gate-proof.md
</context>

<precondition>Tree is clean-committed before red-gate mutations: `git status --porcelain` is empty for geohist/index.html and js/i18n/ so git restore is the safe revert; if any deferred-commit state is ever active, fall back to sha256 snapshot-copy restore (Phase 10 precedent — git checkout unusable there).</precondition>

<artifacts-this-phase-produces>
Phase-level symbols (see 11-01 for the full list). This plan creates:
- scripts/i18n-keycheck.mjs: header rule 3 (star-uniqueness, Phase 11 / P-10-3 / ADR-550 D4); constants `TIER1_NS = 'geohist.tier1.'` and `STAR = '\u2605'` (escape form — never a raw star char in source); the all-values star sweep inside the per-dictionary value loop; the markup check after the dictionary loop; FAIL message prefix "star uniqueness:" and the dictionary FAIL line naming file + key
- .planning/phases/11-close-v2-0-audit-debt-f-1-agents-md-rewrite-doc-hygiene-batc/red-gate-proof.md (new file)
- geohist/index.html and js/i18n/es.json are TRANSIENT mutations only — restored byte-identical; final state unchanged
</artifacts-this-phase-produces>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Star-uniqueness gate + missing-direction red-gate cycle (D-10)</name>
  <files>scripts/i18n-keycheck.mjs, geohist/index.html (transient mutation, byte-identical restore)</files>
  <read_first>
    - scripts/i18n-keycheck.mjs (full — insertion points: header comment; PUNCT constants block lines 41-50; value loop lines 126-137; final OK line 155)
    - .planning/phases/11-close-v2-0-audit-debt-f-1-agents-md-rewrite-doc-hygiene-batc/11-RESEARCH.md § "P-10-3 Extension Design" (the verified design + interplay note)
    - geohist/index.html lines 86-95 (the protected Tier-1 row — class="proof-row-star" on the single star SVG)
    - .planning/phases/10-gated-social-proof/10-VERIFICATION.md (the verified invariant this gate locks: 0 star chars in all 19 dictionaries, exactly 1 star SVG, 0 star literals in markup)
    - .planning/phases/10-gated-social-proof/10-CONTEXT.md decisions D-04/D-05 (tier1 fragment shape; star inline-SVG currentColor)
    - .planning/phases/10-gated-social-proof/10-RUNBOOK.md (the flip ritual the gate must not break)
  </read_first>
  <behavior>
    - Dictionary sweep: ANY dictionary value in ANY js/i18n/*.json containing U+2605 → gate FAIL exit 1, message names the file and the key (scope pinned: all values, Option B — research OQ2)
    - Markup presence: geohist/index.html containing anything other than exactly 1 occurrence of the star-SVG class token → gate FAIL exit 1 ("N proof-row-star SVG(s) (expected exactly 1)")
    - Markup purity: geohist/index.html containing ≥1 raw star character → gate FAIL exit 1 ("M star literal(s) in markup (expected 0)")
    - Flip-compat: removing the hidden attribute or editing the 0.0 score span does NOT touch the star SVG → gate stays green
    - Fail-closed: gate must fail the build (exit 1) on any of the above, never warn-and-pass
  </behavior>
  <action>Edit scripts/i18n-keycheck.mjs (zero-dependency, node built-ins only):
  1. Header comment: add rule 3 — star-uniqueness (Phase 11, P-10-3 / ADR-550 D4): the Tier-1 row's star is exactly ONE inline SVG; the star character (U+2605) never appears as text — not in any dictionary value, not in the markup. Missing or duplicated = red (fail-closed). Flip-compat: the owner's Tier-1 flip (remove hidden, edit the score span per 10-RUNBOOK §2) never touches the SVG, so the flip cannot red this gate.
  2. Constants (beside the PUNCT block): declare TIER1_NS with the value geohist.tier1. (trailing dot) and STAR with the escape sequence backslash-u-2605 inside a string literal — NEVER a raw star character in this script's source.
  3. Dictionary sweep (inside the existing per-dictionary value loop, after the CJK-punct check): if the string value includes STAR → console.error a FAIL line naming the file and key (message: contains a literal star (U+2605) — the Tier-1 star is the row's single inline SVG; prefix the message with geohist.tier1 context when the key startsWith TIER1_NS) and set failed = true. Scope: ALL values (pinned Option B), not only tier1 keys.
  4. Markup check (once, after the dictionary loop, before the final OK): read geohist/index.html with readFileSync; count star-SVG class tokens with the regex proof-row-star followed by a negative lookahead for word characters or hyphen (proof-row-star(?![\\w-])) — the lookahead is REQUIRED: a plain substring count would count a renamed probe like proof-row-star-probe as a hit and let the missing-direction mutation pass falsely; expect exactly 1. Count raw star characters via split(STAR).length - 1; expect 0. Either expectation violated → console.error one FAIL line prefixed "star uniqueness:" reporting both counts and both expectations; set failed = true. The existing failed → exit 1 flow already handles propagation — no new exit path.
  5. Missing-direction red-gate cycle (the tracer's one path, end-to-end): snapshot geohist/index.html hash; rename the star SVG's class from proof-row-star to proof-row-star-probe in geohist/index.html; run node scripts/i18n-keycheck.mjs → MUST FAIL with the star-uniqueness message showing 0 SVGs and exit 1; restore geohist/index.html via git restore (precondition guarantees a clean tree); verify hash matches pre-mutation; run node scripts/i18n-keycheck.mjs → PASS, exit 0.
  Interplay note for the header or proof record: U+2605 is not in the CJK_PUNCT regex, so this extension adds real coverage rather than duplicating the punct gate.
  </action>
  <verify>
    node scripts/i18n-keycheck.mjs on the pristine tree → PASS x19 at 178 keys + "i18n-keycheck: OK", exit 0
    Missing-direction cycle observed: gate exit 1 with "0 proof-row-star" in the star-uniqueness FAIL line (record the verbatim output for Task 2's proof file)
    Get-FileHash geohist/index.html before mutation == after restore
    rg -n "u2605|2605" scripts/i18n-keycheck.mjs → ≥2 matches (constant + message); rg -n "★" scripts/i18n-keycheck.mjs → no raw star char in source (escape form only)
  </verify>
  <acceptance_criteria>
    - Test command: node scripts/i18n-keycheck.mjs exits 0 on the pristine tree (PASS x19 at the 178-key surface, OK tail)
    - Behavior assertion: with the star SVG's class renamed, the gate exits 1 and its output names the star-uniqueness failure with an SVG count of 0
    - Source assertion: scripts/i18n-keycheck.mjs declares TIER1_NS and STAR (the \\u2605 escape) and contains no raw star character
    - Source assertion: the star-SVG count regex uses the negative lookahead (proof-row-star(?![\\w-])) — a renamed class token cannot satisfy the exactly-1 expectation
    - Source assertion: the dictionary sweep checks every value (not only TIER1_NS-prefixed keys) and the markup check asserts both exactly-1 SVG and 0 star literals
    - Behavior assertion: geohist/index.html restored byte-identical (hash match)
  </acceptance_criteria>
  <done>Star-uniqueness enforcement is live and fail-closed, proven red in the missing direction end-to-end through the real gate; pristine tree green.</done>
  <reversibility rating="reversible">Gate addition is one assertion block; mutations restored byte-identical.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Duplicate-direction red-gate cycles + red-gate-proof.md + full battery + D-06-shape commit 4</name>
  <files>scripts/i18n-keycheck.mjs, geohist/index.html (transient), js/i18n/es.json (transient), .planning/phases/11-close-v2-0-audit-debt-f-1-agents-md-rewrite-doc-hygiene-batc/red-gate-proof.md</files>
  <read_first>
    - .planning/phases/06-changelog-page/red-gate-proof.md (the Phase 6 template: cycles with mutation / command / observed output / exit code + post-revert cleanliness + final green battery)
    - js/i18n/es.json (locate the geohist.tier1.suffix value — "en Google Play" region, keys at the file tail)
    - geohist/index.html lines 86-95 (for the duplicate-SVG mutation)
    - .planning/phases/11-close-v2-0-audit-debt-f-1-agents-md-rewrite-doc-hygiene-batc/11-RESEARCH.md § "Red-gate proof cycles" (cycles 2 and 3 specs) and § "Pattern 3: Red-gate proof"
  </read_first>
  <action>
  1. Cycle 2 — duplicated star in a DICTIONARY VALUE: append the raw star character to the geohist.tier1.suffix VALUE in js/i18n/es.json (a transient probe mutation); run node scripts/i18n-keycheck.mjs → MUST FAIL exit 1 naming es.json and the geohist.tier1.suffix key (es.json is outside the ja/zh punct scope, so the star check is provably the gate that fires — include this observation in the record); restore via git restore; re-run → PASS exit 0; hash-verify the restore.
  2. Cycle 3 — duplicated star SVG in MARKUP: duplicate the existing star SVG element inside the Tier-1 row in geohist/index.html (transient); run node scripts/i18n-keycheck.mjs → MUST FAIL exit 1 with "2 proof-row-star SVG(s)"; restore via git restore; re-run → PASS exit 0; hash-verify.
  3. Write .planning/phases/11-close-v2-0-audit-debt-f-1-agents-md-rewrite-doc-hygiene-batc/red-gate-proof.md per the Phase 6 template: header (proven date, gate = node scripts/i18n-keycheck.mjs star-uniqueness extension, surface = 178 keys x 19 dictionaries); the three cycles from Task 1 (missing SVG) and this task (star in es.json value; duplicate SVG) — each with mutation, command, observed output verbatim, exit code; restore verification notes (git restore on clean tree; sha256 snapshot-copy as the documented fallback); post-revert cleanliness (git status --porcelain empty for the mutated files); final green battery (node scripts/i18n-keycheck.mjs → OK exit 0; npm run validate → exit 0); the flip-compat note (owner flip never touches the SVG — gate cannot red on the flip).
  4. Run the full battery: npm run validate → exit 0 (the extended gate now rides validate:i18n in the chain).
  5. Land the P-10-3 commit (4th atomic commit — research OQ5, pinned; keeps D-06's three F-item commits intact): stage exactly scripts/i18n-keycheck.mjs and the phase red-gate-proof.md and commit locally with subject "feat(11): star-uniqueness fail-closed gate in i18n-keycheck (P-10-3)". Do NOT push; local commit only (docs+gate-only change set; owner pushes).
  </action>
  <verify>
    Cycle 2 observed: gate exit 1 naming es.json + geohist.tier1.suffix; cycle 3 observed: gate exit 1 with 2 proof-row-star
    All three cycles' observed outputs and exit codes recorded in red-gate-proof.md (rg -c "Exit code: \\*\\*1\\*\\*" red-gate-proof.md → ≥3; final battery section present)
    node scripts/i18n-keycheck.mjs → exit 0; npm run validate → exit 0
    git status --porcelain → empty for scripts/i18n-keycheck.mjs, geohist/index.html, js/i18n/es.json after commit
    git log -1 --format=%s → "feat(11): star-uniqueness fail-closed gate in i18n-keycheck (P-10-3)"
  </verify>
  <acceptance_criteria>
    - Behavior assertion: star character inside one dictionary value → gate exit 1 naming es.json and the key; duplicate star SVG → gate exit 1 with an SVG count of 2 (both observed, not reasoned)
    - Source assertion: red-gate-proof.md exists in the phase dir, follows the Phase 6 template, records 3 cycles each with verbatim observed output and exit code 1, plus the final green battery (keycheck OK + npm run validate exit 0) and the flip-compat note
    - Test command: npm run validate exits 0 on the clean tree (extended gate chained)
    - Source assertion: geohist/index.html and js/i18n/es.json byte-identical to their pre-phase state (hash or git diff clean)
    - CLI output: commit 4 subject matches the pinned string; staged files were exactly the gate script + proof record
  </acceptance_criteria>
  <done>Star-uniqueness gate proven red in both directions with recorded evidence, green on the pristine tree, chained into CI; D-06-shape commit 4 landed locally — the last audit nit is permanently closed.</done>
  <reversibility rating="reversible">Gate block removable; proof file documents every mutation and restore.</reversibility>
</task>

</tasks>

<verification>
- node scripts/i18n-keycheck.mjs → PASS x19 at 178 keys + OK, exit 0 (pristine tree)
- Red-gate proof: 3 cycles recorded with observed FAIL output + exit 1 (missing SVG / star in es.json value / duplicate SVG), restores hash-verified
- npm run validate → exit 0 (extended gate in the chain; html/domain/links/i18n-detect untouched and green)
- red-gate-proof.md exists per the Phase 6 template; commit 4 landed locally with the pinned subject
</verification>

<success_criteria>
- D-10 satisfied: fail-closed star-uniqueness on the tier1 invariant, zero new dependencies, red-gate proven both directions
- Pinned scope decision (research OQ2): all-values sweep + markup checks, stronger than the minimum wording
- Pinned commit placement (research OQ5): 4th atomic commit — D-06's three F-item commits stay intact
- The gate survives the owner's Tier-1 flip by construction (documented flip-compat)
- No remote ops; no product deploy expectations
</success_criteria>

<output>
Create .planning/phases/11-close-v2-0-audit-debt-f-1-agents-md-rewrite-doc-hygiene-batc/11-03-SUMMARY.md when done
</output>
