---
phase: 10-gated-social-proof
plan: 02
type: execute
wave: 2
depends_on: [10-01]
files_modified:
  - geohist/index.html
  - .planning/phases/10-gated-social-proof/10-RUNBOOK.md
autonomous: false
requirements: [SEO-07]
estimate:
  tokens: 36000
  raw_tokens: 18000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "The served JSON-LD is byte-identical to the pre-phase state — the script block parses as valid JSON and carries no rating markup of any kind (SEO-07: the schema ships permanently OFF; D-06)"
    - "The rating template + precondition live in an inert HTML comment placed adjacent to but OUTSIDE the JSON-LD script element, with no double-hyphen sequences inside the comment (HTML comment grammar) — uncommenting-by-typo cannot silently activate schema (D-06, Pitfall 7)"
    - "The in-file comment and 10-RUNBOOK.md document the SAME precondition: the only trigger for ever adding the rating key is an on-site review/rating source collected on this site itself (none exists today) — Google Play ratings may never be copied into the page's structured data even when real (review-snippet policy, verbatim-cited) — D-06/D-07"
    - "10-RUNBOOK.md gives the owner the complete flip path: evidence gate (Play listing live AND a real aggregate rating visible on the Play page, owner eyeballs, no minimum-count floor), the two-edit flip (remove the hidden attribute, replace the 0.0 number, decimal-dot format), zero dictionary churn, the Rich Results Test + JSON-LD parse ritual, rollback (re-add the attribute), and the session-convention refresh rule (D-07/D-08)"
    - "The 5 agent-drafted icons (4 pill icons + star) were owner-vetoed or approved PRE-SHIP, with the strip layout and copy spot-checked in the browser before anything deploys (D-09)"
    - "The phase is shipped: prod serves the facts strip and the OFF row (hidden attribute present in the served HTML), the full validate chain and smoke-check are green, and the Actions run on the remote main sha is green"
  artifacts:
    - "geohist/index.html — one new HTML comment block adjacent to the JSON-LD script (outside it): permanent-OFF statement, verbatim policy citation, on-site-source precondition, commented template fields (ratingValue in decimal-dot format, ratingCount); served schema untouched"
    - ".planning/phases/10-gated-social-proof/10-RUNBOOK.md — owner flip doc in the 08-RUNBOOK structural pattern (current state, evidence gate, flip steps, validation ritual, rollback, refresh rule, Tier-2 precondition); console-UI instructions only, no secrets (the .planning tree is publicly served)"
    - "Deployed main carrying plans 10-01 + 10-02; prod serves the strip, the OFF row, and the byte-identical JSON-LD"
  key_links:
    - "In-file comment ↔ runbook Tier-2 section: same precondition wording (on-site review source), so a future agent reading either surface gets the same, correct trigger (P-10-4)"
    - "Prod JSON-LD ↔ local HEAD JSON-LD: byte-identity asserted after deploy (the SEO-07 guarantee survives shipping)"
    - "Runbook flip steps ↔ the exact markup surface plan 10-01 created: the div with class proof-row and hidden attribute + the span with class proof-row-score holding 0.0 — the two-edit flip names these exact artifacts"
    - "Runbook validation ritual ↔ Google Rich Results Test (search.google.com/test/rich-results) + a zero-dep JSON parse check — the D-08 manual validation chain, no new CI deps"
  prohibitions:
    - statement: "The Tier-2 in-file template and the runbook must never document a Play-listing precondition (listing live, ratings visible on Play, any Play-derived number) as the trigger for adding the rating key — the only documented trigger is an on-site review/rating source; a future agent reading a wrong precondition would commit the review-snippet-policy violation this phase exists to prevent"
      status: resolved
      verification: judgment
---

<objective>
Close Phase 10 with the documented-OFF surfaces and the ship (SEO-07, per D-06/D-07/D-08/D-09):

1. **Owner pre-ship veto (D-09):** the 5 agent-drafted icons (4 pill icons + star), strip layout, and copy get owner review in a live local preview BEFORE anything deploys — agent-drafted, owner-vetoed pre-ship.
2. **Tier-2 permanent OFF (D-06):** the commented rating template + precondition land as an HTML comment OUTSIDE the JSON-LD script element; the served schema stays byte-identical to today's.
3. **Owner runbook (D-07/D-08):** `10-RUNBOOK.md` documents the evidence gate, the two-edit flip, the Rich Results Test ritual, rollback, and the session-convention refresh rule — then the phase deploys to prod and smokes green.

Purpose: SEO-06's flip must be executable by the owner alone (runbook), and SEO-07's "never mirror Play ratings" rule must survive future agents (precondition documented in-file AND in the runbook — never a Play-listing trigger).
Output: 1 HTML comment + 1 runbook + deployed main with prod smoke green.
</objective>

<execution_context>
@C:/Users/Familia/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/Familia/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/10-gated-social-proof/10-CONTEXT.md
@.planning/phases/10-gated-social-proof/10-RESEARCH.md
@geohist/index.html
@.planning/phases/08-custom-domain-migration/08-RUNBOOK.md
@.planning/phases/09-app-check-monitor-first/09-RUNBOOK.md
</context>

<artifacts_this_phase_produces>
<!-- Symbols THIS plan creates (the full phase-wide symbol list lives in plan 10-01's "Artifacts this
     phase produces" section — i18n keys, CSS selectors, and HTML structure are all 10-01 work). -->

**New in geohist/index.html:**
- One HTML comment block adjacent to the JSON-LD `<script type="application/ld+json">` element, placed OUTSIDE it (before its opening tag), containing: the permanent-OFF statement, the verbatim Google review-snippet policy citation, the on-site-review-source precondition, and a commented template with `ratingValue` (decimal-dot format) and `ratingCount` fields
- NO new elements, classes, ids, or attributes — the comment is inert; no new CSS, no new i18n keys

**New file:**
- `.planning/phases/10-gated-social-proof/10-RUNBOOK.md` — sections: §0 current state (row OFF, 0.0 placeholder, strip live), §1 evidence gate, §2 flip steps, §3 validation ritual, §4 rollback, §5 refresh rule, §6 Tier-2 precondition
</artifacts_this_phase_produces>

<flagged_assumptions>
<!-- Spec-less probe fallback accounting: $COVERAGE surfaced 4 unresolved items; plan 10-01 carries
     A-10-05, A-10-06a, A-10-06b; this plan carries the 4th. 4 surfaced == 3 + 1. None resolved, none
     dropped. -->
1. **A-10-07 [SEO-07 — probe: unclassified, "review manually"]** — Flagged assumption: the Tier-2 surface is an inert static HTML comment — no empty/single/null-input or encoding edge class applies to non-executed comment text. The mechanical guarantee is structural (script-block byte-identity + the rating literal present only outside the script element), verified in Task 2 and again on prod in Task 3; if the comment is ever edited or moved, those checks must be re-run.
</flagged_assumptions>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Owner pre-ship veto — icons, strip layout, copy (D-09)</name>
  <files></files>
  <read_first>
    - geohist/index.html (post-10-01 state — the strip section and the OFF row markup)
    - css/base.css (the .proof-* block)
    - .planning/phases/10-gated-social-proof/10-CONTEXT.md (D-09: agent-drafted, owner-vetoed pre-ship; 09-CONTEXT D-09 copy-review pattern)
  </read_first>
  <action>
Serve the working tree locally so the owner reviews the real markup (no throwaway preview artifacts, nothing committed beyond 10-01):

1. From the repo root start a static server (try `python -m http.server 8000`; if unavailable use `npx -y serve -l 8000 .` — a temp runtime, not a repo dependency). Wait for the port, then give the owner the URL `http://localhost:8000/geohist/`.
2. Owner checklist (present verbatim): (a) the 4-pill facts strip appears between the hero and "What's packed inside", in EN, with the 4 glyph drafts (globe = languages, cloud-off = offline, map-pin = modes, phone = Android) in the 24px stroke teal style; (b) narrow the window to phone width — the pill row wraps, nothing overlaps, no texture behind the pills; (c) switch language to ES and PT-BR via the existing switcher — strip text swaps, icons stay, layout holds; (d) scroll the hero — NO rating row is visible (the OFF state must be invisible in every language); (e) veto or approve each of the 5 glyphs (4 pill icons + the star inside the hidden row — show the star's SVG path separately for approval since the row itself never renders while OFF).
3. Record the owner's ruling: approved glyphs as-is, or specific glyph swaps / copy edits (any edit routes back through the 10-01 files and re-runs the 10-01 verify gates before proceeding).
4. Stop the server after the ruling. No repo changes in this task.
  </action>
  <verify>
    <human-check>Owner approval (or post-veto re-review approval) for the 5 glyphs + strip layout + copy is recorded in the conversation and will be echoed in the phase summary — the veto gate is the D-09 pre-ship condition</human-check>
  </verify>
  <what-built>The complete Phase-10 markup and styling from plan 10-01, committed but NOT deployed: the 4-pill facts strip (5 keyed strings in 19 languages) and the OFF Tier-1 rating row, plus the 5 agent-drafted inline SVGs (4 pill glyphs + star) that need the D-09 owner ruling</what-built>
  <how-to-verify>Owner opens http://localhost:8000/geohist/ (local static server from the repo root, steps in the action): strip visible in EN, ES, PT-BR via the existing switcher; pill row wraps at phone width; no rating row visible anywhere; the star SVG path reviewed separately (the row never renders while OFF)</how-to-verify>
  <resume-signal>Continue when the owner replies with an explicit glyph/copy ruling — "approved" proceeds to Tasks 2-3; any veto routes the edit back through plan 10-01's files and verify gates, then re-presents this checkpoint</resume-signal>
  <done>Owner has seen the strip live in EN/ES/PT-BR and mobile width, confirmed the hidden row renders nothing, and approved (or redirected) all 5 icon drafts.</done>
</task>

<task type="auto">
  <name>Task 2: Tier-2 permanent-OFF comment + 10-RUNBOOK.md (D-06/D-07/D-08)</name>
  <files>geohist/index.html, .planning/phases/10-gated-social-proof/10-RUNBOOK.md</files>
  <read_first>
    - geohist/index.html (JSON-LD block lines 20-40 — the byte-identity baseline; do not touch the script element)
    - .planning/phases/10-gated-social-proof/10-CONTEXT.md (D-06/D-07/D-08)
    - .planning/phases/10-gated-social-proof/10-RESEARCH.md (Tier-2 comment code example; Pitfall 7; the owner-flip runbook sketch)
    - .planning/phases/08-custom-domain-migration/08-RUNBOOK.md (structural pattern: numbered sections, current state, owner steps, do-not-touch, rollback)
    - .planning/phases/09-app-check-monitor-first/09-RUNBOOK.md (revision-note + no-secrets conventions)
  </read_first>
  <reversibility rating="reversible">Inert comment + runbook doc; deletion or edit costs nothing downstream.</reversibility>
  <action>
<!-- planner-discipline-allow: aggregateRating -->
1. **In-file comment (geohist/index.html):** insert one HTML comment immediately BEFORE the `<script type="application/ld+json">` opening tag (adjacent, strictly OUTSIDE the script element — D-06). Content, modeled on the research example: (a) "PERMANENTLY OFF" header tied to SEO-07; (b) the verbatim policy citation from Google's review-snippet guidelines — do-not-aggregate-other-sites ratings — fetched 2026-09-09; (c) the precondition: a review/rating source collected ON THIS SITE must exist before the rating key may ever be added (none exists today), and Play ratings may NEVER be copied into this page's structured data even when real; (d) a commented template showing the field shape: `"@type": "AggregateRating"`, `ratingValue` (decimal-dot format, e.g. 4.4 — never comma), `ratingCount`. Grammar constraint: no double-hyphen sequences anywhere inside the comment body (HTML comment grammar; write ranges with "to", not dashes-with-spaces that form pairs). Do not modify the script element itself by a single byte.

2. **10-RUNBOOK.md** in the phase dir, 08-RUNBOOK structural pattern. Top note: this file lives under `.planning` which is publicly served — console-UI instructions only, no secrets, no keys. Sections:
   - §0 Current state: the rating row ships OFF (hidden div with the 0.0 placeholder) under the hero badge; the facts strip is live; the schema has no rating markup and must never gain Play-sourced numbers (cross-link to §6).
   - §1 Evidence gate: Play listing live AND a real aggregate rating visible on the Play page — the owner eyeballs it; NO minimum-count floor (any honest real rating flips; fabrication is the enemy, not small numbers — 10-CONTEXT specifics).
   - §2 Flip steps (the only owner edits): in `geohist/index.html` remove the `hidden` attribute from the div with class `proof-row`, and replace the number `0.0` inside the span with class `proof-row-score` by the real rating in decimal-dot format (e.g. 4.5). NOTHING else changes — zero dictionary churn in either direction (D-04). Commit.
   - §3 Validation ritual (D-08, run pre-ship AND again at flip): `npm run validate` (full chain) locally; after deploy run Google's Rich Results Test on the live URL and a zero-dep JSON parse check on the JSON-LD block (a `node -e` one-liner parsing the script content — give the exact command in the runbook). No new CI deps — zero-build CI cannot run Google's tool.
   - §4 Rollback: re-add the `hidden` attribute (and, if already flipped, restore `0.0`); one attribute + one number; deploy; re-smoke.
   - §5 Refresh rule: session convention — during any agent session that touches app-version facts, also check the rating number against the live Play page and update if changed (rides the established changelog-freshness habit; no automation, no calendar ritual — D-07).
   - §6 Tier-2 precondition (mirrors the in-file comment verbatim in meaning): the rating key stays out of the schema permanently unless an on-site review source exists; Play-derived numbers are barred from markup even when real; visible attributed display (the Tier-1 row) is the sanctioned path.

3. Run the full local validate chain; fix anything red. Commit the comment + runbook together (docs commit).
  </action>
  <verify>
    <automated>npm run validate && node -e "const fs=require('fs');const h=fs.readFileSync('geohist/index.html','utf8');const s=h.match(/<script type=\"application\/ld\+json\">[\s\S]*?<\/script>/)[0];if(s.includes('aggregateRating'))process.exit(1);if(!h.includes('aggregateRating'))process.exit(2);const cIdx=h.indexOf('PERMANENTLY OFF');const sIdx=h.indexOf('<script type=\"application/ld+json\">');if(cIdx<0||sIdx<0||cIdx>sIdx)process.exit(3);const open=h.lastIndexOf('<!--',cIdx);const close=h.indexOf('-->',cIdx);if(open<0||close<0||open>sIdx)process.exit(4);const inner=h.slice(open+4,close);if(inner.includes('--'))process.exit(5);if(!/ratingValue/.test(inner)||!/ratingCount/.test(inner))process.exit(6);JSON.parse(s.replace(/<\/?script[^>]*>/g,''))" ; node -e "const {execSync}=require('child_process');const fs=require('fs');const ex=t=>t.match(/<script type=\"application\/ld\+json\">[\s\S]*?<\/script>/)[0];const cur=ex(fs.readFileSync('geohist/index.html','utf8'));const old=ex(execSync('git show HEAD~1:geohist/index.html').toString());if(cur!==old)process.exit(7)" ; rg -c 'PERMANENTLY OFF' geohist/index.html | ForEach-Object { if ($_ -ne '1') { exit 8 } } && rg -q 'no minimum' .planning/phases/10-gated-social-proof/10-RUNBOOK.md && rg -q 'proof-row' .planning/phases/10-gated-social-proof/10-RUNBOOK.md && rg -q '0\.0' .planning/phases/10-gated-social-proof/10-RUNBOOK.md && rg -q 'Rich Results Test' .planning/phases/10-gated-social-proof/10-RUNBOOK.md && rg -q 'Refresh' .planning/phases/10-gated-social-proof/10-RUNBOOK.md && rg -q 'Rollback' .planning/phases/10-gated-social-proof/10-RUNBOOK.md && rg -q 'on-site' .planning/phases/10-gated-social-proof/10-RUNBOOK.md</automated>
  </verify>
  <acceptance_criteria>
    - npm run validate green on the full chain
    - The JSON-LD script block in geohist/index.html is byte-identical to the pre-Task-2 commit (git-show comparison passes) and parses as valid JSON via a zero-dep node check
    - The rating-key literal appears zero times inside the script block and at least once in the file (it exists only in the HTML comment)
    - The comment sits before the script opening tag, is the only comment carrying the permanent-OFF header, contains no double-hyphen sequence, and includes both ratingValue and ratingCount template fields
    - 10-RUNBOOK.md contains: the evidence gate with the no-minimum-floor statement, the proof-row/0.0 flip steps, the Rich Results Test ritual, a rollback section, the refresh rule, and the on-site-source Tier-2 precondition; no secrets anywhere in the file
  </acceptance_criteria>
  <done>The schema guarantee is locked (byte-identical served JSON-LD, comment outside the script, precondition documented in-file and mirrored in the runbook) and the owner flip path is fully documented.</done>
</task>

<task type="auto">
  <name>Task 3: Ship — validate chain, deploy (push or Git Data API bridge), prod smoke</name>
  <files>.planning/phases/10-gated-social-proof/10-RUNBOOK.md</files>
  <read_first>
    - .planning/STATE.md (deploy-bridge precedent + MANDATORY blob-sha assertions after the empty-blob incident; gh CLI as the sanctioned remote channel)
    - scripts/smoke-check.sh (existing URL list — zero edits expected; run post-deploy)
    - .planning/phases/09-app-check-monitor-first/09-05-PLAN.md Task 3 (the bridge + prod-smoke pattern to reuse verbatim in shape)
  </read_first>
  <action>
1. Run `npm run validate` (full chain) on the final tree; must be green before any deploy.
2. Commit any uncommitted runbook edits via the sanctioned gsd-tools commit path (raw bash git commit/push are permission-blocked in this harness — Phase 9 precedent).
3. Deploy to main: try the normal push first; if the harness blocks it, reuse the Phase 9 GitHub Git Data API bridge shape — blobs (base64) → tree with base_tree = current main tip tree → commit parented on the main tip → PATCH refs/heads/main with force:false (strict fast-forward, no force-push, no history rewrite). MANDATORY per STATE.md: after the bridge, assert each created blob sha equals `git rev-parse HEAD:<path>` (the empty-blob incident made sha assertions non-negotiable).
4. Watch the Actions run for the deployed sha (remote main sha — after a bridge the local HEAD is not a remote object; select the run by the remote sha, retry once after ~30s if no run exists yet) and require it green (validate + deploy).
5. Prod smoke (assert, never print-and-hope; exit non-zero on any failure), against https://geohisttrivia.com/geohist/:
   - the served HTML contains the strip's keyed aria-label attribute pair (data-i18n-attr with the proof aria key) and at least 4 pill keyed spans;
   - the served HTML still contains the row with class proof-row carrying the hidden attribute (row shipped OFF);
   - the served JSON-LD block is byte-identical to the local HEAD block;
   - the served script block contains zero occurrences of the rating-key literal (region-scoped);
   - `scripts/smoke-check.sh` run reports ALL PASS (URL list unchanged this phase).
6. Append the deploy sha + Actions run id to the runbook's §0 as a one-line ship record.
  </action>
  <verify>
    <automated>_SHA="$(gh api "repos/$(gh repo view --json nameWithOwner -q .nameWithOwner)/commits/main" --jq .sha)" && _RID="$(gh run list --commit "$_SHA" --json databaseId --jq '.[0].databaseId // empty')" && { test -n "$_RID" || { sleep 30; _RID="$(gh run list --commit "$_SHA" --json databaseId --jq '.[0].databaseId // empty')"; }; } && gh run watch "$_RID" --exit-status && bash scripts/smoke-check.sh && test "$(curl.exe -s https://geohisttrivia.com/geohist/ | rg -c 'data-i18n-attr="aria-label:geohist\.proof\.aria"')" = "1" && test "$(curl.exe -s https://geohisttrivia.com/geohist/ | rg -c 'data-i18n="geohist\.proof\.pill-')" = "4" && test "$(curl.exe -s https://geohisttrivia.com/geohist/ | rg -c '<div class="proof-row" hidden>')" = "1" && node -e "const {execSync}=require('child_process');const https=require('https');const ex=t=>t.match(/<script type=\"application\/ld\+json\">[\s\S]*?<\/script>/)[0];const local=ex(require('fs').readFileSync('geohist/index.html','utf8'));https.get('https://geohisttrivia.com/geohist/',r=>{let b='';r.on('data',d=>b+=d);r.on('end',()=>{if(ex(b)!==local)process.exit(1);if(ex(b).includes('aggregateRating'))process.exit(2);process.exit(0)})})"</automated>
  </verify>
  <acceptance_criteria>
    - npm run validate green locally pre-deploy
    - Deploy landed as a strict fast-forward (normal push, or bridge with per-blob sha assertions recorded); no force-push, no history rewrite
    - Actions run for the remote main sha is green (validate + deploy jobs)
    - Prod serves: the strip (aria key + 4 pill keys), the OFF row (hidden attribute present), and the byte-identical JSON-LD block with zero rating-key occurrences inside the script element
    - scripts/smoke-check.sh reports ALL PASS
  </acceptance_criteria>
  <done>Phase 10 is live on prod: facts strip visible, rating row OFF and invisible, schema untouched, owner runbook in the deployed tree, ship record appended.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| served JSON-LD → Google rich-results pipeline | The schema is what Google's manual-action systems judge; any rating key in served output is a policy-violation surface |
| runbook + in-file comment → future agents/owner | Documentation is the control surface that prevents a future "helpful" flip into the barred mirror |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-05 | Tampering | JSON-LD script block | high | mitigate | Byte-identity gate (git-show comparison Task 2 + prod comparison Task 3) + rating literal confined to the inert comment outside the script element (region-scoped zero-occurrence check) |
| T-10-06 | Repudiation | comment/typo accidentally activating schema | low | mitigate | Comment lives outside the script element (D-06 forbids in-script comments); no double-hyphen sequences (HTML grammar check); uncommenting a whole comment block is a visible, reviewable diff |
| T-10-07 | Spoofing | wrong precondition documented (Play-listing trigger) | medium | mitigate | P-10-4 + runbook §6 mirrors the in-file comment wording; acceptance greps pin the on-site-source phrasing in both surfaces |
| T-10-08 | DoS | deploy via Git Data API bridge | medium | mitigate | Strict fast-forward only (force:false), mandatory per-blob sha assertions (STATE.md post-incident rule), Actions run gate before smoke |
| T-10-SC | Tampering | package installs in this plan | low | accept | Zero installs: one HTML comment, one markdown doc, deploy tooling only; Package Legitimacy Audit table is empty (RESEARCH.md) |
</threat_model>

<verification>
- npm run validate green on the final tree (chain unchanged — zero CI edits this phase)
- Script-block byte-identity proven twice: pre-deploy (git-show vs working file) and post-deploy (prod fetch vs local HEAD); rating-key literal confined to the comment region
- 10-RUNBOOK.md complete: evidence gate (no minimum floor), two-edit flip, Rich Results Test + parse ritual, rollback, refresh rule, Tier-2 precondition; no secrets
- Owner veto recorded pre-ship (Task 1 human-check); deploy only after the ruling
- Prod smoke asserts: strip served, OFF row served hidden, JSON-LD byte-identical, smoke-check ALL PASS, Actions green on the remote main sha
</verification>

<success_criteria>
- The served schema is byte-identical to pre-Phase-10 and carries no rating markup — now and by documented precondition, never
- The in-file comment + runbook give any future agent or the owner the SAME correct trigger (on-site review source), so the Play mirror can never be rationalized into existence
- The owner can execute the flip alone: two edits, zero dictionary churn, validation ritual spelled out, rollback one attribute
- The icons shipped are the owner-approved set (D-09 honored)
- Phase 10 fully deployed and green: strip visible in 20 languages, rating row OFF, CI/smoke green
</success_criteria>

<output>
Create `.planning/phases/10-gated-social-proof/10-02-SUMMARY.md` when done (record the owner's D-09 icon ruling and the deploy sha)
</output>
