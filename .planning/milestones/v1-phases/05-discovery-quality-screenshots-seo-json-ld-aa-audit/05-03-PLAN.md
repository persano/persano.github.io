---
phase: 05-discovery-quality-screenshots-seo-json-ld-aa-audit
plan: 03
type: execute
wave: 3
depends_on: ["05-02"]
files_modified:
  - scripts/a11y-audit.mjs
  - package.json
  - .gitignore
  - css/base.css
  - index.html
  - geohist/index.html
  - geohist/guide.html
  - geohist/contact.html
  - geohist/privacy.html
  - GeoHist_Trivia_Privacy_Policy.html
  - GeoHist_Trivia_Privacy_Policy.md
  - GeoHist_Trivia_Privacy_Policy.pdf
autonomous: false
requirements: [A11Y-01]
user_setup:
  - service: google-play-console
    why: "The Play Console privacy-URL field is a console-side owner action — the agent has no API surface for it (D-69 step 1, D-70 ordering gate)"
    dashboard_config:
      - task: "Set the app's privacy policy URL to https://persano.github.io/geohist/privacy.html"
        location: "Play Console -> App content -> Privacy policy"
  - service: google-search-console
    why: "Sitemap submission is a Search Console owner action after the deploy containing sitemap.xml is live (D-65, D-69 step 2)"
    dashboard_config:
      - task: "Submit https://persano.github.io/sitemap.xml as a sitemap"
        location: "Search Console -> Sitemaps (property persano.github.io; verification file already in repo)"
estimate:
  tokens: 26000
  raw_tokens: 26000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "scripts/a11y-audit.mjs runs the phase's automated AA gate: serves the repo root over http://127.0.0.1:<port> (never via the file scheme — absolute /css and /js paths must resolve, Pitfall 7), scans the 5 content URLs with axe (zero critical/serious violations) and Lighthouse (--only-categories=accessibility, accessibility score >= 95) per page, writes JSON output plus a summary under reports/ (gitignored), and exits non-zero on any gate failure."
    - "npm script audit:a11y exists and exits 0 at plan close — every scanned page reports axe critical=0, serious=0, and a Lighthouse accessibility score >= 95; any violation found during the battery is fixed with the smallest possible change (keyed-node text-only rule intact) before the plan closes."
    - "The palette is verified, not redesigned: contrast pairs were math-proven at design time (Phase 02, css/base.css header records all 8 ratios) — the battery's color-contrast rule confirms them on the rendered pages; css/base.css color token values are byte-unchanged by this phase unless a battery failure proves otherwise."
    - statement: "Owner manual AA battery — keyboard navigation through nav/links/FAQ details/form/language switcher, visible focus states on every interactive element, form labels announced by the screen reader, empty-submit inline keyed validation on the contact form, then a language-switch re-check (switch to ES and pt-BR: documentElement.lang syncs and the keyboard + contrast pass re-runs) — is PENDING until the owner runs and reports it (honest close-out contract, Phase-04 precedent)."
      verification: backstop
    - statement: "Rich Results Test on the live /geohist/ detects the SoftwareApplication with zero errors; the absent-optional-field note about ratings is expected and acceptable (Pitfall 8) — owner-side check bundled into the manual battery."
      verification: backstop
    - "The owner confirms BOTH console steps in the D-69 checkpoint, in order: (1) Play Console privacy-policy URL field set to https://persano.github.io/geohist/privacy.html, (2) Search Console sitemap submission of https://persano.github.io/sitemap.xml — strictly after the deploy containing sitemap.xml is live (Pitfall 10 timing)."
    - "The three superseded root policy files (GeoHist_Trivia_Privacy_Policy.html/.md/.pdf) are deleted from the repo ONLY after the owner's D-69 confirmation, and that deletion rides the phase's final deploy (D-70 ordering gate) — the live privacy URL at /geohist/privacy.html stays 200 throughout."
  artifacts:
    - "scripts/a11y-audit.mjs + npm script audit:a11y — the repeatable automated AA gate (axe + Lighthouse vs a local http server), reports under reports/ (gitignored)"
    - ".gitignore += reports/ (battery output is generated evidence, recorded in SUMMARY, not committed)"
    - "A11y fixes (if the battery finds violations) — smallest-change edits to the offending markup/CSS"
    - "Deletions: GeoHist_Trivia_Privacy_Policy.html, .md, .pdf (D-70, gated on D-69 confirmation)"
  key_links:
    - "battery <-> local http server rooted at the repo root (absolute asset paths resolve — Pitfall 7)"
    - "battery thresholds <-> phase SC5 (axe zero critical/serious; Lighthouse accessibility >= 95) and <-> A11Y-01"
    - "manual battery <-> SC5 human half (keyboard nav, focus states, form labels, language-switch re-check) — honest close-out"
    - "D-70 deletion <-> D-69 owner confirmation — ordering gate; deletion must never precede it"
    - "GSC submission <-> live sitemap deploy — timing gate inside the D-69 checkpoint (Pitfall 10)"
  prohibitions:
    - statement: "An owner-executed battery must never be claimed as passed when it was not run — unexecuted batteries are recorded PENDING with human_judgment true and transferred, never asserted (honest close-out contract, Phase-04 precedent)."
      status: enforced
      verification: "the SUMMARY records every owner battery item with its actual reported status; any item without a reported result is marked PENDING/human_needed, never pass"
    - statement: "The superseded policy file must never be deleted before the owner confirms the Play Console field update — early deletion would 404 the URL Play reviewers may still reference (D-70)."
      status: enforced
      verification: "git log shows the deletion commit strictly after the checkpoint resume-signal that records the owner's confirmation; the deletion rides the phase's final deploy"
    - statement: "The audit battery must never scan pages via the file scheme — the battery serves the repo root over local http so absolute /css and /js paths resolve (Pitfall 7)."
      status: enforced
      verification: "scripts/a11y-audit.mjs contains an http server binding 127.0.0.1 and zero file-scheme URLs in its scan list"

# Spec-less probe fallback — row dispositions (never silent drops):
# A11Y-01 empty    | FLAGGED backstop -> truth 4: empty-form submit surfaces the inline keyed validation error without navigation (built Phase 04); provable only in the owner's manual battery -> human_needed if the verifier cannot confirm
# A11Y-01 encoding | FLAGGED backstop -> truth 4: after a language switch documentElement.lang matches the active language and the contrast/keyboard pass re-runs clean (I18N-04 shipped Phase 03; audit re-checks per SC5) -> owner battery, human_needed if abstained

---

# Plan 05-03: WCAG 2.1 AA Audit Gate + Trace Cleanup Checkpoint (A11Y-01, D-69, D-70)

**User story:** As the developer gating the Play submission, I get a scripted accessibility audit (axe + Lighthouse) over the real site, a human keyboard/focus/language-switch battery, and the two console steps done in the enforced order — so the AA audit passes as the explicit exit gate before the app goes live.

## Artifacts this phase produces

Full phase artifact list at the top of 05-01-PLAN.md. **This plan contributes:** `scripts/a11y-audit.mjs` + npm script `audit:a11y`, the gitignored `reports/` output, any a11y fixes the battery demands, and the gated deletion of the 3 superseded root policy files.

<execution_context>
@C:/Users/Familia/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/Familia/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-discovery-quality-screenshots-seo-json-ld-aa-audit/05-CONTEXT.md
@.planning/phases/05-discovery-quality-screenshots-seo-json-ld-aa-audit/05-RESEARCH.md
@.planning/phases/04-consent-gate-firebase-analytics-contact-form/04-02-SUMMARY.md
@css/base.css
@package.json
@geohist/index.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: scripts/a11y-audit.mjs — automated axe + Lighthouse battery, run it, fix violations</name>
  <files>scripts/a11y-audit.mjs, package.json, .gitignore, css/base.css, index.html, geohist/index.html, geohist/guide.html, geohist/contact.html, geohist/privacy.html</files>
  <read_first>
  - 05-RESEARCH.md — Pattern 7 (battery sketch + thresholds), Pitfall 7 (local http server, not the file scheme; Chrome path C:\Program Files (x86)\Google\Chrome\Application\chrome.exe for Lighthouse), Assumption A2 (CLI flags — check --help on first run)
  - 05-CONTEXT.md — the agent's Discretion (battery structure single-script vs per-page, report format under reports/); Specifics (thresholds: Lighthouse a11y >= 95, zero critical/serious axe)
  - package.json — where audit:a11y slots next to the validate chain
  - css/base.css lines 1-12 — the recorded contrast-pair table (verify, do not redesign)
  - js/consent.js surface note — the battery runs against pages that render with the consent banner hidden/persisted; axe must scan the shipped default state
  </read_first>
  <action>
(DevDeps @axe-core/cli 4.13.0 + lighthouse 13.4.1 are already installed from 05-01 — no install here.)

Write scripts/a11y-audit.mjs — single-script battery (discretion confirmed), zero new dependencies:
- Spawn an internal static file server (Node http module) bound to 127.0.0.1 on an ephemeral port, serving the REPO ROOT — so the pages' absolute /css and /js paths resolve (Pitfall 7).
- For each of the 5 content URLs (/ , /geohist/ , /geohist/guide.html , /geohist/contact.html , /geohist/privacy.html): run axe via the local @axe-core/cli binary against the local URL with its exit-on-violation flag (research A2 — verify the exact flag name against --help on first run; the pinned form is --exit), capturing JSON so impacts are machine-readable; run Lighthouse against the same URL with --only-categories=accessibility, headless Chrome flags, JSON output to reports/<page>-lh.json (set CHROME_PATH to the system Chrome if auto-detect fails, Pitfall 7). CLI invocation details = discretion (npx vs local .bin) — pick one and keep it deterministic.
- Gate per page: axe violations with impact critical or serious must number 0, AND the Lighthouse accessibility category score must be >= 95. Write reports/a11y-summary.json (per-page axe counts by impact + LH a11y score + pass/fail) and a human-readable console table. Exit non-zero if any gate fails.
- Add npm script audit:a11y = node scripts/a11y-audit.mjs. Add reports/ to .gitignore (generated evidence — results recorded in the SUMMARY, files not committed).
- RUN the battery. For every violation found: fix with the smallest possible change in the offending markup/CSS (the fix files are already in this plan's files list), respecting the established rules — keyed nodes carry plain text only, palette tokens stay untouched (contrast pairs pre-proven; if a contrast failure appears it is investigated as a rendering composition issue, not a token redesign), no behavioral rewrites. Re-run until the gate is green. Commit script + .gitignore + npm script + fixes.
  </action>
  <verify>
    <automated>npm run audit:a11y && node -e "const fs=require('fs');const s=JSON.parse(fs.readFileSync('reports/a11y-summary.json','utf8'));const pages=Array.isArray(s)?s:s.pages;if(!Array.isArray(pages)||pages.length!==5){console.error('FAIL summary pages '+pages.length);process.exit(1)};for(const p of pages){const ax=p.axe||p;if((ax.critical||0)!==0||(ax.serious||0)!==0){console.error('FAIL axe impacts on '+(p.url||p.page));process.exit(1)};const lh=p.lighthouse!==undefined?p.lighthouse:p.a11yScore;if(!(lh>=95)){console.error('FAIL LH a11y '+lh+' on '+(p.url||p.page));process.exit(1)}};const gi=fs.readFileSync('.gitignore','utf8');if(!/^reports\/?$/m.test(gi)){console.error('FAIL reports/ not gitignored');process.exit(1)};const css=fs.readFileSync('css/base.css','utf8');for(const tok of ['#1a1410','#241c14','#f0e6d2','#c9b89a','#d9a951','#8fc3bd','#2a1f12']){if(!css.includes(tok)){console.error('FAIL color token changed: '+tok);process.exit(1)}};console.log('PASS: battery green on 5 pages, thresholds met, palette tokens intact')"</automated>
  </verify>
  <acceptance_criteria>
  - npm run audit:a11y exits 0; reports/a11y-summary.json lists exactly 5 pages, each with axe critical=0 and serious=0 and a Lighthouse accessibility score >= 95
  - reports/ is gitignored; the summary's numbers are quoted in the plan SUMMARY (committed evidence trail)
  - All 7 palette color token values are still present in css/base.css (battery verified them; nothing redesigned)
  - Any violation fix commits name the axe rule ID it resolved (traceability)
  - The battery served pages over local http (script binds 127.0.0.1; no file-scheme scan URLs)
  </acceptance_criteria>
  <done>The automated half of A11Y-01 is scripted, repeatable, and green across all 5 content pages at the pinned thresholds — with any found violations fixed and re-proven.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: CHECKPOINT — owner manual AA battery (keyboard, focus, labels, language-switch re-check) + Rich Results Test</name>
  <files>(none — human verification; fixes land as ordinary commits if anything fails)</files>
  <read_first>
  - 05-RESEARCH.md — Pattern 7 (the manual side stays an owner battery per the honest close-out contract), Pitfall 8 (rich-result warnings vs failures)
  - .planning/phases/04-consent-gate-firebase-analytics-contact-form/04-02-SUMMARY.md — the honest close-out precedent (batteries recorded PENDING until the owner actually runs them)
  - geohist/contact.html — the form the keyboard/label pass exercises
  </read_first>
  <precondition>The wave-2 deploy is live (or the battery runs against the local server at http://127.0.0.1:<port> after npm run audit:a11y's server is available) — the owner needs the real rendered pages.</precondition>
  <action>Human verification task — see <instructions>. Executor resumes on the resume-signal reply; any failing item gets an executor fix + re-run of that item before the phase closes. If the owner does not run the battery, it is recorded PENDING (human_needed) and transferred to phase verification — never claimed passed.</action>
  <what-built>The complete site (all 5 content pages with screenshots, discovery metadata, JSON-LD) — deployed or locally servable; the automated AA battery from Task 1 is green. The owner now verifies the human-only half of the AA gate.</what-built>
  <how-to-verify>
  (1) Keyboard-only pass on /geohist/ — nav links, Play badge, FAQ details toggles, footer links incl. the Consent re-open link: every stop shows a visible focus indicator, tab order logical.
  (2) Contact form — keyboard reaches each labeled field; labels announced; EMPTY submit shows the inline keyed validation error without navigation (probe row A11Y-01 empty).
  (3) Language-switch re-check — switch to ES then pt-BR via the footer switcher: documentElement lang attribute updates; keyboard + contrast pass re-runs clean (probe row A11Y-01 encoding; SC5).
  (4) Rich Results Test (https://search.google.com/test/rich-results) on https://persano.github.io/geohist/ — SoftwareApplication detected, zero errors; the absent-optional-rating note is acceptable (Pitfall 8).
  (5) Optional: re-run npm run audit:a11y post-switch if any visual doubt remains (translated strings change length, not colors).
  </how-to-verify>
  <instructions>Pause for the owner. Battery (SC5's human half + SC4's rich-result check):
  (1) Keyboard-only pass on the deployed /geohist/: Tab through the nav links, Play badge, every FAQ details toggle (open/close with Enter/Space), gallery images (not interactive — confirm they impose no tab stop), footer links including the Consent re-open link; verify a visible focus indicator on every stop and a logical tab order.
  (2) Contact form: tab reaches each labeled field; the screen reader (or label-visibility check) announces every label; submitting EMPTY shows the inline keyed validation error without navigation or data loss (probe row A11Y-01 empty).
  (3) Language-switch re-check: switch to ES via the footer switcher, confirm documentElement lang attribute updates and the keyboard pass still holds on /geohist/ and the contact form; repeat for pt-BR (probe row A11Y-01 encoding; I18N-04 re-check per SC5).
  (4) Rich Results Test (https://search.google.com/test/rich-results) on https://persano.github.io/geohist/: SoftwareApplication detected with ZERO errors; note about the absent optional rating field is acceptable (Pitfall 8).
  (5) Optionally re-run npm run audit:a11y after the language switch if any visual doubt remains — translated strings change text length, not colors, so contrast should be unaffected.
  </instructions>
  <verification>
  Owner reports per item: keyboard nav (1) pass/fail per region; form labels + empty-submit (2); ES switch (3a); pt-BR switch (3b); Rich Results Test result + any warnings quoted (4). Items NOT run are reported as not run — they will be recorded PENDING, never passed.
  </verification>
  <resume-signal>Owner replies with per-item results (pass/fail/not-run). All run items pass -> resume. Any fail -> report which item + what was observed so the executor fixes and re-runs that item. No reply / not run -> record PENDING + human_needed and continue to Task 3 with the debt explicitly carried.</resume-signal>
  <acceptance_criteria>
  - Keyboard navigation reaches and operates every interactive element on /geohist/ with a visible focus state (owner-confirmed)
  - Every contact-form field has an announced label; empty submit shows the inline keyed validation error without navigation (owner-confirmed)
  - After ES and pt-BR switches: html lang attribute matches the active language and the keyboard pass holds (owner-confirmed) — or recorded PENDING as human_needed
  - Rich Results Test shows the SoftwareApplication with zero errors (owner-confirmed; rating-note acceptable) — or recorded PENDING as human_needed
  - The SUMMARY records each item's actual reported status; zero unexecuted items claimed passed
  </acceptance_criteria>
  <done>The human half of the AA gate is executed and reported by the owner — or its debt is explicitly carried as human_needed with no false claims (honest close-out).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: CHECKPOINT — D-69 console steps (owner) then D-70 deletion + final deploy + smoke</name>
  <files>GeoHist_Trivia_Privacy_Policy.html, GeoHist_Trivia_Privacy_Policy.md, GeoHist_Trivia_Privacy_Policy.pdf</files>
  <read_first>
  - 05-CONTEXT.md — D-69 (both console steps, order matters) and D-70 (deletion gated on confirmation; rides the final deploy)
  - 05-RESEARCH.md — Pitfall 10 (submit timing: only after the deploy containing sitemap.xml is live); Runtime State Inventory (superseded files row)
  - STATE.md §Blockers/Concerns — the two Phase-01 traces this checkpoint closes
  - scripts/smoke-check.sh — the regression battery re-run after the final deploy
  </read_first>
  <precondition>The wave-2 deploy is live (sitemap.xml reachable at https://persano.github.io/sitemap.xml) — the GSC submission half of D-69 requires it (Pitfall 10). If not yet deployed, halt until the orchestrator's wave-2 push has gone green.</precondition>
  <action>Human checkpoint task — see <instructions>. After the owner's confirmation lands (resume-signal), the EXECUTOR completes the code side: git rm the three superseded root policy files (GeoHist_Trivia_Privacy_Policy.html + its .md and .pdf siblings — one family, deleted together; the .html is the Play-reviewer-reachable one the ordering gate protects), commit the deletion, then the final orchestrator push (or executor push if permitted). After that deploy is green: run npm run validate and bash scripts/smoke-check.sh, plus a one-time manual check that the OLD policy URL now returns 404 while /geohist/privacy.html still returns 200. This is the phase's final deploy (D-70).</action>
  <what-built>The deployed site with sitemap.xml live at https://persano.github.io/sitemap.xml (wave-2 deploy) — everything the two console steps consume is reachable. Code side pending after confirmation: deletion of the 3 superseded root policy files + the phase's final deploy.</what-built>
  <how-to-verify>
  Owner (in order, D-69): (1) Play Console -> App content -> Privacy policy shows https://persano.github.io/geohist/privacy.html after save. (2) Search Console -> Sitemaps -> submit https://persano.github.io/sitemap.xml; GSC reports the submission received.
  Executor after confirmation (D-70): deletion commit exists; after the final deploy — /geohist/privacy.html returns 200, the old root policy URL returns 404, npm run validate exits 0, smoke-check.sh prints ALL PASS.
  </how-to-verify>
  <instructions>Pause for the owner. Console steps IN THIS ORDER (D-69):
  (1) Play Console -> App content -> Privacy policy: set the URL to https://persano.github.io/geohist/privacy.html and save. Confirm the field shows the new value.
  (2) Search Console (property persano.github.io) -> Sitemaps: submit https://persano.github.io/sitemap.xml. Confirm GSC reports the submission as received/success (first-processing status is fine). This step must come AFTER the wave-2 deploy is live — sitemap.xml must be fetchable (Pitfall 10).
  Order matters: the Play field flip is what makes the old root policy URL safe to remove (D-70) — do not reverse the steps.
  </instructions>
  <verification>
  Owner reports: Play Console field value shown after save (exact URL), and the GSC sitemap submission status. Any failure -> report which step + console error so the executor can assist.
  </verification>
  <resume-signal>Owner replies confirming both steps (field updated yes/no + shown URL; sitemap submitted yes/no + GSC status). Both confirmed -> executor proceeds with the D-70 deletion + final deploy + smoke. Not confirmed -> the deletion does NOT happen; record the checkpoint as open and the trace as still-open.</resume-signal>
  <acceptance_criteria>
  - The three superseded policy files are gone from the repo (git status/log shows the deletion commit AFTER the owner's confirmation was recorded)
  - Live checks after the final deploy: https://persano.github.io/geohist/privacy.html returns 200; https://persano.github.io/GeoHist_Trivia_Privacy_Policy.html returns 404; npm run validate exits 0; scripts/smoke-check.sh prints ALL PASS
  - The Play Console privacy-URL field reads https://persano.github.io/geohist/privacy.html and the GSC sitemap submission is recorded — both owner-confirmed, in the D-69 order
  - STATE.md's two Phase-5 traces (Play field update, superseded-file deletion) are closed in the SUMMARY
  </acceptance_criteria>
  <done>Phase exit gate complete: automated + human AA batteries accounted for, console steps done in the enforced order, superseded policy files removed in the final deploy without ever breaking the Play-reviewer URL contract — D-69/D-70 honored, both Phase-01 traces closed.</done>
</task>

</tasks>

<verification>
- npm run audit:a11y exits 0 with 5/5 pages at thresholds (axe zero critical/serious; LH a11y >= 95) — reports/a11y-summary.json + SUMMARY quotes
- npm run validate green after every fix commit and after the final deploy
- Owner battery items each carry their actual reported status in the SUMMARY — PENDING + human_needed for anything unexecuted, zero false claims
- D-69 evidence recorded in order (Play field -> GSC submit); D-70 deletion commit strictly after the confirmation
- Post-final-deploy: /geohist/privacy.html 200, old policy URL 404, smoke ALL PASS
</verification>

<success_criteria>
- A11Y-01: scripted axe + Lighthouse battery green at the pinned thresholds on all 5 content pages; manual keyboard/focus/labels/language-switch battery executed by the owner (or honestly carried as human_needed) — the AA audit passes as the explicit gate
- SC4 adjudicated: Rich Results Test result recorded (SoftwareApplication, zero errors, rating-note acceptable)
- D-65: sitemap submitted in Search Console after the deploy, inside the checkpoint
- D-69/D-70: both console steps confirmed in order; superseded policy files deleted only after confirmation, riding the final deploy
- Probe rows A11Y-01 empty + encoding: surfaced as backstop/human-needed items in the battery — no silent drops
</success_criteria>

<output>
Create `.planning/phases/05-discovery-quality-screenshots-seo-json-ld-aa-audit/05-03-SUMMARY.md` when done
</output>
