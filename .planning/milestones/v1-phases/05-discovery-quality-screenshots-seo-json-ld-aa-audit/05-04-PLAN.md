---
phase: 05-discovery-quality-screenshots-seo-json-ld-aa-audit
plan: 04
type: execute
wave: 1
depends_on: []
gap_closure: true
gap_ids: [G-05-1]
files_modified:
  - geohist/screenshots/screenshot.menu.webp
  - geohist/screenshots/screenshot.map.webp
  - geohist/screenshots/screenshot.flags.webp
  - geohist/screenshots/screenshot.timeline.webp
autonomous: false
requirements: [LNDG-03]
user_setup:
  - service: android-device
    why: "Ad-free activation and the 4 recaptures happen on the owner's physical phone — the agent can drive adb but cannot purchase IAP, navigate the app, or judge framing"
    dashboard_config:
      - task: "Plug the phone in, accept the USB-debugging prompt, keep the screen unlocked; owner activates the ad-free state (pick the mechanism that works with the installed build) and navigates to each requested screen on the agent's cue"
        location: "On the phone; confirm with c:\\platform-tools\\adb.exe devices"
estimate:
  tokens: 48000
  raw_tokens: 22000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The live gallery at https://persano.github.io/geohist/ shows 4 real captures with NO AdMob test banner ('Anuncio de prueba' / test-ad label) on any tile — G-05-1 closed with owner visual confirmation."
    - "geohist/screenshots/ still contains exactly the 4 pinned WebPs (screenshot.menu/map/flags/timeline.webp) at 720x1600, each under 300 KB — regenerated from banner-free raws via the existing converter, so 05-02's JSON-LD screenshot array stays valid without any HTML change."
    - "Zero code change: git history for this plan touches ONLY the 4 WebP assets — geohist/index.html, sitemap.xml, JSON-LD, and both dictionaries are byte-identical to the pre-fix state."
    - "The 4 raw PNGs live in a fresh v2 temp directory outside the repo (originals in geohist-captures left untouched); no raw capture is ever committed."
    - "Post-deploy smoke: the 4 live WebP URLs return 200 and are byte-identical to the committed files; CI validate + Pages deploy green on the push."
    - "Honest close-out: every owner-gated step (ad-free activation, per-shot approval, live visual re-check) is confirmed by the owner in the execution conversation — never claimed unexecuted."
  artifacts:
    - "C:/Users/Familia/AppData/Local/Temp/opencode/geohist-captures-v2/raw-screenshot.{menu,map,flags,timeline}.png — 4 banner-free owner-approved 720x1600 raws (outside repo, never committed)"
    - "geohist/screenshots/screenshot.menu.webp / screenshot.map.webp / screenshot.flags.webp / screenshot.timeline.webp — regenerated from the v2 raws, committed, deployed"
    - ".planning/phases/05-discovery-quality-screenshots-seo-json-ld-aa-audit/05-04-SUMMARY.md — gap-closure evidence record"
  key_links:
    - "owner ad-free device state (isAdFree=true hides banner app-wide per MainActivity.kt:719) -> banner-free raws -> scripts/make-webp.mjs (CROPS empty, D-53 approved-as-framed) -> pinned WebP paths -> live gallery"
    - "pinned screenshot filenames preserved -> 05-02's JSON-LD screenshot array and all keyed captions/alts keep working with zero markup changes (G-05-1 is asset-only)"
    - "committed assets -> orchestrator-controlled push to main -> Pages deploy -> byte-identical live WebPs -> owner visual re-check closes the UAT gap"
  prohibitions:
    - statement: "No code or config files may change in this plan — the root cause is capture-time device state, not site code (diagnosis confirmed site pipeline correct); any diff beyond the 4 WebPs is a scope violation."
      status: enforced
      verification: "git status --porcelain after Task 2 shows exactly 4 modified paths, all under geohist/screenshots/screenshot.*.webp"
    - statement: "Raw PNGs must never enter the repo — they live only in the temp v2 directory."
      status: enforced
      verification: "git status shows no *.png paths; capture dir is outside the repo tree"

# Gap-closure source audit: UAT Gaps section lists exactly one gap, G-05-1 (test 1, major).
# Its three `missing` items map 1:1: owner ad-free activation + recapture (Task 1), site-side
# reconvert + commit (Task 2), deploy + post-deploy smoke + owner re-check (Task 3).
# UAT tests 2-7 passed and are untouched; test 8 (ES/pt-BR switch re-check) remains owner-pending
# in the UAT ledger — deliberately out of this plan's scope (not a diagnosed gap).

---

# Plan 05-04: Gap Closure G-05-1 — Ad-Free Recapture + Asset-Only Redeploy (LNDG-03)

**User story:** As a prospective player visiting the landing page, I see clean screenshots of the game — with no "Anuncio de prueba" test-ad banners baked into the screens — so the gallery looks professional instead of broken.

**Gap linkage:** This plan closes UAT gap G-05-1 (test 1, major): all 4 gallery captures show AdMob TEST banners. Root cause (diagnosed, `.planning/debug/admob-test-ads-in-gallery-captures.md`): raws were captured on a DEBUG build (which deterministically serves Google's public test banner unit per AdBanner.kt:24,27) on a non-ad-free device (isAdFree=false); the banner was baked into the raw PNGs and converted 1:1 to the deployed WebPs. Site code is correct — the fix is device-state + asset replacement. Zero HTML/sitemap/JSON changes expected.

**Two-stage shape (per diagnosis):** Stage 1 = owner device actions (checkpoint). Stage 2 = site-side asset replacement + deploy (auto tasks, gated on owner confirmation).

<execution_context>
@C:/Users/Familia/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/Familia/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-discovery-quality-screenshots-seo-json-ld-aa-audit/05-UAT.md
@.planning/debug/admob-test-ads-in-gallery-captures.md
@.planning/phases/05-discovery-quality-screenshots-seo-json-ld-aa-audit/05-01-SUMMARY.md
@scripts/make-webp.mjs
@package.json
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 1: CHECKPOINT — Owner: activate ad-free state + recapture the 4 screens banner-free (G-05-1 device stage)</name>
  <files>(no repo files — raw captures land in a v2 temp dir OUTSIDE the repo)</files>
  <read_first>
  - .planning/debug/admob-test-ads-in-gallery-captures.md — root cause, BillingRepository/SettingsScreen ad-free mechanics, blind spot on billing simulation with an adb-installed debug build, fix rationale
  - 05-UAT.md Gaps G-05-1 — the three `missing` items this plan implements
  - 05-01-PLAN.md Task 1 — the ADB capture command pattern (binary-safe redirect, cmd /c wrapper) and the D-52/D-53 framing contract (4 screens, per-shot approval, full panel)
  </read_first>
  <precondition>An Android phone is plugged in over USB with debugging authorized (adb devices lists a device), GeoHist Trivia is installed on it, and the owner is at the desk to activate the ad-free state and approve captures. If adb lists no device, halt and ask the owner to replug/reauthorize.</precondition>
  <action>Human action task — see <instructions>. The executor drives every adb command; the owner performs the ad-free activation, navigates the app, and approves each shot. No repo files change in this task.</action>
  <instructions>Pause for the owner. Flow:
  (1) Confirm the device: run c:\platform-tools\adb.exe devices — a device must be listed. Record adb shell wm size and confirm the panel is still 720x1600 (same as the original captures — required for identical framing; if the panel changed, stop and surface to the orchestrator).
  (2) Create the raw directory C:\Users\Familia\AppData\Local\Temp\opencode\geohist-captures-v2 (sibling of the original geohist-captures dir — the contaminated originals stay untouched as evidence). Raws are never committed.
  (3) OWNER: activate the ad-free state on the device. Pick whichever mechanism works with the installed build (diagnosis blind spot: Play license-testing purchases may not complete on an adb-installed debug build — do not force one path):
      - Option A: in-app 'Quitar anuncios' IAP purchase with the Play license-testing account (no charge) — try first.
      - Option B: install the internal-testing-track (Play-distributed) build signed in with the license tester account and complete the purchase there.
      - Option C: any equivalent ad-free state the owner can produce on this install.
    On-device verification that ad-free is active: open the app Settings — the 'Quitar anuncios' button must be GONE (it renders only while not ad-free). Also spot-check one gameplay screen: no banner at the bottom.
  (4) Pre-capture gate per screen: for each of the 4 screens in order — main menu, map/city mode, flags mode, timeline mode (D-52) — the owner navigates there and both owner and executor visually confirm NO test-ad banner is rendered before any capture. If any screen still shows the banner, ad-free is not active: stop, resolve step 3, re-verify, only then continue.
  (5) Capture each screen with the binary-safe redirect (05-01 Pitfall 2 pattern): cmd /c "adb exec-out screencap -p > C:\Users\Familia\AppData\Local\Temp\opencode\geohist-captures-v2\raw-screenshot.<name>.png" where name is menu / map / flags / timeline. Framing = identical to the originals: full panel, no crop (D-53 approved-as-framed; make-webp.mjs CROPS map stays empty). Wait for any purchase confirmation toast/dialog to fully clear before capturing.
  (6) After each capture: verify the file (PNG magic, non-trivial size, 720x1600 via the IHDR check in verify), the executor opens the PNG (image view) to confirm it is banner-free, and the owner approves the framing. A rejected shot -> owner re-frames, that screen's capture re-runs only.
  </instructions>
  <verify>
    <automated>node -e "const fs=require('fs');const dir='C:/Users/Familia/AppData/Local/Temp/opencode/geohist-captures-v2';const names=['menu','map','flags','timeline'];for(const n of names){const p=dir+'/raw-screenshot.'+n+'.png';if(!fs.existsSync(p)){console.error('FAIL missing raw capture: '+p);process.exit(1)};const b=fs.readFileSync(p);if(b.length<100*1024){console.error('FAIL suspiciously small ('+b.length+' bytes): '+p);process.exit(1)};if(b[0]!==0x89||b[1]!==0x50||b[2]!==0x4e||b[3]!==0x47){console.error('FAIL not a PNG: '+p);process.exit(1)};const w=b.readUInt32BE(16),h=b.readUInt32BE(20);if(w!==720||h!==1600){console.error('FAIL dims '+w+'x'+h+' (expected 720x1600): '+p);process.exit(1)}}console.log('PASS: 4 raw PNG captures present, valid magic, 720x1600, non-trivial size')"</automated>
  </verify>
  <verification>
  Owner confirms: ad-free state active (Settings 'Quitar anuncios' button gone), and each of the 4 shots individually approved as banner-free with identical framing to the originals. The executor's own image-view check of each PNG is recorded in the execution conversation (banner text absent on all 4).
  </verification>
  <resume-signal>All 4 raws captured, verified banner-free by owner AND executor, owner-approved per shot -> resume to Task 2. Banner still visible on any screen -> ad-free not active: return to step 3, try another mechanism, recapture. Device missing -> replug/reauthorize and retry.</resume-signal>
  <acceptance_criteria>
  - All four raw files exist: raw-screenshot.menu.png, raw-screenshot.map.png, raw-screenshot.flags.png, raw-screenshot.timeline.png in C:\Users\Familia\AppData\Local\Temp\opencode\geohist-captures-v2
  - Each file starts with the PNG magic bytes, is larger than 100 KB, and measures exactly 720x1600
  - The owner confirmed the ad-free state is active on the device (Settings shows no purchase button; screens show no banner)
  - The executor visually inspected each of the 4 PNGs and found no test-ad banner, and the owner approved each shot individually (recorded in the execution conversation)
  - The original contaminated raws in geohist-captures are untouched, and no raw capture was committed (git status clean apart from planning files)
  </acceptance_criteria>
  <done>Four owner-approved banner-free raw device captures exist in the v2 temp directory, one per feature group, same framing as the originals, ready for conversion.</done>
</task>

<task type="auto">
  <name>Task 2: Reconvert v2 raws -> 4 pinned WebPs, verify asset-only diff, commit (G-05-1 site stage)</name>
  <files>geohist/screenshots/screenshot.menu.webp, geohist/screenshots/screenshot.map.webp, geohist/screenshots/screenshot.flags.webp, geohist/screenshots/screenshot.timeline.webp</files>
  <read_first>
  - scripts/make-webp.mjs — converter contract: takes raw dir as CLI arg, consumes the 4 pinned raw names, quality 82, prints pixel dimensions + byte size per output, exits non-zero on missing/corrupt input; CROPS map intentionally empty per D-53
  - 05-01-SUMMARY.md — conventions: quality 82 no downscale (28-68 KB outputs), NODE_OPTIONS=--dns-result-order=ipv4first needed for npm NETWORK operations on this machine (npm run itself needs no network; plain node scripts/make-webp.mjs is the fallback if npm run misbehaves)
  - package.json — npm script convert:screenshots = node scripts/make-webp.mjs
  </read_first>
  <action>
Run the existing conversion pipeline against the v2 capture directory: npm run convert:screenshots -- C:/Users/Familia/AppData/Local/Temp/opencode/geohist-captures-v2 — this regenerates the 4 pinned WebP paths (screenshot.menu/map/flags/timeline.webp) from the clean raws at quality 82, 1:1 (CROPS empty per D-53 approved-as-framed). If npm run stalls or fails on this machine, invoke node scripts/make-webp.mjs directly with the same argument.

Confirm from the converter's printed output that all four outputs are 720x1600 and lean; no code, markup, or dictionary edits are permitted anywhere (the gap is asset-only — the converter, gallery markup, JSON-LD, sitemap, and both dictionaries from 05-01/05-02 are already correct and must remain byte-identical).

Run the automated verify below. Only after it passes, commit the 4 WebPs via the gsd-tools commit handler (Phase-02 precedent: direct git commit is denied by environment permission rules) with message: fix(05-04): replace gallery captures with ad-free recaptures (banner-free, same framing). Commit ONLY the 4 WebP paths. If the verify reports any unexpected modified path, stop and surface to the orchestrator instead of committing.
  </action>
  <verify>
    <automated>npm run convert:screenshots -- C:/Users/Familia/AppData/Local/Temp/opencode/geohist-captures-v2 && node -e "const fs=require('fs');const names=['menu','map','flags','timeline'];let webpCount=0;for(const f of fs.readdirSync('geohist/screenshots')){if(f.endsWith('.webp'))webpCount++};if(webpCount!==4){console.error('FAIL expected exactly 4 webp files, found '+webpCount);process.exit(1)};import('sharp').then(async({default:sharp})=>{for(const n of names){const p='geohist/screenshots/screenshot.'+n+'.webp';const b=fs.readFileSync(p);if(b.slice(0,4).toString()!=='RIFF'||b.slice(8,12).toString()!=='WEBP'){console.error('FAIL not WebP: '+p);process.exit(1)};if(b.length>300*1024){console.error('FAIL too heavy ('+b.length+' bytes): '+p);process.exit(1)};const m=await sharp(p).metadata();if(m.width!==720||m.height!==1600){console.error('FAIL dims '+m.width+'x'+m.height+': '+p);process.exit(1)}};console.log('PASS: 4 WebPs regenerated, 720x1600, valid+lean')})" && node -e "const {execSync}=require('child_process');const lines=execSync('git status --porcelain',{encoding:'utf8'}).trim().split(/\r?\n/).filter(Boolean);const webps=lines.filter(l=>l.includes('geohist/screenshots/screenshot.'));const other=lines.filter(l=>!l.includes('geohist/screenshots/screenshot.'));if(other.length){console.error('FAIL diff is not asset-only: '+other.join(' | '));process.exit(1)};if(webps.length!==4){console.error('FAIL expected 4 webp entries, got '+webps.length);process.exit(1)};console.log('PASS: working-tree diff is exactly the 4 gallery WebPs')"</automated>
  </verify>
  <acceptance_criteria>
  - The converter ran clean against the v2 directory and printed 720x1600 dimensions for all four outputs
  - Each of the 4 WebPs is RIFF/WEBP, exactly 720x1600, under 300 KB; geohist/screenshots/ holds exactly those 4 files
  - git status --porcelain shows exactly 4 modified paths, all geohist/screenshots/screenshot.*.webp — no HTML, sitemap, JSON-LD, dictionary, package, or planning-file changes staged with the fix
  - The 4 WebPs are committed (commit contains only those 4 paths); working tree clean after commit
  </acceptance_criteria>
  <done>The 4 pinned gallery WebPs are regenerated from ad-free raws and committed as an asset-only change, ready for deploy.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 3: CHECKPOINT — Orchestrator-controlled push + post-deploy smoke + owner live gallery re-check (G-05-1 close-out)</name>
  <files>(no repo files)</files>
  <read_first>
  - STATE.md Accumulated Context, Phase-01 decisions — deploy pipeline: push-to-main is the only deploy action; the orchestrator (not the executor) performed the single controlled push
  - 05-01-SUMMARY.md / 05-02-SUMMARY.md — smoke battery precedent
  </read_first>
  <precondition>Task 2 committed and the working tree is clean; origin is the persano.github.io GitHub repo with the Pages validate+deploy workflow on main.</precondition>
  <action>Human action task — see <instructions>. The executor prepares everything, the orchestrator performs the push, then the executor runs the smoke battery and the owner closes the gap visually.</action>
  <instructions>
  (1) Confirm the tree is clean and gather the commit hash(es) from Task 2.
  (2) PAUSE — request the ORCHESTRATOR perform the single controlled push to main (Phase-01 pattern: executor push is denied by environment permission rules). Hand over the commit hash. Do not push directly, do not force, do not touch other branches.
  (3) After the push, monitor CI (gh run list/watch for the repo): the validate job must pass and the Pages deploy must complete. A red run -> surface the failure to the orchestrator, do not self-fix workflow files.
  (4) Once deployed, run the automated smoke below: it fetches each live WebP URL and byte-compares it against the committed local file — this both proves deploy success and catches stale CDN copies.
  (5) Executor visual check: download and view the live WebPs (or trust the byte-identity with the already-inspected local files) — confirm no test-ad banner on any tile.
  (6) OWNER visual re-check: the owner opens https://persano.github.io/geohist/ (hard-refresh / incognito to bypass cache) and confirms the gallery shows 4 clean captures — this is the UAT test 1 re-check and formally closes G-05-1.
  </instructions>
  <verify>
    <automated>node -e "(async()=>{const fs=require('fs');const names=['menu','map','flags','timeline'];for(const n of names){const local=fs.readFileSync('geohist/screenshots/screenshot.'+n+'.webp');const r=await fetch('https://persano.github.io/geohist/screenshots/screenshot.'+n+'.webp');if(r.status!==200){console.error('FAIL HTTP '+r.status+' on screenshot.'+n+'.webp');process.exit(1)};const buf=Buffer.from(await r.arrayBuffer());if(!buf.equals(local)){console.error('FAIL live bytes differ from committed file for '+n+' (stale deploy/CDN — recheck after cache TTL)');process.exit(1)}}console.log('PASS: live site serves the 4 new ad-free WebPs byte-identical to the committed files')})()"</automated>
  </verify>
  <verification>
  Orchestrator executed the push; CI validate + Pages deploy green; automated smoke PASS (4 live URLs 200 + byte-identical); owner confirms the live gallery is clean. Record the CI run URL and the owner confirmation in the execution conversation.
  </verification>
  <resume-signal>Owner confirms the live gallery shows 4 clean ad-free captures -> plan complete; write 05-04-SUMMARY.md with the gap-closure evidence (commit hash, CI run URL, smoke PASS, owner confirmation). Stale CDN bytes -> wait for cache TTL and re-run the smoke; persistent mismatch -> surface to orchestrator.</resume-signal>
  <acceptance_criteria>
  - The push to main was executed by the orchestrator (executor did not push) and contained only the Task-2 asset commit
  - CI validate + Pages deploy green (run URL recorded)
  - All 4 live WebP URLs return 200 and are byte-identical to the committed local files
  - The owner visually confirmed the live gallery at https://persano.github.io/geohist/ shows 4 clean captures with no test-ad banners (UAT test 1 re-check)
  - No HTML, sitemap, JSON-LD, or dictionary changes were deployed (asset-only deploy)
  </acceptance_criteria>
  <done>The live gallery shows the 4 ad-free recaptures byte-identical to the committed assets; G-05-1 is closed with automated + owner confirmation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| device capture -> repo assets | Owner-supplied screenshot pixels cross into the committed site assets (the exact boundary where the original defect entered) |
| repo -> GitHub Pages CDN | Committed WebPs deploy verbatim to the live site |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-04-01 | Tampering | gallery WebP assets | low | mitigate | byte-level verify (magic, dims 720x1600, size cap) + executor image inspection + owner visual confirmation at capture, pre-commit, and post-deploy |
| T-05-04-02 | Repudiation | owner-gated steps | low | mitigate | honest close-out contract: ad-free activation, per-shot approval, and live re-check each recorded as explicit owner confirmations in the execution conversation and SUMMARY |
| T-05-04-SC | Tampering | npm installs | high | mitigate | no package installs in this plan (pipeline and devDeps already pinned and shipped in 05-01) — nothing new to vet |
</threat_model>

<verification>
- Task 1 verify: 4 v2 raw PNGs present, PNG magic, 720x1600, >100 KB each (automated) + owner/executor banner-free visual confirmations
- Task 2 verify: 4 regenerated WebPs RIFF/WEBP, 720x1600, <300 KB; git diff exactly the 4 WebP paths (automated) + asset-only commit
- Task 3 verify: 4 live URLs 200 + byte-identical to committed files (automated) + CI green + owner live confirmation
- UAT reconciliation: G-05-1 resolvable as fixed via gap_ids frontmatter; UAT tests 2-7 unaffected; test 8 (ES/pt-BR re-check) remains an owner item in the UAT ledger, untouched by this plan
</verification>

<success_criteria>
- G-05-1 closed: https://persano.github.io/geohist/ gallery shows the 4 recaptured screens with zero AdMob test banners, confirmed by automated byte checks AND the owner
- LNDG-03 remains satisfied with clean assets: same 4 pinned filenames, 720x1600, WebP, lazy-loaded gallery — no markup or dictionary changes needed
- Asset-only discipline held: the plan's entire footprint is 4 WebP files across exactly one commit plus planning docs
- Deploy path honored: single orchestrator-controlled push, CI green, byte-identical live assets
</success_criteria>

<output>
Create `.planning/phases/05-discovery-quality-screenshots-seo-json-ld-aa-audit/05-04-SUMMARY.md` when done
</output>
