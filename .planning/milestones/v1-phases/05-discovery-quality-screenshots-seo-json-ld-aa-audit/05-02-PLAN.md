---
phase: 05-discovery-quality-screenshots-seo-json-ld-aa-audit
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - package.json
  - scripts/og-image.mjs
  - geohist/og-image.png
  - geohist/index.html
  - index.html
  - geohist/guide.html
  - geohist/contact.html
  - geohist/privacy.html
  - sitemap.xml
  - robots.txt
  - scripts/smoke-check.sh
autonomous: true
requirements: [SEO-01, SEO-02, SEO-03, SEO-04]
estimate:
  tokens: 26000
  raw_tokens: 26000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "scripts/og-image.mjs regenerates geohist/og-image.png at exactly 1200x630 (D-57/D-58): canvas base #1a1410 with parchment/gold accents (#f0e6d2, #d9a951) taken from the css/base.css tokens, Georgia display-stack text 'GeoHist Trivia' + the EN hero tagline, geohist/icon.png composited — regenerable in-repo, no design tool."
    - "All 5 content pages carry the full discovery block: link rel=canonical (directory form for the two section roots, absolute against https://persano.github.io), og:title + og:description copied byte-identical from that page's raw EN title/meta description, og:type website, og:url mirroring canonical exactly, og:image + og:image:width 1200 + og:image:height 630, twitter:card summary_large_image + twitter:title/description/image — og tags use property=, twitter tags use name= (D-66/D-67/D-68; Pattern 1; Pitfall 9)."
    - "geohist/index.html additionally carries the D-61 JSON-LD block in its corrected rich-result form (flagged deviation, see below): @type array ['SoftwareApplication','MobileApplication'], name, operatingSystem ANDROID, applicationCategory GameApplication, description byte-identical to the page's meta description (D-62 single source), url, image (icon), screenshot array of the 4 absolute WebP URLs from 05-01, author Person Santiago David Postorivo, sameAs the Play package URL, offers price 0 USD — and NO rating field of any kind (D-61 ban; v2 SEO-05)."
    - "The JSON-LD block exists on /geohist/ ONLY — the hub and all other pages stay schema-clean (D-60: no WebSite/Person schema anywhere)."
    - "404.html requires no edit this phase: it already carries noindex + title and gains no canonical/OG (D-66)."
    - "sitemap.xml at the repo root lists exactly 5 URLs (/, /geohist/, /geohist/guide.html, /geohist/contact.html, /geohist/privacy.html) with no date elements (D-64) and no alternate-language links of any kind (single-URL i18n — alternates are out of scope per REQUIREMENTS.md); robots.txt at the repo root is exactly allow-all plus the sitemap line (SEO-03, D-63)."
    - "Every canonical URL equals its og:url byte-for-byte, and every sitemap loc matches the page's canonical form — one URL form per page (D-68; Pitfall 5)."
    - statement: "Rich Results Test on the live /geohist/ detects the SoftwareApplication with zero errors — a manual/owner-side check (no CI API); notes about the absent optional rating field are expected and acceptable (Pitfall 8)."
      verification: backstop
  artifacts:
    - "geohist/og-image.png — 1200x630 generated PNG (D-59), referenced by all 5 pages"
    - "scripts/og-image.mjs — sharp composition (SVG background + text layer + icon composite), npm script og:image"
    - "Head discovery blocks on index.html, geohist/index.html, geohist/guide.html, geohist/contact.html, geohist/privacy.html (canonical + OG + Twitter each; JSON-LD on geohist/index.html only)"
    - "sitemap.xml (5 URLs, no date elements) + robots.txt (allow-all + Sitemap line) at the repo root"
    - "scripts/smoke-check.sh URL list += /geohist/, /geohist/guide.html, /sitemap.xml, /robots.txt, /geohist/og-image.png"
  key_links:
    - "og-image.mjs <-> css/base.css design tokens (colors + display font quoted verbatim, D-57)"
    - "JSON-LD description value <-> geohist meta description content attribute — one string, two surfaces, byte-identical (D-62/Pitfall 6)"
    - "JSON-LD screenshot array <-> 05-01's committed /geohist/screenshots/*.webp (pinned names)"
    - "canonical <-> og:url <-> sitemap loc — one URL form per page (D-68/Pitfall 5)"
    - "heads + og-image + sitemap land in the SAME deploy — live URL checks (linkinator on og:image, smoke 200s) go green only after the wave-2 push"
  prohibitions:
    - statement: "No rating field may appear in the JSON-LD until real Play ratings exist (banned; v2 SEO-05) — Rich Results Test may note its absence, which is acceptable, but the field must never be fabricated."
      status: enforced
      verification: "the parsed JSON-LD object from geohist/index.html contains zero rating-related keys; grep of the banned field name in geohist/index.html returns 0"
    - statement: "No canonical or OG markup on 404.html — an error page carries noindex + title only (D-66)."
      status: enforced
      verification: "404.html contains no rel=canonical link and no og:/twitter: meta tags after this plan (git diff on 404.html is empty)"
    - statement: "Twitter card tags must use the name attribute and og tags the property attribute — a property-attributed twitter tag is ignored by the X scraper (Pitfall 9)."
      status: enforced
      verification: "count of meta tags matching name=twitter: equals the count of all twitter: tags per page; count of property=twitter: is 0"
    - statement: "The JSON-LD is static EN markup and must never be routed through the i18n engine — no data-i18n attributes on or inside the ld+json script (D-62)."
      status: enforced
      verification: "the script block carries no data-i18n attributes; keycheck gate stays green (it counts keyed nodes only)"

# Spec-less probe fallback — row dispositions (never silent drops):
# SEO-01 adjacency | RESOLVED explicit -> truth 7: one URL form per page (canonical == og:url byte-equal, sitemap loc matches, directory form for roots — D-68)
# SEO-01 empty     | RESOLVED explicit -> truth 2: 5/5 content pages carry non-empty title + description + canonical; 404 keeps noindex + title, nothing more (D-66)
# SEO-01 ordering  | FLAGGED assumption (backstop): head-block tag order follows research Pattern 1 (canonical, og:*, og:image dims, twitter:*); not load-bearing for crawlers — html-validate green is the checkable surface
# SEO-02 adjacency | RESOLVED explicit -> truth 2 + D-59: one site-wide og-image.png for all 5 pages (no per-section variants), absolute URL + declared 1200/630 dims
# SEO-03 adjacency | RESOLVED explicit -> truth 6: sitemap = exactly the 5 canonical URLs, no duplicates, directory forms matching canonicals; 404 / GSC verification file / app-ads.txt excluded (D-63)
# SEO-03 empty     | RESOLVED explicit -> truth 6: sitemap has exactly 5 url entries (never zero/empty); robots.txt is exactly 3 content lines (allow-all + Sitemap)
# SEO-03 ordering  | FLAGGED assumption (backstop): sitemap spec permits any URL order; pinned order hub -> geohist -> guide -> contact -> privacy for diff stability, not SEO
# SEO-04 boundary  | RESOLVED explicit -> truth 3: offers.price is exactly the string 0 with priceCurrency USD; the absent-optional-field note in Rich Results Test is acceptable (Pitfall 8) — the test itself is owner-side (backstop truth 8)
# SEO-04 empty     | RESOLVED explicit -> truth 3: the parsed JSON-LD object has every D-61 field present and non-empty
# SEO-04 encoding  | RESOLVED explicit -> truth 3: JSON-LD description is byte-identical to the meta description content attribute (D-62 single source)
# SEO-04 precision | RESOLVED explicit -> truth 3: price is a string literal, never a computed/numeric value — no rounding surface exists

# FLAGGED DECISION DEVIATION (recorded per research Pitfall 1, planner authority: evidence-backed correction):
# D-61 pins @type GameApplication. Google's software-app structured-data doc (fetched 2026-09-03, page updated 2025-12-10)
# supports @type values SoftwareApplication / MobileApplication / WebApplication (+ VideoGame co-typed);
# GameApplication is a supported applicationCategory VALUE, and https://schema.org/GameApplication returns 404 —
# the type does not exist. D-61's applicationCategory "GAME" also differs from the documented value "GameApplication".
# Correction (preserves every other D-61 field): @type ["SoftwareApplication","MobileApplication"] +
# applicationCategory "GameApplication". The phase's own Rich Results Test criterion (SC4) adjudicates; as written,
# literal D-61 would fail it. Recorded here per the yolo-mode rule for evidence-backed corrections.

---

# Plan 05-02: Discoverability — OG Image, Heads, JSON-LD, Sitemap, Robots (SEO-01..04)

**User story:** As a player sharing the site in a chat or finding it through search, every page presents the right title, description, image card, and canonical address — and Google can render a rich app result for the landing page — so the site is discoverable and rich-result ready before the app goes live.

## Artifacts this phase produces

Full phase artifact list at the top of 05-01-PLAN.md. **This plan contributes:** `geohist/og-image.png` + `scripts/og-image.mjs` + npm script `og:image`, the canonical/OG/Twitter head blocks on all 5 content pages, the SoftwareApplication JSON-LD block on `/geohist/` only, `sitemap.xml` + `robots.txt` at the repo root, and the extended smoke-check URL list.

<execution_context>
@C:/Users/Familia/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/Familia/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-discovery-quality-screenshots-seo-json-ld-aa-audit/05-CONTEXT.md
@.planning/phases/05-discovery-quality-screenshots-seo-json-ld-aa-audit/05-RESEARCH.md
@geohist/index.html
@index.html
@geohist/guide.html
@geohist/contact.html
@geohist/privacy.html
@css/base.css
@package.json
</context>

<tasks>

<task type="tracer">
  <name>Task 1: /geohist/ discoverable end-to-end — og-image generator + full head block + JSON-LD (D-57..D-62)</name>
  <files>package.json, scripts/og-image.mjs, geohist/og-image.png, geohist/index.html</files>
  <read_first>
  - 05-RESEARCH.md — Pattern 1 (head metadata block, exact tag shape + attribute types), Pattern 2 (corrected JSON-LD example — copy field-by-field), Pattern 5 (OG image composition + the verbatim token values from css/base.css:15-23), Pitfall 1 (@type correction), Pitfall 6 (description single-source), Pitfall 9 (twitter name= vs og property=), Anti-Patterns (keying OG values, relative URLs, running JSON-LD through i18n)
  - css/base.css lines 14-26 — the design tokens: --color-bg #1a1410, --color-surface #241c14, --color-fg #f0e6d2, --color-muted #c9b89a, --color-accent #d9a951, --color-ink-on-gold #2a1f12, --font-display Georgia stack
  - geohist/index.html lines 1-12 — the current head (charset/viewport/keyed description/keyed title/scripts) the block extends; line 27 — the raw EN hero tagline for the OG composition
  - 05-CONTEXT.md — D-57/D-58/D-59 (og-image), D-60/D-61/D-62 (JSON-LD)
  </read_first>
  <action>
TRACER — one page (/geohist/) fully discoverable through every discovery layer: asset generation, head markup, structured data, and the gates. Later tasks replicate this pattern to the remaining pages.

Create scripts/og-image.mjs (ESM, imports sharp — already a devDep from 05-01): build a 1200x630 canvas as an SVG — full-bleed rect filled #1a1410, restrained parchment/gold accents using #f0e6d2 and #d9a951 (thin rules or corner flourishes; tasteful, antique — discretion), a text layer rendering 'GeoHist Trivia' large in the Georgia display stack plus the EN hero tagline 'Pack your bags — we're traveling through time.' (raw EN value from geohist/index.html line 27 — D-58: text is baked pixels, EN only), then composite geohist/icon.png resized onto the canvas (sharp composite). Encode to PNG and write geohist/og-image.png. Typography specifics (sizes, spacing) = discretion, but the result must read as the same brand as the site. Add npm script og:image = node scripts/og-image.mjs to package.json and run it once.

Extend the head of geohist/index.html — insert the block after the title element, before the stylesheet link, following research Pattern 1 exactly:
- link rel=canonical href=https://persano.github.io/geohist/ (directory form, D-68)
- og:title = the page's existing raw EN title string byte-identical (GeoHist Trivia — history and geography trivia for Android); og:description = the page's existing raw EN meta description string byte-identical; og:type website; og:url = the canonical exactly; og:image = https://persano.github.io/geohist/og-image.png; og:image:width 1200; og:image:height 630
- twitter:card summary_large_image; twitter:title and twitter:description = the same two EN strings; twitter:image = the same og:image URL — twitter tags use the name attribute (Pitfall 9)
- All values static EN, NOT keyed — no data-i18n on any of these nodes (scrapers do not execute JS, D-67; the anti-pattern ban holds even though title/desc elsewhere are keyed)
- Then the JSON-LD block (D-60/D-61/D-62): one script type=application/ld+json with the corrected field set from research Pattern 2 — @type array ['SoftwareApplication','MobileApplication'] (flagged deviation from literal D-61, recorded in this plan's header), name GeoHist Trivia, operatingSystem ANDROID, applicationCategory GameApplication (documented casing), description = the same EN meta description string byte-identical (Pitfall 6), url = the canonical, image = https://persano.github.io/geohist/icon.png, screenshot = the four absolute URLs https://persano.github.io/geohist/screenshots/screenshot.menu.webp / screenshot.map.webp / screenshot.flags.webp / screenshot.timeline.webp (05-01's pinned names), author as a Person node named Santiago David Postorivo, sameAs = https://play.google.com/store/apps/details?id=com.persano.geohisttrivia, offers as an Offer node with price 0 (string) and priceCurrency USD — and the banned rating field from the prohibitions is simply not present. Zero rating-related keys (see prohibitions).
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const b=fs.readFileSync('geohist/og-image.png');if(b.readUInt32BE(16)!==1200||b.readUInt32BE(20)!==630){console.error('FAIL og-image dims '+b.readUInt32BE(16)+'x'+b.readUInt32BE(20));process.exit(1)};const h=fs.readFileSync('geohist/index.html','utf8');const c=(h.match(/rel=\"canonical\" href=\"([^\"]+)\"/)||[])[1];const u=(h.match(/property=\"og:url\" content=\"([^\"]+)\"/)||[])[1];if(!c||!u||c!==u||c!=='https://persano.github.io/geohist/'){console.error('FAIL canonical/og:url: '+c+' vs '+u);process.exit(1)};const m=h,_d=(m.match(/<meta name=\"description\" content=\"([^\"]+)\"/)||[])[1];if(!_d){console.error('FAIL meta description not found');process.exit(1)};const s=h.match(/<script type=\"application\/ld\+json\">([\s\S]*?)<\/script>/);if(!s){console.error('FAIL ld+json missing');process.exit(1)};const o=JSON.parse(s[1]);const J=JSON.stringify;const exp=J(['SoftwareApplication','MobileApplication']);if(J(o['@type'])!==exp){console.error('FAIL @type '+J(o['@type']));process.exit(1)};if(o.applicationCategory!=='GameApplication'||o.operatingSystem!=='ANDROID'){console.error('FAIL category/OS');process.exit(1)};if(o.description!==_d){console.error('FAIL description not byte-identical to meta');process.exit(1)};if(o.offers.price!=='0'||o.offers.priceCurrency!=='USD'){console.error('FAIL offers');process.exit(1)};if(!Array.isArray(o.screenshot)||o.screenshot.length!==4){console.error('FAIL screenshot array');process.exit(1)};for(const k of Object.keys(o)){if(/rating/i.test(k)){console.error('FAIL rating key present: '+k);process.exit(1)}};if(o.author.name!=='Santiago David Postorivo'||o.sameAs!=='https://play.google.com/store/apps/details?id=com.persano.geohisttrivia'){console.error('FAIL author/sameAs');process.exit(1)};const tw=h.match(/<meta name=\"twitter:/g)||[];const twp=h.match(/<meta property=\"twitter:/g)||[];if(tw.length<4||twp.length!==0){console.error('FAIL twitter attrs '+tw.length+'/'+twp.length);process.exit(1)};const og=h.match(/<meta property=\"og:image:width\" content=\"1200\"/);if(!og||!h.includes('og:image:height\" content=\"630')){console.error('FAIL og:image dims');process.exit(1)};const sc=s[0];if(/data-i18n/.test(sc)){console.error('FAIL ld+json keyed');process.exit(1)};const pkg=JSON.parse(fs.readFileSync('package.json','utf8'));if(!pkg.scripts['og:image']){console.error('FAIL og:image script');process.exit(1)};console.log('PASS: og-image 1200x630, canonical==og:url, corrected JSON-LD complete, twitter name=, no rating key')"</automated>
  </verify>
  <acceptance_criteria>
  - geohist/og-image.png exists and its PNG IHDR reports exactly 1200x630; npm script og:image exists in package.json
  - geohist/index.html: rel=canonical equals https://persano.github.io/geohist/ and equals the og:url content byte-for-byte
  - The parsed ld+json object: @type array equals [SoftwareApplication, MobileApplication]; applicationCategory equals GameApplication; operatingSystem equals ANDROID; offers.price equals the string 0 with priceCurrency USD; description is byte-identical to the meta description content attribute; screenshot array has the 4 pinned absolute WebP URLs; author name Santiago David Postorivo; sameAs the Play package URL; zero keys matching /rating/i
  - At least 4 twitter meta tags use name= and zero twitter meta tags use property=; og:image:width 1200 and og:image:height 630 present
  - The ld+json script element contains zero data-i18n attributes
  - npm run validate:html exits 0 (extended head stays valid markup)
  </acceptance_criteria>
  <done>/geohist/ is discoverable end-to-end in raw HTML: regenerable brand og-image, canonical + OG + Twitter set, and the corrected rich-result JSON-LD — the pattern proven for the remaining pages.</done>
</task>

<task type="auto">
  <name>Task 2: Replicate the head block to the remaining 4 content pages (D-66, D-67, D-68)</name>
  <files>index.html, geohist/guide.html, geohist/contact.html, geohist/privacy.html</files>
  <read_first>
  - geohist/index.html (post-Task-1) — the finished block to replicate verbatim in shape
  - 05-RESEARCH.md — Pattern 1 + Pitfall 9 + Anti-Patterns (relative URLs banned)
  - index.html lines 1-12 — hub head: raw EN title (Santiago David Postorivo — Personal Apps Hub) + description
  - geohist/guide.html lines 1-12 — raw EN title (How to play — GeoHist Trivia) + description
  - geohist/contact.html lines 1-13 — raw EN title (Contact — GeoHist Trivia) + description
  - geohist/privacy.html lines 1-9 — raw EN title (Privacy Policy — GeoHist Trivia) + description; note: no i18n scripts on this page
  - 404.html lines 1-12 — already D-66-compliant (noindex + title); this plan must not touch it
  </read_first>
  <action>
Insert the same discovery block into the four remaining content pages, after each page's title element and before its stylesheet link, following the Task-1 shape exactly. Per-page values (og:title / og:description are each page's OWN raw EN title and meta description, copied byte-identical — single source, no re-drafting; og:url mirrors the canonical exactly):
- index.html (hub): canonical https://persano.github.io/ (directory form); title string Santiago David Postorivo — Personal Apps Hub; description string The personal apps hub of Santiago David Postorivo — home of GeoHist Trivia, an offline history and geography trivia game for Android.
- geohist/guide.html: canonical https://persano.github.io/geohist/guide.html; title How to play — GeoHist Trivia; description How to play GeoHist Trivia — a quick guide to the basics, every game mode, and the World Passport journey.
- geohist/contact.html: canonical https://persano.github.io/geohist/contact.html; title Contact — GeoHist Trivia; description Contact GeoHist Trivia — send a question, report a bug, share feedback, or request deletion of your data.
- geohist/privacy.html: canonical https://persano.github.io/geohist/privacy.html; title Privacy Policy — GeoHist Trivia; description Privacy policy for GeoHist Trivia — how this offline history and geography trivia game and its website handle your data.
Every page gets the same og:type website, og:image + 1200/630 dims, and the same twitter set pointing at the one site-wide og-image URL (D-59). All values static EN, unkeyed (D-67). NO JSON-LD on any of these four (D-60). Do not touch 404.html (already correct per D-66 — verify only).
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const pages={'index.html':'https://persano.github.io/','geohist/guide.html':'https://persano.github.io/geohist/guide.html','geohist/contact.html':'https://persano.github.io/geohist/contact.html','geohist/privacy.html':'https://persano.github.io/geohist/privacy.html'};for(const [f,c] of Object.entries(pages)){const h=fs.readFileSync(f,'utf8');const can=(h.match(/rel=\"canonical\" href=\"([^\"]+)\"/)||[])[1];const u=(h.match(/property=\"og:url\" content=\"([^\"]+)\"/)||[])[1];if(can!==c){console.error('FAIL canonical '+f+': '+can);process.exit(1)};if(u!==can){console.error('FAIL og:url mirror '+f+': '+u);process.exit(1)};if(!h.includes('og:image\" content=\"https://persano.github.io/geohist/og-image.png\"')){console.error('FAIL og:image '+f);process.exit(1)};if(!h.includes('og:image:width\" content=\"1200\"')||!h.includes('og:image:height\" content=\"630\"')){console.error('FAIL og:image dims '+f);process.exit(1)};const tw=h.match(/<meta name=\"twitter:/g)||[];const twp=h.match(/<meta property=\"twitter:/g)||[];if(tw.length<4||twp.length!==0){console.error('FAIL twitter attrs '+f);process.exit(1)};const t=(h.match(/<title[^>]*>([^<]*)<\/title>/)||[])[1];const ot=(h.match(/property=\"og:title\" content=\"([^\"]*)\"/)||[])[1];if(t!==ot){console.error('FAIL og:title != title on '+f);process.exit(1)};if(/<script type=\"application\/ld\+json\"/.test(h)){console.error('FAIL ld+json outside /geohist/: '+f);process.exit(1)}};const nf=fs.readFileSync('404.html','utf8');if(/rel=\"canonical\"/.test(nf)||/og:image/.test(nf)){console.error('FAIL 404 gained discovery markup');process.exit(1)};if(!nf.includes('noindex')){console.error('FAIL 404 noindex lost');process.exit(1)};console.log('PASS: 4 pages blocked, 404 untouched')"</automated>
  </verify>
  <acceptance_criteria>
  - Each of the 4 pages carries: rel=canonical at its pinned URL, og:url byte-equal to it, og:image pointing at https://persano.github.io/geohist/og-image.png with 1200/630 dims declared, at least 4 twitter name= tags and zero property= twitter tags
  - Each page's og:title equals its title element text exactly, and og:description equals its meta description content attribute exactly
  - Zero ld+json script on any of the 4 pages (schema lives on /geohist/ only, D-60)
  - 404.html is unchanged by this phase (git diff empty on it): noindex intact, no canonical/OG added
  - npm run validate:html exits 0
  </acceptance_criteria>
  <done>SEO-01 and SEO-02 complete in code: all 5 content pages carry title/description/canonical + OG/Twitter cards with absolute URLs and the 1200x630 image, in one URL form per page.</done>
</task>

<task type="auto">
  <name>Task 3: sitemap.xml + robots.txt + smoke-check extension + wave-2 deploy handoff (D-63, D-64, D-65 prep)</name>
  <files>sitemap.xml, robots.txt, scripts/smoke-check.sh</files>
  <read_first>
  - 05-RESEARCH.md — Pattern 6 (exact sitemap + robots content), Pitfall 5 (URL forms), Pitfall 10 (submit timing — submission happens in 05-03's checkpoint, after the deploy)
  - 05-CONTEXT.md — D-63 (5-URL list, exclusions), D-64 (no date elements), D-65 (submission is the 05-03 owner step)
  - scripts/smoke-check.sh — the 200-check URL list to extend
  </read_first>
  <action>
Create sitemap.xml at the repo root per research Pattern 6: xml declaration + urlset in the sitemaps.org 0.9 namespace, exactly five url entries with loc values https://persano.github.io/ , https://persano.github.io/geohist/ , https://persano.github.io/geohist/guide.html , https://persano.github.io/geohist/contact.html , https://persano.github.io/geohist/privacy.html — in that pinned order (any order is spec-valid; pinned for diff stability). No date elements inside any url entry (D-64). No alternate-language links of any kind inside the sitemap — the site is single-URL JS-swap i18n and per-language alternates are out of scope (REQUIREMENTS.md). The 404 page, the Search Console verification file, and app-ads.txt are excluded (D-63).

Create robots.txt at the repo root, exactly the SEO-03 pinned content — three lines total: User-agent: * , then Allow: / , then a blank line, then Sitemap: https://persano.github.io/sitemap.xml . Nothing else (no crawl directives of any other kind).

Extend scripts/smoke-check.sh's 200-check URL list with: $BASE/geohist/ , $BASE/geohist/guide.html , $BASE/sitemap.xml , $BASE/robots.txt , $BASE/geohist/og-image.png (the existing entries stay — they are the OPS-03 regression set).

Commit everything, then the wave-2 deploy handoff: the single controlled push to main is orchestrator-owned (Phase-01/02/04 pattern) — if the executor holds push permission it performs the one push, otherwise it records the handoff and stops. After the deploy is green: run npm run validate (linkinator now reaches the live og-image URL) and bash scripts/smoke-check.sh — all must pass. If the deploy has not happened at execution time, record the post-deploy checks as PENDING for the orchestrator (honest close-out, Phase-04 contract) — never claim them.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const x=fs.readFileSync('sitemap.xml','utf8');const locs=x.match(/<loc>([^<]+)<\/loc>/g)||[];if(locs.length!==5){console.error('FAIL sitemap has '+locs.length+' locs');process.exit(1)};const want=['https://persano.github.io/','https://persano.github.io/geohist/','https://persano.github.io/geohist/guide.html','https://persano.github.io/geohist/contact.html','https://persano.github.io/geohist/privacy.html'];for(let i=0;i<5;i++){if(!locs[i].includes(want[i])){console.error('FAIL loc['+i+']: '+locs[i]);process.exit(1)}};if(/lastmod/i.test(x)){console.error('FAIL date element present');process.exit(1)};if(/xhtml:link|hreflang/i.test(x)){console.error('FAIL alternate links present');process.exit(1)};const r=fs.readFileSync('robots.txt','utf8');const lines=r.split(/\r?\n/).filter(l=>l.trim()!=='');if(lines.length!==3||lines[0]!=='User-agent: *'||lines[1]!=='Allow: /'||lines[2]!=='Sitemap: https://persano.github.io/sitemap.xml'){console.error('FAIL robots content: '+JSON.stringify(lines));process.exit(1)};const sm=fs.readFileSync('scripts/smoke-check.sh','utf8');for(const u of ['/geohist/','/geohist/guide.html','/sitemap.xml','/robots.txt','/geohist/og-image.png']){if(!sm.includes(u)){console.error('FAIL smoke list missing '+u);process.exit(1)}};console.log('PASS: sitemap 5 URLs pinned order, no enrichment; robots exact; smoke list extended')"</automated>
  </verify>
  <acceptance_criteria>
  - sitemap.xml contains exactly 5 loc entries matching the pinned list in the pinned order; zero date elements; zero alternate-language markup
  - robots.txt is exactly the 3 content lines: User-agent: * / Allow: / / Sitemap: https://persano.github.io/sitemap.xml
  - scripts/smoke-check.sh's 200-list contains all 5 new URLs and keeps the existing OPS-03 set
  - After the wave-2 deploy: npm run validate exits 0 and scripts/smoke-check.sh prints ALL PASS (og-image, sitemap, robots, all 5 pages live 200) — or the checks are recorded PENDING pending the orchestrator push, never claimed
  - Search Console submission does NOT happen in this plan — it is 05-03 Task 3's owner step, strictly after this deploy (D-65/Pitfall 10)
  </acceptance_criteria>
  <done>SEO-03 complete in code and deployed: the full URL set is crawlable via sitemap + robots, the brand image is live at its absolute URL, and the smoke battery covers the new surfaces — submission waits for the ordered owner checkpoint.</done>
</task>

</tasks>

<verification>
- npm run validate:html && npm run validate:i18n — green (keycheck untouched: head additions are unkeyed static EN)
- Per-page assertions (Task 1+2 node batteries): canonical == og:url, og:image absolute with 1200/630 declared, twitter name= counts, og:title/og:description byte-identical to each page's own strings
- JSON-LD battery (Task 1): parses, corrected @type, complete D-61 field set, description single-source, zero rating keys, zero data-i18n
- sitemap/robots battery (Task 3): 5 pinned URLs, no enrichment, robots exact
- git diff: 404.html empty; css/base.css untouched by this plan
- Post-deploy (orchestrator push): npm run validate green (linkinator reaches live og:image) + smoke-check.sh ALL PASS
</verification>

<success_criteria>
- SEO-01: every content page has meta title/description + canonical URL (directory form for roots, absolute, og:url mirrored) — 5/5
- SEO-02: OG/Twitter cards absolute everywhere, one site-wide og-image.png 1200x630 declared with dims — 5/5
- SEO-03: sitemap.xml (5 URLs, no date elements, no alternates) + robots.txt (allow-all + sitemap line) live and smoke-covered; submission deferred to the ordered owner checkpoint
- SEO-04: corrected SoftwareApplication JSON-LD on /geohist/ only — Rich Results Test-ready (owner-side check adjudicates in 05-03), zero rating fields, description single-sourced
- Probe rows SEO-01/02/03/04 (10 rows): 8 resolved explicit, 2 flagged backstop assumptions (head-tag order, sitemap URL order) — no silent drops
</success_criteria>

<output>
Create `.planning/phases/05-discovery-quality-screenshots-seo-json-ld-aa-audit/05-02-SUMMARY.md` when done
</output>
