---
phase: 02-geohist-landing-hub-content-en-i18n-keys-baked-in
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - geohist/guide.html
  - index.html
autonomous: true
requirements: [LNDG-05, CONT-02, CONT-04, CMPL-04]

estimate:
  tokens: 12000
  raw_tokens: 12000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - /geohist/guide.html explains how to play (basics) and overviews the game modes, ~1-2 screens (CONT-02, D-19)
    - Hub /index.html shows the Persano brand intro plus exactly one GeoHist app card inside a real grid — no visible placeholders, no empty cells, no "more apps coming" filler (CONT-04, D-15)
    - Guide and hub reuse the Task-1 design system (base.css component classes) — no palette or token edits
    - All new user-visible strings carry data-i18n / data-i18n-attr keys with guide.* / hub.* prefixes (D-20, D-21)
    - All internal links resolve (nav Guide link, hub card link, footer Back-to-hub) so CI linkinator stays green on the single controlled push
    - Existing pages regression-safe: privacy.html inherits the theme untouched; 404.html stays self-contained
  artifacts:
    - geohist/guide.html (how-to-play + game modes, nav + footer, keys baked)
    - index.html (full hub: brand intro + single grid-ready app card)
  key_links:
    - hub app card -> /geohist/ (Learn-more link; icon reuses /geohist/icon.png)
    - landing nav "Guide" -> /geohist/guide.html (dead until this plan — resolved here, before the single push)
    - guide.* / hub.* keys -> Phase 3 dictionaries (1:1, D-20)
---

<objective>
Game guide page + full Persano hub, completing Phase 2's English content.

Purpose: With plan 02-01's theme and landing in place, this plan adds the remaining two pages — the concise guide (CONT-02) and the full hub (CONT-04) — so the phase goal is met end-to-end and the single controlled push deploys all new pages together with green CI.
Output: geohist/guide.html (new), index.html (expanded hub), cross-page regression checks.
</objective>

<execution_context>
@C:/Users/Familia/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/Familia/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-geohist-landing-hub-content-en-i18n-keys-baked-in/02-CONTEXT.md
@geohist/index.html
@index.html
@geohist/privacy.html
@css/base.css
</context>

## User Story

> **As a** visitor to persano.github.io, **I want to** read a concise guide to playing GeoHist Trivia and see a hub that introduces the Persano brand with its one app, **so that** I know how the game works and where to go next.

(Second half of the ROADMAP Phase 2 goal, restated 1:1 — guide + hub. Design system and palette arrive from plan 02-01; no styling decisions here.)

## Artifacts this phase produces

**Files (this plan):** `geohist/guide.html` (new), `index.html` (modified — full hub).
**Reuses from 02-01 (defined there, consumed here):** tokens `--color-*`/`--font-display`, classes `.site-header`, `.site-nav`, `.hero-icon`, `.app-grid`, `.app-card`, `.app-card-icon`, `.mode-list`, `.mode-item`, `.footer-links`, `#lang-switcher-slot`.
**data-i18n namespaces:** `guide.*` (guide page), `hub.*` (hub page) — mapping 1:1 into Phase 3 dictionaries.
**No new CSS:** base.css is complete after plan 02-01; this plan modifies markup only.

## Flagged Assumptions (spec-less probes — rows for this plan's requirements surfaced, none dropped)

| # | Req | Probe | Disposition |
|---|-----|-------|-------------|
| 12 | CONT-02 | unclassified | Flagged: guide is static hand-authored markup, ~1-2 screens, no dynamic behavior; mode list is a fixed authored enumeration from README facts. |
| 13 | CONT-03 | unclassified | Flagged: brand intro is static prose (About-dev detail lives on the landing page per D-12). |
| 14 | CONT-04 | unclassified | Flagged: hub grid is real CSS grid but ships exactly one card — card max-width prevents stretching; no placeholder cells or filler text ship. |
| 15 | CMPL-04 | unclassified | Flagged + enforced: guide/hub copy contains zero data-collection claims, so no mirror drift is possible; any data phrasing would be a violation (see prohibitions in 02-01-PLAN.md). |
| 8 | LNDG-05 | unclassified | Flagged: guide/hub use the shared responsive foundations (fluid type, auto-fill grids, viewport meta); visual phone-width sweep is the post-deploy human check. |

(Companion to the full 15-row table in 02-01-PLAN.md — same dispositions.)

## must_haves.prohibitions (descriptor-less)

- **No data-claim drift:** guide and hub copy must contain zero data-collection/retention claims — data wording exists only in the landing FAQ and privacy policy, verbatim (CMPL-04, D-17).
- **No placeholder CTA / no hub filler:** hub ships exactly one app card with its Learn-more link — no empty grid cells, no "more apps coming" text, no second card stub (CONT-04, D-15). Play badge href remains the package URL from plan 02-01 wherever reused.
- **No superseded i18n approach:** no per-language subdirectories, no hreflang alternates, no redirect/detect script — locked JS-dictionary-swap decision (AGENTS.md STACK.md §7 is STALE).

<tasks>

<task type="auto">
  <name>Task 1: guide.html — how to play + game modes</name>
  <files>geohist/guide.html</files>
  <read_first>
    - geohist/index.html (nav + footer + head scaffolding pattern established in plan 02-01 — replicate exactly)
    - geohist/privacy.html (secondary scaffolding reference)
    - css/base.css (available component classes: .site-header, .site-nav, .mode-list, .mode-item, .footer-links — use, do not edit)
    - C:\Users\Familia\antigravity\GeoHist-Trivia\README.md (game-mode facts, lines 72–79: FlagCountry, FlagCapital, MapFigure, MapCity, MapIsland, EventYearPhoto, FigureArt, CoatOfArmsCountry + World Passport screen; minSdk 24)
    - .planning/phases/02-geohist-landing-hub-content-en-i18n-keys-baked-in/02-CONTEXT.md (D-08, D-14, D-19)
  </read_first>
  <action>
    Create geohist/guide.html per D-19 (~1-2 screens, concise): head replicating the established scaffolding (charset, viewport, keyed-tolerant description, `<title data-i18n="guide.meta.title">`, /css/base.css link), same `.site-header`/`.site-nav` and footer as the landing page (footer includes privacy link, contact email, Back to hub, copyright, #lang-switcher-slot).

    Body sections: (1) h1 "How to play" (keyed `guide.how.title`) + short intro paragraph (keyed `guide.how.intro`) + 3-4 step basics — pick a category and mode, answer timed questions, build streaks, earn medals — playful D-08 voice, all keyed (`guide.how.step.1`…). (2) h2 "Game modes" (keyed `guide.modes.title`) + intro line (keyed `guide.modes.intro`) + a `.mode-list` grid of `.mode-item` entries drawn from the README-verified modes: guess the country from its flag, match capitals, identify figures on the map, find cities and islands, place historical events in time (EventYearPhoto), recognize figures in art (FigureArt), match coats of arms, plus the World Passport journey — each with a one-line playful description, all keyed (`guide.modes.<n>.name`, `guide.modes.<n>.desc`). (3) Closing line linking back to the landing (/geohist/) for features and FAQ, keyed.

    Zero data-collection claims anywhere (prohibition 1 — data questions live only in the FAQ/policy). Zero JS. No CSS edits — the plan 02-01 design system covers these classes.
  </action>
  <verify>
    <automated>Test-Path geohist\guide.html; (Select-String -Path geohist\guide.html -Pattern 'href="/css/base.css"').Count -eq 1; (Select-String -Path geohist\guide.html -Pattern 'data-i18n="guide\.').Count -ge 15; (Select-String -Path geohist\guide.html -Pattern '<script').Count -eq 0; (Select-String -Path geohist\guide.html -Pattern 'href="/geohist/"').Count -ge 1</automated>
  </verify>
  <acceptance_criteria>
    - geohist/guide.html exists with the established head scaffolding and single /css/base.css link
    - how-to-play section present with keyed h1 + intro + at least 3 keyed steps
    - game-modes section lists at least 7 .mode-item entries sourced from the README-verified mode facts
    - every user-visible string node carries data-i18n with guide.* prefix; title carries data-i18n="guide.meta.title"
    - nav + footer replicate the landing pattern (privacy link, mailto, Back-to-hub, copyright, #lang-switcher-slot)
    - zero <script> elements; zero data-claim sentences
  </acceptance_criteria>
  <done>Guide page live: concise how-to-play + game-mode overview, fully keyed, consistent nav/footer.</done>
</task>

<task type="auto">
  <name>Task 2: Full hub — Persano brand intro + single GeoHist app card</name>
  <files>index.html</files>
  <read_first>
    - index.html (current hub skeleton to expand in place — keep head structure, replace body)
    - geohist/index.html (source for the card's icon path /geohist/icon.png, app name, and blurb voice)
    - .planning/phases/02-geohist-landing-hub-content-en-i18n-keys-baked-in/02-CONTEXT.md (D-15, D-24, CONT-04)
    - .planning/phases/02-geohist-landing-hub-content-en-i18n-keys-baked-in/02-RESEARCH.md (Pattern 5 hub grid)
  </read_first>
  <action>
    Expand index.html into the full hub per CONT-04/D-15. Keep the existing head (title "Persano — Personal Apps Hub", description, /css/base.css). Body: h1 "Persano" (keyed `hub.brand`), brand intro paragraph(s) keyed `hub.intro.1` (and `hub.intro.2` if a second line) carrying the Persano brand and naming Santiago David Postorivo — the existing hub copy is the seed, extend with the personal-apps-hub framing. Then a single `.app-grid` containing exactly one `.app-card`: icon img `/geohist/icon.png` (keyed alt via `data-i18n-attr="alt:hub.card.icon-alt" data-i18n-attr-only`), app name h2 keyed `hub.card.name` = "GeoHist Trivia", one-line description keyed `hub.card.desc` (offline history-and-geography trivia for Android), and a "Learn more" link keyed `hub.card.cta` pointing to `/geohist/` (rel follows internal-link pattern; no external links added here). The grid is real (card #2 slots in later) but with one card it renders as a single well-proportioned card — no empty cells, no stretching (Pattern 5 CSS from plan 02-01).

    No placeholder text of any kind — no "more apps" filler, no empty tiles (prohibition 3). Footer: same `.footer-links` pattern — privacy link, contact email, copyright "© Persano — Santiago David Postorivo" (keyed), `#lang-switcher-slot` — minus the Back-to-hub entry (the hub IS the hub). Every user-visible string keyed with hub.* prefix.
  </action>
  <verify>
    <automated>(Select-String -Path index.html -Pattern 'app-card').Count -eq 1; (Select-String -Path index.html -Pattern 'data-i18n="hub\.').Count -ge 6; (Select-String -Path index.html -Pattern 'href="/geohist/"').Count -ge 1; (Select-String -Path index.html -Pattern 'lang-switcher-slot').Count -eq 1; (Select-String -Path index.html -Pattern 'href="/geohist/privacy\.html"').Count -eq 1; (Select-String -Path index.html -Pattern '<script').Count -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - index.html contains exactly one .app-card inside a real .app-grid — no additional card stubs or placeholder cells
    - brand intro carries the Persano brand and Santiago David Postorivo's name, all nodes keyed hub.*
    - app card links to /geohist/ and reuses /geohist/icon.png with keyed alt
    - footer keeps the privacy link (CMPL-01) and includes #lang-switcher-slot
    - zero user-visible strings without a data-i18n key; zero <script> elements
    - no second-app placeholder text anywhere in the file
  </acceptance_criteria>
  <done>Hub complete: Persano brand intro + exactly one GeoHist app card, grid-ready for future apps, no visible placeholders.</done>
</task>

<task type="auto">
  <name>Task 3: Cross-page regression battery + deploy handoff</name>
  <files>geohist/guide.html, index.html, geohist/index.html, css/base.css, geohist/privacy.html, 404.html</files>
  <read_first>
    - geohist/index.html, geohist/guide.html, index.html (all Phase 2 pages — final state)
    - geohist/privacy.html (regression surface: inherits the dark theme via shared tokens; content untouched)
    - 404.html (self-contained by design — link targets unchanged, no shared CSS dependency)
    - package.json (validate globs + linkinator skip regex — CI is the arbiter)
  </read_first>
  <action>
    Run the cross-page battery (read-only): (1) every page — index.html, 404.html, geohist/index.html, geohist/guide.html, geohist/privacy.html — footer privacy link present (CMPL-01 regression). (2) Internal link graph resolves: /geohist/ ↔ /geohist/guide.html ↔ hub / links all point at files that now exist, so linkinator's internal check passes on the single controlled push. (3) Heading order sequential per page; no duplicate ids across each page. (4) Zero script tags across all Phase 2 pages; zero per-language-subdirectory artifacts anywhere (prohibition 3). (5) 404.html diff-untouched since Phase 1. (6) privacy.html diff-untouched this phase (theme arrives via base.css only).

    Then hand off for deploy per the Phase-01 pattern (STATE.md: push-to-main is the only deploy action; the orchestrator executes the single controlled push): one push deploys BOTH plans' commits together — nav's Guide link, guide page, and hub all go live in the same CI run, so no dead internal links ever exist on the live site. After the CI run goes green, confirm /geohist/, /geohist/guide.html, /, and /geohist/privacy.html all return 200.
  </action>
  <verify>
    <automated>$pages = 'index.html','404.html','geohist\index.html','geohist\guide.html','geohist\privacy.html'; ($pages | Where-Object { -not (Select-String -Path $_ -Pattern '/geohist/privacy\.html' -Quiet) }).Count -eq 0; ($pages | Where-Object { Select-String -Path $_ -Pattern '<script' -Quiet }).Count -eq 0; git diff --stat HEAD -- 404.html geohist/privacy.html</automated>
  </verify>
  <acceptance_criteria>
    - all five pages carry the footer privacy link (CMPL-01)
    - zero <script> elements across all pages
    - every internal href in every page resolves to an existing file in the repo
    - 404.html and geohist/privacy.html show no content diff for this phase
    - single controlled push executed by orchestrator; CI validate job green; /geohist/, /geohist/guide.html, /, /geohist/privacy.html return 200 post-deploy
  </acceptance_criteria>
  <done>Phase 2 pages validated as a set, deploy handed off, live URLs smoke-checked.</done>
</task>

</tasks>

<verification>
- Local source-assertion battery passes (no local npm — CI arbitrates on push).
- Single controlled push to main (Phase-01 pattern): CI `npm run validate` = html-validate over `index.html 404.html geohist/*.html` + linkinator internal-link check; external package URL skipped by regex (package.json:7).
- Post-deploy smoke: all three new/expanded pages + privacy.html return 200.
</verification>

<human-check>
Post-deploy phone-width sweep (320/375px): hub card proportions correct with exactly one card, guide fits ~1-2 screens, nav wraps cleanly, texture never behind body copy on any page (owner reviews copy per D-09 after execution).
</human-check>

<success_criteria>
- CONT-02 (guide: how to play + game modes) and CONT-04 (hub: brand intro + single app card, no placeholders) delivered
- LNDG-05 holds across all new pages via shared responsive foundations
- CMPL-04 preserved: zero data claims outside policy-verbatim FAQ
- CI green on the single controlled push; live URLs verified
</success_criteria>

<output>
Create `.planning/phases/02-geohist-landing-hub-content-en-i18n-keys-baked-in/02-02-SUMMARY.md` when done
</output>
