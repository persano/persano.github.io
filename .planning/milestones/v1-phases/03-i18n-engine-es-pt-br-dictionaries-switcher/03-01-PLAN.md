---
phase: 03-i18n-engine-es-pt-br-dictionaries-switcher
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - js/i18n.js
  - js/i18n/es.json
  - index.html
  - geohist/index.html
  - geohist/guide.html
  - scripts/i18n-keycheck.mjs
autonomous: true
requirements: [I18N-01, I18N-02, I18N-03, I18N-04]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A browser whose preferences include Spanish (any es-* tag, even non-primary) sees Spanish copy applied in place on hub, landing, and guide — same URL, no redirect, no navigation (I18N-01, I18N-02, D-29, D-31, D-32)"
    - "Language resolution order is: valid localStorage persano.lang value > first supported match scanning navigator.languages (pt-* folds to pt-BR, es-* to es, en-* to en) > en (D-32, D-33, D-34)"
    - "Any dictionary fetch, parse, or validation failure — offline, 404, malformed JSON, empty object — silently keeps the shipped EN content with no error UI and no persistence (SC3, I18N-02, D-30)"
    - "On every applied language change, document.documentElement.lang, <title>, and the meta description update atomically with the visible text (I18N-04, D-27, D-29)"
    - "Manual switcher renders three endonym text entries (English · Español · Português) inside the un-hidden #lang-switcher-slot footer span on exactly the 3 keyed pages; the active language is a non-interactive span carrying aria-current (I18N-03, D-36, D-37, D-38)"
    - "Clicking a switcher entry applies that language without reload, persists it to localStorage persano.lang only after a successful apply, and dispatches a persano:langchange CustomEvent on document as the last step; a failed switch is a silent no-op (I18N-03, D-29, D-30, D-35)"
    - "Switching back to EN restores exactly the shipped markup text and attributes — ES→EN→ES round-trips cleanly; no en dictionary file exists (D-28)"
    - "EN visitors fetch zero extra bytes — no dictionary request fires when the resolved target is en (D-25)"
    - "The es dictionary maps 1:1 to the live data-i18n key surface — zero missing keys, zero extra keys (I18N-01, D-25)"
  artifacts:
    - path: js/i18n.js
      provides: "i18n engine — classic defer script, module-free: EN DOM snapshot (D-28), detection chain (D-32/D-33), same-origin dictionary fetch with silent fallback (D-30), in-place swap + lang/title/meta sync (D-29), footer switcher (D-36–D-38), persano:langchange hook (D-35)"
    - path: js/i18n/es.json
      provides: "es-419 dictionary, flat, one entry per live data-i18n key — 99 shipped keys + 3 meta-desc keys = 102 entries (D-25, D-27, D-40)"
    - path: scripts/i18n-keycheck.mjs
      provides: "zero-dependency key-coverage gate — extracts the data-i18n/data-i18n-attr key surface from the 3 pages, asserts exact set equality with every js/i18n/*.json dictionary"
    - path: index.html
      provides: "script defer tag + keyed meta description (data-i18n-attr=\"content:hub.meta.desc\", D-26, D-27)"
    - path: geohist/index.html
      provides: "script defer tag + keyed meta description (geohist.meta.desc)"
    - path: geohist/guide.html
      provides: "script defer tag + keyed meta description (guide.meta.desc)"
  key_links:
    - from: "index.html / geohist/index.html / geohist/guide.html"
      to: "js/i18n.js"
      via: "<script defer src=\"/js/i18n.js\"> on exactly the 3 keyed pages (D-26); absent on privacy.html and 404.html (D-37)"
      pattern: "script defer src=\"/js/i18n\\.js\""
    - from: "js/i18n.js"
      to: "js/i18n/es.json"
      via: "same-origin fetch of /js/i18n/<lang>.json when the resolved target is not en (D-25)"
      pattern: "js/i18n/"
    - from: "markup data-i18n / data-i18n-attr nodes"
      to: "js/i18n/es.json flat keys"
      via: "1:1 key surface — scripts/i18n-keycheck.mjs asserts set equality (D-25)"
      pattern: "data-i18n"
    - from: "js/i18n.js"
      to: "Phase 4 FIRE-03"
      via: "document CustomEvent persano:langchange with detail {from,to}, dispatched last on every applied switch (D-35)"
      pattern: "persano:langchange"
    - from: "js/i18n.js"
      to: "localStorage"
      via: "shared key persano.lang, membership-validated against SUPPORTED, written only after a successful apply (D-30, D-34)"
      pattern: "persano\\.lang"
---

<objective>
Site's first and only JavaScript: the i18n engine (/js/i18n.js) wired end-to-end on the 3 keyed pages with the complete ES dictionary — auto-detect, in-place swap, EN snapshot fallback, lang/title/meta sync — plus the key-coverage gate that keeps dictionaries and markup drift-proof.

Purpose: Spanish visitors read the site in their language with zero URL changes (I18N-01/02/04), every mechanism Phase 4 hooks into (persano:langchange, switcher) exists, and a deterministic gate proves the dictionary exactly covers the live key surface (Pitfall 8 defense).
Output: js/i18n.js (engine), js/i18n/es.json (102-key es-419 dictionary), script tags + keyed meta descriptions on the 3 pages, scripts/i18n-keycheck.mjs (key-coverage gate).
</objective>

<execution_context>
@C:/Users/Familia/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/Familia/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-i18n-engine-es-pt-br-dictionaries-switcher/03-CONTEXT.md
@.planning/phases/03-i18n-engine-es-pt-br-dictionaries-switcher/03-RESEARCH.md
@.planning/phases/02-geohist-landing-hub-content-en-i18n-keys-baked-in/02-01-PLAN.md
@index.html
@geohist/index.html
@geohist/guide.html
@geohist/privacy.html
</context>

## User Story

> **As a** Spanish-speaking visitor to persano.github.io, **I want to** read the hub and the GeoHist pages in Spanish without leaving the page I'm on, **so that** I can learn about GeoHist Trivia in the language I'm most comfortable with.

(Reframed 1:1 from the ROADMAP Phase 3 Goal line — "Spanish and pt-BR visitors read the site in their language with no URL changes or redirects; English always remains the safe, indexable fallback." pt-BR copy lands in plan 03-02; the switcher lands in Task 2 here.)

## Assumption Delta Decision

<assumption_delta_decision>
- **Noun:** source of truth for user-visible site copy (EN raw HTML vs the data-i18n key surface)
- **Decision:** `no-change`
- **Rationale:** EN raw HTML remains the locked content baseline (I18N-01, D-28 DOM snapshot, D-31 crawler/no-JS guarantee) — it is not demoted. The data-i18n key surface already IS the addressing identity (baked in Phase 2), so the ES/pt-BR JSON dictionaries are per-language data consumers of that surface, not a competing second source. Nothing is added alongside the primary; no accepted debt. A later `promote` (dictionaries become the generative source) would only be forced by adopting a build step or per-language subdirs — both locked out (REQUIREMENTS.md Out of Scope; v2 I18N-05 reconsideration territory).
- **Companion invariant accepted:** "every keyed node on the 3 pages has a dictionary entry for every shipped language" — encoded as scripts/i18n-keycheck.mjs (Task 3 here), wired into CI in plan 03-02.
</assumption_delta_decision>

## Artifacts this phase produces (plan 01 half)

**Files:** `js/i18n.js` (new — the site's only script), `js/i18n/es.json` (new), `scripts/i18n-keycheck.mjs` (new), `index.html` / `geohist/index.html` / `geohist/guide.html` (modified — script tag + keyed meta description only).
**Engine symbols (all internal to the IIFE, no globals):** `SUPPORTED = ['en','es','pt-BR']`, `STORAGE_KEY = 'persano.lang'`, dictionary URL shape `/js/i18n/<lang>.json`, EN snapshot capture routine, detection chain (stored pref → navigator.languages first-match → en), apply pass (textContent + setAttribute), `switchTo(lang)`, switcher renderer for `#lang-switcher-slot`, `persano:langchange` CustomEvent on `document` with `detail: {from, to}`.
**New data-i18n keys baked into markup:** `hub.meta.desc`, `geohist.meta.desc`, `guide.meta.desc` (via `data-i18n-attr="content:…"` + `data-i18n-attr-only`, D-27).
**Switcher DOM contract:** inactive entries = `<a href="#" lang="…" hreflang="…" data-persano-lang="<lang>">`, active entry = `<span lang="…" aria-current="true">`, separator " · " text, inside the footer span whose `hidden` attribute the engine removes.
**Event names:** `persano:langchange` (document-level, consumed by Phase 4 FIRE-03).
**Storage keys:** `persano.lang` (localStorage, values restricted to `en | es | pt-BR`).
**Dictionary namespace counts (after D-27):** hub 12, geohist 53, guide 37 = 102 per language file.

## Flagged Assumptions (spec-less edge probe — all 4 unresolved rows surfaced, none auto-dismissed)

| # | Req | Probe | Disposition |
|---|-----|-------|-------------|
| 1 | I18N-01 | unclassified — review manually | Flagged: copy surface is fully enumerated (102 keys, exact live extraction) and enforced 1:1 by scripts/i18n-keycheck.mjs (Task 3); ES copy agent-drafted per D-39/D-40 with owner review after execution. |
| 2 | I18N-02 | unclassified — review manually | Flagged: detection/fallback chain is source-asserted in the Task 3 battery (stored-pref priority, pt-*/es-* folding, silent-failure guards); true browser behavior is proven in the human-check + deployed smoke, not by a unit harness (zero-dependency constraint bars a DOM test framework). |
| 3 | I18N-03 | unclassified — review manually | Flagged: switcher persistence contract (persist-only-after-apply, membership-validated reads) is asserted at source level; cross-reload persistence is verified behaviorally in the human-check. |
| 4 | I18N-04 | unclassified — review manually | Flagged: lang/title/meta sync rides the same snapshot walk as body text (D-27/D-28 — no engine special-casing); asserted via source battery + meta-attr greps; visual tab-title check rides the human-check. |

No probe row auto-dismissed, none resolved with a backstop marker — 4 surfaced / 4 flagged (no-silent-drop equality holds).

## must_haves.prohibitions (descriptor-less — flagged-unverified, per spec-less fallback divergence)

- **No policy-claim drift in translations (P1, I18N-01, values/transparency):** ES (and, in plan 03-02, pt-BR) FAQ answers and copy that restate policy-derived claims (data collection, offline, retention, IAP handling) MUST mirror the meaning of `geohist/privacy.html` and never contradict it — EN remains the legally authoritative text (D-41). Not mechanically checkable (translation semantics) — flagged-unverified; owner reviews copy after execution (D-39).
- **No language data leaves the browser (P2, I18N-02, privacy):** the engine MUST NOT transmit the visitor's language preference or detection inputs to any server or third party — detection reads `navigator.languages` client-side and persistence is localStorage-only; the only network calls are same-origin dictionary fetches (D-25, D-34). Flagged-unverified.
- **Storage holds only a language code (P3, I18N-03, privacy/transparency):** localStorage key `persano.lang` MUST hold nothing but one of `en | es | pt-BR` — no timestamps, identifiers, or analytics-adjacent values; it is functional preference storage with zero Phase-4 consent coupling (D-34, D-35). Flagged-unverified.
- **No flag icons or country imagery for language selection (P4, I18N-03, values):** the switcher MUST be three endonym text entries (English · Español · Português) — flags map to countries, not languages; no icon/emoji/image may encode language choice (D-36). Flagged-unverified.

Canon referral (not minted): markup-injection via DOM assignment is canon XSS territory — mitigated here by the locked plain-text keyed-node contract (Phase 2, STATE.md) and the textContent/setAttribute-only rule (battery-asserted in Task 3); consent/GDPR canon is Phase 4's surface, not minted here.

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end ES — engine + page wiring + complete es-419 dictionary</name>
  <files>js/i18n.js, js/i18n/es.json, index.html, geohist/index.html, geohist/guide.html</files>
  <read_first>
    - index.html (current hub — meta description at line 6, #lang-switcher-slot at line 30, the 11 hub.* keys)
    - geohist/index.html (landing — meta line 6, footer slot at line 142, the 52 geohist.* keys, multi-node shared keys gallery.coming / gallery.tile.aria)
    - geohist/guide.html (guide — meta line 6, footer slot at line 82, the 36 guide.* keys incl. 8 mode pairs)
    - .planning/phases/03-i18n-engine-es-pt-br-dictionaries-switcher/03-CONTEXT.md (D-25…D-41 — locked engine architecture)
    - .planning/phases/03-i18n-engine-es-pt-br-dictionaries-switcher/03-RESEARCH.md (Patterns 1–4, anti-patterns, pitfalls 1–8, exact key surface listing, D-27 bake points, engine pseudocode)
    - .planning/phases/02-geohist-landing-hub-content-en-i18n-keys-baked-in/02-01-PLAN.md §i18n Key Contract (locked data-i18n / data-i18n-attr / data-i18n-attr-only syntax)
    - geohist/privacy.html (authoritative EN wording — D-41 meaning-mirror source for FAQ translations)
    - C:\Users\Familia\antigravity\GeoHist-Trivia\translations.json (app's ES glossary — proper-noun spellings for FAQ/guide copy; grep for relevant terms, do not read the whole file)
  </read_first>
  <action>
    Create js/i18n.js as the site's first and only script (D-26): a single classic script, module-free, wrapped in an IIFE with 'use strict', zero dependencies, zero network calls except the same-origin dictionary fetch. Wrap init in a DOMContentLoaded listener (belt-and-suspenders per research A2 — defer already guarantees post-parse execution). Constants: SUPPORTED array ['en','es','pt-BR'], STORAGE_KEY string 'persano.lang' (D-34), dictionary URL shape '/js/i18n/' + lang + '.json' (D-25).

    Init sequence — the order is load-bearing (Pitfall 1): (1) CAPTURE the EN snapshot exactly once, before any apply: walk document.querySelectorAll('[data-i18n], [data-i18n-attr]') and record, per element, its textContent plus every data-i18n-attr pair parsed per the locked syntax "attr:key[,attr:key…]" — split on commas, then split each pair on its FIRST colon, trim both sides (Pitfall 5); (2) RESOLVE the target language: stored preference first — read localStorage inside try/catch and accept only values present in SUPPORTED, treating anything else as absent (Pitfall 4); when absent, scan all entries of navigator.languages in order (D-32), lowercased prefix matching with folding: any tag starting pt maps to 'pt-BR', any starting es maps to 'es', any starting en maps to 'en' (D-33), the whole scan guarded in try/catch with fallback 'en'; (3) if the resolved target is 'en', re-apply the snapshot (restore path) — no fetch of any kind; (4) otherwise fetch the language file: guard response.ok, parse JSON, validate the result is a non-null non-array object with at least one own key; ANY failure (network, 404, malformed JSON, wrong shape) stays on the shipped EN content silently — no error UI, no persistence (D-30, SC3); (5) APPLY in exactly one synchronous walk over the snapshot entries: set textContent from the dictionary for keyed text nodes (when a key is missing from the dictionary, keep the snapshot EN value — per-node silent retention) and setAttribute for every recorded attr pair from dictionary values with the same per-pair fallback; never use markup-parsing DOM assignment — textContent and setAttribute only, because keyed nodes carry plain text by the Phase-2 contract (STATE.md); (6) SYNC document.documentElement.lang to the applied language in the same pass — <title> and the meta description are ordinary nodes in this walk, zero special-casing (D-27, D-29, I18N-04); (7) PERSIST nothing at init (auto-detected language re-detects each visit — manual choices only persist, landed in Task 2); (8) DISPATCH a CustomEvent named persano:langchange on document with detail {from: 'en', to: <applied>} as the LAST step, only after the visible state is fully applied — the initial auto-detect apply counts as an applied switch (D-35, research Open Question 1 recommendation adopted).

    Accept the EN first-paint flash: no hiding of content, no render-blocking, no CSS changes anywhere (D-31).

    Then wire the pages (D-26/D-27): add the script tag <script defer src="/js/i18n.js"></script> to the <head> of exactly three files — index.html, geohist/index.html, geohist/guide.html — placed after the base.css link, before </head>. Do NOT touch geohist/privacy.html or 404.html (D-37). On each of the three meta description tags (currently unkeyed — index.html line 6, geohist/index.html line 6, geohist/guide.html line 6) add data-i18n-attr="content:hub.meta.desc" / "content:geohist.meta.desc" / "content:guide.meta.desc" respectively plus data-i18n-attr-only, leaving the EN content values untouched (D-27 — reuses the D-20 mechanism, no engine special-casing for meta).

    Create js/i18n/es.json (D-25): flat JSON object, exactly one entry per live key — the 99 keys extracted from markup today (hub 11, geohist 52, guide 36) plus the 3 new meta keys (hub.meta.desc, geohist.meta.desc, guide.meta.desc) = 102 entries. The live markup surface is authoritative — re-verify with the extraction command pattern in 03-RESEARCH.md §Code Examples, not the "~103" estimate. Author neutral Latin American Spanish (es-419, D-40) from the EN strings in the three HTML files: friendly second-person register consistent with the playful site voice, complete final sentences (no interpolation, no plurals machinery — RESEARCH §Don't Hand-Roll). Identity strings translate to themselves: geohist.about.email and geohist.faq.report.email keep the literal santiagopostorivo@gmail.com; hub.footer.copyright, geohist.footer.copyright, and guide.footer.copyright keep © Persano — Santiago David Postorivo. "GeoHist Trivia", "Persano", "Google Play", and "Android" stay untranslated. For proper nouns (countries, cities, figures) appearing in FAQ/guide copy, match the app's ES glossary in translations.json. FAQ answers carrying policy-derived claims translate with meaning-mirror to geohist/privacy.html — never contradict the EN policy (D-41); the three meta-desc keys mirror the meaning of the EN meta descriptions as complete Spanish sentences.
  </action>
  <verify>
    <automated>Test-Path js\i18n.js; Test-Path js\i18n\es.json; node -e "const fs=require('fs');const d=JSON.parse(fs.readFileSync('js/i18n/es.json','utf8'));const k=Object.keys(d);if(k.length!==102)process.exit(1);if(k.filter(x=>x.startsWith('hub.')).length!==12)process.exit(2);if(k.filter(x=>x.startsWith('geohist.')).length!==53)process.exit(3);if(k.filter(x=>x.startsWith('guide.')).length!==37)process.exit(4)"; (Select-String -Path index.html -Pattern 'script defer src="/js/i18n\.js"').Count -eq 1; (Select-String -Path geohist\index.html -Pattern 'script defer src="/js/i18n\.js"').Count -eq 1; (Select-String -Path geohist\guide.html -Pattern 'script defer src="/js/i18n\.js"').Count -eq 1; (Select-String -Path geohist\privacy.html -Pattern '<script').Count -eq 0; (Select-String -Path 404.html -Pattern '<script').Count -eq 0; (Select-String -Path index.html -Pattern 'data-i18n-attr="content:hub\.meta\.desc"').Count -eq 1; (Select-String -Path geohist\index.html -Pattern 'data-i18n-attr="content:geohist\.meta\.desc"').Count -eq 1; (Select-String -Path geohist\guide.html -Pattern 'data-i18n-attr="content:guide\.meta\.desc"').Count -eq 1; (Select-String -Path js\i18n\es.json -Pattern 'santiagopostorivo@gmail\.com').Count -ge 1; (Select-String -Path js\i18n\es.json -Pattern '© Persano — Santiago David Postorivo').Count -ge 1</automated>
  </verify>
  <acceptance_criteria>
    - js/i18n.js exists and is a classic defer script — no module syntax of any kind, no external files referenced beyond the same-origin dictionary fetch
    - js/i18n/es.json parses; exactly 102 keys with namespace counts hub 12 / geohist 53 / guide 37 (node -e gate above exits 0)
    - each of the 3 keyed pages contains exactly one script tag pointing at /js/i18n.js with the defer attribute; geohist/privacy.html and 404.html contain zero script tags
    - meta descriptions on all 3 pages carry the D-27 keyed attr pairs (content:hub.meta.desc / content:geohist.meta.desc / content:guide.meta.desc) each with data-i18n-attr-only, EN content values unchanged
    - es.json identity strings equal the EN values: both email keys = santiagopostorivo@gmail.com; all three copyright keys = © Persano — Santiago David Postorivo
    - hub.meta.desc / geohist.meta.desc / guide.meta.desc are non-empty complete Spanish sentences that mirror the EN meta descriptions' meaning (not EN copies, not empty)
    - the ES dictionary contains zero entries whose value is identical to its EN source string, except the identity strings listed above (spot-check: geohist.hero.tagline, geohist.faq.title, guide.modes.1.name all differ from their EN values)
    - engine source contains: the SUPPORTED array with en/es/pt-BR, STORAGE_KEY persano.lang, the dictionary URL prefix js/i18n/, the navigator.languages scan with pt→pt-BR and es→es folding, documentElement.lang assignment, and the persano:langchange dispatch as the final init step
  </acceptance_criteria>
  <done>A Spanish-configured browser loading any of the three pages (via a local HTTP server rooted at the repo, or the deployed site) reads Spanish copy applied in place — same URL, tab title and html lang flip to es — and any dictionary fetch failure leaves the page on shipped EN with no error UI.</done>
  <reversibility rating="costly">Dictionary delivery (runtime JSON fetch, D-25) plus the engine's fetch/fallback path are the architecture this phase hangs on — switching delivery later rewrites the engine, re-proves the SC3 fallback behavior, and re-tests all 3 pages (user-rated costly in D-25).</reversibility>
</task>

<task type="auto">
  <name>Task 2: Manual switcher — endonym entries, persistence, langchange event</name>
  <files>js/i18n.js</files>
  <read_first>
    - js/i18n.js (state after Task 1 — extend the same IIFE, same apply path)
    - .planning/phases/03-i18n-engine-es-pt-br-dictionaries-switcher/03-CONTEXT.md (D-29, D-30, D-34, D-35, D-36, D-37, D-38)
    - .planning/phases/03-i18n-engine-es-pt-br-dictionaries-switcher/03-RESEARCH.md (Pattern 3 switcher rendering + delegated listener; Pitfall 6 event ordering; Pitfall 2 storage guards; anti-pattern persist-before-apply)
    - index.html + geohist/index.html + geohist/guide.html footer regions (the reserved #lang-switcher-slot spans — engine un-hides and populates; zero markup changes)
  </read_first>
  <action>
    Extend js/i18n.js with the manual switch layer (I18N-03), reusing the Task-1 apply path — no second apply implementation:

    Render the switcher into #lang-switcher-slot (D-36): locate the span by id on the current page, remove its hidden attribute, and build three endonym text entries — English, Español, Português — separated by " · " text, matching the .footer-links visual family (plain links riding existing footer styles; zero new CSS, zero new classes). Active language renders as a non-interactive span carrying aria-current="true" and a lang attribute matching the active language (D-38 + granted a11y discretion). Inactive entries render as anchors with href="#", lang and hreflang attributes for their target language, and a data-persano-lang attribute carrying the machine value ('en' / 'es' / 'pt-BR').

    Bind exactly ONE delegated click listener on the slot container at init — re-renders of the entry set must never stack listeners (research anti-pattern): on a click, find the closest ancestor carrying data-persano-lang; when present, prevent the default navigation and call switchTo with that value; clicks on the active entry's span are naturally inert (no data-persano-lang attribute on the active span).

    switchTo(lang) semantics (D-29/D-30): no-op with no side effects when lang equals the current language — no fetch, no persist, no persano:langchange dispatch (Pitfall 6: only APPLIED switches emit). Otherwise reuse the Task-1 path minus detection: target 'en' restores the snapshot with zero fetch; 'es'/'pt-BR' fetch the dictionary (cached in memory per language) and apply. A failed fetch/apply leaves the current language, storage, and switcher state untouched — silent no-op. After a SUCCESSFUL apply and only then: persist the chosen language to localStorage persano.lang inside try/catch (manual choices only — auto-detected language is never persisted; restricted-storage failures are skipped silently), re-render the switcher's active state in place (aria-current moves to the new active entry, which becomes the non-interactive span), and dispatch persano:langchange with detail {from, to} as the last step.

    Guard rails carried over (D-30): every storage read/write, fetch, and JSON parse stays individually try/catch-wrapped; the switcher builder no-ops cleanly when the slot is absent (defense in depth — the script never loads where the slot does not exist, D-37).
  </action>
  <verify>
    <automated>(Select-String -Path js\i18n.js -Pattern 'lang-switcher-slot').Count -ge 1; (Select-String -Path js\i18n.js -Pattern 'data-persano-lang').Count -ge 1; (Select-String -Path js\i18n.js -Pattern 'aria-current').Count -ge 1; (Select-String -Path js\i18n.js -Pattern 'persano:langchange').Count -ge 1; (Select-String -Path js\i18n.js -Pattern 'persano\.lang').Count -ge 1; (Select-String -Path js\i18n.js -Pattern 'addEventListener').Count -eq 1</automated>
  </verify>
  <acceptance_criteria>
    - js/i18n.js renders the three endonym entries into #lang-switcher-slot and removes its hidden attribute; active entry is a non-interactive span with aria-current="true"; inactive entries are anchors carrying data-persano-lang values en / es / pt-BR
    - exactly one delegated click listener exists in the engine source (addEventListener count -eq 1) — no per-render listener stacking
    - clicking an inactive entry applies that language without reload (URL unchanged), persists persano.lang only after the apply succeeds, moves aria-current, and dispatches persano:langchange {from,to} as the last step
    - clicking the already-active language produces zero side effects: no fetch, no storage write, no event dispatch
    - a failed dictionary fetch during a manual switch leaves language, storage, and switcher state untouched (silent no-op, D-30)
    - stored preference wins over navigator.languages detection on reload (priority chain: valid stored value > browser detect > en)
    - localStorage write path is try/catch-guarded; reads accept only en/es/pt-BR values
  </acceptance_criteria>
  <done>A visitor can flip between English, Español, and Português from any keyed page's footer; the choice survives reload; ES→EN→ES round-trips restore the shipped markup exactly.</done>
</task>

<task type="auto">
  <name>Task 3: Key-coverage gate (scripts/i18n-keycheck.mjs) + engine-contract battery</name>
  <files>scripts/i18n-keycheck.mjs, js/i18n.js</files>
  <read_first>
    - js/i18n.js (state after Task 2 — battery asserts its contract)
    - js/i18n/es.json (dictionary under gate)
    - .planning/phases/03-i18n-engine-es-pt-br-dictionaries-switcher/03-RESEARCH.md (Pitfall 8 key-count drift, Pitfall 5 multi-attr parsing, Pattern 4 guards)
    - package.json (validate script shape — read-only here; wiring happens in plan 03-02)
  </read_first>
  <action>
    Create scripts/i18n-keycheck.mjs — a zero-dependency Node gate (node:fs / node:path only, no npm packages): (1) extract the live key surface from index.html, geohist/index.html, geohist/guide.html by matching data-i18n="key" and data-i18n-attr="attr:key[,attr:key…]" attributes, splitting comma-separated pairs on the first colon and taking the key side, trimming, and deduplicating into one set (data-i18n-attr-only itself is not a pair and yields nothing); (2) scan js/i18n/*.json, JSON.parse-validating each file; (3) for every dictionary, assert its key set EQUALS the extracted surface — exit 0 when every dictionary matches exactly, exit 1 printing the missing and extra keys per file otherwise. The script must pass with only es.json present (it gates whatever dictionaries exist) so plan 03-02 extends coverage to pt-BR.json without edits. Sanity-proof the gate has teeth: copy es.json to a temp file with one key removed, run the extraction logic against it, and confirm the mismatch is detected (manual one-off check, record the result).

    Then run the engine-contract battery over js/i18n.js and the three pages, recording PASS marks in the task output (Phase-2 battery style): (a) 'use strict' present; (b) module syntax absent — zero import/export tokens; (c) markup-parsing DOM assignment absent — the banned property (spelled in the acceptance criteria) count is zero; (d) at least 3 try/catch guards (storage read, storage write, fetch/parse); (e) the snapshot capture call precedes every apply invocation in source order on all code paths (D-28 / Pitfall 1); (f) persano:langchange and persano.lang literals present; (g) pt→pt-BR and es→es folding present; (h) documentElement.lang assignment present; (i) script tag exactly once per keyed page, zero script tags in geohist/privacy.html and 404.html; (j) the three keyed meta attrs present per D-27.

    Note for the record: local npm dev tooling is proxy-broken on this machine (Phase-1 known issue) — html-validate/linkinator run in CI on push; the battery is the local gate. If the battery surfaces engine gaps, fix js/i18n.js and re-run the full battery before finishing the task.
  </action>
  <verify>
    <automated>node scripts/i18n-keycheck.mjs; if ($LASTEXITCODE -ne 0) { exit 1 }; (Select-String -Path js\i18n.js -Pattern 'use strict').Count -ge 1; (Select-String -Path js\i18n.js -Pattern '\b(import|export)\b').Count -eq 0; (Select-String -Path js\i18n.js -Pattern 'innerHTML').Count -eq 0; (Select-String -Path js\i18n.js -Pattern 'try').Count -ge 3; (Select-String -Path js\i18n.js -Pattern 'documentElement\.lang').Count -ge 1; (Select-String -Path js\i18n.js -Pattern 'navigator\.languages').Count -ge 1</automated>
  </verify>
  <acceptance_criteria>
    - scripts/i18n-keycheck.mjs exists, uses only node built-ins, exits 0 against the current state (es.json exactly covering the 102-key surface)
    - the gate fails loud when a dictionary is missing one key or carries one extra key (fail-first sanity check performed and recorded)
    - engine source passes the battery: use-strict present, zero import/export tokens, zero markup-parsing DOM property (innerHTML count 0), ≥3 try/catch guards, documentElement.lang assignment, navigator.languages scan present
    - snapshot-before-apply ordering holds in source: capture is invoked before any dictionary apply on every code path
    - script tag exactly once in each of the 3 keyed pages; zero in privacy.html and 404.html; three D-27 meta attrs present
    - battery results recorded with PASS marks; any failure fixed and re-run to green before task completion
  </acceptance_criteria>
  <done>The key-coverage gate exists and is green for es.json; the engine contract battery passes end-to-end; drift between markup and dictionaries is now mechanically impossible to miss in CI (wiring lands in plan 03-02).</done>
</task>

</tasks>

<verification>
- Local battery: Task 1 node gates + Task 3 engine battery + keycheck all pass (npm dev tooling proxy-broken locally — CI arbitrates html-validate/linkinator on push, Phase-1/2 pattern).
- After the orchestrator's single controlled push (Phase-01 pattern, after plan 03-02 lands): CI validate job green — html-validate accepts the script tags and keyed meta attrs; linkinator sees no new hrefs (switcher anchors are JS-injected, dictionaries are fetch-only — invisible to static scanning, research Pitfall 7).
- Post-deploy smoke: /js/i18n/es.json returns 200 with content-type application/json; all five existing pages still 200 (scripts/smoke-check.sh re-run per Phase-1 pattern, plus the two dictionary URLs).
- Behavioral proof (human-check below): ES detection in place, silent fallback, switcher override + persistence, lang/title/meta sync.
</verification>

<human-check>
From a local server (repo root) or the deployed site: (1) set the browser language preference to Spanish (DevTools Sensors locale or chrome://settings) — hub, /geohist/, and /geohist/guide.html render Spanish in place, tab titles translate, html lang="es"; (2) click each switcher entry — language flips without reload, aria-current moves, choice persists across reload; (3) ES→EN→ES round-trip leaves no mixed-language text; (4) DevTools Network: with the dictionary request blocked (offline throttle), pages stay on shipped EN with no error UI (SC3); (5) owner reviews the es-419 copy per D-39/D-40 — flag wording preferences, especially FAQ policy-mirror sentences (D-41) and game-mode names.
</human-check>

<success_criteria>
- I18N-01 (ES half): complete es-419 dictionary covering the exact 102-key surface 1:1; EN still ships as raw HTML
- I18N-02: auto-detect applies Spanish in place from navigator.languages first-match; any failure falls back to shipped EN silently; no redirects, no URL changes
- I18N-03: manual switcher works, persists via persano.lang, overrides detection
- I18N-04: documentElement.lang + <title> + meta description sync on every applied switch
- SC3 hard gate: dictionary fetch/parse failure never changes what the visitor sees
- Key-coverage gate proves dictionary ↔ markup parity (102 = 102)
</success_criteria>

## must_haves.prohibitions carried into verification (descriptor-less, flagged-unverified)

P1–P4 (see section above) dispose flagged-unverified: P1 rides the owner copy review (D-39/D-41, judgment); P2 and P3 ride the Task 3 battery source assertions (no third-party fetch surface exists — the engine's only network call is the same-origin dictionary fetch; storage holds only the validated language code) without minting wired-check descriptors, per the spec-less fallback divergence; P4 is enforced by construction (three text entries, D-36) and asserted in Task 2's acceptance criteria. None auto-dismissed; none dropped.

<output>
Create `.planning/phases/03-i18n-engine-es-pt-br-dictionaries-switcher/03-01-SUMMARY.md` when done
</output>