---
phase: 10-gated-social-proof
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - geohist/index.html
  - css/base.css
  - js/i18n/ar.json
  - js/i18n/bn.json
  - js/i18n/de.json
  - js/i18n/el.json
  - js/i18n/es.json
  - js/i18n/fr.json
  - js/i18n/hi.json
  - js/i18n/id.json
  - js/i18n/it.json
  - js/i18n/ja.json
  - js/i18n/ko.json
  - js/i18n/nl.json
  - js/i18n/pl.json
  - js/i18n/pt-BR.json
  - js/i18n/ru.json
  - js/i18n/tr.json
  - js/i18n/ur.json
  - js/i18n/vi.json
  - js/i18n/zh.json
autonomous: true
requirements: [SEO-05, SEO-06]
estimate:
  tokens: 70000
  raw_tokens: 35000
  tasks: 2
  confidence: med
must_haves:
  truths:
    - "A visitor on the GeoHist landing page sees a 4-pill facts strip between the hero and the features section — 20 languages, Play offline, History + Geography, Android 7.0+ — rendered in their chosen language (keyed nodes ride the existing dictionary-swap pass), static (zero hyperlinks), with no visible h2 (the keyed aria-label names the section, keeping the features/gallery/faq h2 rhythm unbroken) — per D-01/D-02/D-03 (SEO-05)"
    - "Every pill claim matches its on-site anchor: '20 languages' ↔ FAQ languages answer (4 + 17 = 20), 'Play offline' ↔ features offline group, 'History + Geography' ↔ features trivia group, 'Android 7.0+' ↔ FAQ devices answer (SEO-05; drift = the P-10-1 violation)"
    - "The strip mirrors under RTL with zero new directional CSS: the flex row order flips via the existing [dir=\"rtl\"] mechanics (flex row order flips automatically); the new CSS block adds no direction: declarations (D-03)"
    - "The Tier-1 rating row exists in markup as a hidden div directly under the hero badge CTA, the whole row one attributed link (rel=\"noopener\") to the Play package URL, with keyed template fragments around ONE unkeyed number span holding the placeholder 0.0 — the owner flip is one attribute removal + one number edit with zero dictionary churn (D-04/D-05, SEO-06)"
    - "All 7 new keys (5 strip + 2 Tier-1) live in all 19 dictionaries at exact-set equality with the markup surface (171 → 178); the keycheck gate is green; hidden-markup keys are CI-load-bearing and the red gate has been proven in both directions (missing → FAIL naming file/key; extra → FAIL) — D-04/Phase-6 atomic-move precedent"
    - "The OFF row can never render its 0.0 placeholder: the .proof-row CSS carries the [hidden] insurance restatement (the site's established pattern — author display beats the UA hidden rule and must restate it)"
    - "Exactly one star representation ships: the inline-SVG star inside the row; the star character U+2605 appears in zero dictionary values and the keyed suffix text is star-free (Pitfall 6 resolution; the D-09 owner veto is the visual gate)"
  artifacts:
    - "geohist/index.html — new <section class=\"proof-strip\"> between hero close and features open, with keyed aria-label + 4 pill <li> (inline-SVG icon + keyed leaf span each); new hidden <div class=\"proof-row\"> inside .hero after the badge-cta anchor; EN default text lives in markup"
    - "css/base.css — one new .proof-* block: strip flex layout (wrap + gap), pill styling from pre-verified contrast tokens (muted text 9.38:1, teal icons 9.30:1), .proof-row styles + .proof-row[hidden] { display: none } insurance"
    - "js/i18n/*.json (all 19) — geohist.proof.aria, geohist.proof.pill-languages, geohist.proof.pill-offline, geohist.proof.pill-modes, geohist.proof.pill-android, geohist.tier1.prefix, geohist.tier1.suffix at exact 178-key parity; ja/zh values full-width punctuation"
  key_links:
    - "section.proof-strip aria-label → data-i18n-attr=\"aria-label:geohist.proof.aria\" data-i18n-attr-only — same shape as the nav precedent at geohist/index.html:51 (data-i18n-attr=\"aria-label:geohist.nav.aria\")"
    - "Tier-1 keyed fragments → dedicated leaf spans; the unkeyed .proof-row-score span is never touched by the engine (textContent-only contract, js/i18n.js:9-11) — data-i18n never sits on a node with element children"
    - "keycheck exact-set gate: markup surface (including hidden-row keys, extraction is visibility-blind — scripts/i18n-keycheck.mjs:61) ↔ all 19 dictionaries; PASS requires both missing and extra to be empty"
    - "Tier-1 row link → the same Play package URL already shipped at geohist/index.html:37 (JSON-LD sameAs) and :67 (badge CTA)"
  prohibitions:
    - statement: "The proof strip must never state a claim that is not verifiable on-site — pill copy stays in exact semantic agreement with its on-site anchor (FAQ answers / feature lines); no inflated variants (e.g. a '20+' style count, invented download figures, borrowed store marketing lines)"
      status: resolved
      verification: judgment
    - statement: "The flipped Tier-1 row must never display a rating number that is not the real aggregate rating visible on the live Play listing at flip time — never invented, never rounded up or flattered, never a placeholder styled to look real (the 0.0 placeholder exists precisely so an unflipped row self-flags)"
      status: resolved
      verification: judgment
    - statement: "The rating display must never imply more precision or a star-fill it does not have — exactly one star representation total (the inline-SVG star), suffix text star-free (no U+2605 in any dictionary value), no filled-star or multi-star visual"
      status: resolved
      verification: test
---

<objective>
Implement the site-side half of Phase 10's three-tier credibility story on `geohist/index.html` (SEO-05 + SEO-06, per D-01–D-05, D-09):

1. **Facts strip (SEO-05, D-01/D-02/D-03):** a 4-pill stat strip ("20 languages", "Play offline", "History + Geography", "Android 7.0+") as a new section between hero and features — static, keyed, aria-labeled, no visible h2, every claim verifiable on-site.
2. **Tier-1 gated proof row (SEO-06, D-04/D-05):** the "Rated X.X ★ on Google Play" row shipped OFF as hidden markup directly under the hero badge CTA, with its 2 i18n keys already live in all 19 dictionaries, an attributed whole-row link, and the 0.0 self-flagging placeholder — zero dictionary churn at the owner flip.
3. **Icons (D-09):** 5 agent-drafted inline SVGs (4 pill icons + star), 24px stroke, currentColor — owner veto happens pre-ship in plan 10-02's checkpoint.

Both tasks land as **atomic key-surface commits** (markup + CSS + all 19 dictionaries in the same commit — the keycheck gate extracts keys from hidden markup, so markup without dictionaries = CI red; Phase 6 red-gate precedent). Plan 10-02 (wave 2) adds the Tier-2 comment, the owner runbook, and the deploy.

Purpose: the landing page proves credibility with verifiable facts today, and the rating row's flip path is real and translated in 20 languages the moment the Play listing goes live — never fabricating ratings.
Output: 2 atomic commits on `geohist/index.html` + `css/base.css` + 19 dictionaries; key surface 171 → 178; validate chain green.
</objective>

<execution_context>
@C:/Users/Familia/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/Familia/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/10-gated-social-proof/10-CONTEXT.md
@.planning/phases/10-gated-social-proof/10-RESEARCH.md
@geohist/index.html
@css/base.css
@js/i18n.js
@scripts/i18n-keycheck.mjs
@.planning/phases/07-localization-20-rtl/07-CONTEXT.md
</context>

<artifacts_this_phase_produces>
<!-- MANDATORY: symbols this phase creates. The plan-review-convergence source-grounding pass reads this
     section to exclude newly-created symbols from drift verification. Plan 10-02 adds only the Tier-2
     HTML comment + .planning/phases/10-gated-social-proof/10-RUNBOOK.md (see that plan's section). -->

**New i18n keys (7, exact names pinned — land in all 19 dictionaries, EN default text lives in markup):**
- `geohist.proof.aria` — strip section aria-label, EN default "GeoHist at a glance"
- `geohist.proof.pill-languages` — EN default "20 languages"
- `geohist.proof.pill-offline` — EN default "Play offline"
- `geohist.proof.pill-modes` — EN default "History + Geography"
- `geohist.proof.pill-android` — EN default "Android 7.0+"
- `geohist.tier1.prefix` — EN default "Rated"
- `geohist.tier1.suffix` — EN default "on Google Play" (star-free — no U+2605 in any value)

**New HTML structure in geohist/index.html:**
- `<section class="proof-strip" aria-label="GeoHist at a glance" data-i18n-attr="aria-label:geohist.proof.aria" data-i18n-attr-only>` between `</section>` of hero (line 70) and `<section class="features">` (line 72), containing a plain `<ul>` with exactly 4 `<li>` pills
- Each pill `<li>`: one inline `<svg>` (aria-hidden="true", focusable="false", 24px stroke currentColor; glyph drafts: globe / cloud-off / map-pin / phone, carrying `data-icon="globe|offline|modes|android"`) + one leaf `<span data-i18n="geohist.proof.pill-*">`
- `<div class="proof-row" hidden>` inside `.hero`, after the `</a>` of `.badge-cta` (after line 69, before `</section>` line 70)
- Inside the row: one `<a href="https://play.google.com/store/apps/details?id=com.persano.geohisttrivia" rel="noopener">` wrapping one `<svg class="proof-row-star">` (16px stroke currentColor), `<span data-i18n="geohist.tier1.prefix">Rated</span>`, `<span class="proof-row-score">0.0</span>` (UNKEYED — the owner flip surface), `<span data-i18n="geohist.tier1.suffix">on Google Play</span>`

**New CSS selectors/rules in css/base.css (one block):**
- `.proof-strip`, `.proof-strip ul`, `.proof-strip li`, `.proof-strip svg`
- `.proof-row`, `.proof-row[hidden]` (mandatory insurance restatement), `.proof-row a`, `.proof-row-star`, `.proof-row-score`

**Key-surface size:** 171 → 178 across all 19 dictionaries (keycheck exact-set gate).
</artifacts_this_phase_produces>

<flagged_assumptions>
<!-- Spec-less probe fallback: $COVERAGE reported 4 unresolved items; all 4 are surfaced here and in plan
     10-02 — NONE resolved as edges, NONE backstopped, NONE dropped. Equality: 4 surfaced == 3 (below) + 1 (plan 10-02). -->
1. **A-10-05 [SEO-05 — probe: unclassified, "review manually"]** — Flagged assumption: the strip is 4 fixed static pills with no dynamic input; the empty/single-element/null-input edge class does not apply to static authored markup. Any future change that sources pill values dynamically is out of scope for this phase and must re-run the edge probe before shipping.
2. **A-10-06a [SEO-06 — probe: empty-input, "result for empty/single/null input?"]** — Flagged assumption: the unkeyed number span carries the placeholder `0.0` while OFF (never empty/null at ship). If the owner empties the span during the flip, no runtime validation exists BY DESIGN (zero-build static site) — the runbook's Rich Results Test ritual (plan 10-02) is the only post-flip check.
3. **A-10-06b [SEO-06 — probe: encoding, "whose definition of length/equality applies?"]** — Flagged assumption: the rating number format is Western digits with a decimal dot (e.g. 4.5) in all 20 locales — no per-locale number formatting keys (research A3; de "4,5" cosmetic divergence accepted and documented). Dictionary equality semantics are the keycheck gate's exact-set definition (already mechanical, not an open question).
</flagged_assumptions>

<tasks>

<task type="tracer">
  <name>Task 1 (tracer): Facts strip end-to-end — section + 4 pills + 5 keys in all 19 dictionaries, one atomic commit</name>
  <files>geohist/index.html, css/base.css, js/i18n/ar.json, js/i18n/bn.json, js/i18n/de.json, js/i18n/el.json, js/i18n/es.json, js/i18n/fr.json, js/i18n/hi.json, js/i18n/id.json, js/i18n/it.json, js/i18n/ja.json, js/i18n/ko.json, js/i18n/nl.json, js/i18n/pl.json, js/i18n/pt-BR.json, js/i18n/ru.json, js/i18n/tr.json, js/i18n/ur.json, js/i18n/vi.json, js/i18n/zh.json</files>
  <read_first>
    - geohist/index.html (whole file — current state of hero close at line 70, features open at line 72, the nav aria-label precedent at line 51, FAQ anchors at lines 136-146)
    - js/i18n.js (engine contract header lines 9-11 — textContent/setAttribute ONLY; SUPPORTED array lines 28-36 — the 20-language basis of the "20 languages" pill)
    - scripts/i18n-keycheck.mjs (exact-set-equality gate; visibility-blind extraction at line 61; CJK punct gate lines 44-49)
    - css/base.css (contrast pairs header lines 4-11; texture rule line 12 + line 246; [hidden] insurance precedent lines ~411-414; [dir="rtl"] block ~591; .features/.gallery spacing conventions)
    - .planning/phases/10-gated-social-proof/10-CONTEXT.md (D-01/D-02/D-03/D-09)
    - .planning/phases/10-gated-social-proof/10-RESEARCH.md (Facts Verification Table; Code Examples: strip section; Pitfall 3/4/8)
  </read_first>
  <reversibility rating="reversible">One section + one CSS block + 5 dictionary keys; removal costs nothing downstream (10-CONTEXT D-01).</reversibility>
  <action>
Build the thinnest end-to-end slice that touches every layer this phase modifies — HTML section, keyed spans, all 19 dictionaries, the keycheck gate — as ONE atomic commit (per D-01/D-02/D-03; the 5 strip keys are `geohist.proof.aria`, `geohist.proof.pill-languages`, `geohist.proof.pill-offline`, `geohist.proof.pill-modes`, `geohist.proof.pill-android`).

1. **Markup (geohist/index.html):** insert the strip section between the hero's closing tag (line 70) and the features section opening (line 72). Shape per the research Code Example: section carries `class="proof-strip"`, the keyed aria-label via `data-i18n-attr="aria-label:geohist.proof.aria"` with `data-i18n-attr-only` (the exact nav pattern from line 51), and a plain `<ul>` of exactly 4 `<li>` pills. NO visible h2 anywhere in the section (D-03: the aria-label carries the name; h2 rhythm stays features/gallery/faq). NO hyperlinks in the section (D-03: trust band, not navigation). NO third-party icon bytes — inline SVG only (D-09): 4 glyphs, 24x24, `fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"`, `aria-hidden="true" focusable="false"`, drafts per D-09 suggestions (globe: circle + meridians for languages; cloud-off/download for offline; map-pin for modes; phone outline for Android), each tagged `data-icon="globe|offline|modes|android"`. Each pill's text is a dedicated leaf span keyed `geohist.proof.pill-*` with EN default text exactly as pinned in Artifacts above (D-02 short-stat copy). Never attach a key to the `<li>` or any element that contains the SVG — keyed nodes are leaf text nodes or the engine wipes the icon (Pitfall 4).

2. **Dictionaries (all 19 files in js/i18n/):** add the 5 keys to every dictionary in the SAME commit as the markup (the gate is exact-set: markup-first or dictionaries-first both fail CI). Draft values per the Phase 7 machinery (07-CONTEXT D-06/D-07/D-08): app-strings glossary mining for terms, two-pass drafting, register table from Phase 7 verbatim, brand Latin "Google Play" kept where it appears. Constraints: (a) no empty values anywhere (keycheck fails empty strings); (b) ja.json and zh.json values use full-width punctuation only — no ASCII comma/colon/parens/quotes, no trailing ASCII period; the digit.digit period exception exists but NO pill value needs a period at all (Pitfall 3); (c) no pill value contains the text star character — the visual star lives in SVG only, never in dictionary values; (d) numbers inside values only where the claim itself carries one ("20", "7.0+") — never invent additional figures (P-10-1); (e) keep "20 languages" claims in exact agreement with the FAQ languages answer's count semantics per language (Pitfall 8).

3. **CSS (css/base.css):** one new `.proof-*` block following the research sketch: `.proof-strip` top margin matching the .features/.gallery spacing convention (~1.5rem); `ul` = flex, wrap (D-01 mobile), gap ~0.6rem 1.25rem, justify-content center, list-style none, zero margins; `li` = inline-flex, centered, gap ~0.45rem, text color var(--color-muted) (9.38:1 verified pair — never a new token), font-size ~0.95rem; `svg` color var(--color-accent-2) (teal secondary, 9.30:1). Constraints: NO `direction:` or other directional overrides anywhere in the new block (RTL mirroring rides the existing flex order flip — D-03); no texture utility class on the strip — background stays solid (body `--color-bg` inherits, or `--color-surface` only — the base.css:246 texture rule: solid strip, texture never sits behind copy).

4. Commit markup + CSS + all 19 dictionaries in ONE commit (atomic key-surface move, 171 → 176; Phase 6 red-gate precedent). Do not commit anything for plan 10-02's surfaces in this commit.
  </action>
  <verify>
    <automated>npm run validate:i18n 2>&1 | Select-String -SimpleMatch '176-key' | Measure-Object | ForEach-Object { if ($_.Count -ne 19) { exit 1 } } && npm run validate:html && rg -c 'data-i18n="geohist\.proof\.pill-' geohist/index.html | ForEach-Object { if ($_ -ne '4') { exit 1 } } && rg -c 'data-i18n-attr="aria-label:geohist\.proof\.aria" data-i18n-attr-only' geohist/index.html | ForEach-Object { if ($_ -ne '1') { exit 1 } } && node -e "const fs=require('fs');const h=fs.readFileSync('geohist/index.html','utf8');const m=h.match(/<section class=\"proof-strip\"[\s\S]*?<\/section>/);if(!m)process.exit(1);const s=m[0];if(/<a[\s>]/.test(s))process.exit(2);if(/<h2/.test(s))process.exit(3);const lis=(s.match(/<li>/g)||[]).length;if(lis!==4)process.exit(4);const svgs=(s.match(/<svg/g)||[]).length;if(svgs!==4)process.exit(5);if(!/data-i18n=\"geohist\.proof\.pill-languages\"/.test(s)||!/data-i18n=\"geohist\.proof\.pill-android\"/.test(s))process.exit(6)" && node -e "const fs=require('fs');const files=fs.readdirSync('js/i18n');const keys=['geohist.proof.aria','geohist.proof.pill-languages','geohist.proof.pill-offline','geohist.proof.pill-modes','geohist.proof.pill-android'];for(const f of files){const d=JSON.parse(fs.readFileSync('js/i18n/'+f,'utf8'));for(const k of keys){if(!(k in d))process.exit(10);if(!String(d[k]).trim())process.exit(11);if(String(d[k]).includes('\u2605'))process.exit(12)}};const blocks=require('fs').readFileSync('css/base.css','utf8').split('}').filter(b=>/\.proof-/.test(b));if(blocks.length<6)process.exit(20);for(const b of blocks){if(/direction:/.test(b))process.exit(21)}if(!blocks.some(b=>/color:\s*var\(--color-muted\)/.test(b)))process.exit(22);if(!blocks.some(b=>/var\(--color-accent-2\)/.test(b)))process.exit(23)"</automated>
  </verify>
  <acceptance_criteria>
    - npm run validate:i18n PASS with the 176-key live surface reported for all 19 dictionaries (exact-set equality, zero empty values, CJK punct gate green)
    - npm run validate:html green (strip markup valid)
    - geohist/index.html contains exactly 4 pill keyed spans (geohist.proof.pill-languages / pill-offline / pill-modes / pill-android), exactly 1 keyed aria-label attribute pair, exactly 4 li, exactly 4 inline svgs inside the strip section
    - The strip section region contains zero anchor elements and zero h2 elements (trust band + no-visible-h2 per D-03)
    - All 19 dictionaries contain all 5 strip keys with non-empty values and zero U+2605 occurrences in any value
    - The new CSS block contains zero direction: declarations (RTL rides the flex order flip) and styles pill text with var(--color-muted) and icons with var(--color-accent-2) — both pre-verified AA pairs; no texture utility class on the strip
    - The commit contains markup + css/base.css + all 19 dictionaries together (atomic key-surface move 171 → 176)
  </acceptance_criteria>
  <done>The facts strip renders as a static 4-pill band between hero and features, translated in all 19 dictionary languages via the existing engine, with every claim matching its on-site anchor, and the atomic commit is green on the validate chain.</done>
</task>

<task type="auto">
  <name>Task 2: Tier-1 gated proof row — hidden markup under the badge CTA + 2 keys in all 19 dictionaries + [hidden] insurance + red-gate proof</name>
  <files>geohist/index.html, css/base.css, js/i18n/ar.json, js/i18n/bn.json, js/i18n/de.json, js/i18n/el.json, js/i18n/es.json, js/i18n/fr.json, js/i18n/hi.json, js/i18n/id.json, js/i18n/it.json, js/i18n/ja.json, js/i18n/ko.json, js/i18n/nl.json, js/i18n/pl.json, js/i18n/pt-BR.json, js/i18n/ru.json, js/i18n/tr.json, js/i18n/ur.json, js/i18n/vi.json, js/i18n/zh.json</files>
  <read_first>
    - geohist/index.html (post-Task-1 state — hero section lines 60-70, badge-cta anchor lines 67-69, insertion point before the hero close)
    - js/i18n.js (engine contract: textContent-only, hidden nodes ARE snapshotted — captureSnapshot has no visibility filter, so the hidden row is translated in all 19 languages the moment keys land)
    - css/base.css ([hidden] insurance precedents: .consent-banner[hidden] and .form-status[hidden] with the "author display beats [hidden] UA style — must restate the rule" comments)
    - .planning/phases/10-gated-social-proof/10-RESEARCH.md (Code Example: Tier-1 row; Pitfall 1 [hidden] display-override trap; Pitfall 4 fragment placement; Pitfall 5 position-free fragments; Pitfall 6 star-free suffix)
    - .planning/phases/10-gated-social-proof/10-CONTEXT.md (D-04/D-05)
  </read_first>
  <reversibility rating="reversible">Flip is one attribute + one number; rollback re-adds the attribute (10-CONTEXT D-04).</reversibility>
  <action>
Add the Tier-1 rating row OFF, per D-04/D-05, as a SECOND atomic commit (markup + 2 new keys across all 19 dictionaries together; surface moves 176 → 178).

1. **Markup (geohist/index.html):** inside the hero section, immediately after the badge-cta anchor's closing tag (after line 69), insert the row exactly in the research Code Example shape: a `div` with `class="proof-row"` carrying the `hidden` attribute (D-04: authoring-time gate, not commented-out markup, not a JS flag). Inside it, exactly one anchor to `https://play.google.com/store/apps/details?id=com.persano.geohisttrivia` with `rel="noopener"` (D-05: whole-row attributed link — the same package URL already shipped at lines 37/67). The anchor contains: one inline SVG star (`class="proof-row-star"`, 16x16, viewBox 0 0 24 24, fill="none" stroke="currentColor" stroke-width="2", aria-hidden="true" focusable="false" — the row's ONLY star representation), then three sibling spans in fixed DOM slot order: `<span data-i18n="geohist.tier1.prefix">Rated</span>`, `<span class="proof-row-score">0.0</span>` (UNKEYED — the D-04 owner-edit surface; placeholder 0.0 self-flags if the hidden attribute is ever lost), `<span data-i18n="geohist.tier1.suffix">on Google Play</span>`. The keyed spans are dedicated leaf wrappers — never attach a key to the anchor itself (the engine's textContent assignment would wipe the star and the number span; Pitfall 4). Never add a third fragment or a second number span (Pitfall 5: re-draft fragment strings, never the DOM).

2. **Dictionaries (all 19):** add `geohist.tier1.prefix` and `geohist.tier1.suffix` to every dictionary in the SAME commit. Draft them as POSITION-FREE fragments — each language's two strings must make `[prefix] 4.5 [suffix]` read naturally in the fixed DOM slot order (ja draft pattern: prefix carrying the store phrase, suffix the rating noun; zh analog; RTL locales flow with the number slot staying valid, Western digits per site convention). Two-pass drafting + register table per Phase 7. Constraints: (a) values contain NO rating number and NO second number — the number lives only in the unkeyed span (a value-borne number would double-print beside the span; the digit.digit punct exception is irrelevant here because the span is unkeyed); (b) values contain no text star character and no "star" wording that would double the SVG's star (Pitfall 6 — the suffix names the store, the SVG is the star); (c) ja/zh full-width punctuation only, no trailing ASCII period (Pitfall 3); (d) the Play brand stays Latin in all 19 (Phase 7 precedent); (e) no empty values.

3. **CSS (css/base.css, same .proof-* block area):** `.proof-row` centered under the badge; anchor styled from the verified gold pair var(--color-accent), ~0.9rem; the star inherits currentColor. MANDATORY: add the `[hidden]` insurance restatement `.proof-row[hidden] { display: none; }` in the same block with the established comment pattern (author display beats the UA hidden rule — must restate; Pitfall 1: without it, any future display rule on .proof-row renders the 0.0 row visibly in every language).

4. **Red-gate proof (both directions, locally, then restore — Phase 6 precedent):** (a) missing-direction: remove `geohist.tier1.suffix` from ONE dictionary, run the i18n validate — it MUST fail naming that file/key — then restore the file exactly (git checkout) and confirm green; (b) extra-direction: add one bogus key (e.g. `geohist.proof.bogus`) to one dictionary, run the i18n validate — it MUST fail as an extra key — then restore and confirm green. Both proofs are transient local state; the committed tree must be green.

5. Commit markup + CSS + all 19 dictionaries as one atomic commit (176 → 178). Do not touch the JSON-LD block or anything plan 10-02 owns.
  </action>
  <verify>
    <automated>npm run validate:i18n 2>&1 | Select-String -SimpleMatch '178-key' | Measure-Object | ForEach-Object { if ($_.Count -ne 19) { exit 1 } } && npm run validate:html && rg -c '<div class="proof-row" hidden>' geohist/index.html | ForEach-Object { if ($_ -ne '1') { exit 1 } } && rg -c '<span class="proof-row-score">0\.0</span>' geohist/index.html | ForEach-Object { if ($_ -ne '1') { exit 1 } } && rg -c 'rel="noopener"' geohist/index.html | ForEach-Object { if ($_ -lt '2') { exit 1 } } && rg -c '\.proof-row\[hidden\]' css/base.css | ForEach-Object { if ($_ -ne '1') { exit 1 } } && node -e "const fs=require('fs');const h=fs.readFileSync('geohist/index.html','utf8');const m=h.match(/<div class=\"proof-row\" hidden>[\s\S]*?<\/div>/);if(!m)process.exit(1);const s=m[0];if(!/rel=\"noopener\"/.test(s))process.exit(2);if(!/play\.google\.com\/store\/apps\/details\?id=com\.persano\.geohisttrivia/.test(s))process.exit(3);if((s.match(/<svg/g)||[]).length!==1)process.exit(4);if(!/data-i18n=\"geohist\.tier1\.prefix\"/.test(s)||!/data-i18n=\"geohist\.tier1\.suffix\"/.test(s))process.exit(5);if(/data-i18n[^>]*proof-row-score/.test(s))process.exit(6);if((s.match(/data-i18n=/g)||[]).length!==2)process.exit(7)" && node -e "const fs=require('fs');for(const f of fs.readdirSync('js/i18n')){const d=JSON.parse(fs.readFileSync('js/i18n/'+f,'utf8'));for(const v of Object.values(d)){if(String(v).includes('\u2605'))process.exit(10)}if(!('geohist.tier1.prefix' in d)||!('geohist.tier1.suffix' in d))process.exit(11);if(!String(d['geohist.tier1.prefix']).trim()||!String(d['geohist.tier1.suffix']).trim())process.exit(12);if(/\d/.test(String(d['geohist.tier1.prefix'])+String(d['geohist.tier1.suffix'])))process.exit(13)}" && node -e "const fs=require('fs');const p='js/i18n/es.json';const d=JSON.parse(fs.readFileSync(p,'utf8'));delete d['geohist.tier1.suffix'];fs.writeFileSync(p,JSON.stringify(d,null,2)+'\n')"; npm run validate:i18n 2>&1 | Out-Null; $redMissing = $LASTEXITCODE -ne 0; git checkout -- js/i18n/es.json; node -e "const fs=require('fs');const p='js/i18n/es.json';const d=JSON.parse(fs.readFileSync(p,'utf8'));d['geohist.proof.bogus']='x';fs.writeFileSync(p,JSON.stringify(d,null,2)+'\n')"; npm run validate:i18n 2>&1 | Out-Null; $redExtra = $LASTEXITCODE -ne 0; git checkout -- js/i18n/es.json; if (-not $redMissing) { exit 40 }; if (-not $redExtra) { exit 41 }; npm run validate:i18n 2>&1 | Select-String -SimpleMatch '178-key' | Measure-Object | ForEach-Object { if ($_.Count -ne 19) { exit 42 } }</automated>
  </verify>
  <acceptance_criteria>
    - npm run validate:i18n PASS with the 178-key live surface for all 19 dictionaries; validate:html green
    - geohist/index.html contains exactly 1 `<div class="proof-row" hidden>`, inside the hero section after the badge-cta anchor; the row's single anchor carries rel="noopener" and the exact Play package URL
    - The row contains exactly 1 svg (the star), exactly 2 keyed leaf spans (tier1.prefix / tier1.suffix), and exactly 1 unkeyed number span whose text is the placeholder 0.0; the number span carries no key attribute
    - css/base.css contains exactly 1 `.proof-row[hidden]` insurance rule
    - All 19 dictionaries contain both tier1 keys, non-empty, with zero digits inside the fragment values and zero U+2605 star characters in any value of any dictionary
    - Red gate proven both directions locally: missing key → validate FAIL naming file/key; extra key → validate FAIL; tree restored and green after each
    - The commit contains markup + CSS + all 19 dictionaries together (atomic move 176 → 178)
  </acceptance_criteria>
  <done>The Tier-1 row ships OFF exactly as specified: hidden attributed-link row under the badge CTA, translated-in-advance fragments in all 19 dictionaries, 0.0 self-flagging placeholder, [hidden] insurance CSS, red gate proven, atomic commit green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| site markup → Google crawler/rich-results pipeline | Structured data and visible claims are what Google's spam/manual-action systems judge; fabricated ratings or drifted claims here are search-manipulation surface |
| owner flip action → displayed rating | The unkeyed number span is human-edited at flip; nothing mechanical validates it against the real Play aggregate |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-01 | Spoofing | css/base.css display rule vs hidden attribute | medium | mitigate | `.proof-row[hidden] { display: none }` insurance restatement (Pitfall 1) + acceptance grep — the 0.0 placeholder can never render while OFF |
| T-10-02 | Tampering | unkeyed .proof-row-score span (owner flip) | medium | accept | By design (D-04): the flip is a deliberate human edit gated by the 10-RUNBOOK evidence gate (listing live AND real aggregate visible); P-10-2 records the honesty constraint; no runtime validation exists in a zero-build site |
| T-10-03 | Repudiation | pill claims drifting from on-site anchors | medium | mitigate | Every pill claim pinned to its verbatim on-site anchor (Facts Verification Table) + keyed values make future wording edits dictionary edits riding the session refresh convention (D-07) |
| T-10-04 | Tampering | markup/dictionary split commits | high | mitigate | Atomic-commit rule + red-gate proof in both directions (this plan's Task 2 verify) — CI red is the correct outcome for a split |
| T-10-SC | Tampering | package installs in this plan | low | accept | Zero installs: pure HTML/CSS/JSON edits; Package Legitimacy Audit table is empty (RESEARCH.md) |
</threat_model>

<verification>
- npm run validate (full chain: html, domain, links, i18n-detect, i18n) green on the final tree with the 178-key surface across 19 dictionaries
- Structural asserts (Task 1 + Task 2 verify blocks): 4 pills + aria-label, zero anchors/h2 in strip, hidden row with attributed link + 0.0 span, [hidden] insurance rule present, U+2605 in zero dictionary values, no directional CSS in the new block
- Red gate proven both directions locally (missing + extra), tree restored green
- Two atomic commits: strip (171→176) and Tier-1 row (176→178), each carrying markup + CSS + all 19 dictionaries
- Deploy and owner veto are plan 10-02's wave-2 work (D-09 pre-ship gate) — nothing in this plan pushes to main
</verification>

<success_criteria>
- A visitor sees the 4-pill facts strip in their chosen language between hero and features; every claim verifiable on-site; strip is static (no links) with no visible h2
- The facts survive a language switch in any of the 20 locales (keys live in all 19 dictionaries; engine snapshots hidden AND visible nodes)
- The Tier-1 row is invisible in every language while OFF; if the hidden attribute were ever lost, the row self-flags with 0.0 rather than looking real
- The owner flip path is exactly two edits (remove attribute, replace 0.0) with zero dictionary work
- CI green throughout: exact-set keycheck 178, html-validate, CJK punct gate
</success_criteria>

<output>
Create `.planning/phases/10-gated-social-proof/10-01-SUMMARY.md` when done
</output>
