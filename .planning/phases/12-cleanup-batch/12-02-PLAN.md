---
phase: 12-cleanup-batch
plan: 02
type: execute
wave: 2
depends_on: ["12-01"]
files_modified:
  - .planning/phases/12-cleanup-batch/12-RECORDS.md
  - .planning/phases/12-cleanup-batch/12-UAT.md
  - .planning/STATE.md
  - .planning/WINDOWS.md
  - css/base.css
autonomous: false
requirements: [CLEAN-02, CLEAN-03]

estimate:
  tokens: 50000
  raw_tokens: 28000
  tasks: 3
  confidence: low

user_setup:
  - service: owner-device
    why: "CLEAN-03 is a human-only visual check of Urdu Nastaliq rendering on the owner's real Android device — the agent cannot execute it"
    dashboard_config:
      - task: "Execute the 12-UAT.md checklist on the real device (Chrome on Android, https://geohisttrivia.com) and report per-criterion outcomes + device metadata at the Task 3 checkpoint"

must_haves:
  truths:
    - "12-RECORDS.md §3 documents zh as Simplified-only with verbatim evidence from both source trees (site engine + dictionary; app values-zh + locales_config) and states the zh-TW/zh-Hant fold consequence explicitly"
    - "js/i18n/ still holds exactly 19 dictionaries and zh.json remains the only zh* file — no Traditional variant added (roadmap success criterion 3)"
    - "12-UAT.md exists per the NN-UAT convention with the five-criterion Urdu device-check checklist and valueless result lines before the owner check — nothing pre-filled"
    - "Owner device check recorded from the owner's real device: all five criteria carry the owner-reported outcome with device metadata (model / Android version / browser version); the ur page renders RTL with its line-height override and readable Nastaliq text"
    - "STATE.md Urdu blocker line keeps its original text and carries a dated bracketed supersession note citing 12-UAT.md; WINDOWS.md row 10 is marked fixed (pass branch)"
  artifacts:
    - ".planning/phases/12-cleanup-batch/12-RECORDS.md (§3 zh variant confirmation appended)"
    - ".planning/phases/12-cleanup-batch/12-UAT.md (new: five-criterion device-check record, filled at the Task 3 checkpoint)"
    - ".planning/STATE.md (dated supersession note on the Urdu blocker line — pass branch)"
    - ".planning/WINDOWS.md (row 10 status fixed + open_count decremented — pass branch)"
    - "css/base.css (CONDITIONAL: trivial line-height/font tweak inside the existing html[lang=\"ur\"] rules only if the device check reports clipping — then re-validate and the owner re-checks)"
  key_links:
    - "DETECT_TABLE fold (js/i18n.js:122, doc at :110) → every zh-* browser locale receives the single zh.json — the §3 record must state this consequence, not just the list membership"
    - "12-UAT.md criteria → css/base.css:667-675 (what the owner observes must match the shipped values: body 2.0, h1-h3 1.9) and js/i18n.js:43/:91 (RTL_LANGS + dir flip)"
    - "12-UAT.md outcome → WINDOWS.md row 10 closure (row 10's three deferred owner-rendered items — ur line-height, select operability, ar mirror — are exactly criteria 5, 4, and 2/1 of the checklist)"
  prohibitions:
    - statement: "MUST NOT add any zh-TW / zh-Hant / zh-CN dictionary or a second zh entry — the site keeps exactly one zh dictionary and 19 total, matching the app's single values-zh (roadmap success criterion 3; explicit Deferred Idea)"
      status: resolved
      verification: test
      reason: "Product parity with the app's locale surface; ×19 churn for zero user benefit (REQUIREMENTS.md Out of Scope)"
    - statement: "MUST NOT document the zh variant while obscuring the fold — the record must state plainly that zh-TW/zh-Hant browser locales receive the Simplified dictionary, by design"
      status: resolved
      verification: judgment
      reason: "Transparency of the record: a reader must learn the fold consequence, not just 'zh exists'"
    - statement: "MUST NOT pre-fill or assume the owner device-check outcome — every criterion's result line stays valueless until the owner reports from the real device; no synthetic pass"
      status: resolved
      verification: judgment
      reason: "Honesty of the record (AGENTS.md mechanical predicate: records land clean or with explicit issue framing; a fabricated pass would be a false blocker-closure)"
    - statement: "MUST NOT silently delete historical record text — the STATE.md Urdu blocker line and the WINDOWS.md row keep their original text; closure rides the dated supersession note and the ledger CLI"
      status: resolved
      verification: judgment
      reason: "AGENTS.md supersession-note policy for historical records"
---

<objective>
Confirm the zh variant against the app's real locale surface and document it (CLEAN-02), and record the Urdu Nastaliq check from the owner's real device (CLEAN-03): the agent verifies the code plumbing, prepares the exact checklist + record, and the owner executes the device check at a blocking checkpoint.

Purpose: closes the two locale edge-case debts from STATE.md's blocker list before Phase 13's home migration multiplies the pages every locale touches.
Output: 12-RECORDS.md §3 (zh evidence), 12-UAT.md (device-check record, filled by the owner), dated supersession note on the Urdu blocker line, WINDOWS.md row 10 fixed.
</objective>

<execution_context>
@C:/Users/Familia/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/Familia/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/12-cleanup-batch/12-RESEARCH.md
@.planning/phases/12-cleanup-batch/12-RECORDS.md
@js/i18n.js
@css/base.css

# App repo (read-only source of truth for CLEAN-02 evidence):
# C:/Users/Familia/antigravity/GeoHist-Trivia/shared/src/androidMain/res/values-zh/strings.xml
# C:/Users/Familia/antigravity/GeoHist-Trivia/shared/src/androidMain/res/xml/locales_config.xml
# UAT convention template: .planning/milestones/v2.0-phases/10-gated-social-proof/10-UAT.md
</context>

## Artifacts this phase produces

See plan 12-01's "Artifacts this phase produces" table (authoritative full-phase set). This plan adds:
`12-RECORDS.md` §3, `12-UAT.md`, the STATE.md Urdu-blocker supersession note, the WINDOWS.md row-10
closure (pass branch), and — only if the device check reports clipping — a trivial tweak confined to
the existing `html[lang="ur"]` rules in `css/base.css`.

## Planner assumption notes (probe edges — surfaced, no silent drops)

CLEAN-02 and CLEAN-03 probe edges arrived `unresolved`/`unclassified` and were resolved by the
planner into explicit acceptance criteria (full 4-edge table in plan 12-01): CLEAN-02 → §3 record
with verbatim two-tree evidence; CLEAN-03 → five-criterion 12-UAT.md record with mechanical
pass/issue predicate, gated by the blocking owner checkpoint (plan is `autonomous: false`).
Canon breadcrumb (dropped, not minted): secrets in the UAT record → `.planning/` public-serving
rule + /gsd-secure-phase; the record carries only device model/OS/browser.

<tasks>

<task type="auto">
  <name>Task 1: zh Simplified-only confirmation record (CLEAN-02)</name>
  <files>.planning/phases/12-cleanup-batch/12-RECORDS.md</files>
  <read_first>
    - js/i18n.js (lines 28-36 SUPPORTED list — single 'zh' at line 35; lines 105-125 DETECT_TABLE — fold doc at line 110, 'zh': 'zh' at line 122)
    - js/i18n/zh.json (header + the two sample keys cited below; confirm the values are Simplified)
    - C:/Users/Familia/antigravity/GeoHist-Trivia/shared/src/androidMain/res/values-zh/strings.xml (app_name + subtitle + a win-threshold string)
    - C:/Users/Familia/antigravity/GeoHist-Trivia/shared/src/androidMain/res/xml/locales_config.xml (20 entries, single bare zh)
    - Directory listing of C:/Users/Familia/antigravity/GeoHist-Trivia/shared/src/androidMain/res/ (confirm values-zh exists and NO values-zh-rTW / values-zh-rCN / values-zh-TW)
    - .planning/phases/12-cleanup-batch/12-RESEARCH.md ("CLEAN-02 confirmation record" evidence table — the authoritative evidence set)
    - .planning/phases/12-cleanup-batch/12-RECORDS.md (as left by plan 12-01 — append §3 after §2)
    - scripts/check-no-old-domain.mjs (skim — no legacy-host literal in the record)
  </read_first>
  <action>
    Append `## 3. zh variant confirmation (CLEAN-02)` to 12-RECORDS.md with these subsections, every claim carrying its file:line evidence:
    (a) Site engine evidence: SUPPORTED list holds a single 'zh' and no 'zh-TW' (js/i18n.js:35); DETECT_TABLE maps 'zh' → 'zh' (js/i18n.js:122); the table's documented purpose is folding multi-subtag locales (js/i18n.js:110, example zh-Hant-CN → zh).
    (b) Dictionary evidence: js/i18n/zh.json is the ONLY zh* dictionary on the site (19 dictionaries total); quote two verbatim Simplified samples — "changelog.back.link": "返回游戏" and "changelog.meta.title": "最新动态 — 地史知识问答".
    (c) App parity evidence: the app res tree ships exactly one values-zh directory — no values-zh-rTW, values-zh-rCN, or values-zh-TW; app_name is 地史知识问答; locales_config.xml lists a single bare zh among its 20 entries.
    (d) Confirmation statement: zh is Simplified-only on both sides. A visitor whose browser reports zh-TW, zh-Hant, or zh-CN folds via DETECT_TABLE to the same zh dictionary and receives Simplified Chinese — this is the documented consequence of the single-URL keyed engine, not a gap; the app ships no Traditional strings, so no Traditional dictionary is added (per roadmap success criterion 3, no zh-TW surface unprompted).
    (e) Observation (documented, no action): the app ships generic pt (values-pt) while the site ships pt-BR — established in Phase 3, unrelated to the zh scope.
    Then run `npm run validate:i18n` (must exit 0 — 178 keys × 19 set-equality). No secrets, no legacy-host literal, do NOT modify zh.json or any dictionary, do NOT touch js/i18n.js.
  </action>
  <verify>
    <automated>
$r='.planning/phases/12-cleanup-batch/12-RECORDS.md'
(Select-String -Path $r -Pattern '## 3\. zh variant confirmation \(CLEAN-02\)').Count -eq 1
(Select-String -Path $r -Pattern '返回游戏').Count -ge 1
(Select-String -Path $r -Pattern '地史知识问答').Count -ge 2
(Select-String -Path $r -Pattern 'values-zh').Count -ge 2
(Get-ChildItem js/i18n -Filter zh*.json | Measure-Object).Count -eq 1
(Get-ChildItem js/i18n -Filter *.json | Measure-Object).Count -eq 19
npm run validate:i18n; "KEYCHECK_EXIT=$LASTEXITCODE"
    </automated>
  </verify>
  <acceptance_criteria>
    - SOURCE: §3 exists with the exact heading "## 3. zh variant confirmation (CLEAN-02)" and subsections (a)-(e)
    - SOURCE: §3 quotes both verbatim Simplified dictionary samples and the app_name value 地史知识问答
    - SOURCE: §3 states the fold consequence explicitly (zh-TW / zh-Hant / zh-CN browser locales receive the Simplified dictionary by design) and the no-Traditional-dictionary decision
    - SOURCE: §3 cites file:line for the site-side claims (js/i18n.js:35, :110, :122) and the app-side surface (values-zh only; locales_config.xml single bare zh)
    - SOURCE: js/i18n/ contains exactly 19 JSON dictionaries and exactly one zh* file (zh.json) — nothing added
    - COMMAND: `npm run validate:i18n` exits 0
  </acceptance_criteria>
  <done>zh variant confirmed Simplified-only against both source trees with verbatim evidence recorded in §3; dictionary set untouched (19 × zh.json sole zh*); keycheck green</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Prepare 12-UAT.md device-check record (CLEAN-03 agent side)</name>
  <files>.planning/phases/12-cleanup-batch/12-UAT.md</files>
  <read_first>
    - .planning/milestones/v2.0-phases/10-gated-social-proof/10-UAT.md (NN-UAT convention: YAML frontmatter status/phase/source/started/updated, ## Current Test, ## Tests with numbered blocks each ending in a result line, ## Summary totals, ## Gaps)
    - .planning/phases/12-cleanup-batch/12-RESEARCH.md ("CLEAN-03 owner device-check checklist" — the five criteria and the pitfalls: file:// voids the check; the footer select is the only language override)
    - css/base.css (lines 657-675: empty [dir="rtl"] seed block, html[lang="ur"] body line-height 2, h1-h3 1.9)
    - js/i18n.js (line 43 RTL_LANGS with ar+ur; line 91 dir flip in the same pass as lang sync)
    - .planning/WINDOWS.md (row 10 — the three deferred owner-rendered items this record will close)
    - .planning/STATE.md (Blockers — the Urdu line this record's outcome will supersede)
  </read_first>
  <action>
    Create `.planning/phases/12-cleanup-batch/12-UAT.md` per the NN-UAT convention:
    (a) YAML frontmatter: status: pending, phase: 12-cleanup-batch, source: [12-02-PLAN.md], started/updated = current ISO timestamps (get via node C:/Users/Familia/.config/opencode/gsd-core/bin/gsd-tools.cjs current-timestamp).
    (b) `## Current Test`: [awaiting owner device check].
    (c) `## Device` block with EMPTY slots the owner fills: Device model / Android version / Browser + version.
    (d) `## Owner checklist` (the execution instructions): use a real Android device with Chrome; open https://geohisttrivia.com directly — never via file:// (the same-origin dictionary fetch fails silently there and the page stays EN, voiding the check); the footer language select is the only override path (no query parameter exists — localStorage persano.lang carries the choice across pages); take optional screenshots.
    (e) `## Tests` with FIVE numbered blocks, each with a concrete `expected:` line and then a bare result line carrying NO value (the mechanical predicate fills it at Task 3; nothing pre-filled anywhere in the file):
      1. ur direction flip — expected: after selecting اردو in the footer switcher the page renders RTL: text right-aligned, nav/footer/lists mirrored via the logical properties, html element carries dir="rtl" lang="ur"
      2. ur line-height override — expected: body text visually double-spaced (line-height 2.0 per css/base.css:667-669), headings ~1.9 (:671-675), no clipped ascenders/descenders on multi-line Urdu paragraphs
      3. ur Nastaliq shaping — expected: Urdu renders as flowing Nastaliq (Android system font), letters correctly joined, no tofu/box glyphs, no isolated-letter fallback
      4. ar mirror sanity — expected: switching to العربية flips RTL the same way (closes the WINDOWS.md row 10 mirrored-layout item); game-screenshot gallery tiles stay NOT mirrored (intentional — the game's own rendering)
      5. switcher operability — expected: the footer select opens, اردو is selectable, and the choice persists when navigating to another page (closes the WINDOWS.md row 10 select-operability item)
    (f) `## Summary` skeleton: total: 5, passed: 0, issues: 0, pending: 5, skipped: 0, blocked: 0.
    (g) `## Gaps`: empty.
    Discipline: the file is publicly served — only device model/OS/browser, never secrets; no legacy-host literal; leave every result line valueless.
  </action>
  <verify>
    <automated>
$u='.planning/phases/12-cleanup-batch/12-UAT.md'
(Select-String -Path $u -Pattern '^result:\s*$').Count -eq 5
(Select-String -Path $u -Pattern '^result: ').Count -eq 0
(Select-String -Path $u -Pattern 'status: pending').Count -ge 1
(Select-String -Path $u -Pattern 'https://geohisttrivia\.com').Count -ge 1
(Select-String -Path $u -Pattern 'pending: 5').Count -ge 1
    </automated>
  </verify>
  <acceptance_criteria>
    - SOURCE: 12-UAT.md exists with NN-UAT frontmatter (status: pending), ## Current Test, ## Device slots (empty), ## Owner checklist, ## Tests (5 numbered blocks), ## Summary skeleton (total: 5 / pending: 5), ## Gaps
    - SOURCE: exactly five result lines, all valueless (anchored regex for a filled outcome matches zero times — nothing pre-filled)
    - SOURCE: checklist contains https://geohisttrivia.com and the file:// voidance warning; contains the footer-select-only override note
    - SOURCE: expected lines cite the shipped values (2.0 / 1.9 line-heights) and the dir="rtl" lang="ur" element state
    - SOURCE: no secrets, no legacy-host literal
  </acceptance_criteria>
  <done>Device-check record prepared per NN-UAT convention: five concrete criteria, valueless result lines, empty device slots, execution checklist embedded — ready for the owner checkpoint</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: OWNER DEVICE CHECK — Urdu Nastaliq on real device (CLEAN-03 human side)</name>
  <files>.planning/phases/12-cleanup-batch/12-UAT.md, .planning/STATE.md, .planning/WINDOWS.md, css/base.css</files>
  <read_first>
    - .planning/phases/12-cleanup-batch/12-UAT.md (the checklist Task 2 prepared — present it verbatim to the owner)
    - css/base.css (lines 661-675 — the ur rules a trivial fix would touch)
    - .planning/STATE.md (Blockers — the Urdu line)
    - .planning/WINDOWS.md (row 10 + frontmatter open_count: 16)
  </read_first>
  <what-built>The RTL flip, ur line-height overrides (body 2.0 / h1-h3 1.9), and the footer switcher shipped in Phase 7 and are verified in code (js/i18n.js:43/:91, css/base.css:657-675); what remains is the human visual check of real-device Nastaliq rendering — the exact debt STATE.md's blocker line names.</what-built>
  <how-to-verify>
    On the owner's REAL Android device (Chrome): open https://geohisttrivia.com (never file://), use the footer language switcher → اردو, then walk the five criteria in 12-UAT.md: (1) RTL direction flip incl. mirrored nav/footer/lists, (2) line-height 2.0 body / 1.9 headings with no clipped ascenders/descenders, (3) flowing Nastaliq shaping — joined letters, no tofu, no isolated-letter fallback, (4) العربية mirror sanity (gallery tiles intentionally NOT mirrored), (5) switcher operability + persistence across pages. Report per-criterion outcomes plus device model, Android version, browser version.
  </how-to-verify>
  <resume-signal>
    Owner replies with all five outcomes (pass or a description of what is wrong per criterion) + device metadata. "All pass" → executor takes the PASS branch. Any issue → executor takes the ISSUE branch.
  </resume-signal>
  <action>
    PASS branch: fill each of the five result lines in 12-UAT.md with the owner-reported outcome (all pass), update the frontmatter status to complete and both timestamps, fill ## Device, set ## Summary to total: 5 / passed: 5 / issues: 0 / pending: 0, then: (1) append a dated bracketed supersession note to the STATE.md Urdu blocker line — original text byte-intact, format "[superseded YYYY-MM-DD, Phase 12: owner device check pass — see 12-UAT.md]" with the execution date; (2) close the broken-windows ledger row with the CLI: node C:/Users/Familia/.config/opencode/gsd-core/bin/gsd-tools.cjs windows fixed 10 (updates row 10 status to fixed and decrements open_count 16 → 15 — never hand-edit the ledger rows); (3) no CSS change (css/base.css stays untouched on this branch).
    ISSUE branch: record the affected criteria with the outcome value issue (mechanical blocker per AGENTS.md — no gap-awareness), fill ## Device and ## Summary accordingly, do NOT touch the STATE.md blocker or the WINDOWS row, and then: if the reported problem is a trivial line-height/font tweak confined to the existing html[lang="ur"] rules in css/base.css, apply it within this phase, re-run the full local validate chain, and re-present the checkpoint for the owner to re-check on device; if non-trivial, leave the issue recorded, keep the STATE.md blocker open, and add a todo instead of improvising a fix.
    In both branches the record must carry only device metadata (model / OS / browser) — no secrets, no legacy-host literal, original historical text never deleted.
  </action>
  <verify>
    <automated>
$u='.planning/phases/12-cleanup-batch/12-UAT.md'
(Select-String -Path $u -Pattern '^result: pass').Count -eq 5
(Select-String -Path $u -Pattern 'status: complete').Count -ge 1
(Select-String -Path .planning/STATE.md -Pattern 'owner device check pass').Count -ge 1
(Select-String -Path .planning/WINDOWS.md -Pattern 'open_count: 15').Count -eq 1
node C:/Users/Familia/.config/opencode/gsd-core/bin/gsd-tools.cjs windows list 2>$null | Select-String -Pattern 'fixed' | Select-Object -First 3
    </automated>
  </verify>
  <acceptance_criteria>
    - BEHAVIOR: the checkpoint halts execution until the owner reports real-device outcomes — no outcome is assumed or synthesized by the agent (prohibition 3 of this plan)
    - SOURCE (pass branch): all five result lines in 12-UAT.md carry the pass outcome; frontmatter status reads complete; ## Device filled; ## Summary total: 5 / passed: 5 / pending: 0
    - SOURCE (pass branch): STATE.md Urdu blocker line = original text + bracketed dated supersession note citing 12-UAT.md; WINDOWS.md row 10 status reads fixed (table + JSON block) and frontmatter open_count reads 15
    - SOURCE (issue branch): affected criteria carry the issue outcome; ## Summary issues count matches; STATE.md Urdu blocker line untouched; WINDOWS.md row 10 still open; css/base.css changed ONLY if the trivial-tweak condition held (diff confined to the html[lang="ur"] rules) with the local validate chain green after it
    - SOURCE: both branches — no secrets, no legacy-host literal, no historical text deleted
  </acceptance_criteria>
  <done>Urdu Nastaliq device check recorded from the owner's real device with the mechanical per-criterion predicate; STATE.md blocker closed with a dated supersession note and WINDOWS.md row 10 fixed (pass branch) — or the issue is recorded with the blocker honestly left open (issue branch)</done>
</task>

</tasks>

<verification>
- Task 1: §3 evidence record complete; dictionary set untouched (19 total, single zh* file); `npm run validate:i18n` exit 0
- Task 2: 12-UAT.md prepared per NN-UAT convention; zero pre-filled outcomes (anchored regex gate)
- Task 3: owner checkpoint resolved on-device; pass branch closes STATE.md blocker + WINDOWS row 10 via the ledger CLI; issue branch records the blocker honestly and loops only a trivial CSS tweak
- Post-phase: no zh-TW dictionary anywhere; js/i18n/ still exactly 19 dictionaries; STATE.md carries both supersession notes (lockfile note from 12-01, Urdu note from here)
</verification>

<success_criteria>
- zh variant documented as Simplified-only with verbatim two-tree evidence and the fold consequence stated (roadmap success criterion 3)
- Urdu device check recorded from the owner's real device: ur page renders RTL with its line-height override and readable text (roadmap success criterion 4) — pass recorded, or an issue recorded as an honest blocker
- Both historical records preserved under the supersession-note policy
</success_criteria>

<output>
Create `.planning/phases/12-cleanup-batch/12-02-SUMMARY.md` when done — include: §3 evidence summary, the 12-UAT.md outcome (five criteria + device metadata), the STATE.md/WINDOWS.md closure state, and any issue-branch loop (CSS tweak + re-check) verbatim.
</output>
