---
phase: 12-cleanup-batch
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/deploy.yml
  - AGENTS.md
  - .planning/phases/12-cleanup-batch/12-RECORDS.md
  - .planning/STATE.md
autonomous: true
requirements: [CLEAN-01, CLEAN-04]

estimate:
  tokens: 35000
  raw_tokens: 20000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "deploy.yml validate job installs with npm ci and actions/setup-node@v7 carries `cache: npm` in its with: block; the stale three-line lockfile comment (lines 24-26) is gone; every other line of the file is unchanged"
    - "Lockfile consistency re-verified with both raw command outputs recorded in 12-RECORDS.md §1: git ls-files package-lock.json returns the path and npm ci --dry-run exits 0"
    - "AGENTS.md CI deploy chain row states the shipped install reality (npm ci + setup-node `cache: npm`, lockfile committed since Phase 5) with no pre-lockfile clause remaining"
    - "STATE.md Blockers line about the lockfile research disagreement keeps its original text and carries a dated bracketed supersession note pointing at 12-RECORDS.md §1"
    - statement: "Post-ship CI: the full pipeline (validate job with the lockfile-frozen install + deploy job) completes green; the first post-change run is an expected cache MISS with save, the second run is a cache HIT"
      verification: backstop
  artifacts:
    - ".github/workflows/deploy.yml (edited: cache input added, install step swapped to npm ci, stale comment removed — 2 insertions / 4 deletions)"
    - ".planning/phases/12-cleanup-batch/12-RECORDS.md (new: §1 lockfile re-verification verdict + §2 post-ship CI verification template)"
    - "AGENTS.md (CI deploy chain row updated to shipped reality)"
    - ".planning/STATE.md (dated supersession note appended to the lockfile blocker line)"
  key_links:
    - "setup-node `cache: npm` → package-lock.json hash is the cache key (any lockfile change invalidates; cache save runs in the post step on success only — first run after the change is a MISS by design)"
    - "npm ci success ↔ committed package-lock.json ↔ package.json sync (npm ci --dry-run exit 0 is the plan-time proof; npm ci in CI re-proves it on every run, erroring on drift instead of silently rewriting the lock)"
  prohibitions:
    - statement: "MUST NOT hand-edit package-lock.json — including to normalize the caret/exact root-metadata drift in the lockfile root — to make the consistency check pass (only npm's mutating install-family commands may rewrite it; none run in this phase)"
      status: resolved
      verification: test
      reason: "npm ci accepts the lockfile as-is; the dry-run exit code is the verdict (12-RESEARCH.md Pitfall 4 / Anti-Patterns)"
    - statement: "MUST NOT add a packageManager or devEngines.packageManager field to package.json to obtain setup-node auto-caching"
      status: resolved
      verification: test
      reason: "The explicit cache: npm input is sufficient; the field changes corepack/local tooling behavior (12-RESEARCH.md Alternatives + Deferred Ideas)"
    - statement: "MUST NOT restructure the validate chain, job permissions, or the deploy job while editing deploy.yml — the diff touches exactly the three target areas"
      status: resolved
      verification: test
      reason: "Scope discipline: this phase changes install+cache config only (12-RESEARCH.md Pitfall 5; permissions widening is canon security — breadcrumb to /gsd-secure-phase, not minted here)"
    - statement: "MUST NOT remove the stale lockfile NOTE comment before the re-verification verdict is recorded — the record gates the removal (roadmap success criterion 2 ties them)"
      status: resolved
      verification: test
      reason: "12-RESEARCH.md Anti-Patterns: comment removal as a drive-by before the lockfile verdict is recorded"
    - statement: "MUST NOT silently delete historical record text — the STATE.md blocker line keeps its original text; closure rides a dated bracketed supersession note"
      status: resolved
      verification: judgment
      reason: "AGENTS.md supersession-note policy for historical records"
---

<objective>
Make CI reproducible (CLEAN-01) and close the lockfile research discrepancy with recorded evidence (CLEAN-04): the validate job installs from the committed lockfile with the npm cache restored, the stale "no lockfile" NOTE comment comes out in the same unit of work as the recorded re-verification, and AGENTS.md stops claiming the pre-lockfile install.

Purpose: every later v2.1 phase runs the validate chain repeatedly — frozen installs + cache restore make each cycle reproducible and faster, and the recorded verdict removes the last "researchers disagreed" ambiguity before the gate-heavy Phase 13.
Output: edited deploy.yml + AGENTS.md, new 12-RECORDS.md (§1 verdict + §2 post-ship CI template), dated supersession note on the STATE.md blocker line.
</objective>

<execution_context>
@C:/Users/Familia/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/Familia/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/12-cleanup-batch/12-RESEARCH.md
@.github/workflows/deploy.yml
@package.json

# Plan-time re-verification already run by the planner (2026-09-13, fresh evidence):
#   git ls-files package-lock.json  -> package-lock.json (tracked)
#   npm ci --dry-run                -> exit 0 (sync PASS; chromedriver@152.0.3 allowScripts warning is warn-only)
#   git log --diff-filter=A -- package-lock.json -> f0f56ca (2026-09-02, Phase 5)
# The executor re-runs both commands and records ITS OWN fresh output in 12-RECORDS.md §1.
</context>

## Artifacts this phase produces

Phase 12 (both plans) produces exactly this set — no code symbols, no new CI keys, no secrets:

| Artifact | Kind | Plan |
|----------|------|------|
| `.github/workflows/deploy.yml` — edited (cache input + lockfile-frozen install + stale comment removed) | CI config edit | 12-01 |
| `AGENTS.md` — CI deploy chain row updated to shipped reality | doc edit | 12-01 |
| `.planning/phases/12-cleanup-batch/12-RECORDS.md` — new phase record: §1 lockfile verdict (CLEAN-04), §2 post-ship CI verification template (CLEAN-01), §3 zh variant confirmation (CLEAN-02, written by plan 12-02) | record doc | 12-01 + 12-02 |
| `.planning/phases/12-cleanup-batch/12-UAT.md` — owner device-check record (CLEAN-03) | record doc | 12-02 |
| `.planning/STATE.md` — dated supersession notes appended to two blocker lines (lockfile, Urdu) | state edit | 12-01 + 12-02 |
| `.planning/WINDOWS.md` — row 10 marked fixed (only if the device check passes) | ledger update | 12-02 |
| `css/base.css` — CONDITIONAL ONLY: trivial line-height/font tweak inside the existing `html[lang="ur"]` rules if the device check reports clipping | CSS fix loop | 12-02 |

## Planner assumption notes (probe edges — surfaced, no silent drops)

The spec-less edge probe returned all 4 requirement edges `unresolved`/`unclassified`. Per the fallback
contract each was resolved by the planner into a defensible acceptance criterion (never auto-dismissed):

| Edge | Resolution | Lands as |
|------|-----------|----------|
| CLEAN-01 (unclassified) | Resolved explicit: deploy.yml target state is source-greppable (cache input present, lockfile-frozen install line, stale comment gone, 2+/4− diff) | `must_haves.truths` #1 |
| CLEAN-01 post-ship green | Resolved backstop: CI green is only confirmable after /gsd-ship pushes; verifier confirms via `gh run list` post-ship or abstains → human_needed | `must_haves.truths` backstop marker |
| CLEAN-02 (unclassified) | Resolved explicit: §3 record with verbatim two-tree evidence + fold consequence (plan 12-02) | plan 12-02 `must_haves.truths` |
| CLEAN-03 (unclassified) | Resolved explicit: 12-UAT.md five-criterion record with mechanical pass/issue predicate, gated by the owner checkpoint (plan 12-02) | plan 12-02 `must_haves.truths` |
| CLEAN-04 (unclassified) | Resolved explicit: both raw command outputs recorded in 12-RECORDS.md §1 + STATE.md supersession note | `must_haves.truths` #2/#4 |

Canon breadcrumbs (dropped, NOT minted as prohibitions): secrets-in-workflow-or-docs → /gsd-secure-phase
+ the `.planning/` public-serving rule; legacy-host literal → already owned by
`scripts/check-no-old-domain.mjs` (validate:domain); job-permission widening → /gsd-secure-phase.

<tasks>

<task type="tracer">
  <name>Task 1: Lockfile verdict → record → supersession note → deploy.yml CI slice → local validation (CLEAN-04 + CLEAN-01)</name>
  <files>.planning/phases/12-cleanup-batch/12-RECORDS.md, .planning/STATE.md, .github/workflows/deploy.yml, AGENTS.md</files>
  <read_first>
    - .github/workflows/deploy.yml (whole file — 48 lines; the NOTE comment block is lines 24-26, the bare install step is line 27, setup-node with: block is lines 22-23)
    - package.json (devDeps exact-pinned; no packageManager field — keep it that way)
    - .planning/STATE.md (Blockers/Concerns section — the first blocker line is the lockfile disagreement)
    - .planning/phases/12-cleanup-batch/12-RESEARCH.md (Pattern 1 target YAML, Pattern 2 evidence, Pitfalls 1/4/5, "Target state of the validate job" code block)
    - AGENTS.md (line 38 CI deploy chain row — the parenthetical carries the pre-lockfile install clause)
    - scripts/check-no-old-domain.mjs (skim — new record prose must never contain the legacy-host literal; phrase any dual-host mention as "legacy Pages host")
  </read_first>
  <action>
    ONE coherent tracer slice, strictly in this order (the record gates the comment removal — roadmap success criterion 2):

    1. RE-VERIFY (fresh, executor-run): `git ls-files package-lock.json` (expect the path back) and `npm ci --dry-run` (expect exit code 0). Capture the dry-run's final lines verbatim (the "up to date" summary, the fund note, and the chromedriver@152.0.3 allowScripts warning — record that warning as a warn-only observation; the validate chain never invokes chromedriver and no `--ignore-scripts` flag is added).
    2. CREATE `.planning/phases/12-cleanup-batch/12-RECORDS.md` with a markdown header (title "Phase 12 — Records", created date, one-line purpose "CLEAN-04/CLEAN-01/CLEAN-02 evidence records; `.planning/` is publicly served — no secrets") and section `## 1. Lockfile consistency re-verification (CLEAN-04)` containing: (a) Commands & raw output — both commands with their verbatim outputs and the dry-run exit code; (b) Provenance — lockfile added in commit f0f56ca (2026-09-02, "chore: dev tooling lockfile + ignore node_modules", Phase 5); (c) Verdict — package.json and package-lock.json are in sync per npm's own validation; the STATE.md research-discrepancy blocker is closed; the stale three-line lockfile NOTE comment in deploy.yml (lines 24-26) is confirmed stale by two weeks of repo history and its removal in step 4 is justified. Do NOT quote the stale comment's text and do NOT write the legacy-host literal anywhere in the file.
    3. STATE.md: append a dated bracketed supersession note to the FIRST Blockers line (the lockfile-consistency one), leaving its original text byte-intact — format per AGENTS.md supersession policy: "[superseded YYYY-MM-DD, Phase 12: lockfile tracked since f0f56ca, npm ci --dry-run exit 0 — see 12-RECORDS.md §1]" with YYYY-MM-DD = execution date. Nothing else in STATE.md moves.
    4. deploy.yml — exactly three areas, nothing else (Pitfall 5): (a) inside the actions/setup-node@v7 with: block add `          cache: npm` after the `node-version: 24` line; (b) delete the stale three-line comment block (lines 24-26) entirely; (c) replace the bare install step (line 27, currently the non-frozen install command) with `- run: npm ci`. Do NOT add `cache-dependency-path` (single root lockfile — README basic usage omits it), do NOT add a packageManager field to package.json, do NOT touch job permissions, the deploy job, or the validate chain step order. Expected net diff: 2 insertions, 4 deletions, file length 46 lines.
    5. AGENTS.md line 38 (CI deploy chain row): replace the row's trailing parenthetical install clause — the parenthetical currently ends with the pre-lockfile install command plus the phrase about waiting for a lockfile — with: `npm ci` + setup-node `cache: npm`, lockfile committed since Phase 5 (keep the `.github/workflows/deploy.yml` reference and Node 24). Row-scoped edit only; the Firebase row's separate npm mention (line 33) is a different claim and stays untouched.
    6. LOCAL VALIDATION: `npm ci --dry-run` again (exit 0 — confirms the committed pair still validates after the edits; dry-run never deletes node_modules) then the full chain `npm run validate` (validate:html && validate:domain && validate:links && validate:i18n-detect && validate:i18n) — must exit 0. No gate logic was mutated, so no red-gate proof is required this phase (research User Constraints).
  </action>
  <verify>
    <automated>
$y='.github/workflows/deploy.yml'
(Select-String -Path $y -Pattern 'cache: npm').Count -eq 1
(Select-String -Path $y -Pattern 'run: npm ci').Count -eq 1
(Select-String -Path $y -Pattern 'npm install').Count -eq 0
(Select-String -Path $y -Pattern 'proxy-broken').Count -eq 0
(Get-Content $y).Count -eq 46
git diff --numstat -- $y   # expect: 2<TAB>4
(Select-String -Path AGENTS.md -Pattern 'until a lockfile is committed').Count -eq 0
(Select-String -Path AGENTS.md -Pattern 'npm ci').Count -ge 1
(Select-String -Path .planning/STATE.md -Pattern 'superseded 20\d\d-').Count -ge 1
(Select-String -Path .planning/phases/12-cleanup-batch/12-RECORDS.md -Pattern 'f0f56ca').Count -ge 1
npm ci --dry-run; "DRYRUN_EXIT=$LASTEXITCODE"
npm run validate; "VALIDATE_EXIT=$LASTEXITCODE"
    </automated>
  </verify>
  <acceptance_criteria>
    - SOURCE: deploy.yml contains exactly one `cache: npm` (inside the setup-node with: block) and exactly one `- run: npm ci`; zero matches for the old install command and zero matches for the stale comment's unique token; file is 46 lines; git diff --numstat = 2 insertions / 4 deletions (three-area-only change)
    - SOURCE: AGENTS.md CI deploy chain row contains "npm ci" + "cache: npm" + "lockfile committed since Phase 5"; the pre-lockfile clause is absent from the whole file
    - SOURCE: 12-RECORDS.md §1 exists with headings "Commands & raw output", "Provenance", "Verdict"; contains both command outputs verbatim, the dry-run exit code, commit f0f56ca, and the dated verdict; does not quote the stale comment text; contains no legacy-host literal
    - SOURCE: STATE.md lockfile blocker line = original text + appended bracketed "[superseded …]" note citing 12-RECORDS.md §1; original wording byte-intact
    - COMMAND: `npm ci --dry-run` exits 0 after the edits
    - COMMAND: `npm run validate` exits 0 (full chain green locally on the edited tree)
    - BEHAVIOR: `cache: npm` present makes setup-node key the npm cache on the package-lock.json hash (per setup-node README); first CI run after ship is an expected MISS-then-save (recorded in §2 by Task 2)
  </acceptance_criteria>
  <done>Lockfile verdict recorded from fresh executor-run commands; STATE.md blocker closed with a dated supersession note; deploy.yml runs the lockfile-frozen install with cache restore and carries no stale comment; AGENTS.md matches shipped reality; full local validate chain green</done>
</task>

<task type="auto">
  <name>Task 2: Post-ship CI verification template in 12-RECORDS.md §2 (CLEAN-01 evidence slot)</name>
  <files>.planning/phases/12-cleanup-batch/12-RECORDS.md</files>
  <read_first>
    - .planning/phases/12-cleanup-batch/12-RECORDS.md (as left by Task 1 — append §2 after §1)
    - .planning/phases/12-cleanup-batch/12-RESEARCH.md (Pitfall 1 first-run cache MISS + Pitfall 2 lockfile-hash key)
  </read_first>
  <action>
    Append `## 2. Post-ship CI verification (CLEAN-01)` to 12-RECORDS.md. Deferred-commit mode means the pipeline runs only after /gsd-ship lands the commits and pushes — this section is the evidence slot that post-ship verification fills. Include, in this order:
    (a) Trigger line: "Fill after /gsd-ship pushes the Phase 12 commits to main and the workflow triggers."
    (b) Commands: `gh run list --limit 2` (get the new run id) and `gh run watch <run-id> --exit-status` (watch to completion).
    (c) Expectations paragraph: run #1 after the change is an EXPECTED cache MISS — the log line "Cache not found for input keys" is designed first-run behavior, not an error; the setup-node post step saves the npm cache keyed on the package-lock.json hash after a successful job, so run #2 restores it (cache HIT) and installs faster. Judge speed on run #2, never run #1.
    (d) Two fill-in result rows (one per run), each with empty slots: Run ID | validate job | deploy job | cache outcome (MISS-save / HIT) | duration | verdict.
    (e) Pass predicate: validate job green with the lockfile-frozen install + cache input active AND deploy job green = CLEAN-01's "CI green" criterion satisfied; record the verdicts in the rows (initial value: pending for both).
    No secrets in the file (it is publicly served); no legacy-host literal.
  </action>
  <verify>
    <automated>
$r='.planning/phases/12-cleanup-batch/12-RECORDS.md'
(Select-String -Path $r -Pattern '## 2\. Post-ship CI verification').Count -eq 1
(Select-String -Path $r -Pattern 'gh run list').Count -ge 1
(Select-String -Path $r -Pattern 'gh run watch').Count -ge 1
(Select-String -Path $r -Pattern 'Cache not found for input keys').Count -ge 1
(Select-String -Path $r -Pattern 'verdict.*pending').Count -eq 2
    </automated>
  </verify>
  <acceptance_criteria>
    - SOURCE: 12-RECORDS.md §2 exists with the exact heading "## 2. Post-ship CI verification (CLEAN-01)"
    - SOURCE: §2 contains both gh commands (`gh run list`, `gh run watch <run-id> --exit-status`), the MISS-then-save expectation naming the "Cache not found for input keys" log line as designed first-run behavior, the lockfile-hash key note, and two result rows whose verdict cells read pending
    - SOURCE: §2 contains no filled verdicts, no secrets, no legacy-host literal
  </acceptance_criteria>
  <done>Post-ship CI verification template recorded with commands, MISS/HIT expectations, and two pending verdict slots — the slot post-ship verification fills to prove CLEAN-01's green pipeline</done>
</task>

</tasks>

<verification>
- All Task 1 + Task 2 automated checks green (source greps + numstat + local dry-run + full validate chain)
- Post-ship (after /gsd-ship pushes): `gh run list --limit 2` shows the new run; `gh run watch <run-id> --exit-status` → validate (npm ci + cache: npm active) and deploy both success; record MISS/HIT outcomes in 12-RECORDS.md §2 rows and flip both verdict cells — first run MISS is expected, second run HIT
- Roadmap success criterion 2 fully closed: verdict recorded AND stale comment gone, in the same task
</verification>

<success_criteria>
- deploy.yml validate job = lockfile-frozen install + npm cache restore, stale comment gone, diff scoped to 2 insertions / 4 deletions
- Lockfile consistency verdict recorded from fresh executor-run commands (tracked + dry-run exit 0) closing the research discrepancy
- AGENTS.md and STATE.md consistent with shipped reality (supersession note, not deletion)
- Post-ship CI template ready with expectations and pending slots
- Full local validate chain green on the edited tree
</success_criteria>

<output>
Create `.planning/phases/12-cleanup-batch/12-01-SUMMARY.md` when done — include: the fresh re-verification outputs (both commands + exit codes), the deploy.yml numstat, local validate-chain result, the backstop CI-green follow-up (post-ship watch per §2), and any deviations (e.g. validator line-count mismatch with the real file).
</output>
