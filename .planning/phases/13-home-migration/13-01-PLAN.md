---
phase: 13-home-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ".github/workflows/deploy.yml"
  - ".gitignore"
  - ".planning/WINDOWS.md"
  - ".planning/config.json"
  - ".planning/phases/12-cleanup-batch/12-01-SUMMARY.md"
  - ".planning/phases/12-cleanup-batch/12-RECORDS.md"
  - ".planning/phases/12-cleanup-batch/12-UAT.md"
  - "AGENTS.md"
  - "index.html"
  - "apps/index.html"
  - "geohist/index.html"
  - "geohist/guide.html"
  - "geohist/contact.html"
  - "geohist/changelog.html"
  - "geohist/privacy.html"
  - "404.html"
  - "sitemap.xml"
  - "scripts/i18n-keycheck.mjs"
  - "scripts/i18n-surface.mjs"
  - "scripts/a11y-audit.mjs"
  - "scripts/smoke-check.sh"
  - "package.json"
  - ".planning/phases/13-home-migration/red-gate-proof.md"
autonomous: true
requirements: [MIG-01, MIG-02, MIG-03, MIG-04, MIG-05, MIG-06, MIG-07, MIG-09]
estimate:
  tokens: 100000
  raw_tokens: 58000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Visitor hitting / sees the full GeoHist landing (hero, proof strip, OFF rating row, features, gallery, FAQ, CTA) with all 20 locales resolving at the new path (MIG-01)"
    - "Visitor hitting /geohist/ (incl. via the legacy *.github.io Pages host path-preserved chain) reaches the root landing via the meta-refresh-0 stub; /geohist/privacy.html still serves its frozen policy at the same path (MIG-02, MIG-07)"
    - "Visitor browsing /apps/ sees the portfolio hub (former root content verbatim, 13 hub.* keys, keyed chrome) with zero future-app placeholder cards (MIG-03)"
    - "All 6 sitemap URLs resolve on apex and each page's canonical == og:url == JSON-LD url at the new paths; no redirected URL listed in sitemap (MIG-04)"
    - "All five gate page-lists + the star-uniqueness path cover the new layout, red-gate proven in both directions, recorded in red-gate-proof.md (MIG-05)"
    - "AGENTS.md reflects the new layout and rides the SAME commit as the migration; old-domain gate still green (MIG-06)"
    - "404 page and every nav/footer anchor point at the new layout; i18n key surface stays exactly 178 keys x 19 dictionaries (MIG-09)"
  artifacts:
    - "index.html — GeoHist landing at root (former geohist/index.html body verbatim + repointed head/hrefs)"
    - "apps/index.html — NEW, portfolio hub (former root index.html verbatim + repointed canonical/og:url/card CTA)"
    - "geohist/index.html — self-contained meta-refresh-0 stub (noindex,follow + canonical / + <a> fallback, zero scripts/keys)"
    - "404.html — hub link href /apps/, visible text unchanged"
    - "sitemap.xml — 6 apex <loc> rows with /geohist/ swapped to /apps/"
    - "scripts/i18n-keycheck.mjs, scripts/i18n-surface.mjs, scripts/a11y-audit.mjs, scripts/smoke-check.sh, package.json — all page-lists repointed"
    - "AGENTS.md — layout sync (MIG-06) + stale 10-RUNBOOK path fix (OQ4)"
    - ".planning/phases/13-home-migration/red-gate-proof.md — both-direction proofs per gate"
  key_links:
    - "i18n-keycheck/i18n-surface pages[] arrays <-> moved markup <-> 19 dictionaries: 178-key set-equality is the atomic-coupling mechanism"
    - "i18n-keycheck.mjs line 184 star-uniqueness path -> root index.html proof-row-star (6th hardcode; misses = loud red on stub's 0 SVGs)"
    - "404.html 'Back to the hub' text <-> smoke-check.sh line 49 grep (text immutable, href-only edit)"
    - "sitemap.xml rows <-> per-page canonical/og:url/JSON-LD url coherence"
    - "AGENTS.md <-> scripts/check-no-old-domain.mjs walk (AGENTS.md NOT allowlisted — same-commit sync enforced)"
  prohibitions:
    - id: P-13-01
      category: transparency
      statement: "MUST NOT serve full landing content at both / and /geohist/ (double indexation) and MUST NOT use a JS-only redirect on the stub — /geohist/ is a declarative meta-refresh-0 + <a>-fallback stub, never a copy, never a script redirect"
    - id: P-13-02
      category: transparency
      statement: "MUST NOT add visible future-app placeholder cards to /apps/ — structure anticipates (one real app card), never advertises vaporware"
    - id: P-13-03
      category: safety
      statement: "MUST NOT move /geohist/privacy.html or edit its policy content mid-Play-review — path + policy content frozen; only permitted change is the single href-only footer back-link (research A1/OQ1)"
---

<objective>
Migrate the URL layout in ONE atomic commit set: root `/` becomes the GeoHist landing, the portfolio hub moves to `/apps/`, `/geohist/` becomes a self-contained meta-refresh-0 stub, and every hard-coded gate page-list (all 5 + the star path) is repointed and red-gate proven — with AGENTS.md synced in the same commit and the 178-key x 19-dictionary surface untouched.

Purpose: STATE locked decision — "home migration is ONE atomic commit — keycheck 178-key set-equality forces both page moves + all 5 gate-list registrations together; one revert = rollback (Phase-8 precedent)". Splitting the moves across commits reds all 19 dictionaries (165 != 178).

Output: root landing + /apps/ hub + stub + repointed gates + synced AGENTS.md, full local `npm run validate` green, `red-gate-proof.md` evidence, runway cleared (Phase 12 deferred commits landed, branch reconciled).
</objective>

<execution_context>
@C:/Users/Familia/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/Familia/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/13-home-migration/13-RESEARCH.md
@AGENTS.md
</context>

## Flagged Assumptions (edge-probe: 16 of 17 phase rows carried here — all status: unresolved, never auto-resolved, no silent drops)

Per spec-less probe contract these are surfaced, not resolved. EA-17 (MIG-08 row) is carried by 13-02-PLAN.md. Union across both plans = 17 rows.

- **EA-01** [MIG-01 / adjacency, unresolved] — the landing at `/` keeps the FULL existing section sequence (hero, proof strip, OFF rating row, features, gallery, FAQ, CTA/about) verbatim; no section dropped, reordered, or rewritten during the move.
- **EA-02** [MIG-01 / empty, unresolved] — after the move, `/geohist/` must not serve an empty/blank page: the stub carries `content="0; url=/"` + visible `<a>` fallback so a non-refreshing client still resolves.
- **EA-03** [MIG-01 / ordering, unresolved] — all 20 locales resolve identically at `/` because `js/i18n.js` is page-agnostic (absolute `DICT_URL_PREFIX`); stored `persano.lang` preference survives the path change. Assumed, not re-tested per-locale here.
- **EA-04** [MIG-04 / adjacency, unresolved] — sitemap keeps exactly 6 `<loc>` rows with row order preserved; only row 2 swaps `/geohist/` → `/apps/`.
- **EA-05** [MIG-04 / empty, unresolved] — no sitemap row points at a redirect (stub row removed, not re-listed); no `<loc>` added for the stub.
- **EA-06** [MIG-04 / ordering, unresolved] — every page ends with canonical == og:url == JSON-LD `url` at its new path; no page left carrying an old-path canonical.
- **EA-07** [MIG-05 / adjacency, unresolved] — all five gate lists + the star-path hardcode (6th) repoint inside the SAME change set; no list left auditing the old layout.
- **EA-08** [MIG-05 / empty, unresolved] — a11y PAGES must NOT include the stub (meta-refresh-0 navigates axe's driver to `/` mid-scan); the list stays 5 real content pages and changelog stays absent as today.
- **EA-09** [MIG-05 / ordering, unresolved] — every gate change is red-gate proven both directions BEFORE ship, restored via snapshot-copy hash-verified (never `git restore` on the dirty tree), recorded in red-gate-proof.md.
- **EA-10** [MIG-09 / adjacency, unresolved] — every nav/footer anchor across all keyed pages points at the new layout (nav.game → `/`, nav.faq → `/#faq`, footer.back → `/apps/`); zero hrefs still assume hub-at-root.
- **EA-11** [MIG-09 / empty, unresolved] — 404 visible text "Back to the hub" is immutable (smoke-check.sh line 49 grep coupling); only the href changes.
- **EA-12** [MIG-09 / ordering, unresolved] — markup moves are verbatim so the key surface stays exactly 178 x 19: zero key additions, renames (incl. hub.* → apps.*), or drops.
- **EA-13** [MIG-02 / unclassified, unresolved] — the stub is fully self-contained: zero scripts, zero data-i18n nodes, zero stylesheet (404.html pattern).
- **EA-14** [MIG-03 / unclassified, unresolved] — the hub moves verbatim: all 13 `hub.*` keys ride along unchanged; card CTA href → `/`; og:image stays `/geohist/og-image.png`.
- **EA-15** [MIG-06 / unclassified, unresolved] — AGENTS.md layout sync lands in the SAME commit as the migration (old-domain gate walks AGENTS.md; not allowlisted).
- **EA-16** [MIG-07 / unclassified, unresolved] — `/geohist/privacy.html` path and its `/geohist/icon.png` asset references never move; at most the ONE href-only footer edit per A1 (research recommendation accepted; zero-edit fallback documented).

## Prohibition-Probe Record (spec-less recall → precision)

Stage-1 raw candidates (~10): full-copy-at-both-URLs; JS-only redirect; visible placeholder cards; hub.*/geohist.* key renames; privacy policy content edits; assets moved out of /geohist/; sitemap listing the redirect row; 404 text rewrite; i18n key additions mid-migration; AGENTS.md drift out of the atomic commit. Stage-2 precision kept 3 bespoke values/safety/transparency prohibitions → `must_haves.prohibitions` P-13-01..P-13-03 (descriptor-less per contract — dispose flagged-unverified, never fabricated checks). Dropped as routine-engineering (already owned by gates/verification): sitemap row correctness (Pitfall 5 + keycheck/validate), 404 grep coupling (Pitfall 6 + smoke-check), key-addition freeze (keycheck 178 x 19 gate owns it mechanically). Canon referral: none minted — markup-only migration creates no injection/PII/consent surface (OWASP/GDPR canon not applicable; no breadcrumb target needed).

## Artifacts this phase produces

- **New file:** `apps/index.html` (portfolio hub at `/apps/`)
- **Replaced bodies:** `index.html` (now the GeoHist landing), `geohist/index.html` (now the stub) — no file deleted, no file renamed
- **New git-tracked planning artifacts:** `.planning/phases/13-home-migration/red-gate-proof.md`; `.planning/phases/13-home-migration/13-RUNBOOK.md` + `13-UAT.md` (authored by 13-02)
- **New script page-list entries:** `apps/index.html` into i18n-keycheck + i18n-surface `pages[]`; `{ url: '/apps/', slug: 'apps' }` into a11y PAGES; `$BASE/apps/` into smoke-check URL list; `apps/index.html` into package.json validate:html glob
- **State changes:** root `/` serves landing; `/geohist/` serves stub; hub relocated; AGENTS.md layout rows rewritten (same commit)

<tasks>

<task type="auto">
  <name>Task 1: Runway — land Phase 12 deferred commits + reconcile origin/main (research OQ5/Pitfall 8)</name>
  <files>.github/workflows/deploy.yml, .gitignore, .planning/WINDOWS.md, .planning/config.json, AGENTS.md, .planning/phases/12-cleanup-batch/12-01-SUMMARY.md, .planning/phases/12-cleanup-batch/12-RECORDS.md, .planning/phases/12-cleanup-batch/12-UAT.md</files>
  <read_first>.planning/phases/12-cleanup-batch/12-01-SUMMARY.md, .planning/phases/12-cleanup-batch/12-02-SUMMARY.md, .planning/phases/13-home-migration/13-RESEARCH.md (## Environment Availability git-state rows + Pitfall 8), AGENTS.md (commit conventions + Git Data API bridge directive)</read_first>
  <action>Record `git status --porcelain` output. Commit Phase 12's deferred working-tree changes as SEPARATE atomic commits grouped by their Phase 12 unit of work with the conventional-commit subjects declared in the 12 plan summaries — deploy.yml + .gitignore as the CI unit (12-01), the 12-* planning docs + WINDOWS.md + config.json as the records unit (12-02); do NOT squash them into the Phase 13 migration commit. Then reconcile the branch: `git fetch origin`; confirm `git rev-list --left-right --count HEAD...origin/main` (currently 10 ahead / 1 behind) and confirm via `git log origin/main -1 --format="%H %s"` that the single behind-commit is the PR #6 merge of this same branch; reconcile WITHOUT force-push and WITHOUT rebase-rewrite — `git merge --no-edit origin/main` (the behind commit is a merge of this branch's own tip, so the merge is content-trivial); verify `git rev-list HEAD..origin/main --count` equals 0 and `git status --porcelain` is empty. Do NOT push in this task — ship pushes. If the merge produces conflicts (unexpected content diff vs the research facts), STOP and surface before any migration edit.</action>
  <verify>
    <automated>git status --porcelain (must be empty) && git rev-list HEAD..origin/main --count (must print 0) && git log --oneline -6 (must show the Phase 12 unit commits + the merge commit)</automated>
  </verify>
  <acceptance_criteria>
    - `git status --porcelain` exits with zero output
    - `git rev-list HEAD..origin/main --count` prints exactly `0`
    - `git log --oneline -6` lists separate Phase 12 commits (CI unit and records unit NOT squashed into one) plus a merge commit for origin/main
    - No force-push flags (`--force`, `-f`) appear anywhere in the executed git history of this task
  </acceptance_criteria>
  <done>Tree clean; branch 0 behind origin/main; Phase 12 work landed as its own atomic commits; migration commit now has a clean base and an intact single-revert rollback story.</done>
  <reversibility rating="reversible">Additive commits + a content-trivial merge; each independently revertable; no force-push, no history rewrite.</reversibility>
</task>

<task type="tracer">
  <name>Task 2: THE atomic migration edit set — tracer slice through every layer (pages, stub, gates, sitemap, 404, AGENTS.md)</name>
  <files>index.html, apps/index.html, geohist/index.html, geohist/guide.html, geohist/contact.html, geohist/changelog.html, geohist/privacy.html, 404.html, sitemap.xml, scripts/i18n-keycheck.mjs, scripts/i18n-surface.mjs, scripts/a11y-audit.mjs, scripts/smoke-check.sh, package.json, AGENTS.md</files>
  <read_first>.planning/phases/13-home-migration/13-RESEARCH.md (Target State File Map + Code Examples 1-14 + Common Pitfalls 1-7 — all values below are sourced there with verified line numbers), index.html, geohist/index.html, geohist/guide.html, geohist/contact.html, geohist/changelog.html, geohist/privacy.html, 404.html, sitemap.xml, package.json, scripts/i18n-keycheck.mjs, scripts/i18n-surface.mjs, scripts/a11y-audit.mjs, scripts/smoke-check.sh, AGENTS.md, .planning/milestones/v2.0-phases/08-custom-domain-migration/08-RUNBOOK.md (one-atomic-commit precedent)</read_first>
  <action>Execute the full target-state edit set in one working pass (intermediate red gate states in the working tree are fine; ONLY the final state must be green). Nothing commits here — deferred-commit mode; ship lands ONE atomic commit containing ALL of these files plus AGENTS.md.

  PAGE MOVES (verbatim markup, zero key changes — the 178-key surface survives by verbatim move):
  1. Move `geohist/index.html` body to root `index.html` verbatim. Then on root index.html edit ONLY: canonical link (line 8) href to `https://geohisttrivia.com/`; og:url meta (line 12) content to `https://geohisttrivia.com/`; JSON-LD (line 44) `url` to `https://geohisttrivia.com/` while `image`/`screenshot` absolute URLs stay on `/geohist/...` assets; nav.game anchor (line 68) href from `/geohist/` to `/`; nav.faq anchor (line 70) href from `/geohist/index.html#faq` to `/#faq`; footer.back anchor (line 232) href from `/` to `/apps/` (key `geohist.footer.back` and visible label unchanged). Nav guide/changelog/privacy anchors unchanged (pages keep paths).
  2. Move former root `index.html` (the hub) verbatim to NEW `apps/index.html`. Then edit ONLY: canonical href to `https://geohisttrivia.com/apps/`; og:url content to `https://geohisttrivia.com/apps/`; card CTA anchor (line 38) href from `/geohist/` to `/`. Everything else byte-identical: all 13 `hub.*` keys, consent banner, switcher slot, og:image on lines 13/19 stays `https://geohisttrivia.com/geohist/og-image.png`, footer anchors to `/geohist/privacy.html` and `/geohist/contact.html` unchanged. ZERO placeholder cards for future apps (P-13-02).
  3. Replace `geohist/index.html` body with the research Pattern 1 stub exactly: head carries charset, viewport, `meta name="robots" content="noindex, follow"`, title `GeoHist Trivia has moved`, canonical href `https://geohisttrivia.com/`, and `meta http-equiv="refresh" content="0; url=/"` in head; body is a single paragraph with one anchor href `/` labeled Continue to GeoHist Trivia. ZERO scripts, ZERO data-i18n nodes, ZERO stylesheet link (P-13-01, EA-13). Do NOT delete the `geohist/` directory — sub-pages, screenshots, og-image, icon, badge stay (Pitfall 5).

  HREF-ONLY REPPOINTS (zero key changes, zero text changes):
  4. `geohist/guide.html`, `geohist/contact.html`, `geohist/changelog.html` — each gets exactly 3 anchor href edits: nav.game `/geohist/` → `/`; nav.faq `/geohist/index.html#faq` → `/#faq`; footer.back `/` → `/apps/`. Canonical/og:url unchanged (paths unchanged). All other nav/footer anchors unchanged.
  5. `geohist/privacy.html` — ONLY the footer.back anchor (line 72) href from `/` to `/apps/` (per research A1/OQ1 recommendation; visible label and policy content byte-untouched; nav lines 28-32 untouched; file path never changes — P-13-03).
  6. `404.html` — line 20 first anchor href from `/` to `/apps/`; visible text "Back to the hub" UNCHANGED (smoke-check.sh line 49 grep coupling, Pitfall 6); second anchor to `/geohist/privacy.html` unchanged.

  SEO: 7. `sitemap.xml` — replace row 2 loc `https://geohisttrivia.com/geohist/` with `https://geohisttrivia.com/apps/`; final 6 rows exactly `/`, `/apps/`, `/geohist/guide.html`, `/geohist/changelog.html`, `/geohist/contact.html`, `/geohist/privacy.html`; no lastmod rows added (EA-04/05). `robots.txt` UNTOUCHED.

  GATE LIST REPPOINTS (all six hardcodes — EA-07):
  8. `scripts/i18n-keycheck.mjs` line 48 pages array becomes exactly: index.html, join('apps','index.html'), join('geohist','guide.html'), join('geohist','contact.html'), join('geohist','changelog.html') (stub dropped — unkeyed forever, OQ2 recommendation). Line 184 star-uniqueness readFileSync path becomes join(repoRoot, 'index.html') (6th hardcode, Pitfall 2).
  9. `scripts/i18n-surface.mjs` line 32 pages array: identical replacement; update the header-comment parenthetical (lines 5-7) to name the new layout.
  10. `scripts/a11y-audit.mjs` PAGES array: replace the `{ url: '/geohist/', slug: 'geohist' }` row with `{ url: '/apps/', slug: 'apps' }`; keep root, guide, contact, privacy rows; do NOT add the stub (EA-08); changelog stays absent.
  11. `scripts/smoke-check.sh` — add `"$BASE/apps/"` to the URL list; keep every existing row (all survive: `/` 200 as landing, `/geohist/` 200 as stub); add the optional stub-content grep: `curl -s "$BASE/geohist/"` piped through case-insensitive grep for `has moved`; update the stale layout comments at lines 21-24; leave the line 49 404 grep byte-untouched.
  12. `package.json` line 6 validate:html script: append ` apps/index.html` to the html-validate file list (geohist/*.html continues to cover the stub + guide + contact + changelog + privacy).

  AGENTS.md SAME-COMMIT SYNC (MIG-06, EA-15): 13. Rewrite the layout claims: Project paragraph (line 9) — root page is now the GeoHist landing and the portfolio hub lives at `/apps/`; Hosting constraint (line 20) — hub at `/apps/`, GeoHist landing at root, `/geohist/` is a meta-refresh-0 stub; Architecture file map rows (lines 119-121) — root index.html = landing, apps/index.html = hub, geohist/index.html = stub, keep all other rows truthful; fix the stale 10-RUNBOOK path at line 79 to `.planning/milestones/v2.0-phases/10-gated-social-proof/10-RUNBOOK.md` (OQ4). Phrase the dual-hosts fact exactly as "legacy `*.github.io` Pages host" — the host literal must never be written (old-domain gate enforces AGENTS.md).

  THEN run the full verification chain below on the final state.</action>
  <verify>
    <automated>node scripts/i18n-keycheck.mjs (PASS x19) && node scripts/i18n-surface.mjs --summary (prints 178 keys across 5 pages) && npm run validate (validate:html + validate:domain + validate:links + validate:i18n-detect + validate:i18n, exit 0) && node scripts/a11y-audit.mjs (5 pages green)</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/i18n-keycheck.mjs` prints PASS for all 19 dictionaries
    - `node scripts/i18n-surface.mjs --summary` prints `178 keys across 5 pages`
    - `npm run validate` exits 0
    - `node scripts/a11y-audit.mjs` completes green over exactly the 5 PAGES rows (root, apps, geohist-guide, geohist-contact, geohist-privacy)
    - `npx html-validate geohist/index.html` exits 0 (stub passes recommended ruleset)
    - Root index.html contains exactly one `data-i18n="geohist.footer.back"` anchor with href `/apps/`; apps/index.html contains exactly one `data-i18n="hub.card.cta"` anchor with href `/`; geohist/index.html contains the string `content="0; url=/"` and zero occurrences of `data-i18n`
    - sitemap.xml contains exactly 6 `<loc>` rows and none equal to `https://geohisttrivia.com/geohist/`
    - `git status --porcelain` shows all 15 task files modified/added and NO other tracked-file changes beyond the red-gate evidence doc
  </acceptance_criteria>
  <done>Final state green across every gate: 178 x 19 key surface intact, links resolve to the new layout, stub validates, a11y battery green, AGENTS.md synced. All migration files staged-in-working-tree (uncommitted) awaiting the single atomic ship commit.</done>
  <reversibility rating="costly">The atomic edit set is single-revert-able at ship (Phase-8 precedent c72b3a2), but the ONE href-only edit on `geohist/privacy.html` sits adjacent to the frozen Play-review compliance surface (A1) — revert is one line, yet a wrong edit ships to reviewers; that is why the edit is scoped to exactly one href attribute and P-13-03 fences policy content.</reversibility>
</task>

<task type="auto">
  <name>Task 3: Red-gate proofs both directions → red-gate-proof.md (snapshot-copy restore, hash-verified)</name>
  <files>.planning/phases/13-home-migration/red-gate-proof.md</files>
  <read_first>.planning/phases/13-home-migration/13-RESEARCH.md (Architecture Patterns red-gate table + Pitfall 9), .planning/milestones/v2.0-phases/11-close-v2.0-audit-debt/red-gate-proof.md (record format + snapshot-copy precedent — confirm exact dir name with glob before reading), scripts/i18n-keycheck.mjs, scripts/i18n-surface.mjs, scripts/a11y-audit.mjs, scripts/smoke-check.sh, package.json</read_first>
  <action>Prove every gate touched by Task 2, both directions, recording each cycle in red-gate-proof.md (mutated state, expected FAIL output line, restore hash, PASS output line). Restore method throughout: snapshot-copy the target file with Copy-Item BEFORE mutating, restore AFTER, verify sha256 hash equality — `git restore` is FORBIDDEN (deferred-commit dirty tree, Phase 10/11 precedent). Cycles:
  1. i18n-keycheck pages[] direction 1: temporarily rewrite line 48 array back to the old paths (with join('geohist','index.html') second entry) → run `node scripts/i18n-keycheck.mjs` → expect FAIL citing 13 missing hub.* keys (surface 165 != 178) → re-apply the target array → PASS. i18n-surface shares the same array shape — record one cycle noting both scripts.
  2. keycheck star-path + direction 2: temporarily rename the `proof-row-star` class in root index.html to a different class name → run keycheck → expect FAIL naming root index.html (proves the gate now reads the NEW path, 0 SVGs found) → restore snapshot hash-verified → PASS. Then flip-compat cycle (expects GREEN by construction, Phase-11 precedent): temporarily remove the `hidden` attribute from the OFF rating row and change the score span text from `0.0` to `4.5` → keycheck PASS → restore hash-verified → PASS (proves the owner's future flip cannot red the gate).
  3. validate:html glob: with the OLD glob string (without apps/index.html) temporarily injected into package.json line 6 AND an unclosed-tag mutation in apps/index.html → `npx html-validate ...` falsely PASSES (proves the old glob's gap) → re-apply the NEW glob → same mutation → FAIL → restore both files hash-verified → PASS.
  4. a11y PAGES: temporarily inject a duplicate h2 element into root index.html → run `node scripts/a11y-audit.mjs` → expect axe/Lighthouse FAIL → restore hash-verified → PASS. Budget ~10-15 min per cycle (5 pages x axe + Lighthouse).
  5. smoke-check list direction 1 (live-targeted): run `bash scripts/smoke-check.sh` against the CURRENT live pre-migration site → the NEW `$BASE/apps/` row prints a 404 FAIL → record as direction-1 evidence that the list covers the new layout. Direction 2 (post-deploy ALL PASS) is a pending row owned by the UAT gate — record it explicitly as pending, not fabricated.
  6. check-no-old-domain: no red-gate required (gate logic untouched) — record the rationale row plus full-chain `npm run validate` green as the pre-ship proof.
  End state: every restored file sha256-identical to its Task 2 final state; keycheck PASS re-run as the closing check.</action>
  <verify>
    <automated>Test-Path .planning/phases/13-home-migration/red-gate-proof.md && node scripts/i18n-keycheck.mjs (closing PASS) && npm run validate (exit 0)</automated>
  </verify>
  <acceptance_criteria>
    - red-gate-proof.md contains a FAIL record AND a PASS record for each of: keycheck pages[], star-path (both the rename-fail cycle and the flip-compat cycle), validate:html glob (old-glob gap proof + new-glob fail), a11y PAGES, smoke-check direction 1 (+ explicit pending row for post-deploy direction 2), and a no-red-gate rationale row for check-no-old-domain
    - Every cycle records a sha256 hash pair (pre-mutation / post-restore) and they are equal
    - `rg -c "git restore" .planning/phases/13-home-migration/red-gate-proof.md` prints 0 (snapshot-copy method only)
    - Closing `node scripts/i18n-keycheck.mjs` prints PASS x19 and `npm run validate` exits 0
  </acceptance_criteria>
  <done>Every gate touched by the atomic change set is proven repointed in both directions with recorded evidence; the working tree is hash-identical to the Task 2 final state.</done>
  <reversibility rating="reversible">Evidence-only task; all mutations restored hash-verified; no committed state changes.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo tracked files → publicly served Pages artifact | `.planning/` ships publicly; no secrets/debug tokens may enter planning docs |
| migration edit set → five CI gates | a wrong/un-repointed gate list silently audits the wrong page set (supply-chain-of-truth tampering class) |
| legacy-host fact → old-domain gate | any tracked file writing the legacy host literal fails CI by design |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-01 | Tampering | five gate page-lists + star path (i18n-keycheck/i18n-surface/a11y-audit/smoke-check/package.json) | high | mitigate | red-gate both directions per gate with hash-verified snapshot-copy restore (Task 3); star path explicitly covered (Pitfall 2) |
| T-13-02 | Information Disclosure | .planning/phases/13-home-migration/red-gate-proof.md + planning docs | medium | mitigate | console-UI-only content, no secrets/tokens; `.planning/` publicly served constraint restated in doc tasks |
| T-13-03 | Tampering | geohist/privacy.html (frozen Play-review surface) | high | mitigate | P-13-03 fences path + policy content; single href-only edit scoped by acceptance criteria; assets never moved (Pitfall 4) |
| T-13-04 | Denial of Service | geohist/index.html stub | low | mitigate | declarative meta-refresh-0 + `<a>` fallback (no JS dependency, no hang path); html-validate + linkinator verified |
| T-13-05 | Spoofing | sitemap/canonical/og:url/JSON-LD coherence | medium | mitigate | acceptance criteria assert 6 canonical rows + per-page canonical==og:url==JSON-LD url; no redirected URL listed |
</threat_model>

<verification>
Phase checks this plan must satisfy before ship:
1. `node scripts/i18n-keycheck.mjs` → PASS ×19; `node scripts/i18n-surface.mjs --summary` → `178 keys across 5 pages` (MIG-09 surface lock)
2. `npm run validate` exit 0 on the final state (all five validate stages, MIG-04/05 local half)
3. `node scripts/a11y-audit.mjs` green over the repointed 5-page PAGES list (MIG-05)
4. red-gate-proof.md complete: both directions for keycheck/surface array, star path (+flip-compat), validate:html glob, a11y PAGES, smoke-check direction 1 (+pending post-deploy row), no-red-gate rationale for check-no-old-domain (MIG-05)
5. AGENTS.md diff shows ONLY the layout-sync + stale-path edits and rides the SAME ship commit as the migration (MIG-06)
6. `geohist/privacy.html` diff is exactly one href attribute on line 72; policy content byte-identical (MIG-07, P-13-03)
7. sitemap.xml has exactly 6 `<loc>` rows: `/`, `/apps/`, guide, changelog, contact, privacy (MIG-04, EA-04/05)
8. 404.html diff is href-only; visible text "Back to the hub" unchanged (MIG-09, EA-11)
</verification>

<success_criteria>
- All three tasks done with their acceptance criteria green
- The working tree holds the complete migration as uncommitted changes ready for ONE atomic ship commit containing: both page moves + stub + all href repoints + sitemap + all five gate lists + star path + 404 + AGENTS.md (STATE locked decision) — ship subject suggestion: `feat(home-migration): root = GeoHist landing, hub -> /apps/, /geohist/ stub, gates repointed (one atomic commit)`
- Planning docs (red-gate-proof.md) commit separately per docs convention
- MIG-01..07 and MIG-09 satisfied locally; MIG-08 (owner GSC steps) authored by 13-02 and executed post-deploy
</success_criteria>

<output>
Create `.planning/phases/13-home-migration/13-01-SUMMARY.md` when done
</output>
