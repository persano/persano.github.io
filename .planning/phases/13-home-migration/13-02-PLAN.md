---
phase: 13-home-migration
plan: 02
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - ".planning/phases/13-home-migration/13-RUNBOOK.md"
  - ".planning/phases/13-home-migration/13-UAT.md"
autonomous: true
requirements: [MIG-08]
estimate:
  tokens: 35000
  raw_tokens: 18000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Owner can execute the post-deploy GSC sitemap resubmit and URL inspection from the Firebase/GSC console UI alone, following 13-RUNBOOK.md, with zero secrets in the doc (MIG-08)"
    - "The runbook explicitly forbids a GSC Change-of-Address refile with the doc-verified rationale — the 180-day window until ~2027-03 stays untouched (STATE locked decision)"
    - "Post-deploy owner steps are pre-scaffolded in 13-UAT.md with mechanical pass/issue recording — any result: issue is a blocker, no gap-awareness"
  artifacts:
    - ".planning/phases/13-home-migration/13-RUNBOOK.md — GSC post-deploy owner steps (console-UI only)"
    - ".planning/phases/13-home-migration/13-UAT.md — UAT scaffold covering MIG-01..09 incl. the owner GSC checkpoint + post-deploy smoke-check direction-2 row"
  key_links:
    - "13-RUNBOOK.md live steps <-> post-deploy timing (site must be deployed first; deploy happens at ship, not during this plan)"
    - "13-UAT.md owner checkpoint <-> mechanical uat-passed predicate (result: issue = blocker)"
    - "13-UAT.md smoke-check direction-2 row <-> red-gate-proof.md pending row (completes MIG-05 direction 2)"
  prohibitions:
    - id: P-13-04
      category: safety
      statement: "MUST NOT refile, cancel, or instruct a GSC Change-of-Address action for this migration — same-domain path moves are doc-excluded ('just add redirects, and update your sitemaps as appropriate'); the active 180-day window until ~2027-03 stays untouched"
---

<objective>
Author the Phase 13 post-deploy owner documentation: `13-RUNBOOK.md` (GSC sitemap resubmit + URL inspection, explicit no-CoA section) and the `13-UAT.md` scaffold that records the owner's post-deploy verification — completing MIG-08's deliverable (the runbook section; the live console steps execute post-deploy and are recorded via the phase UAT gate).

Purpose: research MIG-08 row — "GSC sitemap resubmit + URL inspection done post-deploy (owner console step, runbook section)"; the runbook is authored now (parallel-safe, doc-only), its live steps fire after ship deploys the new layout.

Output: 13-RUNBOOK.md + 13-UAT.md scaffold, both console-UI-only, zero secrets, zero legacy-host literals.
</objective>

<execution_context>
@C:/Users/Familia/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/Familia/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/13-home-migration/13-RESEARCH.md
@AGENTS.md
</context>

## Flagged Assumptions (edge-probe row carried here: 1 of 17 — status: unresolved, never auto-resolved, no silent drops)

Union across both plans = 17 rows (EA-01..EA-16 authored in 13-01-PLAN.md).

- **EA-17** [MIG-08 / unclassified, unresolved] — the owner's GSC sitemap resubmit + URL inspection happen POST-DEPLOY via the owner console (agent cannot access GSC); this plan delivers the runbook + UAT scaffold, and completion of the live steps is recorded at UAT time with the mechanical pass/issue predicate. No silent assumption that resubmission alone re-crawls pages — URL inspection rows exist precisely because resubmission timing is not guaranteed.

## Artifacts this phase produces

- **New git-tracked planning artifact:** `.planning/phases/13-home-migration/13-RUNBOOK.md` (GSC owner steps)
- **New git-tracked planning artifact:** `.planning/phases/13-home-migration/13-UAT.md` (UAT scaffold; live rows filled post-deploy)
- **No code, content, script, or sitemap changes** — doc-only plan; all migration artifacts come from 13-01

<tasks>

<task type="auto">
  <name>Task 1: Author 13-RUNBOOK.md — GSC sitemap resubmit + URL inspection + explicit no-CoA section</name>
  <files>.planning/phases/13-home-migration/13-RUNBOOK.md</files>
  <read_first>.planning/milestones/v2.0-phases/08-custom-domain-migration/08-RUNBOOK.md (runbook format + GSC section shape + one-commit precedent), .planning/phases/13-home-migration/13-RESEARCH.md (MIG-08 row, Sources primary quotes, Open Question 4, Runtime State Inventory GSC rows), .planning/REQUIREMENTS.md (MIG-08 + Out of Scope CoA row), AGENTS.md (.planning publicly served + old-domain phrasing rules)</read_first>
  <action>Create `13-RUNBOOK.md` in the 08-RUNBOOK format (numbered sections, console-UI steps only). Sections:
  1. **Prerequisites** — ship has landed the atomic migration commit and Pages deploy finished green; `bash scripts/smoke-check.sh` against the live site passes ALL rows (the post-deploy direction-2 row from red-gate-proof.md).
  2. **GSC sitemap resubmit** — GSC Domain property `geohisttrivia.com` → Sitemaps → resubmit `https://geohisttrivia.com/sitemap.xml`; expect the 6-URL set (/, /apps/, guide, changelog, contact, privacy) with no redirected URL listed; record the submitted date.
  3. **URL inspection** — inspect the moved/changed URLs: `/` (expect indexed as the GeoHist landing), `/apps/` (expect the hub), `/geohist/` (expect recognized as redirecting/permanently-moved via the meta-refresh classification — do NOT request indexing for it), `/geohist/privacy.html` (expect UNCHANGED indexed state — path-stable compliance surface). Use "Request indexing" only where genuinely useful (/ and /apps/); note that resubmission alone does not guarantee recrawl timing.
  4. **Explicit NO-CoA section** — state that a Change-of-Address refile is prohibited for this migration (P-13-04): same-domain path moves are doc-excluded; quote the rationale ("just add redirects, and update your sitemaps as appropriate"); the existing 180-day CoA window runs until ~2027-03 and must stay untouched — no refile, no cancel.
  5. **Post-checks** — Rich Results check on `/` for the SoftwareApplication JSON-LD; spot-check og:url rendering via the sharing debugger of choice; record observations.
  Constraints enforced in-document: console-UI only; ZERO secrets, tokens, or console credentials (`.planning/` is publicly served); the dual-hosts fact phrased exactly as "legacy `*.github.io` Pages host" — never the host literal itself; no invented GSC UI labels beyond those the 08-RUNBOOK precedent uses.</action>
  <verify>
    <automated>Test-Path .planning/phases/13-home-migration/13-RUNBOOK.md && (rg -c "Sitemaps" .planning/phases/13-home-migration/13-RUNBOOK.md | Out-String) && (rg -c "Change-of-Address" .planning/phases/13-home-migration/13-RUNBOOK.md | Out-String) && (rg -c "github\.io" .planning/phases/13-home-migration/13-RUNBOOK.md; if ($LASTEXITCODE -eq 1) { "0 matches - PASS" })</automated>
  </verify>
  <acceptance_criteria>
    - 13-RUNBOOK.md exists with the 5 numbered sections: Prerequisites, GSC sitemap resubmit, URL inspection, explicit NO-CoA section, Post-checks
    - The NO-CoA section states the 180-day window runs until ~2027-03 and forbids refile/cancel
    - `rg -c "github\.io" 13-RUNBOOK.md` returns 0 matches (no legacy-host literal anywhere in the doc)
    - Zero secret-shaped strings (no API keys, no tokens, no passwords) — the doc names console screens and URLs only
    - The URL inspection section covers all 5 URLs listed in the action (/ , /apps/ , /geohist/ , /geohist/privacy.html) with expected outcomes
  </acceptance_criteria>
  <done>Runbook complete and self-contained: an owner with GSC console access can execute every post-deploy step without improvisation, and the doc cannot be misread as licensing a CoA refile.</done>
  <reversibility rating="reversible">Doc-only deliverable; additive file, trivially revertible.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Author 13-UAT.md scaffold — post-deploy verification rows + owner GSC checkpoint</name>
  <files>.planning/phases/13-home-migration/13-UAT.md</files>
  <read_first>.planning/phases/12-cleanup-batch/12-UAT.md (UAT record format + mechanical predicate precedent), .planning/phases/13-home-migration/13-RESEARCH.md (Pitfall 7 fragment-loss expectation, Runtime State Inventory), .planning/ROADMAP.md (Phase 13 Success Criteria 1-5), .planning/phases/13-home-migration/13-01-PLAN.md (flagged assumptions EA-01..EA-16 — UAT expectations must match them, not silently contradict)</read_first>
  <action>Create `13-UAT.md` as a scaffold with one verification row per MIG requirement (MIG-01..MIG-09), each row carrying: the check, the expected result, and a `result:` field left as `pending` until executed. Rows:
  - MIG-01: `/` serves the full GeoHist landing (all sections present) in EN + spot-check 2 non-EN locales via the switcher (expected per EA-01/EA-03).
  - MIG-02: `/geohist/` redirects to the root landing (stub refresh); via the legacy host path-preserved chain as well; expectation note per Pitfall 7 — a `#faq` fragment does NOT survive the refresh; landing at top is the CORRECT expected result, not an issue.
  - MIG-03: `/apps/` serves the hub with the one real app card and zero placeholder cards.
  - MIG-04: sitemap.xml live-served with 6 URLs, all resolving 200; per-page canonical/og:url spot-check.
  - MIG-05: `bash scripts/smoke-check.sh` against the LIVE site → ALL PASS (this completes red-gate-proof.md's pending direction-2 row — record cross-reference).
  - MIG-06: deployed AGENTS.md content matches the new layout (repo-side check + note that the old-domain gate ran green in CI).
  - MIG-07: `/geohist/privacy.html` serves the frozen policy at the same path, byte-stable content, correct icon.
  - MIG-08: **owner GSC checkpoint** — execute 13-RUNBOOK.md sections 2-5 in the GSC console; record sitemap resubmit date + per-URL inspection outcomes here with `result: pass` or `result: issue` (mechanical predicate: any `result: issue` is a blocker).
  - MIG-09: 404 page link goes to `/apps/` labeled "Back to the hub"; nav/footer spot-checks on guide/contact/changelog.
  Mark every row `status: pending — executes post-deploy at UAT time`; do NOT fabricate results. Include a header note: this file ships in the public Pages artifact — console-UI descriptions only, zero credentials.</action>
  <verify>
    <automated>Test-Path .planning/phases/13-home-migration/13-UAT.md && (rg -c "MIG-0" .planning/phases/13-home-migration/13-UAT.md) && (rg -c "result: pass|result: issue" .planning/phases/13-home-migration/13-UAT.md; if ($LASTEXITCODE -eq 1) { "0 pre-filled results - PASS" })</automated>
  </verify>
  <acceptance_criteria>
    - 13-UAT.md contains one row per MIG-01 through MIG-09 (9 rows minimum, `rg -c "MIG-0"` returns at least 9)
    - The MIG-08 row is explicitly an owner checkpoint referencing 13-RUNBOOK.md sections
    - Zero pre-filled `result: pass` / `result: issue` values (all rows pending — no fabricated outcomes)
    - The Pitfall-7 expectation note (fragment loss → lands at top of landing) is present on the MIG-02 row so UAT does not misread it as an issue
    - No secrets/credentials anywhere in the file
  </acceptance_criteria>
  <done>UAT scaffold ready for post-deploy execution: every MIG row has a check + expected result + pending result field; the owner GSC steps are pre-staged with the mechanical recording predicate.</done>
  <reversibility rating="reversible">Doc-only deliverable; additive file, trivially revertible.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planning docs → publicly served Pages artifact | `.planning/` ships publicly; runbook/UAT must contain zero secrets or console credentials |
| runbook instructions → owner GSC console | a wrong instruction (e.g., CoA refile) causes irreversible SEO-state damage |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-06 | Information Disclosure | 13-RUNBOOK.md, 13-UAT.md | medium | mitigate | console-UI-only content; acceptance criteria grep for secret-shaped strings; no credentials named |
| T-13-07 | Tampering | GSC state via bad runbook instruction | high | mitigate | P-13-04 prohibition + explicit NO-CoA section with doc-verified rationale; 180-day window untouched |
| T-13-08 | Repudiation | UAT results fabricated pre-deploy | medium | mitigate | all rows `pending`; acceptance criteria forbid pre-filled result values; mechanical pass/issue predicate |
</threat_model>

<verification>
Phase checks this plan must satisfy:
1. Both docs exist in `.planning/phases/13-home-migration/` (MIG-08 deliverable)
2. Runbook covers sitemap resubmit + URL inspection + explicit NO-CoA + post-checks, console-UI only
3. UAT scaffold covers MIG-01..09 with pending rows and the owner GSC checkpoint pre-staged
4. Zero legacy-host literals and zero secrets in either doc (publicly served constraint)
5. No code/content/script/sitemap changes anywhere in this plan's diff
</verification>

<success_criteria>
- Both tasks done with acceptance criteria green
- MIG-08's deliverable (runbook section) complete; live owner console steps explicitly marked post-deploy and recorded via the UAT gate
- The runbook + UAT scaffold are ready to ride the phase docs commit alongside red-gate-proof.md
</success_criteria>

<output>
Create `.planning/phases/13-home-migration/13-02-SUMMARY.md` when done
</output>
