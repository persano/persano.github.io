---
phase: 01-foundation-deploy-pipeline-skeleton-privacy-policy
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - index.html
  - css/base.css
  - 404.html
  - .nojekyll
  - geohist/privacy.html
  - GeoHist_Trivia_Privacy_Policy.html
  - GeoHist_Trivia_Privacy_Policy.md
  - GeoHist_Trivia_Privacy_Policy.pdf
autonomous: false
requirements: [CMPL-01, CMPL-02, CONT-05]
estimate:
  tokens: 48000
  raw_tokens: 32000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "index.html renders a minimal Persano hub whose footer contains a link to /geohist/privacy.html (CMPL-01 wiring)"
    - "404.html is fully self-contained (inline <style>, zero external assets, absolute hrefs) and links back to / and /geohist/privacy.html (CONT-05)"
    - "geohist/privacy.html contains all 7 required sections and names all 5 SDKs in actual use: AdMob, Google Play Games Services, Google Play Billing (IAP), Firebase Analytics (consent-gated, fires only after consent), Firestore via contact form (CMPL-02)"
    - "Policy contact email, legal-entity name, and retention/deletion statement are the owner-supplied values from Task 2 — no invented facts (research A1–A3)"
    - "Superseded root policy files are deleted or explicitly retained per the owner decision recorded in Task 2 — exactly one live English policy (research A6 / Open Question 2)"
    - "No placeholder tokens of any kind survive in the committed site HTML"
  artifacts:
    - "index.html — minimal hub skeleton (Persano intro + footer privacy link)"
    - "css/base.css — shared stylesheet (custom properties, system font stack, mobile-first)"
    - "404.html — self-contained not-found page, inline CSS, absolute links"
    - ".nojekyll — empty root file; must be in the first site commit (OPS-02)"
    - "geohist/privacy.html — complete English policy, Play-critical URL"
  key_links:
    - "index.html footer <a href=\"/geohist/privacy.html\"> → policy page"
    - "404.html absolute links → / and /geohist/privacy.html"
    - "geohist/privacy.html <a href=\"/css/base.css\"> → shared stylesheet (absolute path, works from subdir)"
  prohibitions:
    - "No placeholder tokens from the old drafts (any bracketed owner-email marker, TBD, TODO, lorem) may appear in any committed site HTML — this URL is read by Play reviewers"
    - "No second contradictory live privacy policy: after the Task 2 disposition, the old Spanish/PDF root policy must not remain publicly served if deletion was approved"
    - "No Firebase, Analytics, consent-banner, or form code anywhere in Phase 1 output — zero Firebase bytes until the Phase 4 consent gate exists"
---

<objective>
**As a** Google Play reviewer and GeoHist Trivia player, **I want to** open https://persano.github.io and reach a complete, public, non-PDF English privacy policy at `/geohist/privacy.html` (plus a minimal hub and a working 404), **so that** the Play submission can reference a compliant public policy URL before the app goes live.

This plan builds the Phase 1 Walking Skeleton content slice: minimal hub + shared stylesheet + self-contained 404 + `.nojekyll` + the complete English privacy policy with owner-supplied facts, and dispositions the superseded Spanish root policy files. All work is committed locally — the single push that triggers the live deploy happens in Plan 01-02.

Purpose: The privacy URL is a release dependency of GeoHist Trivia; the hub/404 are the site's navigational skeleton every later phase builds on.
Output: 5 new site files (+ up to 3 deletions), committed locally, validated as far as the local environment allows.
</objective>

<execution_context>
@C:/Users/Familia/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/Familia/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-deploy-pipeline-skeleton-privacy-policy/01-RESEARCH.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Walking skeleton slice — minimal hub, shared CSS, self-contained 404, .nojekyll</name>
  <files>index.html, css/base.css, 404.html, .nojekyll</files>
  <read_first>
    .planning/phases/01-foundation-deploy-pipeline-skeleton-privacy-policy/01-RESEARCH.md (Pattern 2: self-contained 404; Recommended Project Structure)
    index.html (does not exist yet — confirm before creating)
    AGENTS.md (constraints: system font stack, no frameworks, mobile-first)
  </read_first>
  <action>
    Create four files (per D-decision: zero build, no framework, absolute paths safe on the user-site root domain):

    1. `index.html` — minimal hub skeleton. `<!DOCTYPE html>`, `<html lang="en">`, `<meta charset="utf-8">`, viewport meta, `<title>Persano — Personal Apps Hub</title>`, meta description (one sentence about Persano as Santiago David Postorivo's personal apps hub). Content: `<h1>Persano</h1>`, one short intro paragraph naming Santiago David Postorivo and the first app GeoHist Trivia, and a `<footer>` containing `<a href="/geohist/privacy.html">Privacy policy</a>`. Stylesheet via absolute `<link rel="stylesheet" href="/css/base.css">`. No app card, no gallery, no placeholder boxes — hub content arrives in Phase 2 (CONT-04); this is the navigable skeleton only. Do NOT add any i18n keys (Phase 2 concern per research A9) and no JS.

    2. `css/base.css` — shared stylesheet for hub + privacy pages. CSS custom properties on `:root` (e.g. `--color-bg`, `--color-fg`, `--color-accent`, `--font-body`), a minimal reset, `body` styled with a system font stack only (system-ui/sans-serif — no webfont imports, locked decision), base typography, and footer styling. Mobile-first: single-column default, no media queries required this phase.

    3. `404.html` — exactly the research Pattern 2 shape: self-contained with inline `<style>` in `<head>` (no external CSS, no JS, no `<link>`/`<script>`/`<img>`), `<meta name="robots" content="noindex">`, `<title>Page not found — Persano</title>`, `<h1>404</h1>`, short message, and links using absolute hrefs: one to `/` ("Back to the hub") and one to `/geohist/privacy.html`. No relative asset paths — unknown paths can be at any depth.

    4. `.nojekyll` — empty file at repo root. Must exist in this first site commit (OPS-02 requirement).

    Commit with a conventional message (e.g. `feat(01-01): add hub skeleton, base css, self-contained 404, .nojekyll`). DO NOT push — Plan 01-02 controls the single push that triggers deploy.
  </action>
  <verify>
    <automated>npx --yes html-validate@11.12.0 index.html 404.html (exit 0). If npx fails due to the known local npm proxy breakage, fall back to the acceptance-criteria source assertions below and defer validation to CI in Plan 01-02. Also: Test-Path .nojekyll → True; index.html contains 'href="/geohist/privacy.html"'; 404.html contains 'href="/"' and contains no 'src=' attribute.</automated>
  </verify>
  <acceptance_criteria>
    - index.html exists, is valid per html-validate (or proxy-fallback assertions), and contains `<a href="/geohist/privacy.html">` in its footer
    - index.html links `/css/base.css` (absolute) and contains no `<script>` tag
    - 404.html contains `<style>` inline, `<meta name="robots" content="noindex">`, `href="/"` and `href="/geohist/privacy.html"`, and contains zero external asset references (no `src=`, no `<link rel="stylesheet">`)
    - .nojekyll exists at repo root with 0 bytes
    - css/base.css defines at least one CSS custom property on `:root` and references only system fonts
  </acceptance_criteria>
  <done>Hub skeleton, base.css, self-contained 404, and .nojekyll are committed locally; footer links to the policy URL; 404 is fully self-contained.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: CHECKPOINT — owner inputs for the privacy policy + superseded-file disposition</name>
  <files>(none — records owner answers for Task 3)</files>
  <read_first>
    .planning/phases/01-foundation-deploy-pipeline-skeleton-privacy-policy/01-RESEARCH.md (Assumptions A1–A3, A6; Open Questions 1–4)
    GeoHist_Trivia_Privacy_Policy.md (current draft — shows the placeholder email token and existing ES content)
  </read_first>
  <action>
    Pause and ask the owner for four facts. Present them as a numbered list; record answers verbatim; do not invent or normalize values (research A1–A3, Open Questions 1–4):

    1. Contact email to publish in the policy and contact sections (A1). The old drafts contain only a bracketed owner-email placeholder — a real address is required before this page can go live.
    2. Legal-entity wording (A2): "Santiago David Postorivo" alone, or a specific form pairing the name with the "Persano" brand (e.g., "Persano, the personal brand of Santiago David Postorivo") — owner's exact wording.
    3. Retention statement for contact-form messages (A3): e.g., "messages are deleted after they are handled" or a concrete period — owner's wording. Also confirm the one-line statement that website analytics is consent-gated (fires only after consent).
    4. Superseded-file disposition (A6 / Open Question 2): has the Play Console privacy-policy field already been given the OLD root URL (`/GeoHist_Trivia_Privacy_Policy.html`)? If no → approve deleting the three old root policy files (HTML/MD/PDF) now. If yes → defer deletion; owner will update the Play Console field to `/geohist/privacy.html` before submission, and deletion happens after that.

    If the owner cannot answer immediately, halt the checkpoint — do not proceed with placeholder values. Pipeline work in Plan 01-02 can proceed in parallel; only Task 3's final text is blocked.
  </action>
  <what-built>Nothing yet — this checkpoint collects the four owner facts (email, entity wording, retention statement, old-file disposition) that Task 3's policy text requires verbatim.</what-built>
  <how-to-verify>All four answers present in the checkpoint response; none invented or placeholder.</how-to-verify>
  <resume-signal>Owner has supplied: (1) contact email, (2) exact entity wording, (3) retention statement, (4) yes/no on deleting the three superseded root policy files. Resume Task 3 with these values verbatim.</resume-signal>
  <verify>
    <automated>Checkpoint response contains: (1) a non-empty email address, (2) entity wording, (3) a retention sentence, (4) an explicit yes/no old-file disposition.</automated>
  </verify>
  <acceptance_criteria>
    - All four answers recorded verbatim in the execution record (checkpoint response or summary notes)
    - No owner fact invented, defaulted, or left as any bracketed placeholder
  </acceptance_criteria>
  <done>Owner supplied email, entity wording, retention wording, and old-file disposition; Task 3 can use them verbatim.</done>
</task>

<task type="auto">
  <name>Task 3: Complete English privacy policy at /geohist/privacy.html + superseded-file disposition</name>
  <files>geohist/privacy.html, GeoHist_Trivia_Privacy_Policy.html (delete per decision), GeoHist_Trivia_Privacy_Policy.md (delete per decision), GeoHist_Trivia_Privacy_Policy.pdf (delete per decision)</files>
  <read_first>
    GeoHist_Trivia_Privacy_Policy.md (source material — existing ES policy sections to translate/reuse: app-data offline/local-only wording, third-party policy links, changes section)
    .planning/phases/01-foundation-deploy-pipeline-skeleton-privacy-policy/01-RESEARCH.md (Pattern 4: privacy page structure — 7 required sections + SDK checklist)
    index.html (footer pattern from Task 1 — policy page reuses it)
  </read_first>
  <action>
    Create `geohist/privacy.html` — complete English policy, all sections in the Pattern 4 order:
    1. Entity identification — exact owner wording from Task 2 answer 2, with a `mailto:` link using the Task 2 answer 1 address.
    2. App data — offline-first, local-only storage (reuse/translate the existing ES policy wording on local storage and uninstall).
    3. SDK inventory — a distinct list item for each of the 5 SDKs, each naming the data it touches: AdMob (advertising ID), Google Play Games Services (player profile, achievements, leaderboards), Google Play Billing (IAP; payment data handled by Google), Firebase Analytics (website analytics, consent-gated — collected only after the visitor grants consent), Firestore via the contact form (message content the visitor submits).
    4. Retention and deletion — Task 2 answer 3 verbatim for message retention; local app data cleared on uninstall; deletion requests via the published contact email.
    5. Third-party policies — link to Google's privacy policy (policies.google.com/privacy), carried over from the source material.
    6. Changes to this policy — carried over from the source material, translated to English.
    7. Contact — the Task 2 email again as a `mailto:` link.

    Page shell: `<html lang="en">`, charset/viewport, `<title>Privacy Policy — GeoHist Trivia</title>`, meta description, stylesheet via absolute `<link rel="stylesheet" href="/css/base.css">`, footer with the same `/geohist/privacy.html` link pattern as the hub (self-link is fine — CMPL-01 requires the link present in every page footer). No JS, no Firebase, no i18n keys.

    Superseded-file disposition per Task 2 answer 4: if deletion approved → `git rm GeoHist_Trivia_Privacy_Policy.html GeoHist_Trivia_Privacy_Policy.md GeoHist_Trivia_Privacy_Policy.pdf` (removes the contradictory PDF from the future deploy); if deferred → leave the three files untouched and record in the summary that the Play Console privacy-URL field must be updated to `/geohist/privacy.html` before Play submission (Phase 5 trace).

    The old drafts are reference material only — do not copy any bracketed owner-email marker or Spanish text into the new page. Commit locally. DO NOT push.
  </action>
  <verify>
    <automated>npx --yes html-validate@11.12.0 geohist/privacy.html (exit 0; proxy-failure fallback: source assertions below). Then: Select-String -Path geohist/privacy.html -Pattern 'AdMob|Play Games Services|Play Billing|Firebase Analytics|Firestore' → all 5 match; Select-String -Pattern 'mailto:' → at least 2 matches; Select-String -Pattern 'TU_CORREO|TBD|TODO|FIXME' → 0 matches; if deletion approved: Test-Path on each of the 3 old root files → False.</automated>
  </verify>
  <acceptance_criteria>
    - geohist/privacy.html exists, is English (`lang="en"`), HTML (non-PDF), and passes html-validate
    - The page contains all 7 Pattern 4 sections and names each of the 5 SDKs with its data description
    - Contact email appears as a `mailto:` link (at least 2 occurrences: entity/contact sections); entity name matches Task 2 answer 2; retention wording matches Task 2 answer 3
    - Zero matches for placeholder markers (owner-email marker from old drafts, TBD, TODO, FIXME) in geohist/privacy.html
    - Old root policy files: deleted (Test-Path False) if approved, else untouched with the Play Console follow-up recorded in the summary
    - Footer of geohist/privacy.html contains `href="/geohist/privacy.html"`
  </acceptance_criteria>
  <done>Play-critical English policy complete with real owner facts at geohist/privacy.html; superseded files dispositioned per owner decision; everything committed locally, nothing pushed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| owner input → published legal text | Owner-supplied email/entity/retention wording is published verbatim to a public URL read by Play reviewers |
| repo content → Pages CDN | Everything committed at root deploys publicly (`path: '.'` artifact) |
| CI → GITHUB_TOKEN | Workflow files define what the repo token can do |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Information Disclosure | geohist/privacy.html content | high | mitigate | Blocking human checkpoint (Task 2) supplies real email/entity/retention; placeholder-marker grep gate in Task 3 acceptance criteria blocks publishing drafted placeholder text |
| T-01-02 | Tampering | Superseded dual policy | medium | mitigate | Explicit owner disposition decision (Task 2) + conditional git rm in Task 3 — no silent coexistence of contradictory policies |
| T-01-03 | Information Disclosure | Repo-wide public deploy of working files | low | accept | `.planning/`, `.serena/` are inert text; no secrets in repo (verified in research Runtime State Inventory); `.git`/`.github` are always excluded from the Pages artifact per upload-pages-artifact semantics |
| T-01-SC | Tampering | npm dev-dependency installs (html-validate 11.12.0, linkinator 8.1.0) | high | mitigate | RESEARCH.md Package Legitimacy Audit: both Approved via direct registry REST (the seam's SLOP verdicts are documented connectivity false-negatives — local npm CLI 404s); no postinstall scripts on either; first CI install run is the working existence proof; no blocking checkpoint required per research disposition |
</threat_model>

<verification>
- html-validate passes on index.html, 404.html, geohist/privacy.html (locally if npm/npx works, else via CI in Plan 01-02)
- Footer→policy link exists on hub and policy pages; 404 has absolute links only
- Policy covers 5 SDKs + entity + retention/deletion + contact, with zero placeholder markers
- Old policy files match the owner-recorded disposition
- All changes committed locally; `git status` clean; nothing pushed
</verification>

<success_criteria>
A visitor could open the (not-yet-deployed) site files and find: a minimal hub linking the privacy policy in its footer, a self-contained 404, and a complete English policy naming every SDK in actual use with the owner's real contact and retention facts — with the superseded Spanish/PDF policy removed or explicitly retained pending the Play Console update.
</success_criteria>

<artifacts_this_phase>
Files this plan creates: `index.html`, `css/base.css`, `404.html`, `.nojekyll`, `geohist/privacy.html`.
Files this plan may delete (owner decision): `GeoHist_Trivia_Privacy_Policy.html`, `GeoHist_Trivia_Privacy_Policy.md`, `GeoHist_Trivia_Privacy_Policy.pdf`.
No code symbols (classes/functions/flags) — Phase 1 is static content only; later phases add JS in `js/` and workflow config.
</artifacts_this_phase>

<output>
Create `.planning/phases/01-foundation-deploy-pipeline-skeleton-privacy-policy/01-01-SUMMARY.md` when done
</output>
