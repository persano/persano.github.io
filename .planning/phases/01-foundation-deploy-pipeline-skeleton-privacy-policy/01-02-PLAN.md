---
phase: 01-foundation-deploy-pipeline-skeleton-privacy-policy
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - .github/workflows/deploy.yml
  - package.json
  - package-lock.json
  - .htmlvalidate.json
  - scripts/smoke-check.sh
autonomous: false
requirements: [OPS-01, OPS-02, OPS-03]
estimate:
  tokens: 50000
  raw_tokens: 33000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Push to main triggers a validate job (html-validate + linkinator) that gates the deploy job via needs: — failed validation never deploys (OPS-01)"
    - "upload-pages-artifact runs with path '.' and include-hidden-files: true, so the committed .nojekyll reaches the served site (OPS-02)"
    - "Before the first deploy, repo Pages build_type is 'workflow' (Settings → Pages → Source: GitHub Actions) — verified via gh api (manual prerequisite, research Pitfall 1)"
    - "After deploy: /, /geohist/privacy.html, /app-ads.txt, /google7da873f4e9609872.html all return 200; an unknown path returns 404 with the custom page linking back to the hub; /.nojekyll returns 200 (OPS-03, CONT-05, research A5)"
    - "CI link gate never depends on third-party uptime (externals skipped pre-deploy)"
  artifacts:
    - ".github/workflows/deploy.yml — two-job workflow: validate (setup-node@v7, npm, html-validate + linkinator) → deploy (checkout@v7, configure-pages@v6, upload-pages-artifact@v5, deploy-pages@v5)"
    - "package.json — dev tooling only, exact-pinned html-validate 11.12.0 + linkinator 8.1.0, validate scripts"
    - "package-lock.json — committed if npm install succeeds locally; otherwise workflow falls back to npm install (no cache: npm)"
    - ".htmlvalidate.json — extends html-validate:recommended"
    - "scripts/smoke-check.sh — re-runnable post-deploy URL smoke check (reused before Play submission)"
  key_links:
    - "deploy job `needs: validate` — the gating edge"
    - "concurrency group 'pages' with cancel-in-progress: false — parallel/interrupted pushes never deploy out of order"
    - "workflow permissions: top-level contents: read; deploy job only pages: write + id-token: write (research Pitfall 6)"
  prohibitions:
    - "No third-party (non-actions/*) GitHub Actions in the workflow — official chain only, pinned to verified major versions"
    - "No external-domain fetches in the pre-deploy validate job — the gate must not fail because Google's servers hiccup"
    - "No deployment may proceed while repo Pages source is still 'legacy' (Deploy from a branch) — the Task 2 checkpoint is blocking"
---

<objective>
**As a** site owner, **I want to** push to main and have CI validate then deploy the site to GitHub Pages automatically, **so that** the Phase 1 content (hub, privacy policy, 404) is live at https://persano.github.io with zero manual deploy steps and all pre-existing root files intact.

This plan completes the Walking Skeleton's pipeline half: the two-job gated workflow, dev tooling, the Pages-source human checkpoint, and the single controlled push that puts everything live — followed by the OPS-03 smoke checks.

Purpose: OPS-01/02/03 are the deploy guarantee the rest of the milestone pushes rely on.
Output: Workflow + tooling committed, Pages source switched, site live, smoke checks green.
</objective>

<execution_context>
@C:/Users/Familia/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/Familia/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-deploy-pipeline-skeleton-privacy-policy/01-RESEARCH.md
@.planning/phases/01-foundation-deploy-pipeline-skeleton-privacy-policy/01-01-SUMMARY.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: CI pipeline slice — two-job validate→deploy workflow + dev tooling, validated locally</name>
  <files>.github/workflows/deploy.yml, package.json, package-lock.json, .htmlvalidate.json</files>
  <read_first>
    .planning/phases/01-foundation-deploy-pipeline-skeleton-privacy-policy/01-RESEARCH.md (complete workflow YAML, package.json, .htmlvalidate.json, Common Pitfalls 3/4/6)
    package.json (does not exist yet — confirm before creating)
    index.html, 404.html, geohist/privacy.html (the files validate:html will scan — created in Plan 01-01)
  </read_first>
  <action>
    Create the CI/dev-tooling slice exactly per the research-verified patterns:

    1. `.github/workflows/deploy.yml` — copy the research "Complete workflow" shape exactly: name "Validate and deploy to GitHub Pages"; `on: push` to `main` + `workflow_dispatch`; top-level `permissions: contents: read`; `concurrency: group: "pages"`, `cancel-in-progress: false`; job `validate` (ubuntu-latest): `actions/checkout@v7`, `actions/setup-node@v7` with `node-version: 24`, then `npm ci` and `npm run validate`; job `deploy` with `needs: validate`, job-level `permissions: pages: write` + `id-token: write`, `environment: name: github-pages` with `url: ${{ steps.deployment.outputs.page_url }}`, steps: `actions/checkout@v7` → `actions/configure-pages@v6` → `actions/upload-pages-artifact@v5` with `path: '.'` and `include-hidden-files: true` → `actions/deploy-pages@v5` with `id: deployment`. No other actions, no third-party actions.

    2. `.htmlvalidate.json` — `{"extends": ["html-validate:recommended"]}`.

    3. `package.json` — name `persano-github-io`, `private: true`, scripts exactly: `validate` = `npm run validate:html && npm run validate:links`; `validate:html` = `html-validate index.html 404.html geohist/*.html` (explicit list — `.planning/` must never be scanned); `validate:links` = `linkinator . --recurse --skip "^https?://(?!persano.github.io)" --skip "planning" --timeout 10000` (externals skipped pre-deploy per research Pitfall 4; the extra planning-skip keeps agent bookkeeping out of the scan). devDependencies exact-pinned: `html-validate` `11.12.0`, `linkinator` `8.1.0`.

    4. Dependency install — try `npm install` with the registry pinned (`--registry=https://registry.npmjs.org`), since the local npm CLI is proxy-broken per research. If it produces `package-lock.json` → commit it, and the workflow's validate job uses `npm ci` + `cache: npm` as written. If npm still fails → commit package.json without a lockfile, remove the `cache: npm` input from setup-node (it errors without a lockfile), and change `npm ci` to `npm install`; note in the summary that `npm ci`+cache must be restored once a lockfile lands.

    5. Run `npm run validate:html` locally (or the npx fallback from Plan 01-01) to pre-flight the strict parser against the three committed pages; fix any flagged markup now, before CI sees it (research Pitfall 3).

    Commit locally. DO NOT push — Task 3 performs the single push after the Task 2 checkpoint.
  </action>
  <verify>
    <automated>npm run validate:html exits 0 (or npx --yes html-validate@11.12.0 index.html 404.html geohist/privacy.html exits 0). Deploy.yml assertions: contains 'needs: validate', 'include-hidden-files: true', 'path: ''.''' , 'actions/checkout@v7', 'actions/configure-pages@v6', 'actions/upload-pages-artifact@v5', 'actions/deploy-pages@v5', 'pages: write', 'id-token: write', 'group: "pages"'. package.json contains '11.12.0' and '8.1.0'.</automated>
  </verify>
  <acceptance_criteria>
    - .github/workflows/deploy.yml exists with both jobs; deploy has `needs: validate`; all four actions pinned at the verified major versions; upload step carries `path: '.'` and `include-hidden-files: true`
    - Workflow permissions: top-level `contents: read`; deploy job `pages: write` + `id-token: write`; concurrency group `pages`, `cancel-in-progress: false`
    - validate:html scans exactly index.html, 404.html, geohist/*.html and exits 0 locally (or documented npm-failure fallback with source assertions)
    - package.json pins html-validate 11.12.0 and linkinator 8.1.0 exactly; validate:links skip pattern excludes all non-persano.github.io externals
    - .htmlvalidate.json extends html-validate:recommended
    - If package-lock.json exists, validate job uses `npm ci` + `cache: npm`; otherwise it uses `npm install` with no cache input
  </acceptance_criteria>
  <done>CI workflow + dev tooling committed locally; HTML gate pre-flighted green locally; nothing pushed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: CHECKPOINT — switch Pages source to GitHub Actions (manual prerequisite before first deploy)</name>
  <files>(none)</files>
  <read_first>
    .planning/phases/01-foundation-deploy-pipeline-skeleton-privacy-policy/01-RESEARCH.md (Pitfall 1: first deploy fails while build_type is legacy; Manual prerequisite section)
  </read_first>
  <action>
    Ask the owner to flip the one-time repo setting: GitHub → persano/persano.github.io → Settings → Pages → Build and deployment → Source: **GitHub Actions**. This cannot be automated with the workflow's default GITHUB_TOKEN (configure-pages `enablement` needs a PAT — research-verified), and `deploy-pages` fails against the current `legacy` source.

    After the owner confirms, verify from the CLI: `gh api repos/persano/persano.github.io/pages --jq .build_type` must output `workflow`. If it still returns `legacy`, halt and re-ask. This checkpoint gates Task 3 only.
  </action>
  <what-built>Nothing — one-time repo settings change performed by the owner (the only step that cannot be automated with the default GITHUB_TOKEN).</what-built>
  <how-to-verify>gh api repos/persano/persano.github.io/pages --jq .build_type returns "workflow".</how-to-verify>
  <resume-signal>Owner has switched Settings → Pages → Source to GitHub Actions AND gh api confirms build_type "workflow" — resume with Task 3.</resume-signal>
  <verify>
    <automated>gh api repos/persano/persano.github.io/pages --jq .build_type → "workflow"</automated>
  </verify>
  <acceptance_criteria>
    - gh api confirms build_type is "workflow" before any push that includes the workflow file
    - Owner instruction given exactly: Settings → Pages → Build and deployment → Source: GitHub Actions
  </acceptance_criteria>
  <done>Pages source switched and API-verified as workflow — first deploy can succeed.</done>
</task>

<task type="auto">
  <name>Task 3: Go-live — single push, CI green, deploy success, post-deploy smoke checks</name>
  <files>scripts/smoke-check.sh</files>
  <read_first>
    .planning/phases/01-foundation-deploy-pipeline-skeleton-privacy-policy/01-RESEARCH.md (Post-deploy smoke check script; Pitfall 2 hidden files)
    scripts/smoke-check.sh (does not exist yet — confirm before creating)
    01-01-SUMMARY.md (recorded old-file disposition — determines the expected status of the old policy URLs)
  </read_first>
  <action>
    1. Create `scripts/smoke-check.sh` (bash, for reuse at Play-submission time): loop over the 5 URLs from the research smoke-check script — `https://persano.github.io/`, `/geohist/privacy.html`, `/app-ads.txt`, `/google7da873f4e9609872.html`, each expecting HTTP 200 via `curl -s -o /dev/null -w '%{http_code}'`; then one unknown path `https://persano.github.io/does-not-exist` expecting 404 status AND body containing the hub-link text from 404.html; plus `https://persano.github.io/.nojekyll` expecting 200 (research Pitfall 2 warning-sign check). Print `url -> status` lines; exit non-zero on any failure. Note in the script header that it targets the live site and needs a short retry window right after a deploy (Pages CDN propagation).

    2. Precondition check: `gh auth status` OK and `git push` access to origin (remote persano/persano.github.io). Halt with a clear message if not.

    3. `git push origin main` — the single controlled push carrying all Phase 1 commits (Plan 01-01 + this plan). Immediately watch: `gh run watch` (or `gh run list` + `gh run view`) until the workflow finishes. Expected: `validate` job green (html-validate + linkinator over the full site), then `deploy` job success.

    4. If `validate` is red: read the job log, fix the flagged markup/config locally (typical first-run hits: html-validate strictness on void elements/implicit closes, linkinator surprises), commit, push again. Iterate until green. Do not skip or weaken the gate to get green.

    5. If deploy fails with Pages-source errors, re-run the Task 2 API check before anything else.

    6. Once deploy succeeds, wait ~60s for CDN propagation, then run the smoke script (bash scripts/smoke-check.sh, or the PowerShell equivalent: curl.exe per URL printing status codes). Confirm: 4 URLs 200, unknown path 404 with hub link in body, .nojekyll 200.

    7. Old-policy URL status per Plan 01-01's recorded disposition: if old files were deleted, `https://persano.github.io/GeoHist_Trivia_Privacy_Policy.html` now 404s (expected); if deferred, it still 200s — record which in the summary alongside the Play Console follow-up.
  </action>
  <verify>
    <automated>bash scripts/smoke-check.sh exits 0 with all five checks passing (4x HTTP 200 + unknown-path 404 serving the custom page + /.nojekyll 200). gh run view shows the deploy job concluded success for the pushed commit.</automated>
  </verify>
  <acceptance_criteria>
    - scripts/smoke-check.sh exists, covers the 5 research URLs + /.nojekyll + hub-link content check, and exits non-zero on failure
    - Latest run on main: validate job success AND deploy job success (gh run view output recorded)
    - https://persano.github.io/ returns 200 and serves the hub with the footer privacy link
    - https://persano.github.io/geohist/privacy.html returns 200 — the Play-critical URL is live
    - /app-ads.txt and /google7da873f4e9609872.html still return 200 (OPS-03 regression check)
    - An unknown path returns 404 whose body links back to the hub; /.nojekyll returns 200 (hidden file survived the artifact)
    - Old policy URL status matches the recorded Plan 01-01 disposition and is documented in the summary
  </acceptance_criteria>
  <done>Site is live at https://persano.github.io via push→CI→Pages with zero manual deploy steps; privacy URL and root files verified 200; 404 and .nojekyll verified live.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| workflow YAML → GITHUB_TOKEN | The workflow defines what the repo's default token can do on every push |
| Pages artifact → public CDN | Everything committed at root is published |
| npm registry → CI runner | validate job installs dev tooling from the public registry |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-04 | Elevation of Privilege | .github/workflows/deploy.yml permissions | high | mitigate | Top-level `contents: read` only; `pages: write` + `id-token: write` on the deploy job alone; official actions/* pinned — no third-party actions that could abuse the elevated context (research Pitfall 6) |
| T-01-05 | Denial of Service | validate job external-link fetches | medium | mitigate | linkinator skip pattern excludes all non-persano.github.io externals pre-deploy; external spot-checks are post-deploy/manual |
| T-01-06 | Tampering | Deployed artifact contents | low | accept | Artifact is built exclusively from pushed repo content via the official chain; site is static with no server-side execution; Jekyll processing disabled by committed .nojekyll |
| T-01-SC | Tampering | npm dev-dependency installs (html-validate 11.12.0, linkinator 8.1.0) | high | mitigate | RESEARCH.md Package Legitimacy Audit: both Approved via direct registry REST (seam SLOP verdicts are documented connectivity false-negatives); no postinstall scripts; exact-pinned in package.json; CI install is the working proof |
</threat_model>

<verification>
- Workflow YAML matches the research-verified chain (4 pinned actions, needs: gating, include-hidden-files, least-privilege permissions, concurrency group)
- Pages build_type verified "workflow" before the first deploy
- CI run on main: validate green → deploy success
- Live smoke checks: 5 URLs 200 + custom 404 content + /.nojekyll 200
- OPS-03 regression: app-ads.txt and Search Console verification still 200
</verification>

<success_criteria>
Pushing to main is the only action required to validate and deploy the site: validate job (html-validate + linkinator) gates the official four-action Pages deploy; https://persano.github.io and https://persano.github.io/geohist/privacy.html return 200; pre-existing root files are unaffected; unknown paths serve the custom 404; .nojekyll is live.
</success_criteria>

<artifacts_this_phase>
Files this plan creates: `.github/workflows/deploy.yml`, `package.json`, `.htmlvalidate.json`, `scripts/smoke-check.sh`, and `package-lock.json` (when npm succeeds locally). Config fields introduced: workflow triggers `push` (branches: [main]) + `workflow_dispatch`; scripts `validate`, `validate:html`, `validate:links`. No runtime code symbols — Phase 1 ships no JavaScript.
</artifacts_this_phase>

<output>
Create `.planning/phases/01-foundation-deploy-pipeline-skeleton-privacy-policy/01-02-SUMMARY.md` when done
</output>
