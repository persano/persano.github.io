---
phase: 05-discovery-quality-screenshots-seo-json-ld-aa-audit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - scripts/make-webp.mjs
  - geohist/screenshots/screenshot.menu.webp
  - geohist/screenshots/screenshot.map.webp
  - geohist/screenshots/screenshot.flags.webp
  - geohist/screenshots/screenshot.timeline.webp
  - geohist/index.html
  - js/i18n/es.json
  - js/i18n/pt-BR.json
autonomous: false
requirements: [LNDG-03]
user_setup:
  - service: android-device
    why: "ADB capture needs the physical phone plugged in over USB with debugging authorized and GeoHist Trivia installed — the agent cannot hold or navigate the device"
    dashboard_config:
      - task: "Plug the phone in, accept the USB-debugging prompt, keep the screen unlocked; owner navigates the app to each requested screen on the agent's cue"
        location: "On the phone; confirm with c:\\platform-tools\\adb.exe devices"
estimate:
  tokens: 22000
  raw_tokens: 22000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "geohist/screenshots/ contains exactly 4 WebP files named screenshot.menu.webp, screenshot.map.webp, screenshot.flags.webp, screenshot.timeline.webp — one per feature group per D-52, satisfying LNDG-03's 3-6 real-screenshot range."
    - "Every screenshot is a real device capture made this phase via adb exec-out screencap (D-53) — not stock art, not emulator mockups; each was approved by the user at capture time."
    - "The gallery in geohist/index.html shows 4 real img tiles: width/height match the actual WebP pixel dimensions, loading=lazy is on every image, alt is keyed via data-i18n-attr (geohist.gallery.alt.*), and each caption is a keyed plain-text p.tile-caption naming the mode (D-55)."
    - "Zero placeholder remnants (D-56): the gallery section has no SVG placeholder tiles, no aria-label/data-i18n-attr on tile list items, no intro paragraph — and the three retired keys are gone from BOTH dictionaries."
    - "npm run validate:i18n passes with both dictionaries at exactly 146 keys (141 current + 8 new gallery keys − 3 retired), 1:1 across es.json and pt-BR.json (Pitfall 3)."
    - statement: "Captured shots read as clean game screens — no accidental home-screen or status-bar junk unless the user explicitly approved a framing/crop decision at capture time (discretion per D-53)."
      verification: backstop
  artifacts:
    - "geohist/screenshots/*.webp — 4 committed WebP captures, names pinned: screenshot.menu.webp / screenshot.map.webp / screenshot.flags.webp / screenshot.timeline.webp (D-52; names feed 05-02's JSON-LD screenshot array)"
    - "scripts/make-webp.mjs — raw-PNG → WebP converter (sharp, D-54); prints pixel dimensions + byte size per output; exits non-zero on missing/corrupt input"
    - "package.json devDependencies += sharp@0.35.4, @axe-core/cli@4.13.0, lighthouse@13.4.1 (one-time install for the whole phase; 05-03 reuses axe/lighthouse) + npm script convert:screenshots"
    - "Gallery markup: 4 × li.gallery-tile (img with width/height/loading=lazy + keyed alt) + keyed caption p — CSS .gallery-tile reused unchanged (D-55)"
    - "8 new dictionary keys: geohist.gallery.alt.{menu,map,flags,timeline} + geohist.gallery.caption.{menu,map,flags,timeline} in es.json AND pt-BR.json; 3 retired keys deleted from both (D-55/D-56)"
  key_links:
    - "raw PNG (temp dir OUTSIDE the repo, never committed) -> scripts/make-webp.mjs -> geohist/screenshots/*.webp -> gallery img src — the script's printed dimensions feed the width/height attributes"
    - "gallery keyed captions/alts <-> js/i18n/es.json + pt-BR.json exact-set parity via scripts/i18n-keycheck.mjs — markup and dictionaries change in the SAME task (Pitfall 3)"
    - "pinned screenshot filenames <-> 05-02's JSON-LD screenshot array — 05-02 depends on these exact paths"
  prohibitions:
    - statement: "No placeholder gallery remnants may survive into the committed gallery: placeholder SVG tiles, list-item-level accessibility labels, and the placeholder caption copy are all removed in the same task that adds the real tiles (D-56)."
      status: enforced
      verification: "geohist/index.html gallery section contains zero svg tiles and zero data-i18n-attr aria-label on gallery li elements; keycheck exact-set parity fails loudly if any retired key lingers in either dictionary"
    - statement: "Real-image tiles must not double-expose accessibility semantics — no img-role wrapper around a native img element (research Pattern 3)."
      status: enforced
      verification: "grep -c of the img-role attribute in geohist/index.html returns 0 after this plan"

# Spec-less probe fallback — row dispositions (EDGE_ABSENT=true; never silent drops)
# LNDG-03 | unclassified | RESOLVED explicit -> truths 1-5 (4 real WebP tiles, one per mode, no placeholder remnants, parity gate green)

---

# Plan 05-01: Real Screenshots — ADB Capture + WebP Pipeline + Gallery (LNDG-03)

**User story:** As a prospective player visiting the landing page, I see real screenshots of the game — main menu and the three feature-group modes — instead of placeholder tiles, so I can see what I am installing before I install it.

## Artifacts this phase produces

Full phase artifact list (all three plans): `geohist/screenshots/*.webp` (4), `geohist/og-image.png` (1200×630), `scripts/make-webp.mjs`, `scripts/og-image.mjs`, `scripts/a11y-audit.mjs`, `sitemap.xml`, `robots.txt`, `reports/` (gitignored output dir), canonical/OG/Twitter head blocks on 5 content pages, the SoftwareApplication JSON-LD block on `/geohist/`, 8 new + 3 retired gallery dictionary keys, devDeps sharp/@axe-core/cli/lighthouse pinned, npm scripts `convert:screenshots` / `og:image` / `audit:a11y`, deletion of the 3 superseded root policy files. **This plan contributes:** the 4 WebP captures, `scripts/make-webp.mjs`, the rebuilt gallery markup, the 8 new + 3 retired dictionary keys, the pinned devDependencies, and npm script `convert:screenshots`.

<execution_context>
@C:/Users/Familia/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/Familia/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-discovery-quality-screenshots-seo-json-ld-aa-audit/05-CONTEXT.md
@.planning/phases/05-discovery-quality-screenshots-seo-json-ld-aa-audit/05-RESEARCH.md
@geohist/index.html
@scripts/i18n-keycheck.mjs
@package.json
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 1: CHECKPOINT — ADB capture of 4 feature-group screenshots with per-shot approval (D-52, D-53)</name>
  <files>(no repo files — raw captures land OUTSIDE the repo)</files>
  <read_first>
  - 05-RESEARCH.md — Pattern 4 (capture command + binary-safe redirect) and Pitfall 2 (PowerShell binary-redirect corruption; cmd /c wrapper)
  - 05-CONTEXT.md — D-52 (4 shots, one per feature group) and D-53 (user navigates + approves each shot; agent drives ADB; no file-handoff folder)
  - geohist/index.html lines 35-63 — the 4 feature groups the shots must mirror (trivia/menu, map, flags, timeline)
  </read_first>
  <precondition>An Android phone is plugged in over USB with debugging authorized (adb devices lists a device), GeoHist Trivia is installed, and the owner is at the desk to navigate screens and approve captures. If adb lists no device, halt this task and ask the owner to replug/reauthorize.</precondition>
  <action>Human action task — see <instructions>. The executor drives every capture command; the owner only navigates the phone and approves each shot. No repo files change in this task.</action>
  <instructions>Pause for the owner at each shot. Flow per D-53:
  (1) Confirm the device: run adb devices (adb lives at c:\platform-tools\adb.exe) — a device must be listed; also grab adb shell wm size and record the panel resolution (feeds Task 2's crop/dimension decisions).
  (2) Create the raw-capture directory at C:\Users\Familia\AppData\Local\Temp\opencode\geohist-captures (outside the repo — D-53 forbids a file-handoff folder in the repo; raws are never committed).
  (3) For each of the 4 screens in order — main menu, map/city mode, flags mode, timeline mode (D-52) — ask the owner to navigate the app to that screen and confirm it is framed as wanted, then capture with the binary-safe redirect per Pitfall 2: cmd /c "adb exec-out screencap -p > C:\Users\Familia\AppData\Local\Temp\opencode\geohist-captures\raw-screenshot.<name>.png" where name is menu / map / flags / timeline. If exec-out through the shell mangles the file (zero-byte or unparseable output), retry via a Node child_process spawn with stdio piped to a file descriptor instead of shell redirection (Pitfall 2 fallback).
  (4) After each capture, verify the file (PNG magic bytes, non-trivial size) and show/confirm with the owner that the shot is acceptable BEFORE moving to the next screen. If the owner rejects a shot, they re-frame and the capture re-runs. Record any approved framing/crop decision (e.g. crop the OS status bar) per shot — Task 2 applies it.
  </instructions>
  <verify>
    <automated>node -e "const fs=require('fs');const dir='C:/Users/Familia/AppData/Local/Temp/opencode/geohist-captures';const names=['menu','map','flags','timeline'];for(const n of names){const p=dir+'/raw-screenshot.'+n+'.png';if(!fs.existsSync(p)){console.error('FAIL missing raw capture: '+p);process.exit(1)};const b=fs.readFileSync(p);if(b.length<100*1024){console.error('FAIL suspiciously small ('+b.length+' bytes): '+p);process.exit(1)};if(b[0]!==0x89||b[1]!==0x50||b[2]!==0x4e||b[3]!==0x47){console.error('FAIL not a PNG: '+p);process.exit(1)}}console.log('PASS: 4 raw PNG captures present, valid magic, non-trivial size')"</automated>
  </verify>
  <verification>
  Owner reports after each shot: screen shown on the phone matches the requested feature group, framing acceptable (or a framing/crop decision recorded), shot approved. Any rejected shot -> owner re-frames, capture re-runs for that screen only.
  </verification>
  <resume-signal>Owner approves each of the 4 shots in sequence (or requests a re-capture). All 4 approved -> resume to Task 2 with the capture directory path + any framing/crop decisions recorded. Device missing -> replug/reauthorize and retry.</resume-signal>
  <acceptance_criteria>
  - All four raw files exist: raw-screenshot.menu.png, raw-screenshot.map.png, raw-screenshot.flags.png, raw-screenshot.timeline.png in C:\Users\Familia\AppData\Local\Temp\opencode\geohist-captures
  - Each file starts with the PNG magic bytes (89 50 4E 47) and is larger than 100 KB
  - The owner approved each of the 4 shots individually (recorded in the execution conversation; any framing/crop decisions noted)
  - No raw capture was committed to the repo (git status shows a clean tree apart from planning files)
  </acceptance_criteria>
  <done>Four owner-approved raw device captures exist outside the repo, one per feature group, ready for conversion.</done>
</task>

<task type="auto">
  <name>Task 2: Pin the 3 devDependencies + scripts/make-webp.mjs + convert to WebP (D-54)</name>
  <files>package.json, package-lock.json, scripts/make-webp.mjs, geohist/screenshots/screenshot.menu.webp, geohist/screenshots/screenshot.map.webp, geohist/screenshots/screenshot.flags.webp, geohist/screenshots/screenshot.timeline.webp</files>
  <read_first>
  - package.json — current scripts + devDependencies shape (html-validate, linkinator)
  - 05-RESEARCH.md — Standard Stack (pinned versions sharp 0.35.4 / @axe-core/cli 4.13.0 / lighthouse 13.4.1, install command), Package Legitimacy Audit (all three Approved — no blocking checkpoint required), Pattern 4 (sharp webp quality + extract), Assumptions A3/A4 (quality 82, optional resize)
  - 05-CONTEXT.md — D-54 (sharp devDep + make-webp.mjs), the agent's Discretion (quality/weight settings, file naming)
  </read_first>
  <action>
Install the three pinned devDependencies in one shot: npm install --save-dev sharp@0.35.4 @axe-core/cli@4.13.0 lighthouse@13.4.1 (axe + lighthouse are installed NOW for the whole phase so 05-03 does not repeat the install; the Package Legitimacy Audit in 05-RESEARCH.md already approves all three, so no legitimacy checkpoint is inserted). Commit package.json and package-lock.json (the lockfile exists in the repo root — npm install updates it; the CI validate job keeps its npm install fallback per the Phase-01 decision, no CI change needed).

Write scripts/make-webp.mjs (ESM, imports sharp): accepts the raw-capture directory as its first CLI argument (required); for each of the four pinned raw names raw-screenshot.menu.png / raw-screenshot.map.png / raw-screenshot.flags.png / raw-screenshot.timeline.png, loads it with sharp, applies an extract crop ONLY for shots where Task 1 recorded an approved framing crop (crop coordinates derived from the recorded decision + the wm size resolution), converts to WebP at quality 82 (A3; adjust if weight is unacceptable — discretion), and writes geohist/screenshots/screenshot.<name>.webp. After writing, read back each output's metadata and console.log the file name, pixel dimensions, and byte size — the gallery task consumes those dimensions for its width/height attributes. Exit non-zero if any raw input is missing or sharp throws (corrupt capture = loud failure, Pitfall 2 warning sign). If any output exceeds ~300 KB, downscale (sharp resize to a width that keeps the file lean, e.g. 720-810 px wide) and re-log the final dimensions (A4, discretion).

Add npm script convert:screenshots = node scripts/make-webp.mjs to package.json. Run the conversion against the Task-1 capture directory. Raw PNGs stay outside the repo — nothing to gitignore.
  </action>
  <verify>
    <automated>npm run convert:screenshots -- C:/Users/Familia/AppData/Local/Temp/opencode/geohist-captures && node -e "const fs=require('fs');const names=['menu','map','flags','timeline'];for(const n of names){const p='geohist/screenshots/screenshot.'+n+'.webp';if(!fs.existsSync(p)){console.error('FAIL missing: '+p);process.exit(1)};const b=fs.readFileSync(p);if(b.slice(0,4).toString()!=='RIFF'||b.slice(8,12).toString()!=='WEBP'){console.error('FAIL not WebP: '+p);process.exit(1)};if(b.length>300*1024){console.error('FAIL too heavy ('+b.length+' bytes): '+p);process.exit(1)}}const pkg=JSON.parse(fs.readFileSync('package.json','utf8'));const dd=pkg.devDependencies;for(const [k,v] of Object.entries({sharp:'0.35.4','@axe-core/cli':'4.13.0',lighthouse:'13.4.1'})){if(dd[k]!==v){console.error('FAIL devDep '+k+' != '+v);process.exit(1)}}if(!pkg.scripts['convert:screenshots']){console.error('FAIL npm script missing');process.exit(1)}console.log('PASS: 4 WebPs valid+lean, devDeps pinned, npm script present')"</automated>
  </verify>
  <acceptance_criteria>
  - npm install exits 0; package.json devDependencies carries the exact pins sharp 0.35.4, @axe-core/cli 4.13.0, lighthouse 13.4.1; package-lock.json updated and committed
  - geohist/screenshots/ contains exactly the four files screenshot.menu.webp, screenshot.map.webp, screenshot.flags.webp, screenshot.timeline.webp and nothing else
  - Each WebP passes the RIFF/WEBP magic check and is under 300 KB
  - scripts/make-webp.mjs printed pixel dimensions + byte size for all four outputs (recorded in the execution log — Task 3 consumes the dimensions)
  - npm script convert:screenshots exists in package.json
  </acceptance_criteria>
  <done>Four lean WebP captures exist at their pinned paths with known dimensions, produced by a re-runnable in-repo script from raws that never enter the repo.</done>
</task>

<task type="auto">
  <name>Task 3: Gallery replacement — real img tiles + keyed captions/alts + dictionary parity (D-55, D-56)</name>
  <files>geohist/index.html, js/i18n/es.json, js/i18n/pt-BR.json</files>
  <read_first>
  - geohist/index.html lines 66-93 — the gallery section being replaced (section shell + title stay; tiles + intro paragraph go)
  - 05-RESEARCH.md — Pattern 3 (gallery tile replacement: drop the img-role wrapper, native img + keyed alt + width/height + lazy), Pitfall 3 (exact-set keycheck), Pitfall 4 (keyed nodes carry plain text only)
  - js/i18n/es.json and js/i18n/pt-BR.json lines ~38-41 — the four current gallery keys (title stays; intro/tile-aria/coming are retired)
  - geohist/index.html line 24 — the data-i18n-attr alt pattern the new keyed alts replicate (geohist.hero.icon.alt precedent)
  - scripts/i18n-keycheck.mjs — exact-set parity semantics (zero missing, zero extra, both directions)
  </read_first>
  <action>
Replace the gallery body in geohist/index.html (lines 66-93) per research Pattern 3, keeping the section shell (class gallery, aria-labelledby) and the h2 title untouched:
- DELETE the intro paragraph under the title (its placeholder promise is false once real shots ship) and all three placeholder list items (SVG tiles + list-item accessibility labels + placeholder caption markup) — every placeholder remnant goes in this same task (D-56).
- Add four tiles, one per captured mode, in the order menu / map / flags / timeline: each is li.gallery-tile containing an img with src=/geohist/screenshots/screenshot.<name>.webp, a KEYED alt attribute (data-i18n-attr="alt:geohist.gallery.alt.<name>" data-i18n-attr-only — same mechanism as the hero icon alt), width and height taken from Task 2's printed pixel dimensions (CLS prevention), and loading="lazy" (gallery is below the fold); followed by p.tile-caption with data-i18n="geohist.gallery.caption.<name>" and plain-text EN content. No list-item-level role or aria-label attributes on the tiles — the native img now carries the semantics (Pattern 3: double exposure anti-pattern).
- Recommended EN values (final wording = discretion, but every caption MUST name its mode per D-55, and every alt must be descriptive): alt.menu "Main menu screen of GeoHist Trivia", alt.map "Map mode screen — placing a city on the world map", alt.flags "Flags mode screen — guessing the country from its flag", alt.timeline "Timeline mode screen — dating a historical event"; caption.menu "Main menu", caption.map "Map mode", caption.flags "Flags mode", caption.timeline "Timeline mode".
- Dictionaries: add the 8 new keys (geohist.gallery.alt.menu/map/flags/timeline + geohist.gallery.caption.menu/map/flags/timeline) to BOTH js/i18n/es.json and js/i18n/pt-BR.json in this same task (Pitfall 3 — markup and dicts move together or the gate fails), with faithful es + pt-BR translations (agent-drafted per the D-39 precedent; owner review remains a standing note). DELETE the three retired keys (geohist.gallery.intro, geohist.gallery.tile.aria, geohist.gallery.coming) from BOTH dictionaries. Expected surface: 141 + 8 − 3 = 146 keys, 1:1.
- Run npm run validate:html and npm run validate:i18n; both must be green. Commit. (The push to main is orchestrator-owned at the wave boundary per the Phase-01/02/04 pattern — this plan ends at commits.)
  </action>
  <verify>
    <automated>npm run validate:html && npm run validate:i18n && node -e "const fs=require('fs');const h=fs.readFileSync('geohist/index.html','utf8');const gal=h.split('class=\"gallery\"')[1].split('</section>')[0];const imgs=(gal.match(/<img /g)||[]).length;if(imgs!==4){console.error('FAIL gallery img count '+imgs);process.exit(1)};const lazy=(gal.match(/loading=\"lazy\"/g)||[]).length;if(lazy!==4){console.error('FAIL lazy count '+lazy);process.exit(1)};for(const n of ['menu','map','flags','timeline']){if(!gal.includes('/geohist/screenshots/screenshot.'+n+'.webp')){console.error('FAIL src missing: '+n);process.exit(1)};if(!gal.includes('geohist.gallery.alt.'+n)||!gal.includes('geohist.gallery.caption.'+n)){console.error('FAIL keyed attr missing: '+n);process.exit(1)};if(!gal.includes('data-i18n-attr=\"alt:geohist.gallery.alt.'+n+'\"')){console.error('FAIL alt keying: '+n);process.exit(1)}};if(gal.includes('<svg')){console.error('FAIL svg placeholder survived');process.exit(1)};if((h.match(/role=\"img\"/g)||[]).length!==0){console.error('FAIL img-role wrapper present');process.exit(1)};if((h.match(/data-i18n-attr=\"aria-label:geohist\.gallery/g)||[]).length!==0){console.error('FAIL li aria-label survived');process.exit(1)};if(/coming soon/i.test(h)){console.error('FAIL placeholder copy survived');process.exit(1)};for(const f of ['js/i18n/es.json','js/i18n/pt-BR.json']){const d=JSON.parse(fs.readFileSync(f,'utf8'));const ks=Object.keys(d);if(ks.length!==146){console.error('FAIL '+f+' has '+ks.length+' keys, expected 146');process.exit(1)};for(const n of ['menu','map','flags','timeline']){if(!d['geohist.gallery.alt.'+n]||!d['geohist.gallery.caption.'+n]){console.error('FAIL new key missing in '+f+': '+n);process.exit(1)}}};console.log('PASS: gallery 4 real tiles, keyed alts+captions, dicts 146 keys 1:1')"</automated>
  </verify>
  <acceptance_criteria>
  - npm run validate:html and npm run validate:i18n both exit 0
  - The gallery section contains exactly 4 img elements, each with loading=lazy, a width attribute, a height attribute, a keyed alt (data-i18n-attr), and a src pointing at the matching pinned WebP path
  - geohist/index.html contains zero svg elements inside the gallery section, zero img-role attributes anywhere, and zero placeholder caption copy (case-insensitive)
  - Both dictionaries parse as exactly 146 keys, contain all 8 new gallery keys, and keycheck's exact-set gate passes (which structurally proves the 3 retired keys are gone — an extra key fails the gate loudly)
  - The intro paragraph under the gallery title is gone from the markup
  </acceptance_criteria>
  <done>The gallery shows the 4 real approved screenshots with keyed captions and alts in all three languages' dictionaries, zero placeholder remnants, and all CI gates green — LNDG-03 complete in code.</done>
</task>

</tasks>

<verification>
- npm run validate:html && npm run validate:i18n && npm run validate:links — all green (link check unaffected: no new outbound links; local gallery srcs resolve)
- geohist/screenshots/ = exactly 4 WebP files at the pinned names, each RIFF/WEBP and < 300 KB
- git status: no raw PNGs committed; the 4 WebPs + gallery markup + both dictionaries + package files staged/committed
- Dictionary surface: 146 keys 1:1 in es.json and pt-BR.json (keycheck exact-set)
- The 4 pinned screenshot paths exist and match exactly what 05-02 will cite in the JSON-LD screenshot array
</verification>

<success_criteria>
- LNDG-03: gallery shows 3-6 (here: 4) real screenshots captured via ADB from the connected phone, shipped as WebP with lazy loading — placeholders fully replaced (D-52..D-56 honored)
- All new user-visible strings keyed 1:1 in both dictionaries; keycheck green
- Probe row LNDG-03 (unclassified) resolved explicitly: 4 real tiles, one per feature group, no placeholder remnants
- Screenshot paths pinned for 05-02's JSON-LD consumption
</success_criteria>

<output>
Create `.planning/phases/05-discovery-quality-screenshots-seo-json-ld-aa-audit/05-01-SUMMARY.md` when done
</output>
