---
phase: 03-i18n-engine-es-pt-br-dictionaries-switcher
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - js/i18n/pt-BR.json
  - package.json
autonomous: true
requirements: [I18N-01, I18N-03]

estimate:
  tokens: 22000
  raw_tokens: 22000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A browser preferring any pt-* tag (pt-BR, pt-PT, pt-AO, bare pt) sees the pt-BR dictionary applied in place on hub, landing, and guide (I18N-01, D-33)"
    - "The switcher's Português entry applies pt-BR with the same persist-then-emit contract as Español (I18N-03, D-29, D-34, D-35)"
    - "npm run validate includes the dictionary gate: both dictionaries parse and exactly cover the live key surface in CI (OPS-01 support, D-25, Pitfall 8)"
    - "ES and pt-BR dictionaries are key-identical — neither language lags the other (D-25 1:1 contract, both languages)"
    - "Identity strings (contact email, three copyright lines) are byte-identical across EN markup, es.json, and pt-BR.json (D-25 identity-string rule)"
  artifacts:
    - path: js/i18n/pt-BR.json
      provides: "pt-BR dictionary, flat, key set exactly equal to es.json — 102 entries incl. the 3 meta-desc keys (D-25, D-27)"
    - path: package.json
      provides: "validate:i18n script (node scripts/i18n-keycheck.mjs) chained into validate after html+links — CI dictionary gate live"
  key_links:
    - from: "js/i18n.js detection chain"
      to: "js/i18n/pt-BR.json"
      via: "pt-* → pt-BR folding feeds the same-origin fetch of /js/i18n/pt-BR.json (D-33, D-25)"
      pattern: "pt-BR"
    - from: "package.json validate chain"
      to: "scripts/i18n-keycheck.mjs"
      via: "validate:i18n runs the key-coverage gate in the CI validate job (OPS-01, D-25)"
      pattern: "validate:i18n"
    - from: "js/i18n/es.json key set"
      to: "js/i18n/pt-BR.json key set"
      via: "exact set equality — parity enforced by the same keycheck gate (D-25)"
      pattern: "i18n-keycheck"
---

<objective>
Complete the trilingual surface: the pt-BR dictionary (key-identical with ES), the CI dictionary gate wired into npm run validate, and the full-site regression battery + deploy handoff.

Purpose: pt-BR visitors get the same in-place experience (D-33 folding already routes them to pt-BR.json), and dictionary↔markup drift becomes a CI failure forever — the slow-burn defect class Pitfall 8 names is closed at the pipeline level.
Output: js/i18n/pt-BR.json (102 keys), package.json validate:i18n wiring, recorded regression battery, deploy handoff for the orchestrator's single controlled push.
</objective>

<execution_context>
@C:/Users/Familia/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/Familia/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-i18n-engine-es-pt-br-dictionaries-switcher/03-CONTEXT.md
@.planning/phases/03-i18n-engine-es-pt-br-dictionaries-switcher/03-RESEARCH.md
@.planning/phases/03-i18n-engine-es-pt-br-dictionaries-switcher/03-01-SUMMARY.md
@index.html
@geohist/index.html
@geohist/guide.html
@geohist/privacy.html
</context>

## User Story

> **As a** Brazilian Portuguese-speaking visitor to persano.github.io, **I want to** read the hub and the GeoHist pages in Portuguese without leaving the page I'm on, **so that** the site speaks my language as naturally as the game does.

(Second half of the ROADMAP Phase 3 Goal line — "Spanish and pt-BR visitors read the site in their language…". ES half landed in plan 03-01; this plan completes I18N-01's pt-BR copy and hardens the pipeline.)

## Assumption Delta Decision

Same outcome as plan 03-01 (`no-change` — EN raw HTML stays the content baseline; the data-i18n key surface stays the addressing identity; pt-BR.json is another per-language consumer of that surface, key-identical with es.json). Companion invariant now enforced for BOTH languages by the CI gate.

## Artifacts this phase produces (plan 02 half)

**Files:** `js/i18n/pt-BR.json` (new), `package.json` (modified — one new script + validate chain extension).
**Dictionary:** pt-BR copy, 102 entries — namespaces hub 12 / geohist 53 / guide 37, key set byte-equal to es.json's.
**CI surface:** `validate:i18n` npm script → `node scripts/i18n-keycheck.mjs`; `validate` = `validate:html && validate:links && validate:i18n`.
**Zero new dependencies** — devDependencies untouched (html-validate 11.12.0 + linkinator 8.1.0 remain the only dev tooling).

## Flagged Assumptions (spec-less edge probe — the same 4 unresolved rows, surfaced here for the pt-BR half)

| # | Req | Probe | Disposition |
|---|-----|-------|-------------|
| 1 | I18N-01 | unclassified — review manually | Flagged: pt-BR copy agent-drafted per D-39 from the EN strings (no app pt glossary exists — translations.json holds es/fr/it only), owner reviews after execution; 1:1 coverage enforced by the keycheck gate. |
| 2 | I18N-02 | unclassified — review manually | Flagged: pt-* folding (D-33) is already in the engine (plan 03-01 battery asserts it); pt-BR behavioral proof rides this plan's human-check. |
| 3 | I18N-03 | unclassified — review manually | Flagged: the Português switcher entry exercises the same switchTo path; persistence behavior verified in the human-check. |
| 4 | I18N-04 | unclassified — review manually | Flagged: pt-BR meta-desc keys ride the same snapshot walk; tab-title/lang check rides the human-check. |

4 surfaced / 4 flagged — no-silent-drop equality holds.

## must_haves.prohibitions (descriptor-less — flagged-unverified, per spec-less fallback divergence)

Same four prohibitions as plan 03-01, now binding the pt-BR copy and the expanded switcher surface — restated here so no plan set drops one:

- **No policy-claim drift in translations (P1, I18N-01, values/transparency):** pt-BR FAQ answers and copy restating policy-derived claims MUST mirror the meaning of `geohist/privacy.html` (D-41); EN stays legally authoritative. Flagged-unverified; owner reviews copy (D-39).
- **No language data leaves the browser (P2, I18N-02, privacy):** unchanged — plan 03-02 adds no network surface; pt-BR.json is a same-origin static file. Flagged-unverified.
- **Storage holds only a language code (P3, I18N-03, privacy/transparency):** unchanged — pt-BR is simply a third legal value of persano.lang. Flagged-unverified.
- **No flag icons or country imagery for language selection (P4, I18N-03, values):** unchanged — the switcher stays three endonym text entries; pt-BR copy never turns the Português entry into imagery. Flagged-unverified.

Canon referral (not minted): same as plan 03-01 — DOM-injection is canon XSS territory mitigated by the textContent/setAttribute-only rule; consent/GDPR canon is Phase 4.

<tasks>

<task type="auto">
  <name>Task 1: pt-BR dictionary — 102 keys, key-identical with es.json</name>
  <files>js/i18n/pt-BR.json</files>
  <read_first>
    - js/i18n/es.json (parity reference — key set, structure, identity-string treatment; pt-BR mirrors this file's key set exactly)
    - index.html + geohist/index.html + geohist/guide.html (EN source strings for every value)
    - .planning/phases/03-i18n-engine-es-pt-br-dictionaries-switcher/03-CONTEXT.md (D-25 flat 1:1 shape, D-39 owner review, D-41 policy meaning-mirror)
    - .planning/phases/03-i18n-engine-es-pt-br-dictionaries-switcher/03-RESEARCH.md (§Dictionary shape, §Exact data-i18n key surface, identity-string rule)
    - geohist/privacy.html (authoritative EN wording — D-41 source for FAQ claim translations)
    - .planning/phases/03-i18n-engine-es-pt-br-dictionaries-switcher/03-01-SUMMARY.md (engine + es.json delivery state, any execution notes)
  </read_first>
  <action>
    Create js/i18n/pt-BR.json (D-25): flat JSON, exactly one entry per live key — the same 102-entry key set as js/i18n/es.json (99 markup keys + hub.meta.desc + geohist.meta.desc + guide.meta.desc). Key set must be exactly equal to es.json's — the keycheck gate from plan 03-01 enforces set equality, so run it as you go.

    Author Brazilian Portuguese from the EN strings in the three HTML files, mirroring the ES dictionary's structure sentence-for-sentence where the languages allow (consistent sentence-per-key mapping keeps the two dictionaries reviewable side by side): friendly second-person "você" register consistent with the playful site voice; complete final sentences; no interpolation, no plural machinery (RESEARCH §Don't Hand-Roll). Game-mode names take fresh pt-BR copy (Flags, Capitals, Map figures, Cities & islands, Events in time, Art detective, Coats of arms, World Passport — research notes the app has NO pt glossary in translations.json, so draft from EN and let owner review per D-39).

    Identity strings translate to themselves: geohist.about.email and geohist.faq.report.email keep the literal santiagopostorivo@gmail.com; hub.footer.copyright, geohist.footer.copyright, guide.footer.copyright keep © Persano — Santiago David Postorivo. "GeoHist Trivia", "Persano", "Google Play", "Android" stay untranslated. FAQ answers carrying policy-derived claims translate with meaning-mirror to geohist/privacy.html — never contradict the EN policy (D-41). The three meta-desc keys mirror the meaning of the EN meta descriptions as complete pt-BR sentences.

    Do NOT touch js/i18n.js, the HTML files, or es.json — this task is a pure data file. If authoring reveals an engine or es.json defect, stop and record it in the summary instead of editing plan-01 artifacts here.
  </action>
  <verify>
    <automated>Test-Path js\i18n\pt-BR.json; node -e "const fs=require('fs');const a=JSON.parse(fs.readFileSync('js/i18n/es.json','utf8'));const b=JSON.parse(fs.readFileSync('js/i18n/pt-BR.json','utf8'));const ka=Object.keys(a).sort(),kb=Object.keys(b).sort();if(JSON.stringify(ka)!==JSON.stringify(kb))process.exit(1);if(kb.length!==102)process.exit(2);for(const k of ['geohist.about.email','geohist.faq.report.email']){if(b[k]!=='santiagopostorivo@gmail.com')process.exit(3)};for(const k of ['hub.footer.copyright','geohist.footer.copyright','guide.footer.copyright']){if(b[k]!=='© Persano — Santiago David Postorivo')process.exit(4)}"; node scripts/i18n-keycheck.mjs; if ($LASTEXITCODE -ne 0) { exit 1 }</automated>
  </verify>
  <acceptance_criteria>
    - js/i18n/pt-BR.json parses; exactly 102 keys; key set exactly equals js/i18n/es.json's key set (set-equality gate exits 0)
    - identity strings byte-equal the EN values: both email keys = santiagopostorivo@gmail.com; all three copyright keys = © Persano — Santiago David Postorivo
    - every value is a non-empty string (no empty entries, no null, no nested objects)
    - pt-BR copy is distinct from both its EN source and the ES copy: spot keys geohist.hero.tagline, geohist.faq.title, guide.modes.1.name differ from their EN values AND from the es.json values
    - the three meta-desc keys are complete pt-BR sentences mirroring the EN meta descriptions' meaning
    - no key references a privacy-policy sentence in a way that contradicts geohist/privacy.html meaning (D-41) — spot-check the five policy-derived FAQ answers against the EN policy wording
  </acceptance_criteria>
  <done>pt-BR visitors get a complete, key-identical dictionary; the trilingual surface is symmetric (102 = 102 = 102).</done>
  <reversibility rating="reversible">Dictionary entries are data edits — swapping or correcting pt-BR copy never touches the engine or markup (D-41 reversibility pattern).</reversibility>
</task>

<task type="auto">
  <name>Task 2: Wire validate:i18n into the CI validate chain</name>
  <files>package.json</files>
  <read_first>
    - package.json (current scripts shape: validate = validate:html && validate:links)
    - scripts/i18n-keycheck.mjs (the gate being wired — must exit 0 for both dictionaries)
    - .planning/phases/03-i18n-engine-es-pt-br-dictionaries-switcher/03-RESEARCH.md (Open Question 3 recommendation: JSON-parse + key-coverage gate in validate; Pitfall 7 CI regression)
  </read_first>
  <action>
    Extend package.json (D-25 Pitfall-8 defense made permanent): add the script "validate:i18n" with the value "node scripts/i18n-keycheck.mjs", and extend the existing "validate" script to "npm run validate:html && npm run validate:links && validate:i18n" — keeping the existing two scripts untouched and the chain order html → links → i18n (cheap structural checks first, dictionary gate last so its diff output is never buried). Do NOT add any devDependency — the gate runs on node built-ins only; devDependencies stay exactly html-validate 11.12.0 + linkinator 8.1.0.

    This rides the existing CI validate job unchanged: .github/workflows/deploy.yml already runs npm run validate before deploy (OPS-01 chain from Phase 1), so the dictionary gate goes live in CI with zero workflow edits.
  </action>
  <verify>
    <automated>node -e "const p=JSON.parse(require('fs').readFileSync('package.json','utf8'));if(!p.scripts['validate:i18n'])process.exit(1);if(!p.scripts.validate.includes('validate:i18n'))process.exit(2);if(JSON.stringify(p.devDependencies)!==JSON.stringify({'html-validate':'11.12.0','linkinator':'8.1.0'}))process.exit(3)"; node scripts/i18n-keycheck.mjs; if ($LASTEXITCODE -ne 0) { exit 1 }</automated>
  </verify>
  <acceptance_criteria>
    - package.json parses; scripts.validate:i18n = "node scripts/i18n-keycheck.mjs"
    - scripts.validate chains validate:html, validate:links, and validate:i18n
    - devDependencies byte-identical to before (zero new packages)
    - node scripts/i18n-keycheck.mjs exits 0 with both dictionaries present (es.json + pt-BR.json)
  </acceptance_criteria>
  <done>The dictionary↔markup parity gate runs in every CI validate job — key drift now fails the deploy pipeline instead of silently shipping partial translations.</done>
</task>

<task type="auto">
  <name>Task 3: Cross-page regression battery + deploy handoff</name>
  <files>index.html, geohist/index.html, geohist/guide.html, geohist/privacy.html, 404.html, js/i18n.js, js/i18n/es.json, js/i18n/pt-BR.json</files>
  <read_first>
    - index.html, geohist/index.html, geohist/guide.html (final state — battery surface)
    - geohist/privacy.html, 404.html (must remain diff-untouched this phase — D-37 regression)
    - js/i18n.js (engine — regression surface for the battery's script/page assertions)
    - .planning/phases/02-geohist-landing-hub-content-en-i18n-keys-baked-in/02-02-PLAN.md (Task 3 battery pattern this task mirrors)
    - .planning/phases/03-i18n-engine-es-pt-br-dictionaries-switcher/03-CONTEXT.md (D-37 pages restriction; deploy handoff = Phase-01 pattern per STATE.md)
  </read_first>
  <action>
    Run the cross-page regression battery (read-only — zero file changes expected; Phase-2 02-02 pattern) and record PASS marks: (1) footer privacy link + contact mailto present on all five pages (CMPL-01 regression); (2) script tag exactly once in each of the 3 keyed pages, ZERO script tags in geohist/privacy.html and 404.html (D-26/D-37); (3) no per-language directory artifacts exist at repo root (Test-Path es / Test-Path pt both false) and no alternate-link language annotations in any HTML file (rel="alternate" count 0 across *.html — the locked no-subdirs decision regression); (4) internal link graph resolves: every static href points at an existing file (linkinator pre-check by hand); (5) heading order sequential and ids unique per page; (6) keyed meta attrs present on all 3 pages (D-27); (7) dictionaries: node scripts/i18n-keycheck.mjs exit 0; both files parse; key sets equal; identity strings equal EN (email + three copyrights); (8) the three files that must not change this phase — geohist/privacy.html, 404.html, css/base.css — show zero diff via git diff HEAD (phase-scoped change list = the 3 keyed pages + js/ + package.json + scripts/); (9) no dictionary file is referenced by any static href (they are fetch-only — linkinator cannot see them; grep the HTML files for "js/i18n" expecting zero matches in href attributes).

    Deploy handoff (STATE.md Phase-01 pattern — executor does NOT push): after the battery is green, the orchestrator executes the single controlled push to main; CI runs validate (now including the dictionary gate) then Pages deploy. Post-deploy smoke: reuse scripts/smoke-check.sh per the Phase-1 pattern and additionally verify /js/i18n/es.json and /js/i18n/pt-BR.json return 200 with a JSON content-type.
  </action>
  <verify>
    <automated>node scripts/i18n-keycheck.mjs; if ($LASTEXITCODE -ne 0) { exit 1 }; (Select-String -Path index.html,geohist\index.html,geohist\guide.html -Pattern 'script defer src="/js/i18n\.js"').Count -eq 3; (Select-String -Path geohist\privacy.html -Pattern '<script').Count -eq 0; (Select-String -Path 404.html -Pattern '<script').Count -eq 0; (Test-Path es) -eq $false; (Test-Path pt) -eq $false; (Select-String -Path *.html -Pattern 'rel="alternate"').Count -eq 0; git diff --name-only HEAD -- geohist/privacy.html 404.html css/base.css | Measure-Object -Line | Select-Object -ExpandProperty Lines</automated>
  </verify>
  <acceptance_criteria>
    - battery items 1–9 all recorded PASS (Phase-2 style, one line each)
    - geohist/privacy.html, 404.html, and css/base.css are diff-untouched this phase (git diff empty for those paths)
    - both dictionaries key-identical (102 = 102), parse cleanly, gate exit 0
    - identity strings consistent across EN markup, es.json, pt-BR.json (email + three copyright lines)
    - zero static href references to js/i18n/*.json anywhere in markup
    - no per-language directory artifacts and no alternate-link language annotations in markup
    - deploy handoff recorded: executor did not push; orchestrator owns the single controlled push; post-deploy smoke list includes the two dictionary URLs
  </acceptance_criteria>
  <done>Full-site battery green across all five pages + engine + both dictionaries; the phase is deploy-ready and the pipeline permanently guards dictionary parity.</done>
</task>

</tasks>

<verification>
- Local: Task 1 set-equality + identity gates, Task 2 package.json gate, Task 3 battery — all green (npm dev tooling proxy-broken locally; CI arbitrates html-validate/linkinator).
- Orchestrator single controlled push (after this plan's docs commit): validate job green including the new dictionary gate, then Pages deploy (OPS-01/OPS-02 chain unchanged).
- Post-deploy smoke: hub, /geohist/, /geohist/guide.html, /geohist/privacy.html, 404 behavior, /js/i18n/es.json, /js/i18n/pt-BR.json — all 200.
- Behavioral proof rides the plan 03-01 human-check plus the pt-BR path: pt-* browser (or DevTools locale pt-BR) renders Portuguese in place; Português switcher entry persists across reload.
</verification>

<human-check>
Same drill as plan 03-01 with Portuguese: set browser locale to pt-BR — all three pages render pt-BR copy in place, tab titles translate, html lang="pt-BR"; switch to Português from English or Español — persists across reload; owner reviews the pt-BR copy per D-39 (especially game-mode names and the FAQ policy-mirror sentences per D-41) alongside the es-419 review.
</human-check>

<success_criteria>
- I18N-01 complete: EN raw HTML + es.json + pt-BR.json — all three languages cover the exact 102-key surface 1:1, parity gate in CI
- I18N-03 (pt-BR half): Português switcher entry applies and persists like the other two
- Dictionary drift is a CI failure (Pitfall 8 closed at pipeline level, Open Question 3 resolved YES)
- Phase 3 SC1–SC4 all hold with both non-English languages; deploy handoff clean per the Phase-01 push pattern
</success_criteria>

<output>
Create `.planning/phases/03-i18n-engine-es-pt-br-dictionaries-switcher/03-02-SUMMARY.md` when done
</output>