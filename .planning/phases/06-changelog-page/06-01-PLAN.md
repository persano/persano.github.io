---
phase: 06-changelog-page
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - geohist/changelog.html
  - css/base.css
  - js/i18n/es.json
  - js/i18n/pt-BR.json
  - scripts/i18n-keycheck.mjs
  - geohist/index.html
  - geohist/guide.html
  - geohist/contact.html
  - geohist/privacy.html
  - sitemap.xml
  - scripts/smoke-check.sh
autonomous: true
requirements:
  - CONT-06
  - CONT-07

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Visiting /geohist/changelog.html shows a Keep-a-Changelog-style timeline list: article rows with version+date header (version first, ISO date in a <time> element), Added/Changed/Fixed subheads, player-facing bullets — newest-first"
    - "Changelog chrome (nav, nav aria-label, headings, keyed <title>/meta description, footer, back link, consent banner) is i18n-keyed and the exact same key set exists in BOTH es.json and pt-BR.json — i18n-keycheck reports PASS (exact set-equality) on the grown surface"
    - "Changelog is reachable from every geohist page: nav link (Game/Guide/FAQ/Changelog/Privacy order, per D-15) + footer link (after Contact, per D-16) on geohist/index.html, guide.html, contact.html; plain unkeyed nav link on privacy.html; root hub /index.html untouched (per D-14)"
    - "scripts/i18n-keycheck.mjs pages array includes join('geohist', 'changelog.html') so missing changelog.* keys or an unregistered page fails CI (per CONT-07)"
    - "sitemap.xml lists https://persano.github.io/geohist/changelog.html and scripts/smoke-check.sh enumerates $BASE/geohist/changelog.html"
    - "Each page namespaces its own keys — no key name is shared across pages; the Changelog nav link exists once per page namespace (geohist.nav.changelog, guide.nav.changelog, contact.nav.changelog, changelog.nav.changelog), and the union gate counts them per-page" 
    - - statement: "Every shipped entry has at least one shown category with 1-4 bullets (empty categories omitted per D-06) and both dictionaries carry non-empty translated values for every added key — the current gate treats empty values as present, so this is a content check the gate cannot do yet (Phase 7 I18N-09 adds it)"
      verification: backstop
  artifacts:
    - path: "geohist/changelog.html"
      provides: "Keyed-chrome changelog page: guide.html frame (header/nav/main/footer/consent banner + 3 defer scripts) + unkeyed EN timeline entries"
    - path: "css/base.css"
      provides: ".changelog-entry card-block styles"
      contains: ".changelog-entry"
    - path: "js/i18n/es.json"
      provides: "changelog.* + per-page nav/footer keys (es)"
      contains: "changelog.meta.title"
    - path: "js/i18n/pt-BR.json"
      provides: "changelog.* + per-page nav/footer keys (pt-BR)"
      contains: "changelog.meta.title"
    - path: "scripts/i18n-keycheck.mjs"
      provides: "pages array registration for the new page"
      contains: "changelog.html"
    - path: "sitemap.xml"
      provides: "/geohist/changelog.html URL entry"
      contains: "geohist/changelog.html"
    - path: "scripts/smoke-check.sh"
      provides: "post-deploy check of the new URL"
      contains: "changelog.html"
  key_links:
    - from: "geohist/changelog.html"
      to: "js/i18n/es.json, js/i18n/pt-BR.json"
      via: "data-i18n / data-i18n-attr keys <-> exact dictionary key set (gate: scripts/i18n-keycheck.mjs)"
      pattern: "changelog\\."
    - from: "geohist/index.html, geohist/guide.html, geohist/contact.html, geohist/privacy.html"
      to: "geohist/changelog.html"
      via: "nav + footer anchors href=/geohist/changelog.html (linkinator-gated)"
      pattern: "changelog\\.html"
    - from: "sitemap.xml"
      to: "geohist/changelog.html"
      via: "<url><loc> entry"
      pattern: "<loc>https://persano\\.github\\.io/geohist/changelog\\.html</loc>"

assumption_delta_decision:
  noun: "language"
  decision: "no-change"
  rationale: "Detector fired on the phrase 'their chosen language' — that describes the v1 dictionary-swap selection already shipped (localStorage persano.lang > navigator.languages > en fallback in js/i18n.js); Phase 6 introduces no second representation of any identity noun, so no promote/add-alongside applies."
---

<objective>
Ship `/geohist/changelog.html` end-to-end: the page frame (keyed chrome), timeline-entry markup with a first real git-verified entry, the entry-row CSS, reachability (nav/footer links on all geohist pages, sitemap, smoke-check), and the ATOMIC i18n key-surface wiring (page chrome keyed + ~23 new keys in es/pt-BR + keycheck `pages` registration in one commit) — then prove the red gate both ways.

Purpose: CONT-06 (page exists, KaC format, EN entries) and CONT-07 (keyed chrome, atomic dictionary keys, red-gate tested, sitemap + nav/footer reachability). The page ships with one real git-verified entry (0.88 — 2026-09-04) under the D-08 honesty rule (real 0.x version + ISO date, no invented dates); the full 4-6 entry curated backfill lands in Plan 06-02 after app-repo mining (per D-01/D-02/D-03). Page growth is uncapped by design (D-12) — no archive or trim mechanism is built.
Output: geohist/changelog.html wired end-to-end, key surface grown 146 → ~169 with the gate green, reachability surfaces updated, red-gate proof recorded.
</objective>

<execution_context>
@C:/Users/Familia/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/Familia/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-changelog-page/06-CONTEXT.md
@.planning/phases/06-changelog-page/06-RESEARCH.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end changelog page — EN-complete, key-surface-neutral</name>
  <files>geohist/changelog.html, css/base.css, sitemap.xml, scripts/smoke-check.sh, geohist/index.html, geohist/guide.html, geohist/contact.html, geohist/privacy.html</files>
  <action>
Build the page frame by copying geohist/guide.html head-to-foot and swapping the main content, keeping the key surface UNCHANGED in this task (plain text only, no data-i18n attributes anywhere yet — the keyed swap is Task 2, per the research's sequenced-green pattern). Per D-10 the frame mirrors guide.html: H1 (working title "What's new in GeoHist Trivia") + one-line intro at top; per D-09 the main content is a timeline list of entries (all visible, no cards, no <details>); per D-10 a back-to-game link at the bottom (href="/geohist/", pointing back to the game landing); per D-09/D-06/D-07 entry rows use: article.changelog-entry > h2 "0.88 — <time datetime="2026-09-04">2026-09-04</time>" (version-first header format per CONTEXT Specifics; the 2026-09-04 date is the git-verified 0.88 date — never the CONTEXT's wrong example 2026-08-28), h3 category subheads limited to Added/Changed/Fixed (empty categories omitted), ul/li with 1-4 player-facing bullets. This task carries exactly ONE real entry: 0.88 (Play release candidate) — verify the date yourself with one git command against the app repo (see read_first mining command) before writing it. Copy guide.html's consent-banner section, lang-switcher-slot span, footer list shape (without the changelog <li> yet), and the three defer scripts (/js/i18n.js, /js/firebase-config.js, /js/consent.js) verbatim. Head metadata per D-11 at guide.html parity: canonical URL https://persano.github.io/geohist/changelog.html, og:* + twitter:* raw EN reusing the existing og-image.png (no new JSON-LD per D-11). Add to css/base.css, at end of file following the .mode-item card convention (base.css:337-343) and the decoration-only rule (no texture behind copy): a .changelog-entry block with background-color var(--color-surface), border var(--hairline), border-radius 0.5rem, padding 0.85rem 1rem, margin-bottom 0.6rem, plus margin/color rules for its h2/h3/ul that match existing section typography. Reachability (key-surface-neutral edits): add one <url><loc>https://persano.github.io/geohist/changelog.html</loc></url> to sitemap.xml (same one-line format as existing entries); add $BASE/geohist/changelog.html to the URL list in scripts/smoke-check.sh; insert a plain-text nav link "Changelog" (href="/geohist/changelog.html") into the nav of geohist/index.html, geohist/guide.html, contact.html positioned immediately BEFORE the Privacy link (D-15), and the same plain link into geohist/privacy.html's unkeyed nav matching that file's existing structure (D-13; privacy.html loads no scripts — never key it, per research Pitfall 5). Insert the same plain-text Changelog link into the footer of geohist/index.html, guide.html, contact.html positioned AFTER the Contact <li> and before Back-to-hub (D-16). Do NOT touch the root hub /index.html (D-14). Do NOT add any data-i18n attribute or touch the dictionaries or keycheck in this task — that is Task 2 and they must move together.
  </action>
  <read_first>
geohist/guide.html (full — the frame source: head metadata lines 6-23, nav 26-33, main sections, footer 88-99, consent banner 101-105)
geohist/index.html (nav + footer regions only)
geohist/contact.html (nav + footer regions only)
geohist/privacy.html (nav region 20-30 — its plain unkeyed structure)
css/base.css (design tokens :root 14-26, .mode-item card pattern 337-343, decoration-only rule 362-377)
sitemap.xml, scripts/smoke-check.sh (files being edited)
.planning/phases/06-changelog-page/06-CONTEXT.md (decisions D-01..D-16)
Mining command for the entry date: git -C C:/Users/Familia/antigravity/GeoHist-Trivia log --date=short --format="%h %ad %s" -1 2cefab4
  </read_first>
  <verify>
    <automated>node scripts/i18n-keycheck.mjs && npm run validate:html && npm run validate:links</automated>
  </verify>
  <acceptance_criteria>
    - geohist/changelog.html exists and passes html-validate as part of the existing geohist/*.html glob (no script edit needed)
    - node scripts/i18n-keycheck.mjs exits 0 with the surface UNCHANGED at 146 keys for both dictionaries (this task adds no keys)
    - The page contains at least one <article class="changelog-entry"> with an h2 whose <time datetime> is "2026-09-04" for version 0.88, and an h3 limited to Added/Changed/Fixed with a ul of bullets
    - Every key-bearing page variant (index/guide/contact) contains an anchor href="/geohist/changelog.html" in both nav and footer; privacy.html contains exactly one such anchor; root /index.html contains no changelog reference (D-14)
    - sitemap.xml contains <loc>https://persano.github.io/geohist/changelog.html</loc>; scripts/smoke-check.sh contains the string $BASE/geohist/changelog.html
    - npm run validate:links exits 0 (all new nav/footer hrefs resolve to the committed page)
    - css/base.css contains a .changelog-entry rule block using var(--color-surface) and var(--hairline) tokens only
  </acceptance_criteria>
  <done>The page is live-in-repo end to end: styled timeline entry, nav/footer/sitemap/smoke-check wired, all local gates green with the key surface untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Atomic i18n wiring — keyed chrome + dictionaries + keycheck registration (ONE commit)</name>
  <files>geohist/changelog.html, geohist/index.html, geohist/guide.html, geohist/contact.html, js/i18n/es.json, js/i18n/pt-BR.json, scripts/i18n-keycheck.mjs</files>
  <action>
This is the atomic key-surface move (research Pattern 2): the page markup keys, the 3 pages' nav/footer key inserts, both dictionaries, and the keycheck pages registration must land in a single commit — any split commit is verifiably red (gate is exact set-equality over a UNION of all registered pages). 1) Key the changelog.html chrome by mirroring guide.html's keyed-node pattern 1:1 with the changelog.* namespace: keyed <title> and meta description via data-i18n-attr (guide.html lines 6-19 pattern — only title and description keyed; og:*/twitter:* stay raw EN per research Pitfall 8); nav aria-label via data-i18n-attr + data-i18n-attr-only; nav links data-i18n changelog.nav.game/.guide/.faq/.changelog (self-link keyed, precedent guide.nav.guide)/.privacy; H1 data-i18n="changelog.title"; intro line data-i18n="changelog.intro"; back link data-i18n="changelog.back.link"; footer links data-i18n="changelog.footer.privacy/.contact/.changelog/.back/.consent" + footer copyright data-i18n="changelog.footer.copyright". The expected new key list is ~23: changelog.meta.title, changelog.meta.desc, changelog.nav.aria, changelog.nav.game, changelog.nav.guide, changelog.nav.faq, changelog.nav.changelog, changelog.nav.privacy, changelog.title, changelog.intro, changelog.back.link, changelog.footer.privacy, changelog.footer.contact, changelog.footer.changelog, changelog.footer.back, changelog.footer.consent, changelog.footer.copyright, geohist.nav.changelog, guide.nav.changelog, contact.nav.changelog, geohist.footer.changelog, guide.footer.changelog, contact.footer.changelog — the FINAL set is exactly what markup lands (gate enforces equality; mirror guide.html's keyed-node set and adjust the list if the actual markup differs). Entry content stays unkeyed EN (documented i18n exception per STATE.md locked decision): entry articles, their h3 subheads and bullets carry no swap keys — keyed nodes must be plain-text-only (no child markup inside any keyed node, per research Pitfall 4). 2) Add data-i18n attributes to the nav and footer Changelog anchors inserted in Task 1 on geohist/index.html (geohist.nav.changelog / geohist.footer.changelog), guide.html (guide.nav.changelog / guide.footer.changelog), contact.html (contact.nav.changelog / contact.footer.changelog) — matching each page's existing namespace exactly. 3) Add the SAME key set with real es and pt-BR translations to BOTH js/i18n/es.json and js/i18n/pt-BR.json, following the tone of existing guide.* values (agent drafts per CONTEXT discretion; owner can veto later); keep translations non-empty. 4) In scripts/i18n-keycheck.mjs line 22, append join('geohist', 'changelog.html') to the pages array. Commit ALL of step 1-4 as a single atomic commit ( Pitfall 1: splitting this across commits makes intermediate commits fail validate:i18n ).
  </action>
  <read_first>
geohist/guide.html (full — the keyed-chrome source of truth: which nodes carry data-i18n vs data-i18n-attr vs data-i18n-attr-only)
geohist/changelog.html (current state from Task 1 — the file being modified)
geohist/index.html, geohist/contact.html (nav + footer regions)
js/i18n/es.json and js/i18n/pt-BR.json (structure + value tone of guide.* entries)
scripts/i18n-keycheck.mjs (pages array line 22 + exact set-equality logic 100-107)
  </read_first>
  <verify>
    <automated>node scripts/i18n-keycheck.mjs && npm run validate:html</automated>
  </verify>
  <acceptance_criteria>
    - node scripts/i18n-keycheck.mjs exits 0 reporting PASS for BOTH es.json and pt-BR.json at the grown surface (~169 keys; exact number computed at build)
    - The pages array in scripts/i18n-keycheck.mjs contains join('geohist', 'changelog.html')
    - Every swap key present in geohist/changelog.html or newly added to the 3 pages' nav/footer markup exists in BOTH dictionaries, and both dictionaries contain no keys absent from the union markup (gate-proven by the passing run)
    - No swap key was added inside any <article class="changelog-entry"> subtree — entries remain key-free EN content
    - geohist/privacy.html gained NO swap keys and NO scripts (its link stays plain text — dead-key check per research Pitfall 5)
    - npm run validate:html exits 0
    - The task's diff shows the markup edits and BOTH dictionary edits and the keycheck edit in ONE commit (atomicity)
  </acceptance_criteria>
  <done>Key surface grown 146 → ~169 in a single atomic commit; es/pt-BR visitors get translated chrome; the gate passes exact set-equality and would fail any one-sided drift.</done>
</task>

<task type="auto">
  <name>Task 3: Red-gate proof (both failure directions) + full validate battery</name>
  <files>.planning/phases/06-changelog-page/red-gate-proof.md</files>
  <action>
Prove the CI gate is actually red-gated, locally, without ever committing a red state (research Pattern 3). Three mutation cycles, each: mutate → run → observe FAIL exit 1 → revert → re-run → observe PASS exit 0. (a) Missing-keys direction: temporarily add a probe swap key (any unused name under the changelog namespace) to one chrome node in geohist/changelog.html WITHOUT touching the dictionaries → node scripts/i18n-keycheck.mjs must FAIL with missing keys and exit 1 → revert the probe attribute. (b) Extra-keys direction: temporarily remove the join('geohist', 'changelog.html') entry from the keycheck pages array → node scripts/i18n-keycheck.mjs must FAIL with extra keys and exit 1 (dictionaries now exceed the union) → restore the line. (c) Link direction: temporarily point one nav Changelog anchor at a nonexistent path → npm run validate:links must FAIL → revert. Additionally confirm validate:html covers the new page purely via its existing geohist/*.html glob (no script edit needed). Record each cycle (mutation, command, observed output line, exit code) in .planning/phases/06-changelog-page/red-gate-proof.md, then finish with a full green run: node scripts/i18n-keycheck.mjs && npm run validate (html + links + i18n). Commit the proof file only.
  </action>
  <read_first>
scripts/i18n-keycheck.mjs (pages array + failure branches: missing-keys report ~line 100, extra-keys report ~line 101)
package.json (validate / validate:html / validate:links script definitions)
geohist/changelog.html (current keyed state from Task 2)
.planning/phases/06-changelog-page/06-RESEARCH.md (Pattern 3 procedures)
  </read_first>
  <verify>
    <automated>node scripts/i18n-keycheck.mjs && npm run validate</automated>
  </verify>
  <acceptance_criteria>
    - .planning/phases/06-changelog-page/red-gate-proof.md documents all three cycles with the observed FAIL output lines and exit code 1, plus the restored PASS runs (exit 0)
    - The missing-keys mutation produced a FAIL naming the probe key as missing; the pages-array removal produced a FAIL reporting extra keys; the broken href produced a validate:links failure
    - After reverts: git status shows no leftover mutations to geohist/changelog.html or scripts/i18n-keycheck.mjs (probe key and broken href absent)
    - Final full battery: node scripts/i18n-keycheck.mjs exits 0 AND npm run validate exits 0
  </acceptance_criteria>
  <done>Phase success criterion 4 is proven: a dictionary missing changelog keys fails (missing direction), an unregistered page fails (extra direction), and a dead nav link fails link validation — each demonstrated red locally then reverted to green.</done>
</task>

</tasks>

<artifacts_section>
## Artifacts this phase produces

- geohist/changelog.html — NEW keyed-chrome page (CONT-06); ~150 lines copied-frame + timeline entries
- css/base.css — new .changelog-entry block (card-pattern addition, decoration-only)
- js/i18n/es.json, js/i18n/pt-BR.json — ~23 new keys: 17 changelog.* + 3 per-page nav + 3 per-page footer
- scripts/i18n-keycheck.mjs — one-line pages array registration (line 22)
- sitemap.xml — one new <url> entry
- scripts/smoke-check.sh — one new URL line
- .planning/phases/06-changelog-page/red-gate-proof.md — CI proof record
- No new JS functions, no new scripts, no CLI flags, no package installs (zero new dependencies)
</artifacts_section>

<flagged_assumptions>
## Flagged Assumptions

- **CONT-06 edge-probe row stayed UNCLASSIFIED (unresolved)** — reviewed manually: the page's edge-relevant shapes are covered by the adjacency and ordering truths and the backstop content truth authored in this plan's must_haves; residual risk is limited to cosmetic copy choices, which the end-of-phase UAT owner review covers. Not silently dropped — carried into Plan 06-02's flagged assumptions for the content work.
- Research Open Question 1 resolved by planner: per-entry category subheads (Added/Changed/Fixed) are EN-unkeyed — they sit inside entries, which are the documented i18n exception; keeps the estimate at ~23 keys (A1).
- Research Open Question 3 resolved by planner: privacy.html gets a nav-only plain unkeyed link; its minimal footer stays as-is (A3).
- D-03 owner review of backfilled entries happens at end-of-phase UAT (human_verify_mode=end-of-phase); the controlled deploy push must wait until that review passes — Plan 06-02 carries the entries.
</flagged_assumptions>

<verification>
- node scripts/i18n-keycheck.mjs → PASS for es.json and pt-BR.json (exact set-equality, grown surface)
- npm run validate (html + links + i18n) exits 0
- /geohist/changelog.html renders one real git-verified entry (0.88 — 2026-09-04) in KaC timeline form
- Nav/footer reachability from all 4 geohist pages; sitemap + smoke-check enumerate the URL
- Red-gate-proof.md shows both keycheck failure directions observed then reverted
</verification>

<success_criteria>
- All 3 tasks complete, each commit green under the validate battery
- Key-surface atomicity held: markup keys and dictionary keys moved in one commit
- No fabricated dates: the shipped entry's date matches the app repo's git-mined 0.88 date
</success_criteria>

<output>
Create `.planning/phases/06-changelog-page/06-01-SUMMARY.md` when done
</output>
