---
phase: 06-changelog-page
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - geohist/changelog.html
  - .planning/phases/06-changelog-page/backfill-draft.md
autonomous: true
requirements:
  - CONT-06

estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Page carries 4-6 curated milestone entries telling the 0.x development arc (first playable -> modes complete -> Play Games wired -> Play release candidate 0.88) — never empty, never a every-version-bump dump, never wave paragraphs (per D-01/D-02)"
    - "Every entry's ISO date equals the git-mined date of the versionName-touching commit in the app repo — e.g. 0.88 -> 2026-09-04 (commit 2cefab4); no entry date is invented, no entry carries a pre-0.x scheme version (8.0/9.0/10.0 era excluded, per research Pitfalls 2+3)"
    - "First entry on the page is the newest/highest version (0.88 today); entry dates are non-increasing top-to-bottom; equal-date entries order by descending version number"
    - "Each entry uses player-facing plain language (guide.html tone), Added/Changed/Fixed subheads only with empty categories omitted, and 1-4 short bullets per shown category (per D-05/D-06/D-07)"
    - "Pre-launch entries keep real 0.x version numbers with ISO dates; no semantic labels like beta/rc, no undated dev notes, and no claimed Play launch entry (that is prepended later per D-04)"
  artifacts:
    - path: ".planning/phases/06-changelog-page/backfill-draft.md"
      provides: "Owner-reviewable draft: 4-6 curated entries with version, git-mined ISO date, milestone arc, Added/Changed/Fixed bullets"
    - path: "geohist/changelog.html"
      provides: "Full curated 0.x backfill replacing the provisional single entry (entries region: 4-6 <article class=changelog-entry> rows)"
      contains: "changelog-entry"
  key_links:
    - from: "app repo C:/Users/Familia/antigravity/GeoHist-Trivia (git log -p -- app/build.gradle.kts)"
      to: "backfill-draft.md entry dates"
      via: "versionName -> commit date pairs mined from history"
      pattern: "versionName"
    - from: "backfill-draft.md + owner UAT review"
      to: "geohist/changelog.html entries region"
      via: "approved draft entries shipped as the timeline list"
      pattern: "changelog-entry"

must_haves_note: "Prohibitions and the CONT-06 unclassified flagged assumption are co-authored with Plan 06-01 (phase-uniform); they bind here operationally — P1 and P2 concentrate in this plan's content work."

prohibitions:
  - statement: "MUST NOT invent or misdate versions: no fabricated version numbers, no dates that do not match a versionName-touching commit in the app repo git history, no undated entries (D-08 honesty — players will later cross-check against Play release history)"
  - statement: "Entries must NOT become a commit-log dump or terse technical build log: curated milestones in player-facing plain language only (KaC anti-pattern + D-02/D-05) — no wave numbers, no build jargon, no internal codenames"
  - statement: "Changelog entry content must stay unkeyed EN (documented i18n exception): no swap keys on entries/subheads/bullets and no translated-entry mechanism (x20 translation is out of scope per REQUIREMENTS.md)"
---

<objective>
Complete CONT-06's content half: mine the app repo's real version-date timeline, draft the 4-6 curated milestone backfill (0.x development arc, newest-first), and ship it into the changelog page's entries region — with the owner reviewing the drafted entries via end-of-phase UAT before any public deploy.

Purpose: D-01/D-02/D-03 demand the page never looks empty and backfills the 0.x arc from real history, owner-reviewed. Plan 06-01 shipped the page with one git-verified entry (0.88); this plan grows it to the full curated arc using git-derived dates only.
Output: backfill-draft.md (curated arc, 4-6 entries) + geohist/changelog.html carrying the full entry list; validate battery green.
</objective>

<execution_context>
@C:/Users/Familia/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/Familia/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-changelog-page/06-CONTEXT.md
@.planning/phases/06-changelog-page/06-RESEARCH.md
@.planning/phases/06-changelog-page/06-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Mine app-repo version timeline + draft curated 0.x milestone entries</name>
  <files>.planning/phases/06-changelog-page/backfill-draft.md</files>
  <precondition>App repo exists at C:/Users/Familia/antigravity/GeoHist-Trivia with readable git history (read-only check: the mining command below returns commits; if it returns nothing or the path is missing, halt and report instead of inventing dates)</precondition>
  <action>
Mine the authoritative version-date timeline from the app repo (research-verified path): run git -C C:/Users/Familia/antigravity/GeoHist-Trivia log --date=short --format="COMMIT %h %ad" -p -- app/build.gradle.kts and extract every versionName -> commit-date pair. Discard the pre-scheme noise: versionNames 8.0/9.0/10.0 predate the 0.x scheme (the switch to 0.2 happened 2026-08-24) — backfill never lists them (Pitfall 3). Cross-check the arc wording against the app repo's .planning/ milestone audits and the CONTEXT-suggested arc: first playable build -> game modes complete -> Play Games Services/leaderboards wired -> Play release candidate 0.88 (date 2026-09-04, commit 2cefab4 — re-verify from the mined log, never trust the CONTEXT's example date 2026-08-28). Curate 4-6 entries per D-02 (milestones that tell the development arc, NOT every bump), each with: real version number, git-mined ISO date, player-facing Added/Changed/Fixed bullets (1-4 per shown category, empty categories omitted per D-06) written in the guide.html plain tone per D-05 (e.g. "New: Art Detective mode — recognize legendary figures in paintings" style, not build-log jargon). Write the full draft to .planning/phases/06-changelog-page/backfill-draft.md with one section per entry, version-first header format matching the page's existing entry markup (0.88 first, then older milestones descending). Keep the 0.88 wording from Plan 06-01 as a starting point and refine it here. Do NOT create a CHANGELOG.md in the site or app repo — the page itself is the changelog (KaC is a content convention here, not a file convention).
  </action>
  <read_first>
.planning/phases/06-changelog-page/06-CONTEXT.md (decisions D-01..D-08 — entry voice, depth, honesty rules)
.planning/phases/06-changelog-page/06-RESEARCH.md (Common Pitfalls 2-3, Assumptions A5, Environment Availability)
geohist/changelog.html (current entries region — tone + markup the draft must match)
App repo mining: git -C C:/Users/Familia/antigravity/GeoHist-Trivia log --date=short --format="COMMIT %h %ad" -p -- app/build.gradle.kts
App repo arc source: list C:/Users/Familia/antigravity/GeoHist-Trivia/.planning/ (milestone audit files) and read the relevant ones for feature-arc wording
  </read_first>
  <verify>
    <automated>$d = '.planning/phases/06-changelog-page/backfill-draft.md'; (Test-Path $d) -and ((Select-String -Path $d -Pattern '^## ' -AllMatches).Matches.Count -ge 4) -and ((Select-String -Path $d -Pattern '2026-09-04' -AllMatches).Matches.Count -ge 1)</automated>
  </verify>
  <acceptance_criteria>
    - backfill-draft.md exists with 4-6 entry sections (markdown ## headers), each carrying version + ISO date + at least one category with 1-4 bullets
    - The first/highest entry is 0.88 dated 2026-09-04; every date in the draft equals a commit date that touched app/build.gradle.kts's versionName in the app repo (spot-checkable with the mining command)
    - No entry version comes from the pre-0.x scheme (no 8.0 / 9.0 / 10.0 headers) and every entry has a date (no undated dev notes)
    - Bullet copy is player-facing plain language; no commit hashes, no wave references, no technical jargon in entry text
  </acceptance_criteria>
  <done>Curated 4-6-entry draft exists with git-verified dates, ready for owner review and shipping.</done>
</task>

<task type="auto">
  <name>Task 2: Ship curated backfill into changelog.html + phase-closing validate</name>
  <files>geohist/changelog.html</files>
  <action>
Replace the provisional single 0.88 entry in geohist/changelog.html with the full curated arc from backfill-draft.md, preserving the page's established entry markup exactly: article.changelog-entry > h2 with version-first header and <time datetime="..."> element, h3 subheads (Added/Changed/Fixed only), ul/li bullets, 0.88 at top, dates non-increasing, version-descending tie-break for equal dates. Entries remain key-free EN content — no swap keys inside entries (see the plan's prohibitions block). Reuse Plan 06-01's entry-row CSS unchanged (content edit only, per D-01 reversibility note). Do not touch dictionaries, keycheck, nav/footer, or sitemap — key surface and reachability are already final from Plan 06-01. After shipping, run the full battery. Owner review rides the end-of-phase UAT (human_verify_mode=end-of-phase): the entries are reviewed in the phase's consolidated UAT before the orchestrator's controlled deploy push — no public push before that review passes.
  </action>
  <read_first>
geohist/changelog.html (current state — provisional entry + keyed chrome)
.planning/phases/06-changelog-page/backfill-draft.md (the approved-arc source content)
.planning/phases/06-changelog-page/06-01-SUMMARY.md (what Plan 06-01 shipped and any notes)
  </read_first>
  <verify>
    <automated>node scripts/i18n-keycheck.mjs && npm run validate</automated>
    <human-check>
Owner content review (end-of-phase UAT, per D-03): open geohist/changelog.html and confirm —
1. The 4-6 backfilled milestones tell the real development arc and read as player-facing plain language (no build-log jargon)
2. Every version+date is honest (owner may cross-check against the app repo history)
3. Tone, depth (1-4 bullets/category), and the 0.88-at-top ordering are acceptable; intro line wording is acceptable (owner veto per CONTEXT discretion)
Resume-signal: type "approved" or describe entry edits; a controlled deploy push happens only after this review passes
    </human-check>
  </verify>
  <acceptance_criteria>
    - geohist/changelog.html entries region contains 4-6 <article class="changelog-entry"> rows, first one headed 0.88 with <time datetime="2026-09-04">
    - Every entry row has at least one shown category with 1-4 bullets; subheads limited to Added/Changed/Fixed; empty categories omitted
    - No swap keys were added inside any entry article (key surface unchanged from Plan 06-01 — gate passes at the ~169 surface)
    - node scripts/i18n-keycheck.mjs exits 0 AND npm run validate exits 0 after the entries land
    - Entry text in the page matches the owner-review draft (post-UAT edits aside); the provisional-entry-only state is gone
  </acceptance_criteria>
  <done>Full curated 0.x backfill lives on the page (D-01 satisfied — page never looks empty, never a placeholder), all local gates green, owner review queued into end-of-phase UAT before public deploy.</done>
</task>

</tasks>

<artifacts_section>
## Artifacts this phase produces

- .planning/phases/06-changelog-page/backfill-draft.md — NEW planning/content artifact (owner-review source)
- geohist/changelog.html — entries region grows from 1 provisional entry to the full 4-6 curated arc (content edit only; zero key-surface change)
- No new files in the site tree, no new CSS, no dictionary edits, no script edits
</artifacts_section>

<flagged_assumptions>
## Flagged Assumptions

- **CONT-06 edge-probe row stayed UNCLASSIFIED (unresolved)** — carried from Plan 06-01; manual review found no additional edge shape beyond the adjacency/ordering/backstop truths already authored; the owner UAT review is the catch-all for content-edge judgment calls.
- A5: milestone arc wording cross-checks the app repo's .planning/ milestone audits — if those audits contradict git-mined dates, git dates win and the discrepancy is noted in the draft for owner review (per D-03).
- If the app repo git history is unavailable at execution time, the task halts at its precondition rather than inventing dates — owner supplies dates manually as the documented fallback.
</flagged_assumptions>

<verification>
- Page carries 4-6 curated EN entries, newest-first, ISO dates, Added/Changed/Fixed subheads, player voice
- Every date git-verifiable against the app repo (0.88 -> 2026-09-04)
- node scripts/i18n-keycheck.mjs exits 0 AND npm run validate exits 0 (key surface untouched by content edits)
- Owner entry review embedded as end-of-phase UAT human-check; deploy push gated on it
</verification>

<success_criteria>
- Both tasks complete; draft and shipped entries satisfy D-01 through D-08
- No fabricated or undated entries; no pre-0.x scheme versions; no translated-entry mechanism
- Phase success criteria 1 (page + KaC format) fully met after this plan
</success_criteria>

<output>
Create `.planning/phases/06-changelog-page/06-02-SUMMARY.md` when done
</output>
