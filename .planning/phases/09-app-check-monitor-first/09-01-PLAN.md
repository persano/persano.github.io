---
phase: 09-app-check-monitor-first
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - js/contact.js
  - js/firebase-config.js
  - js/consent.js
  - geohist/contact.html
  - js/i18n/ar.json
  - js/i18n/bn.json
  - js/i18n/de.json
  - js/i18n/el.json
  - js/i18n/es.json
  - js/i18n/fr.json
  - js/i18n/hi.json
  - js/i18n/id.json
  - js/i18n/it.json
  - js/i18n/ja.json
  - js/i18n/ko.json
  - js/i18n/nl.json
  - js/i18n/pl.json
  - js/i18n/pt-BR.json
  - js/i18n/ru.json
  - js/i18n/tr.json
  - js/i18n/ur.json
  - js/i18n/vi.json
  - js/i18n/zh.json
autonomous: true
requirements: [FIRE-07, FIRE-08]

estimate:
  tokens: 70000
  raw_tokens: 45000
  tasks: 2
  confidence: med

must_haves:
  truths:
    - "Submitting the contact form succeeds exactly as before while App Check runs in monitoring mode — success status, form reset, message lands in Firestore; zero user-visible change on the happy path (FIRE-07, SC1)."
    - "App Check initializes lazily in contact.js's submit path as the 4th pinned 12.18.0 CDN module, after the default app and before auth/firestore; served HTML carries zero attestation-script bytes (FIRE-07)."
    - "A token-fetch failure (appCheck/* family, or post-enforcement permission-denied) shows the contact.status.appcheck keyed status with the email fallback — never a dead form, never auto-retry (D-06, D-07; FIRE-08)."
    - "An appcheck-family failure dispatches a persano:appcheck document event; consent.js routes it to logEventSafe('appcheck_token_failure', {code}) — consent-gated, silent no-op on deny (FIRE-08)."
    - "Resubmit in one session reuses the cached App Check instance — no second initializeAppCheck call possible (idempotency probe resolved)."
    - "Double/parallel submit remains impossible while a submission is in flight; the submit button re-enables in .finally (concurrency probe resolved)."
    - "All 19 JSON dictionaries + markup carry contact.status.appcheck at exact 171-key parity; ja/zh values omit the raw email (CJK punctuation gate) — keycheck green."
  artifacts:
    - "js/contact.js: 4th lazy import (firebase-app-check.js), module-level appCheckInstance cache-once guard, getToken gate, case-tolerant appcheck catch mapping, persano:appcheck dispatch"
    - "js/firebase-config.js: recaptchaSiteKey field (empty string = App Check dormant until owner activation)"
    - "js/consent.js: persano:appcheck listener in wireEvents() emitting appcheck_token_failure via logEventSafe"
    - "geohist/contact.html: pre-authored hidden status node data-status=appcheck / data-i18n=contact.status.appcheck (Pattern 5 shape, plain text)"
    - "js/i18n/*.json (19 files): contact.status.appcheck value in every dictionary, atomic with the markup node"
  key_links:
    - "contact.js getToken gate rejection -> catch mapping -> showStatus('appcheck') -> pre-authored contact.html node (break here = dead form)"
    - "contact.js dispatch persano:appcheck -> consent.js listener -> logEventSafe (break here = silent metric loss, form still fine)"
    - "contact.html data-status=appcheck -> existing statusEls walk in init() (break here = node unreachable, no engine change needed)"
    - "js/firebase-config.js recaptchaSiteKey -> ReCaptchaV3Provider construction (empty key = App Check dormant, legacy path)"
  prohibitions:
    - statement: "No attestation-script URL in any served HTML page — the SDK injects it at submit-time init only (submit-time-only contract)."
      status: enforced
      verification: "grep -i 'google.com/recaptcha' across index.html 404.html geohist/*.html returns 0 matches"
    - statement: "No debug-provider flag or debug token committed anywhere in the repo tree (research Anti-Pattern 5 — tree is publicly served)."
      status: enforced
      verification: "grep for the FIREBASE_APPCHECK_DEBUG_TOKEN literal across js/*.js returns 0 matches"
    - statement: "firebase/firestore.rules stays byte-identical — the token rides request headers via the SDK, never the document payload."
      status: enforced
      verification: "git diff --name-only HEAD -- firebase/firestore.rules is empty after both tasks"
    - statement: "No App Check code in consent.js or any page-load path — only the document-event listener crosses the fork (locked fork split)."
      status: enforced
      verification: "js/consent.js contains no initializeAppCheck call and no firebase-app-check import"
    - statement: "No auto-retry of the token fetch — the first failure shows the appcheck status immediately; the visitor resends manually (D-07)."
      status: enforced
      verification: "contact.js submit catch path contains no retry loop or repeated getToken call"
    - statement: "No enforcement logic client-side — the client attests only; products stay un-enforced console-side until the owner flip (FIRE-10, future)."
      status: enforced
      verification: "no enforcement/console API call exists in any js file; enforcement is documented only in the phase runbook (09-02)"
    - statement: "ja.json and zh.json values for contact.status.appcheck contain no raw email address (CJK half-width punctuation gate would fail)."
      status: enforced
      verification: "node scripts/i18n-keycheck.mjs exits 0 with every dictionary at exact 171-key parity"
---

<assumption_delta_decision>
noun: contact-recovery channel (the email fallback named by the pluralization signal)
decision: no-change
rationale: statusEls is already a keyed multi-variant map by design (sending/success/error/invalid-*), and the email fallback pattern already exists verbatim in contact.status.error — adding one more keyed variant changes no identity model, primary key, or anchor. No promote/add-alongside debt. Invariant already encoded by the keycheck gate + acceptance criteria below; no extra test needed.
</assumption_delta_decision>

<objective>
Wire Firebase App Check (reCAPTCHA v3 provider, D-01) into the contact form's submit path as the 4th lazy CDN module — monitoring mode, zero user-visible change (FIRE-07) — with the token-failure UX: dedicated `contact.status.appcheck` status node + email fallback (D-06) and the consent-gated Analytics token-failure event (FIRE-08).

Purpose: monitoring-mode metrics need a live token seam; without the explicit `getToken()` gate the SDK swallows failures in monitoring mode and neither the status nor the metric could ever fire (research Pattern 1/2 — the load-bearing finding).

Output: 23 files in one coherent change — i18n key #171 surface move (markup + 19 dictionaries, atomic per D-08) plus contact.js/firebase-config.js/consent.js wiring. Both intermediate commits stay green under `npm run validate`.

Task order note: the tracer (Task 2) is deliberately second. The i18n keycheck exact set-equality gate forbids splitting the markup node from the 19 dictionaries across commits (D-08 atomicity), and wiring the catch path before the node exists would leave a commit where a token failure hides every status. Surface move first, tracer second — every commit green.
</objective>

<execution_context>
@C:/Users/Familia/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/Familia/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-app-check-monitor-first/09-CONTEXT.md
@.planning/phases/09-app-check-monitor-first/09-RESEARCH.md
@js/contact.js
@js/firebase-config.js
@js/consent.js
@geohist/contact.html
@scripts/i18n-keycheck.mjs
</context>

<artifacts_this_phase_produces>
Symbols and files this phase creates (source-grounding exclusion list for drift verification):
- `window.persanoFirebaseConfig.recaptchaSiteKey` — new config field in js/firebase-config.js (empty string at ship; owner activation fills it)
- `appCheckInstance` — new module-level variable in js/contact.js (cache-once guard)
- 4th entry in contact.js loadModules() import array: `firebase-app-check.js` under namespace `appCheck` (modules = { app, auth, firestore, appCheck })
- `persano:appcheck` — new CustomEvent name dispatched from contact.js, listened for in consent.js wireEvents()
- `appcheck_token_failure` — new Analytics event name (params: `code`, string, ≤40 chars)
- `contact.status.appcheck` — new i18n key #171 in all 19 js/i18n/*.json dictionaries + one new hidden `<p data-status="appcheck">` node in geohist/contact.html
- No new files; no new npm dependencies; no workflow edits; firebase/firestore.rules untouched
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: i18n key #171 surface move — contact.status.appcheck node + 19 dictionaries, one atomic commit (D-08)</name>
  <files>geohist/contact.html, js/i18n/ar.json, js/i18n/bn.json, js/i18n/de.json, js/i18n/el.json, js/i18n/es.json, js/i18n/fr.json, js/i18n/hi.json, js/i18n/id.json, js/i18n/it.json, js/i18n/ja.json, js/i18n/ko.json, js/i18n/nl.json, js/i18n/pl.json, js/i18n/pt-BR.json, js/i18n/ru.json, js/i18n/tr.json, js/i18n/ur.json, js/i18n/vi.json, js/i18n/zh.json</files>
  <read_first>
    - geohist/contact.html — lines 68–73: the exact Pattern-5 status node shape to mirror (class, role, aria-live, data-status, data-i18n, hidden attribute); note the insertion neighborhood (error node line 70, invalid-email line 71)
    - js/i18n/es.json and js/i18n/ja.json and js/i18n/zh.json — the existing `contact.status.error` values: the per-language email strategy to mirror (es carries the raw email; ja/zh omit it — the CJK gate precedent), plus each dictionary's phrasing register
    - scripts/i18n-keycheck.mjs — the gate semantics: exact set equality, empty-value rejection, CJK half-width punctuation scoped to ja.json + zh.json only (ASCII period allowed only between digits)
    - .planning/phases/09-app-check-monitor-first/09-RESEARCH.md — Pitfall 3 (CJK dictionaries cannot carry the raw email), Pitfall 4 (atomicity set is 19 JSON + markup, no en.json exists), D-06 neutral framing rule
    - .planning/phases/09-app-check-monitor-first/09-CONTEXT.md — D-06 (wording) and D-08 (atomic key #171)
  </read_first>
  <action>
    1. In geohist/contact.html insert ONE new status node between the error node (line 70) and the invalid-email node (line 71), mirroring lines 68–73 exactly: same class form-status, role=status, aria-live=polite, hidden attribute; data-status="appcheck"; data-i18n="contact.status.appcheck". Node text (EN default, D-06 wording verbatim): "We couldn't verify this message — please email santiagopostorivo@gmail.com." Plain text only — no child markup, no anchor tag (D-06: email as text, matching contact.status.error).
    2. In each of the 19 js/i18n/*.json dictionaries add key "contact.status.appcheck" (surface moves 170→171). Drafting rules per D-08 + research Pitfall 3: neutral verify framing ("we could not verify this message" register), NO bot/captcha/verification-jargon, no mention of any anti-abuse mechanism. Email strategy per dictionary MIRRORS that dictionary's existing contact.status.error value: where that value carries the raw email, include it; where it omits the email (verified: ja, zh), omit it here too and keep the neutral verify framing with a retry/contact-form pointer. ja/zh values must use full-width punctuation only (the half-width gate fails otherwise). Do not touch any other key.
    3. Make NO JavaScript changes in this task — the node is dormant until Task 2 wires it (keycheck exact-equality forces this markup+data atomicity; D-08).
    4. Run the keycheck gate; it must pass at 171 keys across all 19 dictionaries. Commit as one atomic commit (Phase 6 precedent: surface move lands in one commit).
  </action>
  <verify>
    <automated>node scripts/i18n-keycheck.mjs && npm run validate:html</automated>
  </verify>
  <acceptance_criteria>
    - node scripts/i18n-keycheck.mjs exits 0 and prints, for each of the 19 dictionary files, "exactly covers the 171-key live surface", then "i18n-keycheck: OK"
    - geohist/contact.html contains exactly one data-status="appcheck" node with data-i18n="contact.status.appcheck" and the hidden attribute, shaped like the line 68–73 nodes (class form-status, role=status, aria-live=polite, no child markup)
    - every js/i18n/*.json parses as JSON and contains the key "contact.status.appcheck" with a non-empty string value
    - ja.json and zh.json contact.status.appcheck values contain no "@" character (mirrors their email-less contact.status.error strategy)
    - git status shows exactly 20 files changed in this commit (1 HTML + 19 JSON)
    - no js/*.js file is modified in this commit (node dormant until Task 2)
  </acceptance_criteria>
  <done>Surface is 171 keys at exact parity across markup + 19 dictionaries; gate green; node present but dormant; one atomic commit.</done>
</task>

<task type="tracer">
  <name>Task 2 (tracer): App Check gate end-to-end — 4th lazy module, cache-once init, getToken seam, catch mapping, consent-gated event</name>
  <files>js/contact.js, js/firebase-config.js, js/consent.js</files>
  <read_first>
    - js/contact.js — the whole file: loadModules() import family (lines 92–105), send() chain (107–139), onSubmit catch path (141–177), module-var block (33–41). The tracer slots into these four seams without restructuring anything else
    - js/firebase-config.js — the single global shape + public-by-design header comment to extend
    - js/consent.js — logEventSafe (116–123) and the persano:langchange listener (132–135): the exact bridge pattern to mirror for the token-failure event; note wireEvents() only runs post-grant
    - .planning/phases/09-app-check-monitor-first/09-RESEARCH.md — Pattern 2 (the getToken gate, cache-once guard, "Can be called only once per app"), Pattern 3 (case-tolerant appCheck/* prefix — the runtime literal is camelCase appCheck/, NOT the hyphenated colloquial form; permission-denied attribution under create-only rules), Pattern 4 (the event bridge), Pitfall 1 (SDK swallows token failures in monitoring mode — the gate is the only signal), Pitfall 2 (initializeAppCheck unguarded in send() throws on resubmit), Pitfall 6 (wrong prefix match), Anti-Patterns 1–5
    - .planning/phases/09-app-check-monitor-first/09-CONTEXT.md — D-06/D-07 (status + mapping + no auto-retry), the agent's Discretion block (event mechanics, init-failure mapping, debug-provider guard, site-key handling)
  </read_first>
  <action>
    1. js/firebase-config.js: add field recaptchaSiteKey with the empty string as value (public-by-design like the rest of the config — the counterpart SECRET key lives only in the reCAPTCHA Admin/Firebase App Check consoles, never in the repo; per D-02). Extend the header comment: the site key is empty until the owner completes console registration + activation (09-RUNBOOK), and an empty key keeps App Check dormant so real users see zero change pre-activation.
    2. js/contact.js — loadModules(): add a 4th dynamic import, the pinned 12.18.0 app-check module from the same CDN_BASE, storing it in modules as namespace `appCheck` (modules becomes { app, auth, firestore, appCheck }). Import-once caching already covers re-submits.
    3. js/contact.js — add module-level var appCheckInstance (cache-once guard, mirroring the modules/importing guard shape; research Pitfall 2: same-options idempotence is NOT relied on). In send(), after the existing default-app try/catch and BEFORE getAuth: when window.persanoFirebaseConfig.recaptchaSiteKey is a non-empty string, initialize the App Check instance once via mods.appCheck.initializeAppCheck(app, { provider: new mods.appCheck.ReCaptchaV3Provider(config.recaptchaSiteKey), isTokenAutoRefreshEnabled: false }) and gate the rest of the chain on mods.appCheck.getToken(appCheckInstance, false) — the explicit seam whose rejection is the only observable token failure in monitoring mode (research Pattern 1/2). When the key is empty, skip the App Check block entirely and run the existing auth/firestore chain unchanged (dormant = zero user-visible change pre-activation). Init order stays app → appCheck → getToken → auth → firestore (docs requirement; use-before-activation error exists for the wrong order). The token is NEVER written into the addDoc payload — it auto-attaches via the X-Firebase-AppCheck header (research Anti-Pattern 2).
    4. js/contact.js — onSubmit catch path: BEFORE the generic error branch, map the failure: extract err.code; if it matches the case-tolerant appcheck family prefix regex (research Pattern 3 — hyphen-or-camel tolerant, case-insensitive) OR equals permission-denied (post-enforcement Firestore wall; under the create-only ruleset attribution is unambiguous per research), then: showStatus('appcheck') and dispatch a CustomEvent 'persano:appcheck' on document with detail { code: String(err.code || 'unknown').slice(0, 40) } — no auto-retry, no rethrow (D-07); otherwise the existing generic error branch runs unchanged (keyed generic status + console.error debuggability retained for both branches). Init-time appcheck-family failures (e.g. use-before-activation, recaptcha-error) ride the same mapping — one family, one status (research Open Question 2 resolution).
    5. js/contact.js — update the header comment: four pinned 12.18.0 modules; App Check is the submit-time attestation layer (FIRE-07/FIRE-08), monitoring mode, silent-failure policy preserved (the page never breaks on attestation failure).
    6. js/consent.js — in wireEvents(), add one listener mirroring the persano:langchange shape (lines 132–135): document.addEventListener('persano:appcheck', ...) reading ev.detail.code and calling logEventSafe('appcheck_token_failure', { code: String(detail.code || 'unknown').slice(0, 40) }) (research Pattern 4; GA4-safe name per the shipped play_badge_click/language_switch precedent). NO App Check imports, no init, no token code in consent.js — only the event listener crosses the fork (locked boundary). No debug-provider flag ships in any file (research Anti-Pattern 5); the local-testing flow is owner-side documentation (09-02).
    7. Run the full local validation chain and commit.
  </action>
  <verify>
    <automated>node --check js/contact.js && node --check js/consent.js && node --check js/firebase-config.js && node scripts/i18n-keycheck.mjs && npm run validate:html && npm run validate:domain</automated>
  </verify>
  <acceptance_criteria>
    - js/contact.js contains: a 4th dynamic import of the pinned 12.18.0 app-check CDN module; the module-level appCheckInstance cache-once guard; initializeAppCheck called exactly once per app guarded by appCheckInstance; ReCaptchaV3Provider constructed from the config site key; isTokenAutoRefreshEnabled set to false; an explicit getToken call gating the auth/firestore chain; the case-tolerant appcheck-family prefix regex (hyphen-or-camel, case-insensitive) plus permission-denied in the catch mapping; a CustomEvent dispatch named persano:appcheck with detail.code; no retry of the token fetch in the catch path
    - js/contact.js init order reads: default app init (existing try/catch) → App Check block → getAuth/signInAnonymously → addDoc, with the App Check block skipped when the config site key is empty
    - js/firebase-config.js contains the recaptchaSiteKey field with an empty-string value and an updated header comment stating public-by-design + dormant-until-activation
    - js/consent.js contains exactly one persano:appcheck listener calling logEventSafe with event name appcheck_token_failure and a string code param; consent.js contains no initializeAppCheck, no app-check module import, no debug-provider flag literal
    - grep -i 'google.com/recaptcha' across index.html 404.html geohist/*.html returns 0 matches (zero attestation bytes in served HTML)
    - node scripts/i18n-keycheck.mjs still exits 0 at 171 keys (Task 1 surface intact)
    - npm run validate:html and npm run validate:domain both exit 0
    - git diff --name-only HEAD -- firebase/firestore.rules is empty
  </acceptance_criteria>
  <done>The one submit path runs end-to-end: submit → lazy 4-module load → app init → dormant-or-active App Check init → explicit getToken gate → anonymous auth → addDoc; an appcheck-family failure shows the appcheck status (email fallback text from Task 1) and emits the consent-gated event; monitoring mode keeps every successful submission identical to today. Committed green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| visitor browser → Google reCAPTCHA | SDK-injected attestation script exchanges a 0.0–1.0 score server-side; loads only at submit-time init |
| visitor browser → Firebase (Auth + Firestore) | untrusted form input crosses here; token auto-attaches via X-Firebase-AppCheck header |
| repo tree → public internet | entire tree is publicly served (upload-pages-artifact path '.') — no secret may enter any committed file |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-01 | Spoofing | contact form → Firestore `messages` (forged REST calls with the public config) | high | mitigate | App Check attestation now rides every auth/firestore request; monitoring-mode metrics classify Unknown origin; enforcement wall is the owner console flip (documented 09-02, FIRE-10 later) |
| T-09-02 | Information Disclosure | reCAPTCHA secret key in repo | high | mitigate | secret key never enters the repo — lives only in reCAPTCHA Admin / Firebase App Check registration; runbook states the rule; site key alone is public-by-design (D-02) |
| T-09-03 | Tampering | debug token committed → anyone forges Verified requests | high | mitigate | no debug-provider flag or token ships in any file (anti-pattern 5); owner-local flow documented in 09-RUNBOOK local-testing section |
| T-09-04 | DoS | reCAPTCHA/app-check outage or adblocker blocks attestation → submit fails | medium | mitigate | gate-abort shows the appcheck status with email fallback (D-06/D-07) — recovery path never dead; monitoring mode keeps backend acceptance unchanged |
| T-09-05 | Elevation of Privilege | localhost added to the reCAPTCHA domain allowlist to "fix" local testing | medium | mitigate | forbidden by docs; runbook documents the debug-token flow instead (09-02) |
| T-09-06 | Repudiation | anonymous form submissions lack identity | low | accept | by design (anonymous auth + create-only rules, v1-shipped); messages carry server timestamps; not in this phase's scope |
</threat_model>

## Flagged Assumptions (specless probes + research log — surfaced, not silently dropped)

Probe fallback report surfaced 6 items; all 6 are resolved explicitly here (equality: 6 surfaced = 6 authored):
1. FIRE-07 idempotency ("runs twice on same input") → resolved: cache-once appCheckInstance guard (Task 2 acceptance criterion 1).
2. FIRE-07 concurrency ("interrupted or parallel") → resolved: existing in-flight double-submit guard (submitButton disabled during send, re-enabled in .finally) + import-once promise caching; unchanged by this phase (truth #6).
3. FIRE-08 unclassified → resolved: dedicated status node + consent-gated event (truths #3–#4, Task 1+2).
4. FIRE-09 boundary (min/max ±1) → resolved owner-side: 09-02 runbook states both sides of the 30-submission floor (below → keep monitoring; met + Verified-rate → flip considered). This plan contains no threshold logic by design.
5. FIRE-09 precision (count unit) → resolved: floor counts successful form submissions (user-facing unit, FIRE-09 wording), read alongside the console Verified split — pinned in 09-02 runbook.
6. CMPL-05 unclassified → resolved: privacy.html disclosure (09-02 Task 2).

Research assumptions carried as flagged (never auto-dismissed):
- A1: GA4 custom-event naming rules allow `appcheck_token_failure` — consistent with shipped play_badge_click/language_switch precedent; worst case a one-line rename.
- A2: reCAPTCHA v3 domain allowlist is per-key UI configuration in reCAPTCHA Admin — owner-side; 09-02 phrases it as the UI flow.
- A3: post-enforcement Firestore rejection surfaces as permission-denied — mapping degrades to the appcheck status per D-07 discretion; a misattribution shows a still-accurate verify message and never breaks the form.
- A4: debug-provider guard — resolved by design: no debug code ships; owner flow documented in 09-RUNBOOK.
- A5: App Check token TTL default phrasing — irrelevant to this phase's submit-time instance; noted, no action.

## Edge Coverage Resolution (specless §C lift)

All six fallback-report items are lifted into must_haves.truths as explicit-verifiable statements (truths #5, #6, #3, and the 09-02-resolved boundary/precision items #4/#5 above; CMPL-05 resolved in 09-02 truths). No unresolved rows remain; no row was dropped.

<verification>
- node scripts/i18n-keycheck.mjs → 171-key exact parity, OK, exit 0
- npm run validate:html && npm run validate:domain → exit 0
- node --check on all three modified JS files → exit 0
- grep gates: attestation-script URL absent from every HTML page; debug flag literal absent from js/
- git diff --name-only HEAD -- firebase/firestore.rules → empty
- Post-deploy manual smoke (agent or owner, non-blocking): submit one real message at /geohist/contact.html → success unchanged; with the site key still empty, submit again and confirm zero behavioral change (dormant path); (post-activation, owner-side) block the attestation script in devtools → appcheck status with email fallback appears and the appcheck_token_failure event fires in the Analytics console — the D-07 failure path
</verification>

<success_criteria>
- FIRE-07: initializeAppCheck is the 4th lazy submit-time CDN module in contact.js, initialized before auth/firestore, zero attestation bytes in served HTML, zero user-visible change (monitoring mode, dormant pre-activation); provider decision reCAPTCHA v3 recorded (D-01)
- FIRE-08: contact.status.appcheck node with email fallback shows on appcheck-family failure; consent-gated appcheck_token_failure event fires via the persano:appcheck bridge; no auto-retry (D-06/D-07)
- Key surface 170→171 in one atomic commit across markup + all 19 dictionaries; keycheck green (D-08)
- Firestore rules untouched; fork split untouched (event, not module, crosses the fork)
</success_criteria>

<output>
Create .planning/phases/09-app-check-monitor-first/09-01-SUMMARY.md when done
</output>
