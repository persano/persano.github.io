---
phase: 09-app-check-monitor-first
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/09-app-check-monitor-first/09-RUNBOOK.md
  - geohist/privacy.html
autonomous: true
requirements: [FIRE-09, CMPL-05]

estimate:
  tokens: 35000
  raw_tokens: 22000
  tasks: 2
  confidence: high

must_haves:
  truths:
    - "09-RUNBOOK.md exists (08-RUNBOOK pattern, phase-dir location) and documents the full owner console chain: reCAPTCHA v3 site key + secret key registration with domain allowlist geohisttrivia.com only (D-01/D-02), Firebase App Check app registration providing the secret key, activation (site key into the config field), weekly monitoring ritual (D-04), evidence gate (D-03), per-product flip + rollback (D-05)."
    - "The flip is gated on evidence, never calendar: the runbook names the ready-to-enforce console signal ('almost all recent requests Verified') AND a floor of 30 successful submissions, with the unit pinned to successful form submissions (not console request counts — one submission is 2+ requests) and both sides of the boundary stated (below 30 → keep monitoring) (D-03; FIRE-09)."
    - "The runbook documents the per-product flip as Security → App Check → APIs tab → Enforce for Firestore AND Authentication with instant rollback (toggle off) and ≤15-minute propagation, and explicitly states Firestore replay protection does not exist as an option (docs refutation; Pitfall 5)."
    - "The runbook documents the weekly ritual reads: the Verified / Outdated client / Unknown origin / Invalid category split plus the successful-submission count, and names appcheck_token_failure as the complementary consent-gated failure-rate metric (D-04)."
    - "privacy.html section 3 SDK inventory gains one reCAPTCHA v3 / Firebase App Check (website only) entry carrying the consent-interplay sentence — anti-abuse transport, loads only when the form is submitted, runs regardless of the analytics cookie choice — and the Last updated line is bumped to the ship date (D-09; CMPL-05)."
  artifacts:
    - ".planning/phases/09-app-check-monitor-first/09-RUNBOOK.md — owner console runbook (no secrets, publicly-served-tree notice, status legend)"
    - "geohist/privacy.html — one new li in the section 3 ul + bumped Last updated date"
  key_links:
    - "runbook activation step → js/firebase-config.js recaptchaSiteKey field (empty at ship; owner/agent paste activates App Check)"
    - "runbook weekly ritual → Firebase console App Check APIs tab metrics + Firestore messages count (the evidence surface)"
    - "runbook metric section → appcheck_token_failure Analytics event shipped by 09-01 (complementary failure-rate signal)"
    - "privacy li → consent.js load-gating model (the 'regardless of the analytics choice' claim must match the shipped architecture)"
  prohibitions:
    - statement: "No secret key, debug token, credential, or API-key-shaped string anywhere in 09-RUNBOOK.md (the tree is publicly served)."
      status: enforced
      verification: "grep -E 'AIza|[0-9A-Za-z_-]{30,}'-shaped secret/site-key literals in 09-RUNBOOK.md returns 0 matches; file states the no-secrets rule up front"
    - statement: "No instruction to add localhost (or any non-production host) to the reCAPTCHA domain allowlist — the runbook must prescribe the debug-token flow for local testing instead."
      status: enforced
      verification: "runbook local-testing section names the debug-token flow and contains an explicit never-add-localhost warning"
    - statement: "No replay-protection option documented for Firestore (it does not exist for standard Google services)."
      status: enforced
      verification: "runbook enforcement section contains no Firestore replay-protection step and states its unavailability"
    - statement: "No calendar-based enforcement trigger (dates or elapsed time as flip criteria)."
      status: enforced
      verification: "the only flip criteria named in the runbook are the Verified-rate signal and the 30-submission floor"
    - statement: "The agent never executes the enforcement flip — the runbook documents it as an owner console step (FIRE-10 stays post-monitoring)."
      status: enforced
      verification: "no workflow/script/CI step in the repo performs an enforcement API call; the runbook attributes the flip to the owner"
---

<assumption_delta_decision>
noun: contact-recovery channel (email fallback) — see 09-01 block. Same no-change resolution applies here: the runbook documents the existing keyed-status surface; it introduces no second identity or source of truth.
</assumption_delta_decision>

<objective>
Ship the two documentation surfaces of Phase 9: (1) the owner enforcement/monitoring runbook — reCAPTCHA v3 registration, App Check app registration, activation, weekly ritual, evidence-gated per-product enforcement flip with rollback (FIRE-09, D-02..D-05) — and (2) the privacy-policy disclosure of reCAPTCHA/App Check with its consent interplay (CMPL-05, D-09).

Purpose: the enforcement decision is evidence-gated and owner-executed; the repo documents the ritual and the flip but never executes them (FIRE-10 stays post-monitoring). The privacy line closes the compliance disclosure before the app ships.

Output: 09-RUNBOOK.md (new, phase dir) + privacy.html section 3 amendment. Zero code changes in this plan.

Parallel note: no file overlap with 09-01 (Wave 1 parallel). The runbook references the 09-01 field name recaptchaSiteKey and event name appcheck_token_failure — both are fixed names in this plan set, not execution dependencies.
</objective>

<execution_context>
@C:/Users/Familia/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/Familia/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-app-check-monitor-first/09-CONTEXT.md
@.planning/phases/09-app-check-monitor-first/09-RESEARCH.md
@.planning/phases/08-custom-domain-migration/08-RUNBOOK.md
@geohist/privacy.html
</context>

<artifacts_this_phase_produces>
Symbols and files this phase creates (source-grounding exclusion list for drift verification):
- `.planning/phases/09-app-check-monitor-first/09-RUNBOOK.md` — new file; sections §0 state table, §1 reCAPTCHA registration, §2 App Check app registration, §3 activation, §4 weekly monitoring ritual, §5 evidence gate, §6 enforcement flip + rollback, §7 local testing (debug-token flow), §8 Analytics metric reference
- `geohist/privacy.html` — one new `<li>` in the section 3 SDK inventory `<ul>` (after the Firestore entry) + "Last updated" date bump
- No code symbols, no config fields, no keys, no workflow edits
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: 09-RUNBOOK.md — owner console chain (register → activate → monitor weekly → evidence-gate the flip)</name>
  <files>.planning/phases/09-app-check-monitor-first/09-RUNBOOK.md</files>
  <read_first>
    - .planning/phases/08-custom-domain-migration/08-RUNBOOK.md — the structural pattern to follow: audience line, public-artifact no-secrets notice (the tree is publicly served), status legend (✅/⬜/🔍), numbered § sections, do-not-touch lists, verify probes, explicit failure shapes
    - .planning/phases/09-app-check-monitor-first/09-RESEARCH.md — the console flows to transcribe: registration (site key + secret key at the reCAPTCHA Admin create page; Apps-tab registration providing the secret key), monitor-metrics console path + category names (Verified / Outdated client / Unknown origin / Invalid / Reused token), ready-to-enforce guideline wording, enforcement flip mechanics + ≤15 min propagation + rollback, debug-provider flow (never commit the token, never add localhost to the allowlist), Pitfalls 5/7/8, State of the Art (replay-protection refutation; category names)
    - .planning/phases/09-app-check-monitor-first/09-CONTEXT.md — D-01..D-05 (provider, key domain, gate, ritual, per-product flip), the agent's Discretion block (runbook file shape)
    - .planning/phases/09-app-check-monitor-first/09-01-PLAN.md — the exact field name recaptchaSiteKey and event name appcheck_token_failure the runbook must reference
  </read_first>
  <action>
    Create .planning/phases/09-app-check-monitor-first/09-RUNBOOK.md following the 08-RUNBOOK skeleton. Required sections:
    - Header: audience (owner), public-artifact no-secrets notice (08 pattern verbatim intent), status legend.
    - §0 Current state: App Check unregistered, no site key in config (recaptchaSiteKey empty = dormant), monitoring metrics not accruing until registration AND code ship (09-01); ritual starts from the code-ship date.
    - §1 reCAPTCHA Admin registration (D-01/D-02): create a v3 site key; domain allowlist = geohisttrivia.com ONLY; record the SITE key (public — paste into js/firebase-config.js recaptchaSiteKey via §3 activation) and treat the SECRET key as console-only (Firebase registration in §2 needs it; NEVER paste it into the repo, chat logs, or this file).
    - §2 Firebase console App Check registration: Security → App Check → Apps tab → register the web app with the reCAPTCHA provider, providing the SECRET key from §1.
    - §3 Activation: paste the site key into js/firebase-config.js recaptchaSiteKey (owner does it or hands it to the agent — public value, one line); verify the form still submits with zero visible change; monitoring metrics begin accruing.
    - §4 Weekly monitoring ritual (D-04): once a week — Firebase console → Security → App Check → APIs tab: read the Verified / Outdated client / Unknown origin / Invalid split for Firestore AND Authentication; count successful submissions (Firestore → messages collection, createdAt-sorted); skim the appcheck_token_failure trend (§8). Category semantics table (research Pitfall 8): Unknown origin = the forged/stolen-config spam vector; Outdated client ≈ 0 here (static deploys, no version skew).
    - §5 Evidence gate (D-03): the flip is considered ONLY when BOTH hold — (a) the console's ready-to-enforce signal ("almost all of the recent requests are from verified clients") reads true on the relevant product, AND (b) ≥ 30 successful submissions have accumulated since code ship. Boundary stated both ways: below 30 → keep monitoring (a "100% Verified" reading at 3 submissions is meaningless); floor unit = successful form submissions, NOT console request rows (one submission = 2+ requests). Never calendar-based — no date or elapsed-time trigger exists.
    - §6 Per-product flip + rollback (D-05): Security → App Check → APIs tab → expand product metrics → Enforce → confirm, separately for Firestore and for Authentication (the form's whole chain); takes up to 15 minutes to take effect; rollback = the same toggle off (also ≤15 min propagation). Explicitly: Firestore flip is baseline session-token protection; a replay-protection option does NOT exist for Firestore (standard Google services — Firebase AI Logic only); do not hunt for it. Agent never flips; execution is FIRE-10, post-monitoring.
    - §7 Local testing (debug flow): self.FIREBASE_APPCHECK_DEBUG_TOKEN set before init prints a debug token; safelist it via Apps tab → ⋮ → Manage debug tokens. Two hard warnings (research Anti-Patterns 4–5): never add localhost to the reCAPTCHA domain allowlist; never commit the debug token or ship the flag — this repo's tree is publicly served. Note: in monitoring mode localhost submissions succeed anyway (token fails → un-attested → accepted), which is exactly how to exercise the failure path locally.
    - §8 Analytics metric reference: appcheck_token_failure {code} — consent-gated custom event via the consent.js listener; where to read it (Firebase console → Analytics → Events); interpretation: it measures would-be-blocked token failures (adblockers, outages), the complement of the console Verified split.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const t=fs.readFileSync('.planning/phases/09-app-check-monitor-first/09-RUNBOOK.md','utf8');const need=['geohisttrivia.com','30 successful submissions','Enforce','15 minutes','Reused token','Unknown origin','appcheck_token_failure','recaptchaSiteKey','never commit','Manage debug tokens'];const miss=need.filter(s=>!t.includes(s));if(miss.length){console.error('MISSING: '+miss.join(', '));process.exit(1)}if(/AIza[\\w-]{10,}/.test(t)){console.error('SECRET-SHAPED STRING FOUND');process.exit(1)}console.log('runbook content gate: OK')"</automated>
  </verify>
  <acceptance_criteria>
    - 09-RUNBOOK.md exists in the phase directory and contains all eight § sections in order (§0–§8)
    - the file contains: the ready-to-enforce console wording, the 30-successful-submissions floor with the unit pinned to form submissions, the boundary stated both directions, the per-product Enforce flow for Firestore AND Authentication, the ≤15-minute propagation note, the rollback-as-toggle-off instruction, the current five-category metric names, the debug-token flow, the never-add-localhost and never-commit-token warnings, the replay-protection refutation for Firestore, and the recaptchaSiteKey activation step
    - the file opens with the public-artifact no-secrets notice (08-RUNBOOK pattern) and contains no AIza-prefixed or site-key-shaped literal
    - no date-based or elapsed-time flip criterion appears anywhere in the file
    - the runbook attributes the flip to the owner and names FIRE-10 as the future execution item
  </acceptance_criteria>
  <done>Owner runbook complete: registration → activation → weekly ritual → evidence gate → flip/rollback, all console-side, zero secrets, agent never flips (FIRE-09 fully documented).</done>
</task>

<task type="auto">
  <name>Task 2: privacy.html CMPL-05 — reCAPTCHA/App Check SDK inventory line + consent-interplay sentence</name>
  <files>geohist/privacy.html</files>
  <read_first>
    - geohist/privacy.html — section 3 ul (lines 46–52): the existing entry phrasing to mirror ("website only" parenthetical like the Firestore entry at line 51), the static-EN lang-notice at line 35, the Last updated line 34 to bump
    - .planning/phases/09-app-check-monitor-first/09-RESEARCH.md — the privacy line shape (Code Examples section): li draft with the consent-interplay sentence; CMPL-05 row; D-09 in 09-CONTEXT.md
    - .planning/phases/09-app-check-monitor-first/09-CONTEXT.md — D-09 (agent drafts, owner reviews before ship)
  </read_first>
  <action>
    1. In geohist/privacy.html section 3 ul, append ONE li after the Firestore entry (line 51), following the research draft shape: bold lead "reCAPTCHA v3 / Firebase App Check (website only):" then prose — when you submit the contact form, Google's reCAPTCHA runs in the background to protect the form from abuse; it is an anti-abuse mechanism, not measurement; it runs regardless of your analytics cookie choice, and only when you submit the form. Keep "website only" phrasing and the one-sentence consent interplay exactly in that spirit (D-09). No anchor tags needed; static EN page is the documented i18n exception (do not add data-i18n attributes — privacy.html is scriptless by design).
    2. Bump the Last updated line (line 34) to the actual ship date of this task (e.g. "September 7, 2026" → the commit date), keeping the existing format.
    3. Nothing else changes — no other sections, no nav edits, no dictionary edits (privacy.html carries no keyed surface).
  </action>
  <verify>
    <automated>npm run validate:html && node -e "const fs=require('fs');const t=fs.readFileSync('geohist/privacy.html','utf8');const need=['reCAPTCHA v3','App Check','website only','regardless of your analytics cookie choice'];const miss=need.filter(s=>!t.includes(s));if(miss.length){console.error('MISSING: '+miss.join(', '));process.exit(1)}console.log('privacy gate: OK')"</automated>
  </verify>
  <acceptance_criteria>
    - geohist/privacy.html section 3 ul contains exactly one new li mentioning reCAPTCHA v3, Firebase App Check, "website only", the form-submit trigger, and the analytics-cookie independence sentence
    - the li carries no data-i18n attribute (static EN page, documented exception)
    - the "Last updated" line shows the task's ship date (not September 2, 2026)
    - npm run validate:html exits 0 (privacy.html included in the validate:html glob)
    - git status shows exactly one file changed in this commit: geohist/privacy.html
  </acceptance_criteria>
  <done>Privacy policy discloses reCAPTCHA v3/App Check with the consent interplay (CMPL-05, D-09); page validates; one-file commit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo tree → public internet | the runbook deploys publicly (upload-pages-artifact path '.') — console instructions only, zero secrets |
| owner console → Firebase/reCAPTCHA | owner performs registration + flip; the repo only documents |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-07 | Information Disclosure | 09-RUNBOOK.md leaking the secret key or a debug token | high | mitigate | no-secrets notice up front; site key is the only paste-able value (public-by-design); secret key + debug tokens are console-only; content gate greps for secret-shaped strings |
| T-09-08 | Elevation of Privilege | runbook tempting a localhost allowlist entry | medium | mitigate | local-testing section prescribes the debug-token flow with an explicit never-add-localhost warning |
| T-09-09 | Tampering | fabricated enforcement evidence (calendar flip) | medium | mitigate | evidence gate names both required signals (Verified-rate + 30-submission floor); no date trigger exists in the runbook |
| T-09-10 | Repudiation | privacy disclosure drift vs shipped behavior | low | accept | D-09 wording mirrors the shipped load-gating architecture; owner reviews before ship |
</threat_model>

## Flagged Assumptions (surfaced, not silently dropped)

Carried from 09-RESEARCH.md Assumptions Log (owner-side, never auto-dismissed):
- A1 (GA4 naming for appcheck_token_failure) — referenced by §8; worst case a one-line rename in 09-01.
- A2 (reCAPTCHA domain allowlist is per-key UI configuration) — §1 phrases it as the UI flow; owner confirms at the console.
- A5 (token TTL default phrasing) — §5/§6 avoid quoting an exact TTL default; the 30-min–7-day range is firm per docs.

Probe-fallback items resolved in THIS plan: FIRE-09 boundary (§5, both sides stated), FIRE-09 precision (§5 unit pinned), CMPL-05 (Task 2). The remaining probe items (FIRE-07 idempotency/concurrency, FIRE-08) resolve in 09-01. Equality holds: 6/6 surfaced across the plan set, 0 dropped.

<verification>
- node runbook content gate (Task 1 automated) → OK, exit 0
- npm run validate:html → exit 0 (privacy.html included)
- privacy gate node check (Task 2 automated) → OK, exit 0
- No grep match for secret-shaped strings in 09-RUNBOOK.md
- Post-ship owner flow (non-blocking, owner-side): work §1–§3, then send one real submission — form behaves identically; monitoring metrics begin accruing (ritual §4 starts)
</verification>

<success_criteria>
- FIRE-09: enforcement flip fully documented as a per-product (Firestore + Authentication), reversible owner console step gated on the Verified-rate signal + 30-successful-submissions floor (never calendar), with the weekly ritual and both boundary directions stated; agent never flips (FIRE-10 stays future)
- CMPL-05: privacy policy mentions reCAPTCHA v3/App Check with the consent-interplay nuance (D-09)
- Zero secrets in any committed file; both commits validate green
</success_criteria>

<output>
Create .planning/phases/09-app-check-monitor-first/09-02-SUMMARY.md when done
</output>
