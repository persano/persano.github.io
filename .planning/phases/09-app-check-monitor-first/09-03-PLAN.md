---
phase: 09-app-check-monitor-first
plan: 03
type: execute
wave: 1
depends_on: []
gap_closure: true
gap_ids: [G-09-2]
files_modified:
  - js/contact.js
  - js/firebase-config.js
  - geohist/privacy.html
  - .planning/phases/09-app-check-monitor-first/COVERAGE.md
  - .planning/phases/09-app-check-monitor-first/09-RUNBOOK.md
autonomous: true
requirements: [FIRE-07, FIRE-08]
user_setup:
  - service: reCAPTCHA (GCP-managed Enterprise key)
    why: "Firebase deprecated classic reCAPTCHA for new App Check registrations and the owner registered web-geohist as reCAPTCHA Enterprise (2026-09-08). The owner's existing key was created as classic v3 (admin console shows a Migrate-keys banner); it must be migrated to GCP/Enterprise so the Firebase Enterprise registration points at a valid Enterprise key and token verification succeeds. Site key value is unchanged by the migration."
    dashboard_config:
      - task: "Run the 'Migrate keys' migration in reCAPTCHA Admin (classic v3 key becomes GCP/Enterprise-managed; site key value unchanged) — owner already completed the key creation (2026-09-08) and the Firebase App Check Enterprise registration"
        location: "reCAPTCHA Admin (google.com/recaptcha/admin) -> GeoHist key -> settings -> Migrate keys banner"
estimate:
  tokens: 40000
  raw_tokens: 20000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Post-activation submit on prod succeeds with zero visible change: success status shows, form resets, message lands in the Firestore messages collection (UAT test 1 repeat, post G-09-2 fix)"
    - "Token-failure path on prod is observable: DevTools-blocked attestation request replaces sending with the contact.status.appcheck status, form stays usable, appcheck_token_failure fires in GA4 only with consent granted, and the message still lands in Firestore either way (UAT test 2)"
    - "The shipped attestation provider is the Enterprise provider class exported by the same exact-pinned 12.18.0 firebase-app-check.js gstatic module (export verified in the pinned bundle during planning)"
    - "COVERAGE.md, 09-RUNBOOK.md, and privacy.html reflect Enterprise reality; the D-01 revision (classic provider deprecated by Firebase, owner console-forced to Enterprise on 2026-09-08) is recorded with a date"
  artifacts:
    - "js/contact.js — submit-path App Check init instantiates the Enterprise provider (line ~151)"
    - "js/firebase-config.js — recaptchaSiteKey carries the owner's public site key (activation; dormant gate retired)"
    - ".planning/phases/09-app-check-monitor-first/COVERAGE.md — Enterprise provider row flipped OPT-OUT -> INTEGRATE with deprecation rationale"
    - ".planning/phases/09-app-check-monitor-first/09-RUNBOOK.md — section 0/1/2/3/7 updated to Enterprise reality incl. the Migrate-keys owner step; dated D-01 revision note"
    - "geohist/privacy.html — SDK-inventory li no longer names a stale provider version"
  key_links:
    - "config.recaptchaSiteKey (firebase-config.js) -> Enterprise provider init in the contact.js submit path -> X-Firebase-AppCheck header -> Firebase App Check Enterprise registration verifies the token"
    - "contact.js getToken rejection -> persano:appcheck document event -> consent.js logEventSafe('appcheck_token_failure') — the existing consent-gated bridge, unchanged by this plan"
    - "reCAPTCHA Admin migrated (GCP/Enterprise-managed) key <-> Firebase Enterprise registration <-> site key value unchanged"
---

<objective>
Close gap G-09-2 from 09-UAT.md: Firebase deprecated the classic reCAPTCHA provider for new App Check registrations (console banner: "reCAPTCHA está obsoleto. Usa reCAPTCHA Enterprise en su lugar") and the owner registered web-geohist as reCAPTCHA Enterprise on 2026-09-08. The shipped code instantiates the classic provider — incompatible with that registration, so every post-activation submit would fail token verification — and the feature ships dormant (`recaptchaSiteKey: ''`), so activation never happened.

This plan swaps the provider to `ReCaptchaEnterpriseProvider` (export verified in the pinned 12.18.0 bundle during planning), activates the site key, updates COVERAGE.md / 09-RUNBOOK.md / privacy.html to Enterprise reality (recording the D-01 revision), and deploys so UAT tests 1 and 2 can be re-run on prod.

Purpose: make the shipped code match the Enterprise registration Firebase now forces, so post-activation submits pass token verification and the D-06/FIRE-08 failure UX becomes exercisable.
Output: 5 modified files, deploy green on prod, owner re-verification checklist ready.
</objective>

<execution_context>
@C:/Users/Familia/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/Familia/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-app-check-monitor-first/09-UAT.md
@.planning/phases/09-app-check-monitor-first/09-RUNBOOK.md
@.planning/phases/09-app-check-monitor-first/COVERAGE.md
@js/contact.js
@js/firebase-config.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enterprise provider swap + site-key activation (G-09-2 code fix)</name>
  <files>js/contact.js, js/firebase-config.js</files>
  <action>
In js/contact.js line 151, replace the classic v3 provider constructor with `new mods.appCheck.ReCaptchaEnterpriseProvider(config.recaptchaSiteKey)`. The class is exported by the same `firebase-app-check.js` gstatic 12.18.0 module already imported as the 4th lazy module — export verified this session against the pinned bundle (`https://www.gstatic.com/firebasejs/12.18.0/firebase-app-check.js` export statement contains the Enterprise provider name). No other contact.js logic changes: the dormant gate, `appCheckInstance` cache-once guard, explicit `getToken(appCheck, false)` seam, case-tolerant appcheck-family catch mapping, event dispatch, and no-auto-retry policy are all untouched (they passed UAT tests 1/3). The contact.js header comment needs no edit — it never names the provider flavor.

In js/firebase-config.js, set `recaptchaSiteKey: '6LfYjbAtAAAAABgPLG-4SuJJ9lggRWO-ZJxxOEPF'` (owner-provided public site key — public-by-design, safe to commit, per the runbook's public-artifact notice; the SECRET key never touches the repo). Update the header comment sentence that currently says the key stays empty until console registration/activation: it is now active as of 2026-09-08 (G-09-2 gap closure, plan 09-03), still public-by-design, secret key still console-only. Keep the existing comment style — no new comment blocks, no code comments beyond that one header edit. Do not touch any other config field.
  </action>
  <verify>
    <automated>node --check js/contact.js && node --check js/firebase-config.js && rg -c "ReCaptchaEnterpriseProvider" js/contact.js | grep -qx 1 && rg -c "6LfYjbAtAAAAABgPLG-4SuJJ9lggRWO-ZJxxOEPF" js/firebase-config.js | grep -qx 1 && (rg -q "ReCaptchaV3Provider" js/ && echo FAIL-classic-provider-still-present || echo OK) && npm run validate:html && npm run validate:domain && npm run validate:i18n</automated>
  </verify>
  <done>contact.js instantiates the Enterprise provider exactly once and nothing else in js/ references the classic provider class; firebase-config.js carries the active public site key; syntax check + validate:html + validate:domain + validate:i18n all exit 0.</done>
</task>

<task type="auto">
  <name>Task 2: Docs to Enterprise reality — COVERAGE.md flip + runbook rewrite + privacy label (D-01 revision record)</name>
  <files>.planning/phases/09-app-check-monitor-first/COVERAGE.md, .planning/phases/09-app-check-monitor-first/09-RUNBOOK.md, geohist/privacy.html</files>
  <action>
COVERAGE.md: (1) header note — replace the "reCAPTCHA v3 attestation provider (D-01)" phrasing with reCAPTCHA Enterprise; (2) flip the `ReCaptchaEnterpriseProvider` row from OPT-OUT to INTEGRATE with reason: Firebase deprecated the classic reCAPTCHA provider for new App Check registrations (console banner, ES), owner registered web-geohist as reCAPTCHA Enterprise 2026-09-08, tokens from the classic provider cannot verify against an Enterprise registration — D-01 revised 2026-09-08. (3) the first row's capability text still names the classic provider class as shipped in 09-01 — leave that row's decision as INTEGRATE but append "(superseded 2026-09-08: provider class swapped to Enterprise, see ReCaptchaEnterpriseProvider row)" so the matrix stays truthful about what 09-01 shipped vs what is live now.

09-RUNBOOK.md (public artifact — console instructions only, no secrets, no tokens; the public-artifact notice stands): (1) add a dated revision note near the top: "Revised 2026-09-08 (G-09-2 / plan 09-03): Firebase deprecated the classic reCAPTCHA provider for new App Check registrations; the owner registered web-geohist as reCAPTCHA Enterprise. D-01 (classic v3, Enterprise then rejected) is revised accordingly; the code now ships the Enterprise provider." — phrasing the old decision without quoting its exact rationale line. (2) §0 table: rows 2–4 statuses -> done (v3 key pair created 2026-09-08; Enterprise registration completed 2026-09-08; site key in code shipped by 09-03), row 5 note updated to "accrues once 09-03 deploys". (3) §1: retitle and rewrite as a completed record — key created 2026-09-08 with domain list `geohisttrivia.com` only (still true) — and ADD the owner step that remains: the key was created as classic v3 and the reCAPTCHA admin shows a "Migrate keys" banner; the owner must run that migration so the key becomes GCP/Enterprise-managed, which is what makes the Firebase Enterprise registration's token verification succeed; the site key value is unchanged by the migration. (4) §2: record the registration as done, noting the console's current flow registers the app with the reCAPTCHA Enterprise provider. (5) §3: record activation as done — site key pasted into `recaptchaSiteKey` by 09-03 (2026-09-08); keep the one-real-submission expectation sentence (zero visible change, monitoring mode). (6) §7: any v3-specific wording updated to Enterprise; the debug-token flow itself, the two hard warnings (never localhost in the domain allowlist, never commit/ship token or flag), and the monitoring-mode side effect are unchanged — keep them verbatim.

geohist/privacy.html line 52: change the bold lead "reCAPTCHA v3 / Firebase App Check (website only):" to "reCAPTCHA / Firebase App Check (website only):" — drop only the stale version label so the disclosure matches the shipped provider; the prose sentence and the consent-interplay sentence stay byte-identical (static EN page, scriptless exception, no data-i18n). This closes the T-09-10 disclosure-drift risk the 09-02 threat register accepted while the wording mirrored shipped behavior.
  </action>
  <verify>
    <automated>(rg -n "ReCaptchaEnterpriseProvider" .planning/phases/09-app-check-monitor-first/COVERAGE.md | rg -q "INTEGRATE" && echo OK-coverage) && (rg -q "Migrate keys" .planning/phases/09-app-check-monitor-first/09-RUNBOOK.md && echo OK-runbook-migrate) && (rg -q "Enterprise rejected" .planning/phases/09-app-check-monitor-first/COVERAGE.md .planning/phases/09-app-check-monitor-first/09-RUNBOOK.md && echo FAIL-stale-rationale || echo OK-no-stale-rationale) && (rg -n "reCAPTCHA v3" geohist/privacy.html || echo OK-privacy-label) && npm run validate:html</automated>
  </verify>
  <done>COVERAGE.md lists the Enterprise provider as INTEGRATE with the deprecation rationale and a dated D-01 revision; the runbook documents the completed console steps plus the remaining Migrate-keys owner step with the two hard warnings intact; privacy.html no longer names a provider version; validate:html exit 0.</done>
</task>

<task type="auto">
  <name>Task 3: Deploy and hand off prod re-verification (UAT tests 1 + 2 repeat)</name>
  <files>js/contact.js, js/firebase-config.js, geohist/privacy.html, .planning/phases/09-app-check-monitor-first/COVERAGE.md, .planning/phases/09-app-check-monitor-first/09-RUNBOOK.md</files>
  <action>
Commit each task atomically with the standard scoped commit flow (docs task and code task separate), then deploy via the project's standard chain: push to main -> GitHub Actions validate job -> Pages deploy (no build step — the pushed tree is the artifact). Watch the run to completion (`gh run list --limit 1` / `gh run watch`) and confirm the deploy job succeeds.

After the deploy propagates, run the automated prod smoke: fetch the live bundle and confirm both signals shipped — the served `js/contact.js` contains the Enterprise provider name exactly once, the served `js/firebase-config.js` contains the site key, and `/geohist/contact.html` returns 200 on the apex domain. Also re-confirm the zero-attestation-bytes invariant: no HTML page serves the attestation script URL (the SDK injects it at App Check init only).

Then present the owner re-verification checklist (the two UAT repeats) — this is the formal gap-closure proof and runs via /gsd-verify-work resume: (a) test 1 repeat — submit the live form with valid data, expect success status, form reset, message in Firestore, zero visible change; (b) test 2 repeat — on prod, DevTools-block the attestation request pattern (`*recaptcha*`), submit with analytics consent granted -> contact.status.appcheck shows, form stays usable, appcheck_token_failure appears in GA4 (up to 24 h event lag), and the message still lands in Firestore; repeat with consent denied -> same status, no such event, message still lands. Localhost is NOT the re-verification surface — the API-key referrer restriction blocks identitytoolkit there (UAT finding); prod only.
  </action>
  <precondition>Owner has completed the reCAPTCHA Admin Migrate-keys migration (classic v3 key now GCP/Enterprise-managed, site key value unchanged) — asserted before re-verification; if unmet, halt and ask the owner (user_setup item).</precondition>
  <verify>
    <automated>git push origin main && gh run watch --exit-status (latest validate+deploy) && (curl.exe -s -o NUL -w "%{http_code}" https://geohisttrivia.com/geohist/contact.html | grep -qx 200) && (curl.exe -s https://geohisttrivia.com/js/contact.js | rg -c "ReCaptchaEnterpriseProvider" | grep -qx 1) && (curl.exe -s https://geohisttrivia.com/js/firebase-config.js | rg -q "6LfYjbAtAAAAABgPLG-4SuJJ9lggRWO-ZJxxOEPF" && echo OK-key-live) && (curl.exe -s https://geohisttrivia.com/geohist/contact.html | rg -q "google.com/recaptcha" && echo FAIL-attestation-bytes-in-html || echo OK-zero-bytes)</automated>
    <human-check>UAT test 1 repeat: valid prod submit -> success status, form resets, message visible in Firestore messages — zero visible change. UAT test 2 repeat: DevTools block `*recaptcha*` on prod — consent granted -> appcheck status + appcheck_token_failure in GA4; consent denied -> appcheck status, no event; message lands in Firestore in both cases. Non-blocking here; formal pass/fail recorded by /gsd-verify-work resume.</human-check>
  </verify>
  <done>Deploy green on prod serving the Enterprise provider + active site key; zero attestation bytes in HTML invariant holds; owner re-verification checklist (tests 1 + 2 repeat) handed off with the migrate-keys precondition stated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser -> Firebase backend | Contact-form submits carry the App Check token via X-Firebase-AppCheck; backend verifies against the Enterprise registration |
| repo (publicly served tree) | Every committed file is publicly served — secrets must never enter the repo |
| owner console | reCAPTCHA Admin / Firebase App Check registration are owner-only surfaces the agent never touches |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-G1 | Information Disclosure | recaptchaSiteKey in js/firebase-config.js | low | accept | Site key is public-by-design (D-02, same rationale as the rest of the Firebase config); the SECRET key stays console-only and never enters the repo |
| T-09-G2 | Information Disclosure | reCAPTCHA secret key / debug tokens | high | mitigate | No task in this plan touches console credentials; runbook public-artifact notice and the two §7 hard warnings preserved verbatim |
| T-09-G3 | Repudiation | privacy.html disclosure vs shipped provider | medium | mitigate | Version label dropped from the SDK-inventory li so the page matches the Enterprise provider actually shipped (T-09-10 drift closed) |
| T-09-G4 | Spoofing | forged-config direct-REST abuse of the public Firebase config | medium | accept | Posture unchanged by this plan — monitoring mode still accepts un-attested requests; enforcement flip remains the evidence-gated owner console step (FIRE-10, post-monitoring) |

No npm/pip/cargo install tasks in this plan — no package-legitimacy checkpoint required.
</threat_model>

<verification>
- `node --check` on both touched JS files exits 0
- Classic provider class: zero references anywhere under js/; Enterprise provider class: exactly one (contact.js init)
- validate:html + validate:domain + validate:i18n all exit 0
- COVERAGE.md Enterprise row = INTEGRATE; runbook contains the Migrate-keys owner step; no stale D-01-era rejection rationale in either doc
- Prod: /geohist/contact.html 200, served contact.js carries the Enterprise provider, served firebase-config.js carries the site key, zero attestation bytes in any served HTML
- UAT tests 1 and 2 re-run on prod via /gsd-verify-work resume; G-09-2 reconciled
</verification>

<success_criteria>
- G-09-2 closed: post-activation submits pass token verification against the reCAPTCHA Enterprise registration with zero user-visible change in monitoring mode, and the token-failure path (status + consent-gated event) is exercisable on prod
- All four gap artifacts updated: contact.js, firebase-config.js, COVERAGE.md, 09-RUNBOOK.md (+ privacy.html label drift closed)
- D-01 revision recorded with date in the docs; owner migrate-keys step documented as the remaining console action
- Deploy green; gap re-verification checklist in the owner's hands
</success_criteria>

<output>
Create `.planning/phases/09-app-check-monitor-first/09-03-SUMMARY.md` when done
</output>
