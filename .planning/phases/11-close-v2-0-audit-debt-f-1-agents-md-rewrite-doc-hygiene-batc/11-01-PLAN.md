---
phase: 11-close-v2-0-audit-debt-f-1-agents-md-rewrite-doc-hygiene-batc
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - AGENTS.md
  - .planning/PROJECT.md
  - scripts/check-no-old-domain.mjs
autonomous: true
requirements: [F-1]
user_setup: []

estimate:
  tokens: 42000
  raw_tokens: 42000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "AGENTS.md states shipped v2.0 reality: apex domain, single-URL keyed-engine i18n (19 JSON dictionaries / 178-key exact surface / 20 supported languages), fork-shaped Firebase split, App Check Enterprise monitoring-mode, social-proof tiers, Firebase 12.18.0 pinned CDN"
    - "Zero legacy-host literals and only the generator-owned profile marker remain in AGENTS.md — stale-injection drift is ended (D-03)"
    - "CONVENTIONS and ARCHITECTURE sections carry the 10 phases' real patterns and the live file map — no placeholder text (D-03)"
    - "The old-domain CI gate ENFORCES AGENTS.md (allowlist entry dropped) and exits 0 against the rewritten doc; a mutation probe proves the enforcement (D-04 discretion, pinned: drop)"
  artifacts:
    - "AGENTS.md — full rewrite replacing all 199 lines (D-01)"
    - ".planning/PROJECT.md line 5 — What-This-Is paragraph names the apex URL (D-02/D-04)"
    - "scripts/check-no-old-domain.mjs — ALLOW set loses the AGENTS.md entry + header comment list updated"
  key_links:
    - "AGENTS.md ↔ scripts/check-no-old-domain.mjs: gate scans the rewritten doc, exits 0; mutation probe proves the scan is real"
    - "AGENTS.md fact claims ↔ STATE.md locked decisions + js/i18n.js header (D-25..D-34): every stack claim sourced from shipped reality, never regenerated from research/STACK.md (D-02)"
    - "scripts/check-no-old-domain.mjs ALLOW ↔ npm run validate chain: validate:domain stays green after the drop"
---

<objective>
F-1: bring AGENTS.md to shipped v2.0 reality so no future agent can be misled into re-architecting working code (per D-01 full rewrite, D-02 hand-written from shipped reality, D-03 marker strip + populated CONVENTIONS/ARCHITECTURE, D-04 URL sweep scope = AGENTS.md + PROJECT.md line 5 only), and flip the old-domain gate from allowlisting AGENTS.md to enforcing it.

Purpose: the audit's only WARNING-severity item — the current doc describes the pre-pivot per-language-subdir i18n design while listing the shipped dictionary-swap architecture under "What NOT to Use" (a future agent following it could break working code), and rots silently because the doc is on the domain-gate allowlist.
Output: rewritten AGENTS.md; PROJECT.md line 5 fixed; `scripts/check-no-old-domain.mjs` enforcing AGENTS.md; D-06 commit 1 landed locally.

This plan is a tracer slice: Task 1 wires the entry point (literal-free doc) through every layer this phase touches — doc content → domain gate → validate battery → commit.
</objective>

<execution_context>
@C:/Users/Familia/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/Familia/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/11-close-v2-0-audit-debt-f-1-agents-md-rewrite-doc-hygiene-batc/11-CONTEXT.md
@.planning/phases/11-close-v2-0-audit-debt-f-1-agents-md-rewrite-doc-hygiene-batc/11-RESEARCH.md
@.planning/v2.0-MILESTONE-AUDIT.md
@AGENTS.md
</context>

<precondition>Sequencing (D-11): /gsd-ship has already reconciled remote main into HEAD — assert `git merge-base --is-ancestor origin/main HEAD` exits 0 and `git status --porcelain` is empty before starting; halt via precondition checkpoint if either fails.</precondition>

<artifacts-this-phase-produces>
Symbols the whole phase creates (this plan creates the first three):
- scripts/check-no-old-domain.mjs — ALLOW set surface change (AGENTS.md entry removed) + header allowlist list updated (constants edit, not a new symbol)
- AGENTS.md — full rewrite; zero legacy-host literals; only the generator-owned `GSD:profile-start` marker retained
- .planning/PROJECT.md — line 5 URL swap (content edit, not a symbol)
- scripts/i18n-keycheck.mjs (plan 11-03) — header rule 3, constants `TIER1_NS` / `STAR` (`'\u2605'` escape), star-uniqueness FAIL branch, markup-check branch, FAIL prefix "star uniqueness:"
- .planning/phases/11-.../red-gate-proof.md (plan 11-03) — new record file
- 06-UAT.md test 14; 09-UAT.md tests 10/11 (plan 11-02) — record shapes, not code symbols
- Supersession note string "[corrected Phase 11: 19 JSON dictionaries — no en.json; …]" (plan 11-02) — content edit, not a symbol
- Planner artifacts already produced: ROADMAP Phase-11 goal/success-criteria backfill; COVERAGE.md false-positive declaration
</artifacts-this-phase-produces>

<tasks>

<task type="tracer">
  <name>Task 1: Hand-rewrite AGENTS.md to shipped v2.0 reality (D-01/D-02/D-03)</name>
  <files>AGENTS.md</files>
  <read_first>
    - AGENTS.md (full current 199 lines — the rewrite target; note stale lines 7/14/67/90 URLs, 36/81-85/89 i18n, 133 What-NOT-to-Use, marker census)
    - .planning/phases/11-close-v2-0-audit-debt-f-1-agents-md-rewrite-doc-hygiene-batc/11-RESEARCH.md § "F-1 rewrite fact pack" (the authoritative content source for every section)
    - .planning/STATE.md ## Accumulated Context → Decisions (locked decisions the rewrite must state)
    - js/i18n.js lines 1-60 (engine header D-25..D-34 — the authoritative engine spec to paraphrase, not invent)
    - .planning/PROJECT.md (hosting truth at line 93, Key Decisions table)
    - geohist/index.html lines 86-95 (Tier-1 row shape — for the social-proof section)
    - package.json lines 5-21 (validate chain + dev deps) and .github/workflows/deploy.yml (deploy action pins)
    - robots.txt (actual Sitemap line names the apex)
  </read_first>
  <action>Replace the ENTIRE file content (Write tool after reading). Hand-write every section fresh from 11-RESEARCH.md § "F-1 rewrite fact pack" + STATE.md locked decisions — do NOT copy v1 sections verbatim and do NOT regenerate from research/STACK.md (D-01/D-02; research Pitfall 4: the wrong 20% is the trap). Required sections and their substance:
  - Project/overview: static GitHub Pages site behind the apex geohisttrivia.com (HTTPS enforced, www canonicalized to apex, protected_domain_state verified); /geohist/ subdir; app in Google Play review. State the still-true dual-hosts fact WITHOUT the legacy-host literal, using the audit-approved phrasing: the repo's legacy *.github.io Pages host dual-serves then 301-redirects path-preserved to the apex. NEVER spell the legacy host string anywhere in the file (the gate needle is that literal and the allowlist entry is being dropped in Task 2).
  - Constraints/stack: plain HTML5/CSS3/ES2020+ vanilla JS, zero build, no SSG/framework; Firebase JS SDK 12.18.0 exact-pinned gstatic ESM CDN, modular API only (never compat builds); products = Analytics + Anonymous Auth + Firestore.
  - Fork-shaped Firebase split (LOCKED): analytics imported only in consent.js post-consent (load-gating — the dynamic import IS the consent); auth+firestore only in contact.js submit path; App Check rides contact.js submit path, never page load.
  - App Check (Phase 9): ReCaptchaEnterpriseProvider (classic v3 deprecated for new registrations); dormant-by-default empty recaptchaSiteKey gate with the site key now activated; ~3s reachability probe before init (blocked reCAPTCHA skips init); bounded ~10s getToken race + record-and-swallow + deliver-anyway; contact.status.appcheck keyed status; consent-gated appcheck_token_failure via the persano:appcheck document-event bridge; enforcement flip = owner console, evidence-gated (≥30 successful submissions + ready-to-enforce signal), never calendar.
  - i18n (the corrected Decision-7 content): single-URL keyed-engine dictionary swap — EN is the shipped raw HTML markup baseline (no en.json exists); js/i18n.js snapshots the baseline, resolves via stored persano.lang preference > first supported match across navigator.languages (table-driven DETECT_TABLE) > en; fetches same-origin flat JSON from /js/i18n/ and swaps keyed text/attrs via textContent/setAttribute only; 20 supported languages (en es pt-BR fr de it nl pl tr vi id ru el hi bn ar ur ja ko zh); 19 JSON dictionaries; 178-key exact surface with set-equality CI-gated; keyed nodes plain-text-only; RTL ar/ur via html dir flip + [dir="rtl"] CSS block + per-language line-height overrides; native select switcher ×20 endonyms in the fixed grouped order.
  - Contact form: signInAnonymously → addDoc to the messages collection; create-only Firestore rules (repo firebase/firestore.rules is source of truth, but console ruleset is NOT byte-identical — future rules edits require console re-paste); honeypot; Firebase config public-by-design, hardening console-side.
  - Social proof (Phase 10): 4-pill keyed facts strip (aria-labeled, static, zero links); Tier-1 rating row shipped OFF (hidden div, unkeyed 0.0 self-flagging span, single star SVG, one attributed rel="noopener" Play link — owner flip = 2 edits per 10-RUNBOOK.md gated on real visible Play data); Tier-2 aggregateRating permanently OFF via inert HTML comment outside the JSON-LD script (on-site-review-source precondition).
  - SEO/CI: sitemap.xml (6 apex loc entries, no lastmod), robots.txt apex Sitemap line, SoftwareApplication + MobileApplication JSON-LD, OG image 1200x630, favicon.ico single-entry ICO, app-ads.txt, GSC verification file; validate chain = validate:html && validate:domain && validate:links && validate:i18n-detect && validate:i18n; deploy chain pins checkout@v7 → configure-pages@v6 → upload-pages-artifact@v5 → deploy-pages@v5; dev deps html-validate 11.12.0, linkinator 8.1.0, @axe-core/cli 4.13.0, lighthouse 13.4.1, sharp 0.35.4.
  - What NOT to Use (re-check row by row): KEEP the still-true rows (SSGs, React/SPA, Tailwind, firebase compat builds, npm firebase at runtime, gtag Consent Mode as primary gate, CMP libraries, JSON-dictionary… INVERTED — see next, Functions/email add-ons, Jekyll i18n plugins); REPLACE the banned JSON-dictionary row with its opposite: per-language HTML subdirs + hreflang is now the anti-pattern at this architecture (120 files of duplicate markup, x20 maintenance — REQUIREMENTS.md Out-of-Scope); ADD a row: aggregateRating mirroring Play ratings stays barred even when the data is real (Google review-snippet policy) unless an on-site review source exists.
  - Conventions (populate — D-03): the real patterns from the 10 phases, per research fact pack: one atomic commit per unit + conventional-commit subjects; supersession-note policy for historical records (original text stays, dated bracketed correction appends — never silent edits); .planning/ is publicly served → no secrets/debug tokens in runbooks/UAT/console docs (console-UI instructions only); uat-passed predicate is mechanical (any result: issue = blocker — records land clean or with supersession framing); red-gate-proof pattern for gate changes (mutate → FAIL → restore byte-identical → PASS, both directions); i18n drafting = two-pass + app strings.xml glossary + register table; CJK punct gate scopes ja+zh (ko exempt, digit-period exception); changelog EN-entries documented i18n exception; deploy via GitHub Git Data API bridge with mandatory per-blob sha assertions when local remote ops are harness-blocked; zero globals, classic defer scripts, inline SVG icons stroke/currentColor, texture utilities decoration-only.
  - Architecture (populate — D-03): the live file map from the fact pack (root index.html hub + 404.html; geohist 5 keyed pages; css/base.css single shared stylesheet with custom properties + [dir="rtl"] block + .proof-* block; js/i18n.js + consent.js + contact.js + firebase-config.js + js/i18n/*.json 19 dicts; scripts/ gate+smoke utilities; firebase/firestore.rules; sitemap.xml, robots.txt, favicon.ico, app-ads.txt, GSC file, .nojekyll) + the runtime data-flow sentence (page load → i18n detect+apply snapshot-walk → consent.js load-gated analytics → contact.js submit path probe → appCheck init → getToken race → auth → firestore).
  - Marker handling (D-03): STRIP the 6 source-wired injection markers (project-start source:PROJECT.md, stack-start source:research/STACK.md, conventions-start, architecture-start, skills-start, workflow-start) and write those sections fresh; PRESERVE the GSD:profile-start block + its marker verbatim (generator-managed by generate-claude-profile — pinned from research A1). Project-skills section states the truth: no project skills installed.
  Forbidden content (self-check before finishing): the legacy-host literal anywhere; any hreflang/per-language-subdir mention; any EN/ES/PT three-language framing; any /es/ or /pt/ directory reference; any "EN lives at…en.json" implication.
  </action>
  <verify>
    rg -n "persano\.github\.io" AGENTS.md → no matches (exit 1 = clean)
    rg -in "hreflang" AGENTS.md → no matches (exit 1)
    rg -n "GSD:.*-start" AGENTS.md → exactly 1 match (profile-start only)
    rg -n "not yet established|not yet mapped" AGENTS.md → no matches (exit 1)
    rg -c "19 JSON|19 dictionaries" AGENTS.md → ≥1; rg -n "geohist/index.html" AGENTS.md → ≥1 (Architecture map populated); rg -n "supersession" AGENTS.md → ≥1 (Conventions populated)
  </verify>
  <acceptance_criteria>
    - Source assertion: AGENTS.md contains zero occurrences of the legacy-host literal (rg pattern persano\.github\.io returns nothing)
    - Source assertion: AGENTS.md contains zero hreflang/per-language-subdir references and zero "/es/" or "/pt/" directory references
    - Source assertion: exactly one GSD marker remains in AGENTS.md and it is the profile-start one; all 6 source-wired markers are gone
    - Source assertion: AGENTS.md contains sections describing the apex domain, the 19-dictionary/178-key keyed-engine i18n, ReCaptchaEnterpriseProvider monitoring-mode App Check, the fork-shaped Firebase split, and the validate chain string "validate:html && validate:domain && validate:links && validate:i18n-detect && validate:i18n"
    - Source assertion: "## Conventions" and "## Architecture" sections contain the real patterns/file map (matchers: supersession, red-gate, persano\.lang, geohist/contact.html, firebase/firestore.rules) and no placeholder text
    - Source assertion: What-NOT-to-Use names per-language HTML subdirs + hreflang as the anti-pattern and aggregateRating mirroring as barred; the banned-JSON-dictionary row is gone
  </acceptance_criteria>
  <done>AGENTS.md on disk is a complete hand-written rewrite of shipped v2.0 reality: zero legacy-host literals, zero stale i18n guidance, populated CONVENTIONS/ARCHITECTURE, only the profile marker retained — ready for gate enforcement.</done>
  <reversibility rating="reversible">Text-only rewrite of one doc; git history preserves the v1 version.</reversibility>
</task>

<task type="auto">
  <name>Task 2: PROJECT.md line-5 URL fix + drop AGENTS.md from the gate allowlist + enforcement red-gate proof (D-04)</name>
  <files>.planning/PROJECT.md, scripts/check-no-old-domain.mjs</files>
  <read_first>
    - .planning/PROJECT.md lines 1-10 (the What-This-Is paragraph = line 5 fix target)
    - scripts/check-no-old-domain.mjs (full 98 lines — ALLOW set at line 39, header allowlist list lines 14-20, runtime-assembled LEGACY_HOST at line 38)
    - AGENTS.md (current working-tree state from Task 1 — the mutation probe appends a probe line to it)
  </read_first>
  <action>Three edits plus one enforcement proof:
  1. .planning/PROJECT.md line 5: swap the stale repo-host URL in the opening What-This-Is paragraph to the apex https://geohisttrivia.com (D-02/D-04 — the only .planning source-file edit in the phase). Change nothing else in the paragraph. (The .planning dir stays gate-allowlisted, so this edit is for truth, not for the gate.)
  2. scripts/check-no-old-domain.mjs line 39: remove the AGENTS.md entry from the ALLOW set, leaving .planning, README.md, .git, node_modules. Update the header comment list (lines 14-20) accordingly — delete the "AGENTS.md hosting documentation" bullet. Keep the runtime-assembled LEGACY_HOST construction untouched (the script source must stay grep-clean of the host literal).
  3. Enforcement red-gate proof (repo evidence standard; AGENTS.md is UNCOMMITTED at this point — never use git restore; snapshot-copy instead): copy AGENTS.md to a scratch file under the temp dir and record its hash (Get-FileHash); append a probe line containing the legacy-host literal to AGENTS.md; run node scripts/check-no-old-domain.mjs → MUST exit 1 naming AGENTS.md:<line>; restore AGENTS.md from the snapshot copy; verify the hash matches the pre-mutation hash (byte-identical restore, Phase 10 precedent); delete the scratch file. Do NOT commit the red state.
  4. Run node scripts/check-no-old-domain.mjs on the restored tree → exit 0 (zero hits across the whole repo including the now-enforced AGENTS.md).
  </action>
  <verify>
    node scripts/check-no-old-domain.mjs → exit 0 (post-restore)
    Mutation cycle recorded: gate exit 1 naming AGENTS.md while the probe line was present
    Get-FileHash AGENTS.md before mutation == after restore
    rg -n "geohisttrivia\.com" .planning/PROJECT.md → ≥1 match on line 5; rg -n "persano\.github\.io" .planning/PROJECT.md → no matches in lines 1-10 (the line-5 swap landed)
    rg -n "AGENTS\.md" scripts/check-no-old-domain.mjs → only the header bullet removed — ALLOW set line no longer contains the AGENTS.md entry
  </verify>
  <acceptance_criteria>
    - Source assertion: the ALLOW Set literal in scripts/check-no-old-domain.mjs reads as the four entries .planning, README.md, .git, node_modules (AGENTS.md absent)
    - Source assertion: the header allowlist comment no longer lists AGENTS.md
    - Behavior assertion: with a probe line carrying the legacy-host literal appended to AGENTS.md, node scripts/check-no-old-domain.mjs exits 1 and its stderr names AGENTS.md with a line number
    - Behavior assertion: after byte-identical restore (hash-verified), node scripts/check-no-old-domain.mjs exits 0
    - Source assertion: .planning/PROJECT.md line 5 references https://geohisttrivia.com and no longer the stale repo-host URL
  </acceptance_criteria>
  <done>Gate now enforces AGENTS.md (probe proven), PROJECT.md line 5 names the apex, gate green on the clean tree — the doc can never silently rot again.</done>
  <reversibility rating="reversible">One-word ALLOW diff + one-line PROJECT edit; re-adding the entry restores prior behavior.</reversibility>
</task>

<task type="auto">
  <name>Task 3: Full validate battery + D-06 commit 1 (F-1 unit)</name>
  <files></files>
  <read_first>
    - package.json lines 5-10 (the five chained validate scripts)
    - .planning/STATE.md Deploy bridge entries (remote-delivery policy — why this plan must NOT push)
  </read_first>
  <action>Run the full battery: npm run validate (html → domain → links → i18n-detect → i18n) — must exit 0. Then land D-06 commit 1: stage exactly AGENTS.md, .planning/PROJECT.md, scripts/check-no-old-domain.mjs and commit locally with subject "docs(11): rewrite AGENTS.md to shipped v2.0 reality (F-1)". Do NOT push and do NOT build a Git Data API bridge — docs-only changes are owner-pushed (planning-context constraint); local commit only. If the battery fails on validate:domain, an old-domain literal survived the rewrite — fix AGENTS.md and re-run before committing.</action>
  <verify>
    npm run validate → exit 0 (all five gates)
    git log -1 --format=%s → "docs(11): rewrite AGENTS.md to shipped v2.0 reality (F-1)"
    git status --porcelain → empty for AGENTS.md, .planning/PROJECT.md, scripts/check-no-old-domain.mjs
  </verify>
  <acceptance_criteria>
    - Test command: npm run validate exits 0 (five chained gates green, including the now-enforcing validate:domain)
    - CLI output: git log -1 --format=%s equals the pinned commit subject
    - Source assertion: the three files are committed (git status --porcelain lists none of them)
    - Behavior assertion: no push and no remote op was attempted (git log origin/main..HEAD shows the new commit ahead, unpushed)
  </acceptance_criteria>
  <done>Commit 1 of the locked D-06 shape landed locally; full battery green with AGENTS.md enforced by the domain gate.</done>
</task>

</tasks>

<verification>
- npm run validate exits 0 (five gates; validate:domain now scans AGENTS.md and finds zero hits)
- rg confirms AGENTS.md is free of the legacy-host literal, hreflang/subdir guidance, and stale markers
- Mutation-probe evidence recorded in the plan execution log: gate exit 1 naming AGENTS.md with probe present, exit 0 after hash-verified restore
</verification>

<success_criteria>
- F-1 closed: no future agent can read re-architecting guidance from AGENTS.md; the doc's stack claims trace to STATE locked decisions and live code (D-01/D-02)
- Stale-injection drift ended: 6 source-wired markers stripped, CONVENTIONS/ARCHITECTURE populated (D-03)
- URL sweep exactly D-04 scope: AGENTS.md + PROJECT.md line 5; ~70 historical .planning refs untouched
- Domain gate enforces AGENTS.md (D-04 discretion item, pinned: drop) — enforcement proven by mutation probe
- D-06 commit 1 landed locally, no remote ops
</success_criteria>

<output>
Create .planning/phases/11-close-v2-0-audit-debt-f-1-agents-md-rewrite-doc-hygiene-batc/11-01-SUMMARY.md when done
</output>
