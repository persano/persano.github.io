---
phase: 04-consent-gate-firebase-analytics-contact-form
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - geohist/contact.html
  - js/contact.js
  - firebase/firestore.rules
  - index.html
  - geohist/index.html
  - geohist/guide.html
  - css/base.css
  - js/i18n/es.json
  - js/i18n/pt-BR.json
  - scripts/i18n-keycheck.mjs
  - scripts/smoke-check.sh
autonomous: false
requirements: [FIRE-04, FIRE-05, FIRE-06, CMPL-03]
user_setup:
  - service: firebase
    why: "The form cannot submit until the Anonymous provider is enabled, production Firestore exists, and the repo rules file is pasted into the console — all console-side owner actions"
    dashboard_config:
      - task: "Enable the Anonymous sign-in provider (and anonymous account auto-clean-up)"
        location: "Firebase Console -> Authentication -> Sign-in method"
      - task: "Create the Firestore database in production mode"
        location: "Firebase Console -> Firestore Database"
      - task: "Paste firebase/firestore.rules verbatim into the Rules editor and publish"
        location: "Firebase Console -> Firestore Database -> Rules"
      - task: "Verify persano.github.io is in Authentication -> Settings -> Authorized domains"
        location: "Firebase Console -> Authentication -> Settings -> Authorized domains"
estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "/geohist/contact.html exists mirroring the guide.html page pattern (keyed head metas, nav Game/Guide/FAQ/Privacy, footer trio + copyright + lang slot, /css/base.css) and carries the 04-01 banner block + consent script tags from birth plus the contact.js script tag (D-46, D-42)."
    - "Form fields per D-47: name (optional, <=100), email (required, <=254), topic select with exactly the four options General question / Bug report / Feedback / Request data deletion (values general|bug|feedback|deletion), message (required, 1..5000) — all labeled and keyed; honeypot is off-screen (not display:none) with aria-hidden + tabindex=-1 + autocomplete off (Pitfall 8)."
    - "Client validation runs BEFORE any network activity: empty email/message, malformed email, out-of-enum topic, and over-cap input each surface a pre-authored keyed status variant (aria-live=polite, hidden-toggled — Pattern 5); status nodes are static keyed elements, never JS textContent injection against dictionaries, and all success/error feedback is the inline under-form status message — no toasts/popups (D-49)."
    - "Client caps equal rules caps exactly (email <=254, message 1..5000, name <=100, both measuring UTF-16 code units) — the FIRE-04/FIRE-05 encoding probes resolve by making both files agree on the same numbers."
    - "Submit pipeline is choice-independent: preventDefault -> honeypot-filled means silent success-swallow (fake success, form reset, zero network) -> lazy one-time dynamic import of the three pinned 12.18.0 CDN modules at submit time -> initializeApp + getAuth, signInAnonymously only when currentUser is absent -> payload built conditionally (the name key is included ONLY when non-empty; null is never sent because a present null fails the rules string-type guard, Pitfall 4 adjacency) -> addDoc into messages with serverTimestamp() -> keyed success/error statuses; the submit button is disabled while in flight so a double click cannot create a duplicate (FIRE-04/05 idempotency probe)."
    - "firebase/firestore.rules: create-only — keys().hasOnly(['name','email','topic','message','createdAt']), topic in the 4-value enum, email/message/name type+length caps, optional-name guard via absent-or-string (never dot-access on a possibly-missing field, Pitfall 4), createdAt == request.time, and read+update+delete denied (FIRE-05 concurrency probe: every accepted write is an independent immutable document)."
    - "CMPL-03 path complete: topic value 'deletion' in the form + a new FAQ entry (geohist.faq.deletion.q/a) + a contact-page explainer, all quoting the privacy.html §4 phrases verbatim (retention sentence + contact email) per the CMPL-04 single-source rule (FIRE-06)."
    - "D-48 rewiring: footer Contact anchors on hub /index.html, /geohist/index.html, /geohist/guide.html point to /geohist/contact.html; the FAQ report answer + its anchor point to the form; geohist/privacy.html and the About-dev section keep their direct mailto: links."
    - "scripts/i18n-keycheck.mjs pages list includes geohist/contact.html and every new key is in BOTH dictionaries — npm run validate:i18n passes (Pitfall 1)."
    - statement: "Live behavior on the deployed site: a schema-conformant submit creates a Firestore document with a server timestamp; a name-less create succeeds for every topic value; an extra/malformed field is denied with permission-denied; the form submits successfully after consent grant AND after a fresh session's consent deny; a honeypot-filled submit shows success but creates no document."
      verification: backstop
  artifacts:
    - "geohist/contact.html — new keyed page mirroring guide.html: nav, form (4 fields + honeypot), pre-authored status variants, banner block, footer with re-open link"
    - "js/contact.js — classic-defer IIFE: honeypot swallow, client validation, in-flight guard, lazy 3-module CDN import, anonymous auth + addDoc, status toggling"
    - "firebase/firestore.rules — create-only schema-locked rules (repo source of truth, pasted to console verbatim)"
    - "css/base.css: .form-field/.form-status/.hp-field styles using existing theme tokens"
    - "scripts/i18n-keycheck.mjs pages list += geohist/contact.html; scripts/smoke-check.sh URL list += /geohist/contact.html"
    - "contact.* key namespace + geohist.faq.deletion.q/a in es.json + pt-BR.json (1:1); rewritten geohist.faq.report.a + repurposed report anchor"
  key_links:
    - "submit -> lazy CDN import -> signInAnonymously -> addDoc(messages): the form path imports ONLY app+auth+firestore modules, never the Analytics module (fork-shaped architecture, research Summary)"
    - "firebase/firestore.rules <-> Firebase console paste (verbatim) — Pitfall 5: production Firestore blocks all writes until the paste"
    - "geohist.faq.deletion.* + contact.deletion.* <-> geohist/privacy.html §4 wording (CMPL-04 single source)"
    - "footer Contact x3 + FAQ report anchor <-> /geohist/contact.html (D-48)"
    - "geohist/contact.html <-> scripts/i18n-keycheck.mjs pages list (Pitfall 1 — same task or the CI gate breaks)"

prohibitions:
  - statement: "Firestore rules must never permit read, update, or delete of messages — create is the only allowed operation on the collection."
    status: enforced
    verification: "firebase/firestore.rules contains the deny line for read+update+delete (Pattern 4 final line) and no other allow rule on /messages/{docId}"
  - statement: "The contact-form pipeline must never be conditioned on the visitor's banner choice — the form is the compliance surface that must survive a deny."
    status: enforced
    verification: "case-insensitive search for the string 'consent' in js/contact.js returns zero matches (the module neither reads nor writes the banner storage key and imports Auth/Firestore unconditionally at submit time)"
  - statement: "Client-side validation must never be the schema gate — the rules file is the enforcement point; client checks are UX only."
    status: enforced
    verification: "rules file carries its own type+length+enum checks independent of contact.js; the Task 3 Rules Playground battery confirms a crafted write violating caps is denied server-side"
---

# Plan 04-02: Contact Page + Form Pipeline + Firestore Rules + CMPL-03 (FIRE-04..06, CMPL-03)

**User story:** As a player (or a Play reviewer), I can reach a working contact form that validates my input, survives even if I declined analytics measurement, and offers a "Request data deletion" topic — completing the documented deletion-request path the privacy policy promises.

## Artifacts this phase produces

(Listed in full at the top of 04-01-PLAN.md — this plan contributes: `geohist/contact.html`, `js/contact.js`, `firebase/firestore.rules`, the `contact.*` i18n namespace + `geohist.faq.deletion.q/a`, `.form-field`/`.form-status`/`.hp-field` CSS classes, the i18n-keycheck pages-list entry, the smoke-check contact URL, and the `messages` collection schema `{name?, email, topic, message, createdAt}` with topic enum `general|bug|feedback|deletion`.)

<objective>
Stand up the contact surface: a new keyed page /geohist/contact.html mirroring the guide.html pattern, a vanilla submit pipeline (honeypot -> client validation -> lazy anonymous auth -> Firestore create) that works identically after either banner choice, create-only schema-locked rules in the repo, the CMPL-03 deletion-request path (form topic + FAQ entry + documented manual process), and the D-48 link rewiring.

Purpose: the form is the compliance surface — it MUST work after consent deny (out-of-scope table row "Analytics/form blocking on consent-deny") — and it completes the data-deletion request path Play requires (CMPL-03).
Output: contact.html + contact.js + firebase/firestore.rules + FAQ/link rewiring + a blocking human checkpoint that completes the Firebase console prerequisites and proves the live submit battery after grant AND after deny.
</objective>

<execution_context>
@C:/Users/Familia/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/Familia/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-consent-gate-firebase-analytics-contact-form/04-CONTEXT.md
@.planning/phases/04-consent-gate-firebase-analytics-contact-form/04-RESEARCH.md
@geohist/guide.html
@geohist/index.html
@scripts/i18n-keycheck.mjs
@scripts/smoke-check.sh
@geohist/privacy.html
</context>

<tasks>

<task type="tracer">
  <name>Task 1: /geohist/contact.html + js/contact.js submit pipeline + create-only Firestore rules + keycheck wiring</name>
  <files>geohist/contact.html, js/contact.js, firebase/firestore.rules, scripts/i18n-keycheck.mjs, js/i18n/es.json, js/i18n/pt-BR.json, css/base.css</files>
  <read_first>
  - geohist/guide.html — the page pattern to mirror exactly (head metas, script-tag position, nav, footer, lang-switcher slot)
  - 04-RESEARCH.md — Pattern 3 (submit pipeline), Pattern 5 (pre-authored keyed status variants), Pattern 4 (rules), Pitfalls 1, 4, 5, 6, 8; "Architectural Responsibility Map" (client validation is UX, rules are the wall)
  - geohist/index.html lines 95-127 — the <details class="faq-item" name="geohist-faq"> pattern (context for Task 2) and the banner block from 04-01 (copy into contact.html)
  - js/i18n.js — snapshot semantics: status variants must be STATIC keyed elements present at DOMContentLoaded (Pattern 5 rationale)
  - geohist/privacy.html §3/§4 — SDK inventory + retention/deletion wording the contact copy must mirror (CMPL-04)
  - js/i18n/es.json, js/i18n/pt-BR.json — flat-key style, 1:1 parity gate
  </read_first>
  <action>
Create geohist/contact.html mirroring the guide.html scaffolding: same head shape with keyed `contact.meta.title` + `contact.meta.desc`; same nav structure with keys `contact.nav.game` / `contact.nav.guide` / `contact.nav.faq` / `contact.nav.privacy` (same hrefs as guide's nav); main section with keyed h1 `contact.form.title` ("Contact") and intro paragraph `contact.intro` (short, mentions the form + that a data-deletion request can be filed here); the form; a deletion explainer paragraph keyed `contact.deletion.text` (CMPL-03 — see below); footer mirroring guide's footer with `contact.footer.privacy` / `contact.footer.contact` (href /geohist/contact.html) / `contact.footer.back` + copyright + `#lang-switcher-slot` + the class `consent-reopen` footer item keyed `contact.footer.consent`; the 04-01 banner block verbatim; head script tags in order i18n.js -> firebase-config.js -> consent.js -> contact.js.

Form (id contact-form, novalidate): fields name (input text, id contact-name, optional, maxlength 100), email (input email, id contact-email, required, maxlength 254), topic (select, id contact-topic, required, four options with values general / bug / feedback / deletion, keyed labels `contact.topic.general` / `contact.topic.bug` / `contact.topic.feedback` / `contact.topic.deletion`), message (textarea, id contact-message, required, maxlength 5000). Every field has a <label for> keyed `contact.form.name` / `contact.form.email` / `contact.form.topic` / `contact.form.message`. Honeypot per research: <div class="hp-field" aria-hidden="true"> wrapping a label "Website" and <input id="hp_website" name="website" type="text" tabindex="-1" autocomplete="off">; CSS positions .hp-field off-screen (left:-9999px), never display:none.

Status block: pre-authored keyed variant paragraphs (Pattern 5) — each `<p class="form-status" role="status" aria-live="polite" data-i18n="contact.status.VARIANT" hidden>` — for variants sending / success / error / invalid-email / invalid-message / invalid-topic. EN copy: sending "Sending…", success "Thanks — your message is on its way.", error "Something went wrong — write to santiagopostorivo@gmail.com.", invalid-email "Please enter a valid email address.", invalid-message "Please write a message (up to 5000 characters).", invalid-topic "Please pick a topic."

Create js/contact.js: classic defer IIFE (i18n.js house style, zero globals). Submit handler: preventDefault; if the honeypot input is non-empty, show the success variant, reset the form, and return (silent bot swallow, zero network); otherwise validate BEFORE any network activity — email non-empty + <=254 + simple shape check, message trimmed length 1..5000, topic value in the 4-value enum, name <=100 when present — showing the matching keyed status variant on failure. On valid input: disable the submit button (in-flight double-submit guard), show the sending variant, then lazily and only once dynamically import the three pinned 12.18.0 CDN modules (app, auth, firestore), initializeApp with window.persanoFirebaseConfig (idempotent default app), getAuth, call signInAnonymously only when auth.currentUser is absent, build the payload { email: trimmed, topic, message: trimmed, createdAt: serverTimestamp() } and include the name key ONLY when the trimmed name is non-empty — never send null for it (a present null fails the rules string-type check; the rules' absent-or-string guard pairs with this), then addDoc into the messages collection; on success show the success variant + reset the form; on failure show the error variant and log the FirebaseError code to console for debuggability; re-enable the button in a finally block. The module never reads or writes the banner storage key and never imports the Analytics module — the fork-shaped split that makes the form work after deny (compliance surface, out-of-scope table).

Create firebase/firestore.rules exactly per research Pattern 4: rules_version '2'; match /databases/{database}/documents / messages/{docId}; allow create gated on auth != null AND keys().hasOnly(['name','email','topic','message','createdAt']) AND topic in ['general','bug','feedback','deletion'] AND email string 1..254 AND message string 1..5000 AND the optional-name guard (!('name' in request.resource.data) || (name is string && size <= 100)) AND createdAt == request.time; final line denies read, update, delete. The repo file is the single source of truth for the console paste (Task 3, Pitfall 5).

Wire the keycheck gate: add join('geohist', 'contact.html') to the pages array in scripts/i18n-keycheck.mjs (line 21) IN THIS TASK — a keyed page invisible to the gate is Pitfall 1.

Add ALL contact.* keys 1:1 to both js/i18n/es.json and js/i18n/pt-BR.json in this same task (page metas, nav, title, intro, form labels, 4 topic labels, 6 status variants, deletion explainer, footer trio + consent link). Translate faithfully; keep the error status's direct email address intact.

Append form styles to css/base.css: .form-field label/input/select/textarea using theme tokens, :focus-visible inherits the existing global rule, .form-status with a modest gold-on-dark error/success look, .hp-field off-screen rule. Keep phone widths first-class (D-47 UX, LNDG-05).
  </action>
  <verify>
    <automated>npm run validate:html && npm run validate:i18n && node -e "const fs=require('fs');const c=fs.readFileSync('js/contact.js','utf8');for(const s of ['signInAnonymously','addDoc','serverTimestamp','hp_website','12.18.0']){if(!c.includes(s)){console.error('FAIL contact.js missing '+s);process.exit(1)}};if(/firebase-analytics/.test(c)){console.error('FAIL: analytics import in contact.js');process.exit(1)};if(/consent/i.test(c)){console.error('FAIL: choice-dependence in contact.js');process.exit(1)};const r=fs.readFileSync('firebase/firestore.rules','utf8');for(const s of ['hasOnly','request.time','allow create','read, update, delete','deletion']){if(!r.includes(s)){console.error('FAIL rules missing '+s);process.exit(1)}};const kc=fs.readFileSync('scripts/i18n-keycheck.mjs','utf8');if(!kc.includes('contact.html')){console.error('FAIL: keycheck page list');process.exit(1)};const p=fs.readFileSync('geohist/contact.html','utf8');for(const s of ['contact-form','hp_website','aria-live','consent-banner','js/contact.js']){if(!p.includes(s)){console.error('FAIL contact.html missing '+s);process.exit(1)}};console.log('PASS')"</automated>
  </verify>
  <acceptance_criteria>
  - `npm run validate:html` and `npm run validate:i18n` both exit 0 (keycheck now covers contact.html's keys 1:1 in both dictionaries)
  - js/contact.js contains: hp_website honeypot check, the 4 topic enum values, the 3 pinned 12.18.0 CDN imports, signInAnonymously with a currentUser guard, addDoc into messages, serverTimestamp, and a submit-button disable/enable in-flight guard; zero matches for 'consent' (case-insensitive) and zero for 'firebase-analytics'
  - firebase/firestore.rules contains: hasOnly over the five fields, the 4-value topic enum, email/message/name caps, the absent-or-string name guard, createdAt == request.time, and the read+update+delete deny line
  - geohist/contact.html contains: 4 script tags in the locked order, the consent-banner block, the form with 4 labeled fields, the honeypot div with aria-hidden + tabindex=-1, and at least 6 aria-live status variants
  - css/base.css gained form/status/honeypot styles using existing tokens
  </acceptance_criteria>
  <done>The form path is complete end-to-end in code: page renders keyed, validation blocks bad input before any network activity, the submit pipeline imports the three modules lazily and writes the exact schema shape, and the rules file locks the schema server-side — live submit proof lands in Task 3.</done>
</task>

<task type="auto">
  <name>Task 2: D-48 link rewiring + CMPL-03 deletion FAQ + contact-page explainer + smoke-check URL</name>
  <files>index.html, geohist/index.html, geohist/guide.html, geohist/contact.html, js/i18n/es.json, js/i18n/pt-BR.json, scripts/smoke-check.sh</files>
  <read_first>
  - index.html line 28, geohist/index.html lines 119-141, geohist/guide.html line 79 — the three footer Contact anchors and the FAQ report entry being rewired
  - geohist/privacy.html — §4 retention/deletion wording: the exact phrases ("Messages are deleted after they are handled", the contact email) the new copy must quote verbatim (CMPL-04 single-source)
  - 04-CONTEXT.md D-47, D-48 — topic list and link-rewiring map
  - scripts/smoke-check.sh — the 200-check URL list (lines 22-28)
  </read_first>
  <action>
Rewire per D-48: the footer Contact anchors on hub index.html (hub.footer.contact, currently mailto:), geohist/index.html (geohist.footer.contact, line 139) and geohist/guide.html (guide.footer.contact, line 79) all change href to /geohist/contact.html — labels and keys unchanged. In the FAQ report entry (geohist/index.html ~line 119-123): update the answer paragraph value (key geohist.faq.report.a) to point readers to the contact form ("Use the contact form and we'll take a look:") and change the anchor's href from the mailto: to /geohist/contact.html — keep the key geohist.faq.report.email, repurposing its value to "Contact form" in all three languages. The About-dev section (geohist/index.html line 133) and geohist/privacy.html KEEP their direct mailto: links untouched (D-48).

Add the deletion FAQ entry to geohist/index.html as one more <details class="faq-item" name="geohist-faq"> right after the report entry: summary keyed `geohist.faq.deletion.q` ("How do I request deletion of my data?") and answer keyed `geohist.faq.deletion.a` — the answer describes the manual process (send the request via the contact form's "Request data deletion" topic or by email; the owner deletes the messages and confirms) and quotes the privacy.html §4 phrases verbatim, including the retention sentence "Messages are deleted after they are handled" and the contact email (CMPL-03 + CMPL-04 single-source; plain text only in keyed nodes).

On geohist/contact.html, fill the `contact.deletion.text` explainer with the same process (form topic or direct email; manual handling by the owner; retention sentence verbatim).

Dictionary parity: update geohist.faq.report.a + geohist.faq.report.email values and add geohist.faq.deletion.q / geohist.faq.deletion.a / contact.deletion.text to BOTH js/i18n/es.json and js/i18n/pt-BR.json (1:1 — same task or the CI gate fails).

Smoke check: add "$BASE/geohist/contact.html" to the URL list in scripts/smoke-check.sh's 200-check loop.
  </action>
  <verify>
    <automated>npm run validate:html && npm run validate:i18n && node -e "const fs=require('fs');for(const f of ['index.html','geohist/index.html','geohist/guide.html']){const p=fs.readFileSync(f,'utf8');if(!p.includes('href=\"/geohist/contact.html\"')){console.error('FAIL footer rewire: '+f);process.exit(1)}};const g=fs.readFileSync('geohist/index.html','utf8');if(!g.includes('geohist.faq.deletion.q')||!g.includes('geohist.faq.deletion.a')){console.error('FAIL: deletion FAQ missing');process.exit(1)};const about=g.split('about-dev')[1]||'';if(!about.includes('mailto:')){console.error('FAIL: About-dev mailto removed');process.exit(1)};const sm=fs.readFileSync('scripts/smoke-check.sh','utf8');if(!sm.includes('/geohist/contact.html')){console.error('FAIL: smoke URL');process.exit(1)};console.log('PASS')"</automated>
  </verify>
  <acceptance_criteria>
  - `npm run validate:html` + `npm run validate:i18n` exit 0
  - All three footer Contact anchors point to /geohist/contact.html
  - geohist/index.html contains the geohist.faq.deletion.q/a entry as a <details class="faq-item" name="geohist-faq"> and its report anchor hrefs to /geohist/contact.html
  - geohist/privacy.html and the About-dev section still contain their mailto: links (git diff shows them untouched where D-48 preserves them)
  - The deletion answer value in both dictionaries contains the exact phrase "Messages are deleted after they are handled"
  - scripts/smoke-check.sh's URL list contains /geohist/contact.html
  </acceptance_criteria>
  <done>CMPL-03's three surfaces exist and agree (form topic + FAQ entry + contact-page explainer, all quoting policy wording); every player-facing route to the developer now leads through the form except the deliberately preserved direct channels; smoke check covers the new URL.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 3: CHECKPOINT — Firebase console prerequisites, Rules Playground battery, live submit proof</name>
  <files>firebase/firestore.rules</files>
  <read_first>
  - firebase/firestore.rules — the exact file the owner pastes verbatim (Pitfall 5: production Firestore blocks all writes until this paste)
  - 04-RESEARCH.md Pitfall 4 (optional-field guard semantics — A2, the one assumption that MUST be exercised), Pitfall 6 (auth error codes pointing at console causes)
  - 04-CONTEXT.md — Manual Prerequisites Checklist items 2-4
  </read_first>
  <precondition>Real Web App config values already live in js/firebase-config.js (04-01 Task 3 checkpoint); without them anonymous auth and Firestore writes cannot succeed and this task halts.</precondition>
  <action>Human action task — see <instructions>. Executor resumes on the resume-signal reply; on any failing battery case, fix the rules file or form code, then the failed portion re-runs after redeploy.</action>
  <instructions>Pause for the owner. Steps: (1) Enable the Anonymous provider (+ auto-clean-up), create the Firestore database in production mode, verify persano.github.io is authorized. (2) Paste firebase/firestore.rules verbatim into the console Rules editor and publish — the repo file is the source of truth. (3) Run the Rules Playground battery listed under verification (research assumption A2 — the optional-name guard and timestamp match MUST be exercised before FIRE-05 is declared done; if any case fails, executor fixes the rules file and the paste + battery re-run). (4) After the orchestrator's controlled push + deploy green, owner runs the live submit battery and reports results.</instructions>
  <verification>
  Rules Playground (Firestore -> Rules -> Playground, allow/deny per case):
  1. Create with all five fields (name filled, email valid, topic general, message short, createdAt = request.time) -> ALLOW
  2. Name-less create, once per topic value: general / bug / feedback / deletion -> ALLOW (Pitfall 4 — the absent-or-string guard)
  3. Create with an extra unknown field -> DENY
  4. Create with topic 'other' or missing topic -> DENY
  5. Create with message of 5001 characters or email of 255 -> DENY
  6. get / update / delete simulation on /messages/{docId} -> DENY
  Live battery on the deployed site:
  7. Accept the banner, submit the form with valid input -> success status; Firestore console shows the doc with a server timestamp and the exact field set
  8. Fresh session: Reject the banner, submit the form -> STILL succeeds (compliance surface, SC3)
  9. Fill the honeypot field and submit -> success shown, NO document created
  10. Double-click submit rapidly -> exactly one document (in-flight guard)
  </verification>
  <resume-signal>Owner replies with: console prerequisites done (yes/no per item), the ten battery results (pass/fail each), and the Rules Playground outcomes. Cases 1-2 ALLOW + 3-6 DENY + live checks pass -> resume; any fail -> report which case failed so the executor can fix the rules file or code and re-run.</resume-signal>
  <acceptance_criteria>
  - Anonymous provider enabled, production Firestore created, rules pasted verbatim (console content == repo file)
  - Rules Playground: cases 1-2 ALLOW, cases 3-6 DENY (FIRE-05 schema lock proven server-side, A2 exercised)
  - Live: successful create after grant AND after deny; honeypot swallow creates no doc; double-click creates one doc (FIRE-04, FIRE-05, probe idempotency/concurrency)
  </acceptance_criteria>
  <done>SC3/SC4/SC5 of the phase are human-verified live: form works after any banner choice, rules enforce the schema server-side, and the deletion-request topic lands in the collection — CMPL-03 complete.</done>
</task>

</tasks>

<verification>
Phase-level checks for this plan (run before the Task 3 checkpoint):
- `npm run validate` exits 0 — keycheck now covers 4 keyed pages, dictionaries 1:1 in both languages
- rg -l 'firebase-analytics' js/ returns exactly js/consent.js (the form path never touches the Analytics module — fork shape intact)
- Case-insensitive 'consent' search in js/contact.js: zero matches
- firebase/firestore.rules deny line present; no allow other than the single create rule
- git diff: geohist/privacy.html untouched except nothing; About-dev + privacy mailto preserved
</verification>

<success_criteria>
- FIRE-04: labeled form with honeypot + client validation + keyed inline statuses; identical behavior after grant and after deny
- FIRE-05: submit-time anonymous auth -> Firestore create under create-only schema-locked rules that live in the repo
- FIRE-06 + CMPL-03: "Request data deletion" topic + FAQ entry + documented manual process quoting privacy.html §4 verbatim
- D-46..D-49 satisfied; new copy translated 1:1 in both dictionaries; all CI gates green
</success_criteria>

<output>
Create `.planning/phases/04-consent-gate-firebase-analytics-contact-form/04-02-SUMMARY.md` when done
</output>
