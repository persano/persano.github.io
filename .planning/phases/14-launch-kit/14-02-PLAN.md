---
phase: 14-launch-kit
plan: 02
type: execute
wave: 2
depends_on: ["14-01"]
files_modified:
  - ".planning/phases/14-launch-kit/14-RUNBOOK.md"
  - ".planning/phases/14-launch-kit/14-UAT.md"
  - ".planning/phases/14-launch-kit/14-RECORDS.md"
  - ".planning/milestones/v2.0-phases/10-gated-social-proof/10-RUNBOOK.md"
autonomous: true
requirements: [LKIT-01, LKIT-02, LKIT-04]
estimate:
  tokens: 44000
  raw_tokens: 28000
  tasks: 3
  confidence: med
must_haves:
  truths:
    - "14-RUNBOOK.md exists in the 13-RUNBOOK shape — Audience line, public-artifact notice (zero secrets), When-to-execute line, status legend, 6 numbered sections, italic trailer"
    - "The pinned flip order appears as the §3 sequence in exact order: step 1 privacy-URL field -> step 2 Play-link 200 verify -> step 3 website field -> step 4 Tier-1 rating flip (gated, may be a later day)"
    - "The swap-ready inventory table lists all 8 research surfaces, each with file:line (or console surface) + exact flip action or an explicit Zero action marker; every file:line re-verified against the tree at write time"
    - "GA4 section present as note-only (4 research Q3 bullets + the /geohist/ historical-path note) with zero code-change implication"
    - "JSON-LD offers refresh-check step present: Rich Results Test on https://geohisttrivia.com/ + the repointed zero-dependency one-liner reading index.html asserting no aggregateRating"
    - "10-RUNBOOK.md carries exactly 5 dated bracketed corrections (lines 3, 38, 80, after the line-84 code fence, 118) — originals verbatim, zero other bytes touched"
    - "14-UAT.md holds only locally-runnable rows; 14-RECORDS.md holds only post-launch rows marked pending-by-design with the do-not-fabricate notice"
  artifacts:
    - ".planning/phases/14-launch-kit/14-RUNBOOK.md — owner launch runbook (LKIT-01 §1/§3 + LKIT-02 §2 + LKIT-04a §4 + LKIT-04c §5 + §6 do-NOT-do guard)"
    - ".planning/milestones/v2.0-phases/10-gated-social-proof/10-RUNBOOK.md — 5 supersession appends (LKIT-04b)"
    - ".planning/phases/14-launch-kit/14-UAT.md — locally-runnable PRE battery scaffold (PRE-01..06)"
    - ".planning/phases/14-launch-kit/14-RECORDS.md — post-launch pending-by-design records scaffold (R-01..06)"
  key_links:
    - "14-RUNBOOK §3 step 4 <-> 10-RUNBOOK §1/§2 (cross-reference, not duplication — the dated corrections in 10-RUNBOOK are the bridge)"
    - "inventory table file:line rows <-> live tree (index.html 12-13, 20-35, 36-56, 53, 83-95; package.json validate chain; js/consent.js play_badge_click)"
    - "14-UAT PRE rows <-> 14-01 artifacts (npm run validate incl. validate:play-links; red-gate-proof.md closing hashes; repointed JSON-LD one-liner)"
    - "14-RECORDS rows <-> 14-RUNBOOK §3/§4/§5 sections (each record row names the runbook step it records)"
  assumptions:
    - "EA-06 [LKIT-01 / unclassified, unresolved -> decided]: the Play Console field flow for the privacy-URL and website fields was never verified end-to-end (STATE carry-over, research A1) — the runbook frames both as console-UI actions with expected outcomes + owner-verify language, NEVER screen-by-screen pixel instructions; a field rejection is a launch-day blocker discovered at the step, rollback = 'field not yet filled; investigate' (no site-side action — the privacy path is frozen and live)."
    - "EA-07 [LKIT-02 / adjacency, unresolved -> decided]: the inventory covers all 8 research surfaces INCLUDING adjacent non-file surfaces (2 Play Console fields, the Play listing liveness, GA4) so 'every Play-launch surface' is exhaustive — no surface outside the table."
    - "EA-08 [LKIT-02 / empty, unresolved -> decided]: a surface requiring no launch-day change is represented by an explicit `Zero action — verify only` marker, never an empty cell and never an omitted row."
    - "EA-09 [LKIT-02 / ordering, unresolved -> decided]: inventory rows numbered 1-8; §3 numbers the flips 1-4 in the pinned order; row 7 (Tier-1) is marked gate-driven watch-item — steps 1-3 close launch day, step 4 fires when real ratings exist (research Risk 9)."
    - "EA-10 [LKIT-02 / concurrency, unresolved -> decided]: the runbook declares ZERO code change on launch day (all 8 surfaces already swap-ready per research Q1/Q2) — the only future code touch is the Tier-1 two-edit flip per 10-RUNBOOK §2 (superseded paths)."
    - "EA-11 [LKIT-04 / unclassified, unresolved -> decided]: GA4 note and JSON-LD refresh-check are addenda-only — explicitly NOT a code/custom-dimension task and NOT an edit step (the offers node already ships price 0 + priceCurrency USD per research Q2; no availability/PreOrder key is added). Research A2 (page_view default params, Play-webview referrer classification) is phrased expect/verify, never as guarantees."
  prohibitions:
    - statement: "Never add aggregateRating to any JSON-LD on launch day or any day — Google review-snippet policy bars mirroring Play ratings even with real data; Tier-2 stays permanently OFF (10-RUNBOOK §6; in-file comment index.html lines 20-35)"
      status: unverified
      flagged: true
    - statement: "Never alter any original 10-RUNBOOK line — supersession is verbatim original + dated bracketed append only; zero other bytes touched"
      status: unverified
      flagged: true
    - statement: "Never gate the launch sequence on a calendar date and never skip or reorder the privacy-field-first step — compliance closes before cosmetic steps; the Tier-1 flip is evidence-gated (10-RUNBOOK §1), never date-gated"
      status: unverified
      flagged: true
---

<objective>
Author the Phase 14 launch kit documents: 14-RUNBOOK.md (owner launch-day runbook in the 13-RUNBOOK shape — pinned flip order, 8-surface swap-ready inventory, GA4 note, JSON-LD offers refresh-check, do-NOT-do guard), the 5 supersession appends in 10-RUNBOOK.md (paths now root), and the 14-UAT.md / 14-RECORDS.md scaffolds in the 12/13 reframe shape.

Purpose: the owner must execute Play launch day from ONE runbook — every surface verified, every flip in pinned order, nothing improvised (ROADMAP Phase 14 goal).

Output: 3 new planning docs + 5 verbatim-preserving corrections in 10-RUNBOOK.md. Zero code changes (the 14-01 gate is the phase's only code artifact).
</objective>

<execution_context>
@C:/Users/Familia/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/Familia/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/14-launch-kit/14-RESEARCH.md
@.planning/phases/14-launch-kit/red-gate-proof.md
@.planning/phases/13-home-migration/13-RUNBOOK.md
@.planning/milestones/v2.0-phases/10-gated-social-proof/10-RUNBOOK.md
@AGENTS.md
</context>

## Flagged Assumptions (spec-less probe contract)

All 6 LKIT-01/02/04-class probe rows are lifted into `must_haves.assumptions` (EA-06..EA-11) as decided behavior. Plan 14-01 carries EA-01..EA-05 (LKIT-03 + research OQ2); union across both plans = 11 rows covering all 10 probe edges. Prohibition-Probe Record: stage-1 raw candidates per the fallback contract — aggregateRating mirroring (KEPT, prohibition 1 — the runbook's §5/§6 and §6 guard are its enforcement surface); 10-RUNBOOK original-line rewrites (KEPT, prohibition 2 — this plan performs the only edit that file receives); calendar-gated launch ordering / privacy-field skip (KEPT, prohibition 3 — §3's ordering is its enforcement surface). Dropped as routine-engineering (already owned by gates/verification): stale Rich-Results URL (the rg zero-count criterion + the line-80 correction own it), inventory drift (the re-verify-before-write criterion owns it), secrets in docs (the rg scan + public-artifact notice own it). Canon security referral: none minted — doc-only plan creates no injection/PII/consent surface (OWASP canon not applicable).

## Artifacts this phase produces

- **New file:** `.planning/phases/14-launch-kit/14-RUNBOOK.md` (this plan — LKIT-01 + LKIT-02 + LKIT-04a + LKIT-04c)
- **New file:** `.planning/phases/14-launch-kit/14-UAT.md` (this plan — locally-runnable PRE battery scaffold)
- **New file:** `.planning/phases/14-launch-kit/14-RECORDS.md` (this plan — post-launch pending-by-design records scaffold)
- **Edited file:** `.planning/milestones/v2.0-phases/10-gated-social-proof/10-RUNBOOK.md` — 5 dated bracketed appends (this plan — LKIT-04b)
- **Referenced (created by 14-01, not modified here):** `scripts/check-play-link.mjs`, package.json `validate:play-links` key, `red-gate-proof.md` closing-hashes table

<tasks>

<task type="auto">
  <name>Task 1: Author 14-RUNBOOK.md — header, §1 prerequisites, §2 inventory, §3 pinned sequence, §4 GA4 note, §5 JSON-LD refresh-check, §6 guard, trailer</name>
  <files>.planning/phases/14-launch-kit/14-RUNBOOK.md</files>
  <read_first>.planning/phases/13-home-migration/13-RUNBOOK.md (FULL — header block, public-artifact notice line 5, status legend, §-numbered table shapes, §4 do-not-do model, italic trailer), .planning/phases/14-launch-kit/14-RESEARCH.md (Q1 8-surface inventory table + console-flow caveat, Q2 JSON-LD offers answer, Q3 GA4 four bullets, Q6 pinned-order rationale, Recommendations LKIT-01/02/04), .planning/milestones/v2.0-phases/10-gated-social-proof/10-RUNBOOK.md (§0/§1/§2 — cross-reference targets; §3 line 84 one-liner source), index.html (lines 8-13, 20-35, 36-56, 53, 83-95 — verify every file:line BEFORE writing the table), .planning/STATE.md (watch items lines 100-105; privacy frozen decision), .planning/research/PITFALLS.md (row 214 — no hl/gl params), AGENTS.md (.planning publicly served rule; no legacy-host literal convention)</read_first>
  <action>Author .planning/phases/14-launch-kit/14-RUNBOOK.md in the 13-RUNBOOK shape, console-UI + browser instructions only, ZERO secrets, ZERO legacy-host literals (phrase the dual-hosts fact as "legacy Pages host" — never the literal, never a wildcard form; repo gate + stricter plan convention).

  HEADER BLOCK: title `# Phase 14 Owner Runbook — Play Launch Day (Pinned Flip Order)`. Audience: Santiago (owner) — every step is a Play Console or browser action; agent-owned steps (validate chain, deploys) are cross-referenced only. Public-artifact notice: near-verbatim 13-RUNBOOK.md line 5 (the repo deploys the entire tree, so this file — like every .planning doc — is publicly served; console-UI instructions only, no secrets, no tokens, no credentials anywhere). When-to-execute: pre-staged here; steps 1-3 fire on launch day once the listing goes live; step 4 is gate-driven and may be a later day (watch-item semantics per 10-RUNBOOK §1 / STATE watch items) — nothing in this runbook is a deploy precondition. Status legend: ⬜ TODO (yours) · ✅ done (agent-verified) · 🔍 soft check (nice-to-have, non-blocking).

  §1 PREREQUISITES: table with # / Prerequisite / How you confirm / Status columns — (1) Phase 13 migration landed + Pages deploy green (Actions tab: latest main run validate ✓ then deploy ✓); (2) local `npm run validate` exit 0 — 6 stages including validate:play-links (the 14-01 package-id gate); (3) the privacy page renders live at https://geohisttrivia.com/geohist/privacy.html (frozen Play Console compliance surface); (4) EXPECTATION GUARD row: the package URL returning 404 pre-launch is the DESIGNED state (the D-22 pre-launch swap shipped the real package URL in all 3 code surfaces; only liveness changes at launch) — do not treat a pre-launch 404 as an issue.

  §2 SURFACE INVENTORY (LKIT-02): FIRST re-verify every file:line against the current tree (run rg -n for the package-id fragment over index.html — expect exactly lines 53, 83, 87; confirm the proof-row div carries `hidden` on line 86, the score span holds 0.0 on line 92, og:url on line 12, og:image on line 13) — write the VERIFIED numbers into the table (research numbers are as-of 2026-09-14; drift guard). THEN the 8-row table with columns # / Surface / Where it lives / Current state / Launch-day action, exactly the research Q1 rows: (1) Play Console privacy-policy URL field — paste https://geohisttrivia.com/geohist/privacy.html (path frozen per MIG-07); (2) Play listing liveness — the package URL https://play.google.com/store/apps/details?id=com.persano.geohisttrivia — owner verifies it returns the live listing (step 2 input); (3) Play Console website field — paste https://geohisttrivia.com/ (root = the GeoHist landing post-Phase-13); (4) Badge CTA — index.html lines 83-85 — `Zero action — verify only`; (5) JSON-LD offers + sameAs — index.html lines 36-56 — `Zero action — refresh-check only (§5)`; (6) og:url / og:image coherence — index.html lines 12-13 — `Zero action — verify only` (og:image intentionally stays under /geohist/ so the frozen privacy page references keep resolving); (7) Tier-1 rating row — index.html lines 86-95 — two edits per 10-RUNBOOK §2 (superseded paths — the dated corrections there point at root index.html); gated, may be a later day; (8) GA4 / Analytics — js/consent.js (load-gated Firebase Analytics; play_badge_click event) — `Zero action — note only (§4)`. After the table, one sentence (EA-10): ZERO code change on launch day — all 8 surfaces are already swap-ready.

  §3 THE PINNED SEQUENCE (LKIT-01) — exactly 4 steps in this order, each with expected outcome + owner-verify framing (EA-06): STEP 1 — privacy-URL field FIRST (compliance precondition closes before cosmetic steps): Play Console app content -> privacy policy URL field -> paste https://geohisttrivia.com/geohist/privacy.html. Expected: the field accepts and saves. State the carry-over caveat: this console flow was never verified end-to-end (bot-blocked during prior research) — verify the actual console screens yourself; if the field rejects the URL for any reason, that is a launch-day blocker discovered at the step and the rollback is simply "field not yet filled; investigate" (no site-side action; the path is frozen and live). STEP 2 — Play-link 200 verify SECOND: open https://play.google.com/store/apps/details?id=com.persano.geohisttrivia in a browser. Expected: the live listing page (the app page exists and is browsable). Note this is gate input #1 of the Tier-1 flip (10-RUNBOOK §1 condition 1). STEP 3 — website field THIRD: Play Console listing -> website field -> paste https://geohisttrivia.com/. Expected: accepts and saves. STEP 4 — Tier-1 rating flip LAST, GATED, may be a later day: both 10-RUNBOOK §1 conditions must hold (listing live + a real aggregate rating visible on the Play page; no minimum-count floor; never enter a number you did not see on the Play page itself). The two edits target ROOT index.html (the 10-RUNBOOK §2 paths are superseded — its dated corrections say so): remove the hidden attribute from the line-86 proof-row div; replace 0.0 in the line-92 proof-row-score span with the real rating in decimal dot format. Then deploy and run the §5 refresh ritual. Cross-reference 10-RUNBOOK §1/§2 instead of duplicating their text — the corrections there are the bridge. Explicitly state (EA-09): steps 1-3 close launch day; step 4 fires when the gate passes, even if that is days or weeks later.

  §4 GA4 NOTE (LKIT-04a) — note-only, the 4 research Q3 bullets: (1) default page_location already covers Play-launch attribution (every landing hit records page_location https://geohisttrivia.com/ and page_path /) — no code change, no custom dimension, no config flip; the note exists so the owner does NOT improvise one; (2) "came from Play" traffic reads via the consent-gated play_badge_click event count (page param = / post-migration) — the Play webview referrer commonly classifies as (direct); phrase expect/verify, never as a guarantee (A2); (3) locale split reads from the default browser-language signal plus the language_switch event — never from distinct page paths (single-URL i18n: there are none); (4) 🔍 soft check: GA4 Realtime during the launch window, consent-filtered by design. Plus the historical-path note (research Risk 10): pre-migration events carry /geohist/ as the page param — comparisons across the migration must account for the path change; no data migration.

  §5 JSON-LD OFFERS REFRESH-CHECK (LKIT-04c) — a VERIFICATION step, not an edit step: the offers node already ships `"offers": { "@type": "Offer", "price": "0", "priceCurrency": "USD" }` on index.html line 54 — zero data change, zero code change (EA-11). After the listing goes live: (1) Google Rich Results Test (search.google.com/test/rich-results) on https://geohisttrivia.com/ — NOT the stale /geohist/ path (superseded; the 10-RUNBOOK line-80 correction says so) — expect a valid SoftwareApplication result with no new errors; confirm the offers shape still reflects reality (app free at download; IAP lives inside the app and does not change the web offers node). (2) Zero-dependency local one-liner: the 10-RUNBOOK §3 command (line 84) with its SINGLE path argument changed from geohist/index.html to index.html — every other character byte-identical; write ONLY the repointed form into the runbook — never quote the stale path form anywhere in this file (the §-verify greps the file for zero occurrences of it); expect the OK line (JSON-LD parses; no aggregateRating key; type SoftwareApplication, MobileApplication); exit code 2 at any time means the rating key appeared in the schema: stop and revert (10-RUNBOOK §4), then check §6.

  §6 DO-NOT-DO GUARD: (a) no aggregateRating key ever — Tier-2 permanently OFF (10-RUNBOOK §6; Google review-snippet policy verbatim: "Don't aggregate reviews or ratings from other websites."); (b) no hl= or gl= experiment parameters on shipped Play URLs (PITFALLS.md row 214; the 14-01 gate would also FAIL any non-canonical form); (c) no Change-of-Address touch (13-RUNBOOK §4 guard stands; the 180-day window runs until ~2027-03); (d) no calendar-based flip — the rating flip is evidence-gated only (prohibition 3).

  TRAILER: italic footer line `*Phase 14 · Launch Kit · runbook authored <execution date YYYY-MM-DD> by plan 14-02 (Task 1) · steps 1-3 fire launch day, step 4 gate-driven · outcomes recorded via 14-RECORDS.md*`.

  Nothing in this file commits — deferred-commit mode; ship lands it with the docs commit.</action>
  <verify>
    <automated>rg -c "^## §" .planning/phases/14-launch-kit/14-RUNBOOK.md (prints 6) && rg -n "https://geohisttrivia.com/geohist/privacy.html" .planning/phases/14-launch-kit/14-RUNBOOK.md (present, §2 row 1 + §3 step 1) && rg -c "geohist/index.html" .planning/phases/14-launch-kit/14-RUNBOOK.md (prints 0 — stale path absent, one-liner repointed) && rg -ci "github\.io" .planning/phases/14-launch-kit/14-RUNBOOK.md (prints 0) && rg -in "api[_-]?key|password|bearer|AKIA|ghp_|AIza" .planning/phases/14-launch-kit/14-RUNBOOK.md (prints 0 matches)</automated>
  </verify>
  <acceptance_criteria>
    - 14-RUNBOOK.md exists and contains exactly 6 `## §` headings (§1 prerequisites, §2 inventory, §3 sequence, §4 GA4, §5 refresh-check, §6 guard)
    - §3 presents steps 1-4 in the pinned order (privacy-URL field -> Play-link 200 verify -> website field -> Tier-1 flip) and step 4 carries the gated/may-be-a-later-day framing with the no-minimum-floor language
    - §2 table has exactly 8 numbered rows; rows 4, 5, 6, 8 each carry an explicit Zero action marker; every file:line citation matches the current tree (rg -n package-id fragment over index.html returns lines 53, 83, 87)
    - §5 contains the Rich Results Test URL https://geohisttrivia.com/ and the price-0/USD reality note; its one-liner reads index.html (rg count of geohist/index.html in the file prints 0)
    - §6 contains all 4 guards (aggregateRating permanent OFF, no hl/gl params, no Change-of-Address, no calendar-based flip)
    - `rg -ci "github\.io" .planning/phases/14-launch-kit/14-RUNBOOK.md` prints 0; secrets scan (api[_-]?key|password|bearer|AKIA|ghp_|AIza, case-insensitive — shapes of real credentials, not the notice's own wording) prints 0 matches
    - Header carries the public-artifact notice and the When-to-execute framing (nothing is a deploy precondition)
  </acceptance_criteria>
  <done>Owner launch runbook complete in the 13-RUNBOOK shape: pinned order explicit, 8-surface inventory with verified file:line citations, GA4 note, JSON-LD refresh-check with repointed one-liner, do-not-do guard, zero secrets, zero legacy-host literals.</done>
  <reversibility rating="reversible">Additive doc; edit/revert freely; no code or gate state touched.</reversibility>
</task>

<task type="auto">
  <name>Task 2: 10-RUNBOOK.md supersession — 5 dated bracketed appends (LKIT-04b, originals verbatim)</name>
  <files>.planning/milestones/v2.0-phases/10-gated-social-proof/10-RUNBOOK.md</files>
  <read_first>.planning/milestones/v2.0-phases/10-gated-social-proof/10-RUNBOOK.md (FULL — verify the 5 stale spots at lines 3, 38, 80, 84-85, 118 yourself and the non-stale sections that must stay untouched: §0 table, §1, §4, §5), .planning/phases/14-launch-kit/14-RESEARCH.md (Q5 table — the 5 stale lines + correction wording + mechanics), AGENTS.md (supersession-note policy bullet), .planning/phases/14-launch-kit/14-RUNBOOK.md (Task 1 — the corrections' cross-references must match what the new runbook says)</read_first>
  <action>Append exactly 5 dated bracketed corrections to 10-RUNBOOK.md. SUPERSESSION POLICY (AGENTS.md): original text stays verbatim — each correction APPENDS on the same line (or, for the code-fence spot, on the line immediately after the closing fence); never rewrite, reorder, or delete any original byte. Use the execution date in every bracket: [corrected Phase 14, <execution date YYYY-MM-DD>: ...]. Cross-reference .planning/phases/14-launch-kit/14-RUNBOOK.md §3 step 4 in the line-3 and line-38 corrections so the owner lands on the new runbook.

  SPOT 1 — line 3 (Audience sentence): after "...(two tiny edits, §2) plus browser tools you already use." append: [corrected Phase 14, <date>: the landing now lives at root `index.html` (Phase 13 home migration) — same two edits, new file path; see .planning/phases/14-launch-kit/14-RUNBOOK.md §3 step 4].

  SPOT 2 — line 38 (§2 heading): after "## §2 · The flip — exactly two edits in `geohist/index.html`" append: [corrected Phase 14, <date>: now `index.html` at repo root — same two edits].

  SPOT 3 — line 80 (§3 step 1 Rich-Results URL): after "test `https://geohisttrivia.com/geohist/`" append: [corrected Phase 14, <date>: test `https://geohisttrivia.com/` — the landing moved to root in Phase 13; `/geohist/` is the meta-refresh-0 stub].

  SPOT 4 — line 84 (the node one-liner INSIDE the powershell fence): DO NOT edit the line itself — the owner copies that command verbatim at flip time and a trailing bracket would corrupt it. Instead insert ONE bracketed correction line immediately AFTER the closing fence of that code block (the line after line 85), reading: [corrected Phase 14, <date>: the one-liner above now reads `index.html` in its readFileSync path argument — the landing is at root; copy the command with the path `index.html`]. This insertion is an append at the stale spot (original bytes preserved); the command line itself stays byte-identical.

  SPOT 5 — line 118 (§6 mirror sentence): after "Mirrors the in-file HTML comment next to the JSON-LD block in `geohist/index.html`" append: [corrected Phase 14, <date>: the comment now lives in root `index.html` (lines 20-35) — same rule, two surfaces].

  NOTHING ELSE: §0 table content is path-neutral (do not touch), §1 gate text path-free (do not touch), §2 code snippets show line content only (do not touch), §4/§5 path-free (do not touch). The file's frontmatter (if any) and the ship-record paragraph stay untouched. Zero other bytes change.</action>
  <verify>
    <automated>rg -c "corrected Phase 14" .planning/milestones/v2.0-phases/10-gated-social-proof/10-RUNBOOK.md (prints exactly 5) && rg -c "geohist/index.html" .planning/milestones/v2.0-phases/10-gated-social-proof/10-RUNBOOK.md (prints exactly 4 — all original occurrences intact) && rg -n "corrected Phase 14" .planning/milestones/v2.0-phases/10-gated-social-proof/10-RUNBOOK.md (5 hits at the §-spots: audience line, §2 heading, §3 step 1, after the one-liner fence, §6)</automated>
  </verify>
  <acceptance_criteria>
    - `rg -c "corrected Phase 14"` on the file prints exactly 5
    - `rg -c "geohist/index.html"` on the file prints exactly 4 — every original occurrence intact (verbatim policy; corrections never repeat the stale path)
    - The §2 heading line still begins with the original text `## §2 · The flip — exactly two edits in `geohist/index.html`` (rg -n confirms original prefix + appended bracket)
    - The line-84 node one-liner is byte-unchanged (its correction sits on the line after the closing fence; git diff on the file shows the insertion as an added line, not a modification of line 84)
    - `git diff` on the file shows changes ONLY at the 5 declared spots (lines 3, 38, 80, the line-85/86 fence region, 118) — zero other hunks
    - Full `npm run validate` still exits 0 (the appends introduce no gate-visible change)
  </acceptance_criteria>
  <done>5 dated corrections appended at the exact stale spots; originals verbatim; the 14-RUNBOOK cross-references bridge the owner from the corrected spots to the new runbook.</done>
  <reversibility rating="reversible">Appends only — original bytes preserved by construction; revert = delete the 5 bracketed spans.</reversibility>
</task>

<task type="auto">
  <name>Task 3: 14-UAT.md + 14-RECORDS.md scaffolds (locally-runnable battery / post-launch pending rows)</name>
  <files>.planning/phases/14-launch-kit/14-UAT.md, .planning/phases/14-launch-kit/14-RECORDS.md</files>
  <read_first>.planning/phases/13-home-migration/13-UAT.md (FULL — frontmatter, reframe note, public-artifact notice, mechanical predicate line 15, PRE row shape, summary block), .planning/phases/13-home-migration/13-RECORDS.md (FULL — scaffold notice, do-not-fabricate framing, pending status legend, R row shape), .planning/phases/14-launch-kit/14-RUNBOOK.md (Task 1 — every R row names the §-step it records), .planning/phases/14-launch-kit/red-gate-proof.md (14-01 artifact — PRE-03 cites its closing-hashes table), .planning/phases/14-launch-kit/14-RESEARCH.md (Q7 item 4 — the UAT/RECORDS split)</read_first>
  <action>Author both scaffolds in the 12/13 reframe shape. NEITHER file is executed during this plan — rows are pre-staged (14-UAT rows execute at ship preflight; 14-RECORDS rows execute post-launch at owner time). Do NOT fill any row with an outcome (do-not-fabricate).

  14-UAT.md: frontmatter — status: pending, phase: 14-launch-kit, source: [14-02-PLAN.md], started + updated = execution date. Title: `# Phase 14 UAT — Locally-Runnable Verification Battery (PRE-01..06)`. Reframe note (12/13 precedent): post-launch rows live in 14-RECORDS.md; this file holds the pre-ship, locally-runnable battery — rows marked `status: pending — executes at ship preflight`. Public-artifact notice (same near-verbatim wording as 13-UAT.md). Mechanical predicate verbatim-style from 13-UAT.md line 15 (any recorded issue is a blocker — no gap-awareness; a row that later re-verifies green keeps its supersession note). Then 6 rows, each with check / expected / status / result lines:
  - PRE-01: check `npm run validate` — expected exit 0, six stages, tail shows the play-links gate OK + keycheck PASS x19.
  - PRE-02: check `node scripts/check-play-link.mjs` — expected `check-play-link: OK`, exit 0.
  - PRE-03: check recompute sha256 (Get-FileHash) of scripts/check-play-link.mjs, package.json, index.html and compare against the closing-hashes table in red-gate-proof.md — expected all prefixes equal (red-gate integrity vs current working-tree bytes).
  - PRE-04: check the repointed JSON-LD one-liner (the 10-RUNBOOK §3 command with path index.html) — expected the OK line: JSON-LD parses; no aggregateRating key; type SoftwareApplication, MobileApplication.
  - PRE-05: check the 10-RUNBOOK supersession state — `rg -c "corrected Phase 14"` prints 5 AND `rg -c "geohist/index.html"` prints 4 — expected both (corrections present, originals verbatim).
  - PRE-06: check 14-RUNBOOK content — 6 §-headings; 8-row inventory; steps 1-4 in pinned order; secrets scan (case-insensitive: api[_-]?key|password|bearer|AKIA|ghp_|AIza) 0 matches; `rg -ci "github\.io"` over the three new 14-* docs prints 0.
  Close with the Summary skeleton (total: 6, passed/issues/pending/skipped/blocked all to fill) and the Gaps section placeholder, then the italic trailer line in the 13-UAT shape.

  14-RECORDS.md: frontmatter — status: pending, phase: 14-launch-kit, source: [14-02-PLAN.md], started + updated = execution date. Title: `# Phase 14 Post-Launch Records — Owner-Executed Checks (LKIT-01 flips)`. Reframe note (12/13 precedent: post-launch rows live here; UAT holds locally-runnable rows). Scaffold notice verbatim-style from 13-RECORDS.md: every row below is pre-staged and marked pending — Do NOT fabricate outcomes; rows fill in only when executed post-launch; nothing here is a deploy precondition. Mechanical predicate (any recorded issue is a blocker). Status legend per row: `status: pending — executes post-launch at owner time`. Then 6 rows (check / expected / status / result), each naming its 14-RUNBOOK section:
  - R-01: privacy-URL field — owner console step per 14-RUNBOOK §3 step 1 — record the observed console outcome (accepted / rejected + any console message); a rejection records as an issue per the mechanical predicate.
  - R-02: Play-link liveness — per §3 step 2 — record the date the package URL https://play.google.com/store/apps/details?id=com.persano.geohisttrivia first returned the live listing.
  - R-03: website field — per §3 step 3 — record the observed console outcome.
  - R-04: Rich Results Test on https://geohisttrivia.com/ post-launch — per §5 — record the SoftwareApplication result and any new errors.
  - R-05: 🔍 GA4 Realtime observation during the launch window (optional soft check, consent-filtered by design) — per §4.
  - R-06: Tier-1 rating flip record — fires when the 10-RUNBOOK §1 gate passes (may be much later; watch-item semantics) — per §3 step 4 + the superseded 10-RUNBOOK §2 — record the actual rating value as seen on the Play page (decimal dot), the flip date, the deploy run, and the §5 ritual result.
  Close with the Summary skeleton (total: 6, all pending) and the italic trailer line in the 13-RECORDS shape.

  ZERO secrets in both files; zero legacy-host literals (including wildcard forms — phrase as "legacy Pages host" if ever needed).</action>
  <verify>
    <automated>Test-Path .planning/phases/14-launch-kit/14-UAT.md && Test-Path .planning/phases/14-launch-kit/14-RECORDS.md && rg -c "^### " .planning/phases/14-launch-kit/14-UAT.md (prints 6) && rg -c "^### " .planning/phases/14-launch-kit/14-RECORDS.md (prints 6) && rg -n "result: pass" .planning/phases/14-launch-kit/14-RECORDS.md (prints nothing — zero fabricated outcomes) && rg -ci "github\.io" .planning/phases/14-launch-kit/14-UAT.md .planning/phases/14-launch-kit/14-RECORDS.md (prints 0) && rg -in "api[_-]?key|password|bearer|AKIA|ghp_|AIza" .planning/phases/14-launch-kit/14-UAT.md .planning/phases/14-launch-kit/14-RECORDS.md (prints 0 matches)</automated>
  </verify>
  <acceptance_criteria>
    - Both files exist with frontmatter status: pending, phase: 14-launch-kit, source: [14-02-PLAN.md]
    - 14-UAT.md contains exactly 6 PRE rows and every row's check is locally runnable (zero live-URL fetches in any UAT check)
    - 14-RECORDS.md contains exactly 6 R rows, each marked pending with the do-not-fabricate scaffold notice; zero `result:` outcome lines are pre-filled anywhere in it
    - Both files carry the public-artifact notice and the mechanical predicate (any recorded issue is a blocker)
    - Each R row names the 14-RUNBOOK §-step it records (R-01 §3 step 1 ... R-06 §3 step 4)
    - Secrets scan (api[_-]?key|password|bearer|AKIA|ghp_|AIza, case-insensitive — real-credential shapes, not the notice's own wording) over both files prints 0 matches; `rg -ci "github\.io"` over both prints 0
  </acceptance_criteria>
  <done>Both scaffolds in the 12/13 reframe shape — UAT battery ready to execute at ship preflight, RECORDS rows pre-staged pending for owner launch-day execution; nothing fabricated.</done>
  <reversibility rating="reversible">Additive scaffold docs; edit/revert freely; no gate or code state touched.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planning docs -> publicly served Pages artifact | 14-RUNBOOK/14-UAT/14-RECORDS ship publicly; console-UI instructions only, zero secrets |
| supersession edits -> historical record integrity | an accidental rewrite of an original 10-RUNBOOK line would destroy the verbatim record the repo convention protects |
| runbook steps -> launch-day behavior | a stale Rich-Results URL or reordered flips would mislead the owner mid-launch (spoofed instruction surface) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-04 | Information Disclosure | 14-RUNBOOK.md + 14-UAT.md + 14-RECORDS.md | low | mitigate | public-artifact notice in each file; rg secrets scan prints 0 matches as an acceptance criterion |
| T-14-05 | Tampering | 10-RUNBOOK.md supersession | high | mitigate | originals-verbatim enforced by grep criteria (4 stale-path occurrences intact + exactly 5 corrections + diff bounded to 5 spots); prohibition 2 |
| T-14-06 | Spoofing | stale launch-day instructions (Rich-Results URL, flip order) | medium | mitigate | rg -c "geohist/index.html" == 0 in 14-RUNBOOK; §3 steps numbered 1-4 in the pinned order; line-80 correction points at the root URL |
</threat_model>

<verification>
1. 14-RUNBOOK.md: 6 §-sections, pinned order in §3, 8-row inventory with verified file:line citations, GA4 note, JSON-LD refresh-check (repointed one-liner), 4-guard §6, zero secrets, zero legacy-host literals (LKIT-01, LKIT-02, LKIT-04a, LKIT-04c)
2. 10-RUNBOOK.md: exactly 5 `corrected Phase 14` appends; `geohist/index.html` count still 4 (originals verbatim); diff bounded to the 5 declared spots (LKIT-04b)
3. 14-UAT.md: 6 locally-runnable PRE rows, all pending, none executed during this plan
4. 14-RECORDS.md: 6 pending R rows with the do-not-fabricate notice; zero pre-filled outcomes
5. `npm run validate` exit 0 on the final tree (docs changes gate-clean; the 14-01 chain stays green)
6. Cross-references coherent: 14-RUNBOOK §3 step 4 <-> 10-RUNBOOK corrections <-> 14-RECORDS R-06
</verification>

<success_criteria>
- All three tasks done with all acceptance criteria green
- Working tree holds the complete plan output uncommitted, ready for ship: 3 new 14-* docs + 5 appends in 10-RUNBOOK.md
- Ship subject suggestion: `docs(14): launch kit — owner runbook, 10-RUNBOOK supersession, UAT/RECORDS scaffolds`
- LKIT-01, LKIT-02, LKIT-04 satisfied locally; LKIT-03 satisfied by 14-01 (its gate artifacts are prerequisites of this plan's UAT rows)
- Phase 14 complete: every roadmap requirement ID covered across the two plans
</success_criteria>

<output>
Create `.planning/phases/14-launch-kit/14-02-SUMMARY.md` when done
</output>
