---
phase: 14-launch-kit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - "scripts/check-play-link.mjs"
  - "package.json"
  - ".planning/phases/14-launch-kit/red-gate-proof.md"
autonomous: true
requirements: [LKIT-03]
estimate:
  tokens: 56000
  raw_tokens: 34000
  tasks: 2
  confidence: med
must_haves:
  truths:
    - "`node scripts/check-play-link.mjs` exits 0 printing `check-play-link: OK` on the current tree — the two bare-domain mentions (package.json:10 --skip flag, index.html:23 comment prose) do NOT trip the URL-shaped needle (positive control)"
    - "Any walked text file whose URL-shaped play.google.com URL lacks the details?id=com.persano.geohisttrivia parameter fails with a file:line hit list on stderr and exit 1"
    - "package.json runs validate:play-links between validate:domain and validate:links; full `npm run validate` (6 stages) exits 0"
    - "red-gate-proof.md records all 6 research cycles (Q4 table) with observed outputs and sha256-verified snapshot-copy restores; at close the working tree is byte-identical to the Task 1 state"
    - "http:// scheme Play URLs also fail the gate (PITFALLS.md:163 recommendation adopted in full)"
  artifacts:
    - "scripts/check-play-link.mjs — NEW zero-dependency gate (node:fs/node:path only): repo walk + ALLOW set + hidden-entry skip + NUL-byte binary skip + per-line URL-shaped needle + package-id requirement after &amp;/&#38; normalization + http-scheme rejection + file:line stderr reporting"
    - "package.json — NEW script key `validate:play-links` (immediately after the validate:domain key) + `validate` chain extended from 5 to 6 stages"
    - ".planning/phases/14-launch-kit/red-gate-proof.md — 6-cycle both-direction evidence with pre/post sha256 hash table"
  key_links:
    - "package.json `validate` chain -> validate:play-links -> scripts/check-play-link.mjs (every push runs the gate in CI)"
    - "gate needle <-> index.html lines 53/83/87 — the 3 canonical id-bearing URLs must stay PASS through every red-gate cycle"
    - "red-gate-proof.md closing hash table <-> scripts/check-play-link.mjs + package.json + index.html (byte-identical close state)"
    - "script header scope note <-> check-no-old-domain.mjs ALLOW pattern (same walk semantics; .planning excluded — verbatim-immutability)"
  assumptions:
    - "EA-01 [LKIT-03 / adjacency, unresolved -> decided]: gate scope = the tracked-text walk with ALLOW {'.planning','README.md','.git','node_modules'} + hidden-entry skip. .planning/ historical docs carrying legitimate id-less Play URLs (v1 02-RESEARCH.md:439 badge PNG intl URL; PITFALLS.md:157 details?id=...&hl= illustration) stay OUT of scope — decided per research Q4 (verbatim-immutability + vacuous-gate prevention) and documented in the script header so a future reader does not 'fix' it."
    - "EA-02 [LKIT-03 / empty, unresolved -> decided]: a tree with ZERO URL-shaped Play URLs exits 0 printing OK — no special case, no warning."
    - "EA-03 [LKIT-03 / ordering, unresolved -> decided]: per-line sequential scan; hit-list ordering carries no semantics; duplicate URL occurrences are each independently required to carry the package id (no dedupe)."
    - "EA-04 [LKIT-03 / concurrency, unresolved -> decided]: static single-pass scan, no concurrency semantics; the gate reads file text only (never HTML-parses), so malformed HTML cannot crash it."
    - "EA-05 [research Open Question 2 -> decided]: http:// scheme matches fail the gate (research recommends yes; PITFALLS.md:163 recommendation adopted in full — one extra condition in the same match loop)."
  prohibitions:
    - statement: "Never mutate the validate:links --skip play.google.com flag or the chain position of any existing stage — linkinator stays liveness-skipped pre-launch; removing the skip turns CI red pre-launch (vacuous-gate inversion, research Risk 5)"
      status: unverified
      flagged: true
    - statement: "Never extend the gate ALLOW set beyond {'.planning','README.md','.git','node_modules'} — adding .planning to gate scope forces FAIL on legitimate historical id-less URLs or forbidden edits of verbatim-immutable records (research Risk 3)"
      status: unverified
      flagged: true
---

<objective>
Ship the LKIT-03 CI gate end-to-end: `scripts/check-play-link.mjs` (every URL-shaped play.google.com URL in walked tracked text files must carry `details?id=com.persano.geohisttrivia`), wired into package.json between validate:domain and validate:links, proven in both directions with 6 red-gate cycles recorded in `red-gate-proof.md`.

Purpose: the gate is the mechanical invariant behind the launch runbook — nobody can add a Play URL to the served tree without the package id (PITFALLS.md:163), and the launch-day surfaces stay pinned to the canonical package URL with zero code change on launch day.

Output: green gate + 6-stage validate chain + both-direction evidence. LKIT-01/02/04 artifacts are authored by 14-02 (wave 2) and reference this plan's gate.
</objective>

<execution_context>
@C:/Users/Familia/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/Familia/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/14-launch-kit/14-RESEARCH.md
@AGENTS.md
</context>

## Flagged Assumptions (spec-less probe contract)

All 5 LKIT-03-class probe rows are lifted into `must_haves.assumptions` (EA-01..EA-05) as decided behavior — each names the unresolved probe and the explicit disposal. The other 5 phase rows (LKIT-01/02/04) ride 14-02-PLAN.md as EA-06..EA-11; union across both plans = 11 rows (10 probe edges + research OQ2). Prohibition-Probe Record: stage-1 raw candidates evaluated per the fallback contract — aggregateRating mirroring (dropped here: no JSON-LD surface in this plan; carried by 14-02); 10-RUNBOOK original-line rewrites (dropped here: file not touched by this plan; carried by 14-02); calendar-gated launch ordering (dropped here: no runbook authored here; carried by 14-02); validate:links skip removal (KEPT — this plan owns the chain edit, the skip-preservation is its safety fence); ALLOW-set extension (KEPT — this plan authors the scope, the freeze is its safety fence). Canon security referral: none minted — a read-only text scanner creates no injection/PII/consent surface (OWASP canon not applicable).

## Artifacts this phase produces

- **New file:** `scripts/check-play-link.mjs` (this plan — the only code artifact of Phase 14)
- **New package.json entries:** `validate:play-links` script key + 6-stage `validate` chain (this plan)
- **New planning artifact:** `.planning/phases/14-launch-kit/red-gate-proof.md` (this plan)
- **New planning artifacts by 14-02:** `14-RUNBOOK.md`, `14-UAT.md`, `14-RECORDS.md` + 5 supersession appends in `10-RUNBOOK.md`
- **New symbol:** the `check-play-link` gate name (stdout OK line + package.json key) — referenced by 14-02's UAT rows and runbook §1 prerequisite

<tasks>

<task type="tracer">
  <name>Task 1: Tracer — play package-id gate end-to-end (script + validate chain slot + positive control green)</name>
  <files>scripts/check-play-link.mjs, package.json</files>
  <read_first>scripts/check-no-old-domain.mjs (FULL — copy the walk/ALLOW/hidden-skip/NUL-skip/repoRoot patterns verbatim), package.json (line 5 validate chain, line 7 validate:domain key, line 10 validate:links — DO NOT TOUCH), .planning/phases/14-launch-kit/14-RESEARCH.md (Q4 gate design: needle regex, normalization, scope decision, chain position; Risks 1-3; Recommendations LKIT-03), index.html (lines 20-35 bare comment prose, lines 53/83/87 canonical id-bearing URLs), .planning/research/PITFALLS.md (Pitfall 8 + rows 154-163 + row 214 — package-id gate design + no hl/gl params)</read_first>
  <action>Create `scripts/check-play-link.mjs`, zero-dependency (node:fs, node:path only), structurally mirroring scripts/check-no-old-domain.mjs:

  HEADER COMMENT: purpose (Phase 14, LKIT-03 — every URL-shaped play.google.com URL in walked text files must carry details?id=com.persano.geohisttrivia); SCOPE NOTE stating .planning/ is excluded via ALLOW because historical planning records are verbatim-immutable and legitimately contain id-less Play URLs (EA-01) — write it so a future reader does not "fix" the exclusion; self-pass note (the script's own source spells the full canonical package URL wherever a URL-shaped literal appears — it self-scans and must self-pass).

  CONSTANTS: ALLOW = new Set with exactly '.planning', 'README.md', '.git', 'node_modules' (same order/values as check-no-old-domain.mjs line 41); the package-id literal com.persano.geohisttrivia as a named constant; the required URL fragment is the 11-char parameter prefix details?id= immediately followed by that constant.

  REPO ROOT: from process.argv[1] via join(dirname(argv[1]), '..') — cwd-independent, identical to check-no-old-domain.mjs lines 45-47.

  SCAN (scanFile): NUL-byte probe in the first 8192 bytes -> treat as binary and skip (identical loop to check-no-old-domain.mjs lines 63-66); split buffer on /\r\n|\n|\r/; PER LINE extract every match of the URL-shaped needle regex /play\.google\.com\/[^\s"'<>\\)]+/g (exact form from research Q4 — the slash-path requirement is the discriminator that keeps the package.json:10 bare --skip flag and the index.html:23 comment prose as NON-hits; a naive substring includes would red the positive control). For each match: normalize HTML-escaped ampersands — replace every &amp; with & and every &#38; with & (2 chained replacements, belt-and-suspenders for hypothetical escaped forms; the id token itself never escapes because the canonical URL puts id first). The match FAILS iff (a) after normalization it does not contain details?id= followed by the package-id constant, OR (b) it begins with the http:// scheme (http://play.google.com — EA-05, PITFALLS.md:163). On fail: record a hit as displayPath:lineNumber plus the offending match text.

  WALK: identical recursion to check-no-old-domain.mjs walk() — ALLOW check first, hidden-entry skip (entry.name starts with a dot), directory recursion, file scan; unreadable entries produce a WARN line and are skipped, never fatal.

  OUTPUT/EXIT: hits -> console.error('Play URLs missing package id:'), one stderr line per hit (file:line + match excerpt), then a FAIL guidance line naming the canonical form, exit 1. Clean -> console.log('check-play-link: OK'), exit 0. Header comment documents the zero-hit-set acceptance (never a magic ref count).

  PACKAGE.JSON (2 edits only): (1) insert the script key "validate:play-links" with value node scripts/check-play-link.mjs immediately after the "validate:domain" key (line 7); (2) extend the "validate" chain (line 5) so it reads: npm run validate:html && npm run validate:domain && npm run validate:play-links && npm run validate:links && npm run validate:i18n-detect && npm run validate:i18n. DO NOT touch the "validate:links" value (its --skip play.google.com flag stays byte-identical — prohibition 1; linkinator stays liveness-skipped pre-launch, the new gate owns the shape invariant). Do not reformat any other package.json byte.

  POSITIVE CONTROL (pre-mutation proof): run the gate on the finished tree — it must print OK because the walk sees exactly 3 URL-shaped matches (index.html:53, 83, 87 — all id-bearing, self-passing script source included) and the 2 bare mentions (index.html:23 prose, package.json:10 skip flag) do not match the URL-shaped needle. Then `npm run validate` — 6 stages, exit 0.

  DEFERRED-COMMIT MODE: nothing commits in this task; /gsd-ship lands the working tree with the planned per-task subjects.</action>
  <verify>
    <automated>node scripts/check-play-link.mjs (stdout `check-play-link: OK`, exit 0) && npm run validate:play-links (exit 0) && npm run validate (exit 0, six stages with validate:play-links between validate:domain and validate:links) && git diff --stat -- index.html (empty — zero landing changes)</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/check-play-link.mjs` prints `check-play-link: OK` and exits 0 on the current tree
    - `npm run validate` exits 0 and its chain contains exactly 6 stages in the order html, domain, play-links, links, i18n-detect, i18n
    - package.json diff is exactly 2 hunks: the new `validate:play-links` key after `validate:domain`, and the extended `validate` chain; the `validate:links` value is byte-unchanged
    - index.html is untouched by this task (`git diff -- index.html` prints nothing)
    - The script source contains zero URL-shaped play.google.com literals lacking the package id (it is scanned by its own walk and passes)
    - The ALLOW set in the script is exactly {'.planning', 'README.md', '.git', 'node_modules'} and the header documents the .planning exclusion rationale (EA-01)
  </acceptance_criteria>
  <done>Gate wired end-to-end: script created, chain slot live, positive control green (bare mentions do not trip; 3 canonical URLs pass), full 6-stage validate green, tree uncommitted awaiting ship.</done>
  <reversibility rating="reversible">Additive: one new script + one JSON key + one chain stage. Revert = delete the file, remove the key, restore the 5-stage chain — no shared state touched.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Red-gate proof — 6 cycles both directions to red-gate-proof.md (snapshot-copy restore, hash-verified)</name>
  <files>.planning/phases/14-launch-kit/red-gate-proof.md</files>
  <read_first>.planning/phases/14-launch-kit/14-RESEARCH.md (Q4 red-gate table — the 6 cycles, mutations and expected results verbatim), .planning/phases/13-home-migration/red-gate-proof.md (record format precedent + the per-file-distinct-snapshot-names lesson in its Deviation note), AGENTS.md (red-gate proof convention bullet + supersession policy), scripts/check-play-link.mjs (Task 1 state — the gate under proof), index.html (lines 83 and 87 — the two mutation targets)</read_first>
  <action>Prove the Task 1 gate both directions, recording every cycle in .planning/phases/14-launch-kit/red-gate-proof.md. METHOD (AGENTS.md red-gate convention, Phase 13 precedent): snapshot-copy each target file with Copy-Item BEFORE mutating, restore AFTER, verify sha256 equality with Get-FileHash. Snapshot copies go under C:/Users/Familia/AppData/Local/Temp/opencode/redgate14/ — OUTSIDE the repo (never walked, never committed), ONE distinct destination name per source file (Phase 13 lesson: multi-source Copy-Item flattens same-named files — root index.html and any other index.html must never share a snapshot name). `git restore` is FORBIDDEN anywhere in this task: the tree is dirty with deferred-commit work and index-based restore would destroy it (Phase 13 red-gate-proof.md Method paragraph). Zero VCS mutation/restore commands of any kind.

  PRE-MUTATION HASH TABLE: capture sha256 (16-hex prefixes are enough in the table, full hashes captured at snapshot time) of scripts/check-play-link.mjs, package.json, index.html — this is the Task 1 close state and the restore target for every cycle.

  CYCLES (research Q4 table, in order):
  1. POSITIVE CONTROL (no mutation): run `node scripts/check-play-link.mjs` on the Task 1 tree -> expect PASS (`check-play-link: OK`) — proves the bare mentions (package.json:10 --skip flag, index.html:23 comment prose) do not trip the URL-shaped needle. Record output.
  2. FAIL DIRECTION (wrong id): snapshot index.html under a distinct name; mutate line 83 href id value com.persano.geohisttrivia -> com.example.wrong; run gate -> expect FAIL with the hit list naming index.html:83; restore hash-verified; re-run -> PASS.
  3. FAIL DIRECTION (id removed): mutate line 87 href to the bare path https://play.google.com/store/apps/ (no query string); run gate -> expect FAIL naming index.html:87; restore hash-verified; re-run -> PASS.
  4. ADJACENT-PARAM PASS: mutate line 83 href appending &hl=en after the id (details?id=com.persano.geohisttrivia&hl=en); run gate -> expect PASS (params allowed, id mandatory); restore hash-verified; re-run -> PASS.
  5. ESCAPED-AMPERSAND PASS: mutate line 83 href to the HTML-escaped form ...trivia&amp;hl=en; run gate -> expect PASS after normalization (EA: escaped forms must not false-fail); restore hash-verified; re-run -> PASS.
  6. NON-CANONICAL FORM FAIL: create scratch file geohist/gate-probe.html containing one anchor whose href is the URL-shaped download form play.google.com/store/apps/download?id=com.persano.geohisttrivia (URL-shaped, has an id query, but NOT the details?id= form); run gate -> expect FAIL (the canonical details?id= form is the only sanctioned one — PITFALLS.md:163); DELETE the scratch file; re-run -> PASS. Confirm the scratch file is gone at close (Test-Path false).

  CLOSING CHECKS: recompute sha256 of scripts/check-play-link.mjs, package.json, index.html — all equal to the pre-mutation table; `node scripts/check-play-link.mjs` -> OK; `npm run validate` -> exit 0. Delete the snapshot temp dir after the closing hash verification.

  RECORD FORMAT (mirror .planning/phases/13-home-migration/red-gate-proof.md): header stating executed date, method (snapshot-copy, git-restore-forbidden rationale, per-file distinct names), the pre-mutation hash table, then one section per cycle with the mutation description, the observed gate output verbatim, the restore hash prefix, and the post-restore PASS line; close with an end-state section (hash table equality + validate exit 0).</action>
  <verify>
    <automated>Test-Path .planning/phases/14-launch-kit/red-gate-proof.md && node scripts/check-play-link.mjs (OK, exit 0) && npm run validate (exit 0) && Test-Path geohist/gate-probe.html (must be False)</automated>
  </verify>
  <acceptance_criteria>
    - red-gate-proof.md contains 6 cycle records: cycle 1 positive-control PASS; cycles 2-3 FAIL records naming index.html:83 and index.html:87 respectively, each followed by a restore + PASS record; cycles 4-5 PASS-then-PASS records (adjacent param + escaped ampersand); cycle 6 scratch-file FAIL record followed by deletion + PASS record
    - Every cycle records sha256 pre-mutation and post-restore prefixes and they are equal to the pre-mutation table
    - `rg -c "git restore" .planning/phases/14-launch-kit/red-gate-proof.md` prints 0 (snapshot-copy method only)
    - Closing hash table shows scripts/check-play-link.mjs, package.json, index.html all equal to the pre-mutation prefixes
    - `Test-Path geohist/gate-probe.html` returns False (scratch probe removed)
    - `node scripts/check-play-link.mjs` prints OK and `npm run validate` exits 0 at close
  </acceptance_criteria>
  <done>Gate proven in both directions (id-bearing forms pass; wrong-id, id-less, http-scheme, and non-canonical forms fail), evidence recorded with hash-verified restores, working tree byte-identical to the Task 1 close state.</done>
  <reversibility rating="reversible">Evidence-only task; every mutation restored hash-verified; no committed state changes; scratch probe deleted.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo tracked files -> CI validate job | a naive gate needle (bare-domain substring) would red CI on the current tree — false-positive gate tampering with every push |
| gate logic -> future contributors | a mis-scoped walk (e.g. .planning included) fails on legitimate historical URLs, tempting forbidden edits of verbatim-immutable records |
| planning docs -> publicly served Pages artifact | red-gate-proof.md ships publicly; no secrets may enter it |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-01 | Tampering | package.json validate chain | medium | mitigate | acceptance criteria assert the exact 6-stage order with validate:play-links between validate:domain and validate:links; validate:links byte-unchanged (prohibition 1) |
| T-14-02 | Tampering | gate needle (naive substring false-red / overly-loose false-green) | high | mitigate | URL-shaped regex + package-id requirement from research Q4; 6 red-gate cycles cover wrong-id, id-less, adjacent-param, escaped-ampersand, and non-canonical probes (Task 2) |
| T-14-03 | Information Disclosure | red-gate-proof.md (public artifact) | low | mitigate | contains only gate outputs, hashes, and the public-by-design package id — no secrets; console-UI/docs-only content rule respected |
</threat_model>

<verification>
1. `node scripts/check-play-link.mjs` -> `check-play-link: OK`, exit 0 (positive control — LKIT-03 green on the current tree)
2. `npm run validate` -> exit 0 with 6 stages, validate:play-links between validate:domain and validate:links (chain wiring, T-14-01)
3. red-gate-proof.md complete: 6 cycles, hash pairs equal, zero git-restore mentions, scratch probe absent (LKIT-03 both directions)
4. index.html byte-untouched at close; package.json diff limited to the 2 declared hunks
5. `node scripts/check-no-old-domain.mjs` still green (inside npm run validate — the new script introduces no legacy-host literal; its needle is the escaped regex form)
</verification>

<success_criteria>
- Both tasks done with all acceptance criteria green
- Working tree holds the complete plan output uncommitted, ready for ship: new scripts/check-play-link.mjs + package.json (2 hunks) + red-gate-proof.md
- Ship subject suggestions: `feat(launch-kit): play package-id CI gate wired into validate chain (check-play-link.mjs)` for the code pair; `docs(14): red-gate proof — play package-id gate, 6 cycles both directions` for the evidence doc
- LKIT-03 satisfied locally (CI-green on next push); LKIT-01, LKIT-02, LKIT-04 authored by 14-02 which depends on this plan's gate artifacts
</success_criteria>

<output>
Create `.planning/phases/14-launch-kit/14-01-SUMMARY.md` when done
</output>
